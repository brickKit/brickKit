# BrickKit 组件合并部署（用户自理）

> **这份文档回答一个问题：能不能把同一个框架的多个组件塞进一个进程、一个实例里跑？**
>
> **能。而且平台的地址注入天然支持它——调用方一个字都不用改。**
> 但从头到尾没有一行是平台帮你做的：没有命令、不生成文件、不做校验、不出警告。
>
> | | |
> | --- | --- |
> | 平台支持状态 | ⛔ **不提供任何支持**，也不打算提供（理由见 `design/012` §2.21） |
> | 这份文档是什么 | 可行性说明 + 关卡清单 + 代价清单 |
> | 这份文档不是什么| 不是教程，不是承诺，不是路线图 |
>
> 想清楚再动手：第 5 章有一条比合并便宜得多的路，第 6 章是你要付的账，
> 第 7 章是这条路上唯一一笔你能**拿回来**的钱。

---

## 1. 什么时候才需要想这件事

真正的动机只有一个：**JVM（或 .NET）组件多，而交付环境的内存是硬约束。**

每个组件一个容器一个进程，成本几乎完全由语言决定（005 §2.5 的表，这里是唯一权威来源）：

| 运行时 | 空跑 RSS 大致范围 | × 20 个组件 |
| --- | --- | --- |
| Go / Rust 静态二进制 | 8–20 MB | 0.2–0.4 G |
| Python / Node.js | 40–90 MB | 0.8–1.8 G |
| .NET | 60–120 MB | 1.2–2.4 G |
| JVM（Spring Boot） | 200–450 MB | **4–9 G** |

差 20 倍。所以：

| 你的项目 | 结论 |
| --- | --- |
| 以 Go / Rust 为主 | **别做。** 20 个组件合起来 0.4G，合并省下的那点内存换不回你要付的账（第 6 章） |
| 以 Python / Node 为主 | 大多数情况别做。先看第 5 章的"按需启用" |
| 有 5 个以上 JVM 组件，且目标机器内存吃紧 | 值得算一算。这份文档就是为你写的 |

典型场景是**私有化交付**：客户给的是一台 8G 的机器，而你的系统里有 8 个 Spring Boot 组件。

---

## 2. 平台的立场

**平台不挡路，也不帮忙。**

| 平台不提供 | 说明 |
| --- | --- |
| `brickkit up --consolidated` 之类的命令 | 没有，也不会有 |
| 合并用的基础镜像 / 外壳工程模板 | 没有 |
| 进程管理、健康检查聚合、日志分流 | 没有 |
| 合并后的迁移编排 | 没有。被你接管的组件，平台不再为它生成迁移容器 / Job |
| 端口冲突、DNS 别名缺失的校验 | 没有。**撞了平台一个字都不说**（第 6 章最后一行） |
| 任何形态保证 | 你的外壳怎么组织，平台不知道也不关心 |

为什么不做——包括"为什么这不是暂缓、而是不做"——见 `design/012` §2.21。

---

## 3. 为什么它可行：调用方本来就不知道对面是什么

这是全文的支点，也是唯一不需要你操心的一段。

- 调用方只读环境变量 `*_ENDPOINT`（附录 C.2），不硬编码任何地址
- 值的格式是 `http://<版本化服务名>:<端口>`，Docker 与 K8s **完全一样**（001 §8.3）
- 服务名是**可推算的**：`/` → `-`、`.` → `-`、全部小写，再接精确版本号（001 §8.2）

```
erp/backend 里看到的永远只有这一行：
    PEOPLE_BASIC_ENDPOINT=http://people-basic-1-0-0:8080

对面是独立容器、是合并容器里的一个进程、还是一个 JVM 里的一个模块——
它不知道，也没有任何办法知道。
```

**只要 `<版本化服务名>:<端口>` 这个地址能被解析、能被连上，你的外壳就是透明的。**

这不是运气。平台没有注册中心（DNS 就是注册中心）、不做 API 代理与路由、不做配置中心（001 §3.2、§5.2）——正因为调用链上没有任何中间层需要同步，你才可以在**只改 DNS 指向**的前提下把对端换成任何东西。换个说法：**平台的克制，正是你能自己动手的原因。**

---

## 4. 五道关卡

按"会不会拦住你"排序。前两道是必须解决的，后三道是必须接手的。

⚠️ **§4.2 与 §4.3 讲的是同一道关卡的两半，不是两道要各自过一遍的关。**
"让 N 个服务名指到外壳"和"让 CLI 别生成这些组件"在 Docker 下是同一个开关
（`local: true`）一起解决的；Docker 网络别名那条路读起来像第二个选项，
但它与 `local: true` **叠加时会失效**，理由在 §4.2.3。第一次做外壳的人
最容易在这里浪费一轮，所以两节的开头都把结论先说了。

### 4.1 端口：唯一真正的拦路石

设计书里后端组件的例子几乎全是 `8080`，前端是 `80`（002 §4.4）。**一个进程 / 一个容器里，10 个模块不能都 bind 8080。**

而且这个问题**不能靠环境变量拨开**：

> ⚠️ 平台不注入"我该监听哪个端口"。环境变量表里只有"别人在哪"（`*_ENDPOINT`），没有"我该监听哪"。进程监听哪个端口由组件自己的代码决定，通常就是 `deployment.port`。
> —— 005 §4.5

两条出路：

| 出路 | 做法 | 代价 |
| --- | --- | --- |
| **A. 端口本来就不撞**（推荐） | 被合并的这一组组件，各自声明不同的 `deployment.port`（8080 / 8081 / 8082…） | 几乎为零。合并本来就要求你有这些组件的源码，那它们的 Manifest 也在你手里。**先做这件事，后面全是顺水** |
| **B. 端口已经撞了** | 只能改 Manifest 的 `deployment.port`，而且必须让 CLI 注入的地址跟着改——唯一能落地的地方是**本地安装源**：把组件放进本地源目录、改掉 `deployment.port`（本地源不吃缓存，004 §7.5；也不受签名约束，008 §8.5.2） | 你从此维护一份与市场版本 drift 的 Manifest（第 6 章） |

⚠️ **别想"一个端口 + 按路径路由"。** 路径不是平台的契约，`*_ENDPOINT` 只给到 `host:port`。两个组件都有 `/healthz`，也都可能有 `/api/v1/...`——撞了没人能替你判。

外壳侧怎么做到"一个进程监听多个端口"，是框架的事，不是平台的事：Spring Boot 可以起多个 connector 或多个 `ServletWebServerFactory`，Go 可以起 N 个 `http.Server`，Django/Flask 可以在一个 ASGI/WSGI 容器里挂多个 app。**外壳应当从每个模块的 `component.yaml` 读 `deployment.port`**，而不是在外壳里另写一份端口表——Manifest 才是权威，抄一份必然过期。

### 4.2 DNS：N 个服务名指到一个外壳

调用方拿到的是 `http://<版本化服务名>:<端口>`，所以那 N 个名字都得解析到你的外壳。

⛔ **Docker 下有两条路，它们是「二选一」，不是两道要一起过的关卡。**
叠加时后者会**让前者一行都不生效**，理由在下面 §4.2.3。先看清楚该走哪条：

| 路线 | 怎么让 CLI 别生成这些组件 | 外壳位置要求 |
| --- | --- | --- |
| **A. 网络别名** | ❌ **没有可用的手段**（见 §4.3）——所以这条路实际走不通 | 接入平台网络即可 |
| **B. `local: true`**（推荐，也是唯一可行的） | ✅ 现成（§4.3） | **必须从宿主机可达**，外壳在容器里就得把端口发布到宿主机 |

K8s 下不存在这个二选一：那里只有一条路（§4.2.2），而 `local: true` 在 K8s 目标下
根本不可用（§4.3 末尾）。

#### 4.2.1 路线 A：Docker 网络别名（写在这里是为了让你不必试）

平台生成的网络名是 `brickkit-<项目名>-net`，compose 项目名是 `brickkit-<项目名>`（005 §3.3）。
你自己的 compose 文件把它当外部网络接进来，给外壳服务挂上全部别名：

```yaml
# 你自己维护的 shell-compose.yaml（平台不生成这个文件）
services:
  my-jvm-shell:
    image: my-registry/my-shell:1.0.0        # 你自己构建、自己签名
    networks:
      brickkit-my-project-net:
        aliases:                              # 每个被合并的组件一个别名
          - people-basic-1-0-0
          - department-tree-1-0-0
          - authorization-rbac-1-0-0

networks:
  brickkit-my-project-net:
    external: true
```

这段配置本身是对的，**但它需要那几个服务名在平台侧空着**——同名的 service
存在时，Docker 内嵌 DNS 会把名字解析到那个 service，别名争不过它。

而"让平台侧空出这几个名字"这件事，现有开关一个都做不到（§4.3 那张表）：
`enabled: false` 会把依赖方一起关掉，从 `brickkit.yaml` 里删掉会让强依赖方直接报错。
唯一做得到的是 `local: true`，而它一旦生效，这段 `aliases` 就成了死配置（§4.2.3）。

**所以路线 A 目前只是一段说明，不是一条能落地的路。** 如实写在这里，
是因为第一次做外壳的人会先想到它——先在这里读到，比试完一轮再发现便宜。

#### 4.2.2 K8s：N 个 Service 指向同一个 Pod

Service 的名字就是 DNS 名，所以做法是"一个 Deployment + N 个 Service"：

```yaml
# 每个被合并的组件一份，name 必须是它的版本化服务名
apiVersion: v1
kind: Service
metadata:
  name: people-basic-1-0-0
  namespace: brickkit-my-project              # 平台的命名空间规则（005 §5.2）
spec:
  selector:
    app: my-jvm-shell                         # 指向你的外壳 Pod
  ports:
    - port: 8080                              # = 该组件 Manifest 里的 deployment.port
      targetPort: 8080
```

K8s 下没有 `extra_hosts` 这回事，所以**这条路不与别的路冲突**——它就是那里唯一的路。
代价是 `brickkit up` 那一键不能直接用，得走 `--dry-run` 再自己挑（§4.3 末尾）。

⚠️ **开了 `deploy.networkPolicy` 的话，你的外壳 Pod 不在依赖图里，会被挡在外面。** 生成的策略只放行"依赖图里的组件"（005 §5.13.1，按 `app: <版本化服务名>` 标签匹配），而外壳 Pod 的标签不是那个。用 `deploy.networkPolicy.allowFrom` 给它开一条口子（005 §5.13.1.1），别把整个 networkPolicy 关掉。

#### 4.2.3 为什么 A 和 B 不能叠加：`/etc/hosts` 赢过网络别名

`local: true` 会在**每个依赖方容器**里写一行 `extra_hosts`，把版本化服务名指向
宿主机网关（005 §4.2）：

```
extra_hosts:
  - "people-basic-1-0-0:host-gateway"
```

`extra_hosts` 落在容器的 `/etc/hosts` 里，而 glibc 与 musl 的解析顺序都是
`hosts: files dns`（`/etc/nsswitch.conf`）——**先查文件，查到就不问 DNS 了**。
于是外壳挂的那些网络别名一次都不会被用到。

两条直接后果，都必须提前知道：

| | |
| --- | --- |
| §4.2.1 那段 `aliases` 是死配置 | 留着无害，但它不是"第二道保险"。以为它在生效，会把排查引向完全错误的方向 |
| **外壳必须从宿主机可达** | 名字解析到 `host-gateway` = 宿主机 IP。外壳跑在容器里完全可以，但**必须把端口发布到宿主机**（`ports:`），否则那个地址连不上——而"接进平台网络"这件事此时一点用都没有 |

### 4.3 路线 B：`local: true`，也是让 CLI 别生成这些组件的唯一手段

这一节与 §4.2 不是两道关卡，**它就是路线 B 的做法本身**：`local: true` 同时解决了
"平台别生成这几个 service"和"那几个名字解析到我的外壳"两件事。

平台没有"这几个组件我自己接管"的表达方式。现有开关里，只有一个能借用：

| 开关 | 能不能用 | 说明 |
| --- | --- | --- |
| `enabled: false` | ❌ **不能** | 强依赖不跑的，依赖方跟着不跑（012 §2.14）。你把 `people/basic` 关掉，`erp/backend` 也跟着不跑了——这跟你想要的正好相反 |
| 从 `brickkit.yaml` 里删掉 | ❌ 不能 | 依赖方在 Manifest 里声明了它，强依赖缺失直接报错（004 §10.2） |
| `local: true` + `localPort` | ⚠️ **Docker 下可以借用** | 见下 |

这张表也解释了**路线 A 为什么走不通**（§4.2.1）：前两行做不到"腾出名字"，
第三行做得到，但它一旦生效，网络别名就被 `/etc/hosts` 盖掉了（§4.2.3）。

`local: true`（005 §4）恰好做了三件你要的事：

1. 不为该组件生成 service 定义
2. 在依赖它的容器里生成 `extra_hosts`，把版本化服务名映射到宿主机
3. **把注入的环境变量里的端口改成 `localPort`**——这一条正是 §4.1 出路 B 在 Docker 下的落地方式

```yaml
components:
  - id: people/basic
    version: 1.0.0
    local: true
    localPort: 8080        # 你的外壳里这个模块监听哪个端口
  - id: department/tree
    version: 1.0.0
    local: true
    localPort: 8081
```

但它的**语义**是"该组件跑在开发者的 IDE 里"，拿它做交付是借用，代价要认：

| 代价 | 说明 |
| --- | --- |
| 外壳必须从宿主机可达 | `extra_hosts` 指向 `host-gateway`（= 宿主机 IP）。外壳跑在容器里也行，但必须把端口**发布到宿主机**。详见 §4.2.3 |
| 迁移你自己跑 | `local: true` 的组件不生成迁移容器（005 §4.1）。不跑就是 `relation does not exist` |
| `expose` / `exposePort` 不生效 | `up` 会警告并给出真实地址（005 §4.6.1） |
| `labels` 不生效 | 同一个理由：没有容器可以挂标签，`up` 会警告（003 §4.11）。外壳的网关标签写在**你自己那份 compose 文件**里 |
| 语义会骗后来的人 | 交付现场的 `brickkit.yaml` 里写着一串 `local: true`，读的人会以为有人正在 IDE 里调试。**在同一个文件里写注释说明** |

⛔ **K8s 下没有任何办法。** `local: true` 只能配 `deploy.target: docker`，K8s 目标下 CLI 在生成阶段直接报错（003 §4.4、005 §5.3.1）。所以 K8s 上做合并，走的是 §4.2.2 那条路，而 `brickkit up` 那一键**不能直接用**：

```
brickkit up --dry-run        # 只生成清单，不 apply（004）
  → 自己把被合并组件的 deployments/*.yaml 挑出来不 apply
  → kubectl apply 剩下的
  → kubectl apply 你自己的外壳 Deployment + N 个 Service
```

**这是设计上的一处空缺，如实写在这里。** 平台不承诺补它——补它意味着平台要开始理解"外壳"这个概念，而那正是 012 §2.21 论证不做的事。

### 4.4 健康检查、迁移、启动顺序：合并之后这三件都是你的

| 事项 | 平台原来做什么 | 合并后变成什么 |
| --- | --- | --- |
| 健康检查 | 每个组件一份 probe / healthcheck（002 §9、005） | 一个容器只有一个。**任何一个模块把进程拖死，整组一起重启**——故障隔离是你付掉的第一样东西。002 §9.4 的禁令依然适用：别在 `/healthz` 里检查别的模块，那会把"一个模块抖动"放大成"整组 CrashLoopBackOff" |
| 数据库迁移 | 声明了 `migration` 的组件，CLI 生成一次性 service / Job，跑完才起主服务（005 §6） | 你自己按顺序跑。⚠️ 合并后多个模块更容易共用一个库——**迁移状态表的主键必须含组件标识**（002 §8.11），否则两个模块的迁移互相顶掉 |
| 启动顺序 | 拓扑排序 + `depends_on` / `service_completed_successfully` | 平台只排它生成的那些。外壳**内部**的模块顺序由你的框架负责（Spring 的 `@DependsOn`、Go 的 `Init()` 调用次序……） |

### 4.5 签名：跑的不再是被签名的那个镜像

- 签名覆盖的是**含 `deployment.image` 的 Manifest**，且 `brickkit publish` 默认把 tag 解析成 digest 再签（008 §8.3.1）
- 生产环境 `requireSignature: true` 是强制的（008 §8.5）

你的外壳镜像是本地构建的，**不在任何签名覆盖范围内**。平台校验的是"我该跑哪个镜像"，而你跑的是另一个。

这不代表不能做，代表**责任转移必须是明确的**：供应链校验从"平台替你验"变成"你自己的构建流程负责"。私有化交付尤其要想清楚——那是唯一"签名本该强制生效"的场景。合并了就把这件事写进交付文档：外壳镜像谁构建、用谁的密钥签、客户怎么验。

---

## 5. 三条别人会先想到的路

| 想法 | 结论 |
| --- | --- |
| **GraalVM Native Image / CDS / 调小堆** | ✅ **先试这条。** JVM 的内存地板从 200–450 MB 掉到几十 MB，而部署形态**一点没变**：还是一个组件一个容器一个进程，平台该做的全都照做，签名、健康检查、迁移、独立扩缩容一样都不用还。它是**组件作者**的事，不需要本文的任何东西。如果你的动机是"JVM 太贵"，这条的性价比高出一个数量级 |
| **Scale-to-Zero（KEDA / Knative 缩到 0）** | ❌ 在 BrickKit 里不通。组件之间 DNS 直连，**调用路径上没有任何东西能唤醒缩到 0 的组件**——请求直接失败。要做必须自己在调用路径插 activator / HTTP 代理，而"不做 API 代理 / 路由"就写在 001 §5.2；更要紧的是它一插进来，"注入的地址直连对端"这条前提就不成立了，第 3 章整章的自由也跟着没了 |
| **内存超卖（`requests` 报低、`limits` 放大）** | ❌ 与 005 §2.5 直接冲突。内存不可压缩：`requests` 远小于 `limits` = Burstable，节点一紧按"实际用量超出自己 requests 多少"排序驱逐，**最先死的正是用得最多、最重要的那个**。005 说的"超卖是正常用法"指的是 **`limits` 之和**可以超容量，不是把内存 `requests` 报低。正确做法是内存 `requests == limits`（Guaranteed）、CPU 只设 `requests` |

**顺序建议：** 先量（`docker stats` 看真实 RSS，别照着谁的表拍数字）→ 换 native image / 调堆 → 按需启用（012 §2.15：50 个组件的项目本地开发时可能只有 4 个容器在跑）→ **最后**才考虑合并。

---

## 6. 你要付的账

| 代价 | 说明 |
| --- | --- |
| **必须有源码** | 闭源组件只有 Manifest + 镜像引用（001 §7.4）。想连镜像一起合（把产物 `COPY --from` 出来）也不行——Manifest 里**没有"产物在镜像内的路径"这个字段**，闭源镜像更无从猜 |
| **只能合同语言同框架的** | 一个 JVM 装不进 Go 二进制。前端组件（nginx serve 静态资源，012 §2.10）也进不来——它们在平台眼里与后端没有区别，但在你的外壳里完全是另一回事 |
| **一起挂** | 一个模块 OOM / 死锁，整组重启（§4.4） |
| **一起升级** | 不能独立扩缩容、不能独立发布回滚。合并那一组从此是一个发布单元——**而这正是 001 §2.1 "组件独立"里被你拿掉的那几条** |
| **Manifest 会 drift** | 走了 §4.1 出路 B，你从此维护一份本地改过 `deployment.port` 的 Manifest。上游发新版本时要手动对齐 |
| **组件独立性会被慢慢磨掉** | 合并成一个 Maven 多模块 / `go.work` 工程之后，"组件之间不共享代码"（001 §2.1）靠的只有纪律。012 §2.16 论证过不支持 monorepo 的三条理由（独立发布单元 / 独立移动单元 / 独立权限单元）会一条条回来找你。**外壳工程里可以引各个组件的模块，但组件之间绝不能互相 import**——一旦破了，你就再也拆不回微服务态了 |
| **平台一个字都不说** | 端口撞了、别名漏了、`allowFrom` 忘了开——平台不校验、不警告。现象是强依赖持续 503 或 connection refused，而**没有任何一处报错指向"合并"**。排障方向会一路跑到组件代码上去 |

**最后一行是最贵的。** 合并了就把这些写下来，放进交付文档：哪些组件被合并、端口怎么分、DNS 别名在哪配、迁移谁跑、外壳镜像谁构建谁签。半年后接手的人只有这份文档。

---

## 7. 唯一一笔你能拿回来的钱：把 N 个连接池并成 1 个

第 6 章整章是账单。这一章是账单上唯一的贷方——**但它只在 PostgreSQL / Oracle 上
存在，而且要求你在建库之前就做对一个决定。**

### 7.1 合并部署顺手制造了一个连接数问题

合并的动机是省 JVM 内存。但把 8 个 Spring Boot 塞进一个进程时，
**8 个模块各自带着自己的连接池一起进来了**：

| | 每实例连接数 | 跑 2 副本 |
| --- | --- | --- |
| 8 个模块各自一个池（HikariCP 默认 `maximumPoolSize=10`） | **80** | **160** |
| 并成一个池（`maximumPoolSize=20`） | **20** | **40** |

PostgreSQL 的 `max_connections` 默认是 **100**。也就是说**一个合并实例就快把
默认配置吃满，两副本直接爆**。而 PG 每条连接是一个独立的服务端进程，
不是一个轻量 handle——80 条和 20 条在库那一侧的内存差距是实打实的。

省下的 JVM 内存在你的机器上，多出来的连接开销在库的机器上。**别只算一边。**

### 7.2 一库一组件时，这个池合不了

PG 的连接绑死两样东西：**一个 database、一个认证角色**。

所以哪怕 8 个模块已经在同一个 JVM、同一个 ClassLoader 里，只要它们指向
8 个不同的库，**这 8 个池物理上就合不了**——一条连到 `people` 库的连接
查不了 `department` 库（006 §7.7）。合并部署在这里一分钱都省不到。

**换成一 schema 一组件，8 个模块指向同一个库，外壳才有资格把池并成一个。**

⚠️ **schema 分组是必要条件，不是充分条件。** 它做的事只是"把不可能变成可能"，
真正省下连接的动作是**外壳去合并池子**。如果外壳仍然让每个模块各自读
`DATABASE_*` 建自己的池，那 8 个池还是 8 个池——一个都没省。
**"共用一个 DataSource"要作为外壳的显式设计目标写进去**（§4 那五道关卡之外的第六件事）。

### 7.3 两个条件，缺一不可

| 条件 | 怎么满足 |
| --- | --- |
| **同一个 database** | 就是 schema 分组：8 个组件 8 个 schema，同一个库 |
| **同一个认证角色** | 连接也绑角色，8 个账号照样是 8 个池 |

条件二与 006 §7.5 的"每个组件一个独立账号（最小权限）"冲突。
**但在合并部署里这个冲突有解**，而且比挂 PgBouncer 那条路好解——
外壳自己控制连接的借出与归还：

```
借出连接 → BEGIN → SET LOCAL ROLE component_a → 跑该模块的 SQL → COMMIT
                   ↑ SET LOCAL 在事务结束时自动还原，连接干净地回到池里
```

用一个共享登录角色登录，借出时切到该模块的角色。**每个模块的权限边界保住了，
池还是一个。** `search_path` 同理，用 `SET LOCAL search_path` 或在 SQL 里全用限定名。

⚠️ **绝不能 `SET search_path`（不带 `LOCAL`）之后把连接还回池子。**
下一个借用者会原样继承它，于是 A 模块的查询打在 B 模块的表上——
**不报错、不崩，只是悄悄读写了别人的数据**，和 006 §7.2 要防的是同一件事。
这是这套写法最容易踩、也最难查的雷。

### 7.4 这个决定有时序性：要在建库之前做

**库一旦按 database 建好、数据进去了，再想改成一库多 schema，就是一次数据迁移。**

所以如果你预见到将来可能走合并部署——典型信号是**以 JVM 为主 + 私有化交付**，
也就是第 1 章那张表里唯一值得算账的那一格——**那么一开始就用 schema 分组是便宜的**，
哪怕你现在还没打算合并。这是全文里唯一一条"还没决定合并时就该考虑"的事。

### 7.5 你还是要付的那部分

| 代价 | 说明 |
| --- | --- |
| **池是共享的，没有舱壁** | 一个模块连接泄漏、或者跑了个长事务，**整组一起等**。分池至少还能各自撑住 |
| **外壳要接管 DataSource** | Spring Boot 各模块自己的 `DataSourceAutoConfiguration` 要被外壳统一顶掉，这是实打实的适配工作 |
| **迁移也得跟着改** | 各模块的迁移脚本要落到自己的 schema 里，`schema_migrations` 表（002 §8.11）也要分开——迁移工具默认往 `public` 里写，不配就会全挤在一起 |

第一行与第 6 章"一起挂"是同一类账：**故障隔离你在合并那一刻就已经付掉了**，
共享池只是把它又推深一层。不是新增一笔，但要知道它在。

### 7.6 非 PG 引擎：这一整章不适用

006 §7.7 的表说得很清楚——**MySQL / SQL Server / MongoDB 的连接根本不绑 database**。
那里一条连接可以 `people.employees JOIN department.nodes`，
所以合并部署时**不需要重新分组，一库一组件也能共用一个池**，本章的前提不成立。

| 引擎 | 合并部署想共池，要不要改成 schema 分组 |
| --- | --- |
| PostgreSQL / Oracle | ✅ 要。连接绑 database / PDB，不改就合不了 |
| MySQL / MariaDB / SQL Server / MongoDB | ❌ 不用。连接本来就能跨库 |

**但账号那一维两边一样**：不管哪个引擎，共用一个池就得共用一个登录身份，
§7.3 那套 `SET LOCAL ROLE`（MySQL 上对应的是换一种权限模型）该做还是要做。

---

## 8. 一句话

**平台把"对端是什么"这件事对调用方隐藏得足够干净，所以你可以自己把 10 个组件变成 1 个实例；也正因为平台干净，它不会在这条路上帮你任何一把。**

先量一遍，再看 native image，再看按需启用。走到这一步还是要合并，那就照第 4 章把五道关卡逐个接手，照第 6 章把账写清楚。

---

## 9. 相关文档

| 文档 | 内容 |
| --- | --- |
| `design/012` §2.21 | 为什么平台不提供合并部署（辩护） |
| `design/012` §2.22 | 为什么默认仍推荐一库一组件，而不是一 schema 一组件 |
| `design/006` §7.7 | database 与 schema 在各引擎里分别是哪一档边界 |
| `design/006` §7.8 | 为什么默认形态下 schema 分组不省连接数（本文第 7 章的前提）|
| `design/005` §2.5 | 资源配额、每个进程的内存地板、一台机器能塞多少个组件 |
| `design/005` §4 | `local: true` / `localPort` 的完整语义与限制 |
| `design/005` §5.13 | NetworkPolicy 与 `allowFrom` |
| `design/005` §6 | 数据库迁移怎么执行 |
| `design/008` §8 | 签名覆盖什么、生产环境怎么强制 |
| `design/012` §2.15 / §2.16 | 按需启用（先试这个）/ 为什么不支持 monorepo |
| 《部署模式》 | 平台原生的两种部署目标（docker / k8s）与市场自身的部署 |
