# 01 · 初始化项目

🎯 **这一节验证什么：** `brickkit init` 生成的骨架长什么样，配置改坏了怎么退回去。

前置：做完 [00-准备.md](00-准备.md)，并且当前终端已经 `export PATH=".../试用指南/bin:$PATH"`。

---

## 1.1 初始化

### ▶️ 操作

```bash
cd 试用指南/playground
brickkit init demo-shop
```

### ✅ 预期

```
✅ 项目已初始化：demo-shop
   📁 brickkit.yaml        项目配置
   📁 components/          组件源码（已配为本地安装源 local-dev）
   📁 .brickkit/           CLI 工作目录
   📁 .brickkit/backup/    配置备份

下一步：
  brickkit add --local               把 components/ 下的组件全加进来
  brickkit add people/basic@1.0.0    从安装源添加组件
  brickkit up                        一键启动
```

生成的 `brickkit.yaml`：

```yaml
# brickkit.yaml - BrickKit 项目配置
# 由 brickkit init 生成，由用户编辑，由 CLI 读取
project: demo-shop

deploy:
  target: docker          # docker | k8s

# 安装源：按声明顺序依次尝试，前一个找不到就试下一个（003 §6.5）
sources:
  - id: local-dev
    type: local
    path: ./components      # brickkit init 已经建好这个目录
  # 组件市场按需取消注释并填上地址：
  # - id: brickkit-market
  #   type: market
  #   url: https://market.example.com/api/v1

components: []
resources: []
```

### 顺手看一眼项目名规则

```bash
brickkit init 试用
```

会明确报错——项目名要用作 K8s 命名空间与 Docker 网络名，只能是小写字母、数字、中划线：

```
❌ 错误：项目名称不合法
   项目名称：试用
   原因：包含非法字符（仅允许小写字母、数字与中划线）
```

---

## 1.2 目录结构

### ▶️ 操作

```bash
ls -a
cat .gitignore
```

### ✅ 预期

```
.brickkit/          CLI 工作目录
.gitignore          init 自动写入（已存在则只补缺失的规则，不覆盖）
brickkit.yaml       你要编辑的唯一文件
components/         组件源码（00 的脚本放进来的）
```

`.brickkit/` 下各目录的分工：

| 目录 | 内容 | 进 Git 吗 |
| --- | --- | --- |
| `backup/` | `brickkit.yaml` 的初始备份与上次备份 | ❌ |
| `manifests/` | 拉下来的 `component.yaml` 缓存 | 可选（默认进） |
| `artifacts/` | 组件产物（proto、openapi.json 等） | 可选（默认进） |
| `generated/` | 生成的部署文件 | ❌ |
| `credentials` | 市场登录 Token | ❌ |

`.gitignore` 里 `.brickkit/generated/`、`.env`、`credentials` 一定是被忽略的——**生成物与密码不进版本库**。

---

## 1.3 改坏了怎么退回去

`add` / `remove` 每次动 `brickkit.yaml` 之前都会自动备份。

### ▶️ 操作

```bash
echo "这是一行手滑写进去的垃圾" >> brickkit.yaml
brickkit order                  # 会报解析失败
brickkit reset --last           # 退回上一次操作前
cat brickkit.yaml
```

### ✅ 预期

`reset --last` 之后配置恢复原样。两种备份：

| 命令 | 恢复到 |
| --- | --- |
| `brickkit reset` | `brickkit init` 刚生成时的样子（`brickkit.yaml.initial`） |
| `brickkit reset --last` | 上一次 `add` / `remove` 之前（`brickkit.yaml.last`） |

> **注意：** 现在这个项目还没执行过 `add`，所以 `.last` 就是 init 时的样子。等你在 02 里 `add` 过之后，`.last` 才会真正有用。

---

## ⏹️ 怎么停

这一节只写了文件，没有启动任何进程与容器。

想从头再来：

```bash
cd <仓库根目录> && ./试用指南/准备.sh --reset
```

---

## 📁 代码在哪

| 能力 | 位置 |
| --- | --- |
| `init` 命令 | [internal/cli/init.go](../internal/cli/init.go) |
| 骨架内容、`.gitignore` 规则 | [internal/config/scaffold.go](../internal/config/scaffold.go) |
| 项目名校验 | [internal/config/project.go](../internal/config/project.go) |
| `.brickkit/` 目录布局 | [internal/config/layout.go](../internal/config/layout.go) |
| `reset` 命令与备份 | [internal/cli/reset.go](../internal/cli/reset.go)、[internal/backup/](../internal/backup/) |

设计书：003 §2（完整结构）、003 §9（备份）、003 §11（`.gitignore` 建议）。

---

➡️ 下一节：[02-添加与移除组件.md](02-添加与移除组件.md)
