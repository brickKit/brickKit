apiVersion: v1
kind: Service
metadata:
  name: {{ include "market.fullname" . }}
  labels:
    {{- include "market.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "market.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
