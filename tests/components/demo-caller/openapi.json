{
  "openapi": "3.0.0",
  "info": { "title": "demo/caller", "version": "1.0.0" },
  "paths": {
    "/healthz": { "get": { "summary": "Liveness check", "responses": { "200": { "description": "ok" } } } },
    "/api/v1/call": { "get": { "summary": "Call demo/hello and echo its response", "responses": { "200": { "description": "ok" } } } },
    "/api/v1/status": { "get": { "summary": "Status of this component and its dependencies", "responses": { "200": { "description": "ok" } } } },
    "/api/v1/env": { "get": { "summary": "Echo the environment variables injected by the platform", "responses": { "200": { "description": "ok" } } } }
  }
}
