---
created: 2026-08-12
updated: 2026-08-12
---
# BrickKit 开发进度追踪

> 本文件是 `开发计划.md` 的执行台账。**每个开发窗口只推进一个 Step**，完成后在此登记，
> 你可以据此查看进度并决定在哪些节点执行 `git commit`。

## 状态图例

| 图例 | 含义 |
| --- | --- |
| ⬜ | 未开始 |
| 🚧 | 进行中 |
| ✅ | 已完成并通过该 Step 的全部测试验证项，且**没有**留给后续 Step 的回填项 |
| ⚠️ | 已通过该 Step 的全部测试验证项，但**有意延后**了部分内容（见"延后实现清单"，逐条列明回填 Step） |

⚠️ 不是"没做完"，而是"当前阶段做不完整、硬做只会做出半成品"——依赖后续 Step 才具备的信息。
每条延后项都在下面的《延后实现清单》里有编号、原因与回填 Step，**不看清单只看总览表会误以为该 Step 全做完了**。

## 使用约定

1. 每个窗口开始时：读取本文件，先看《延后实现清单》里"回填 Step"等于本窗口 Step 的条目，
   再找到第一个 ⬜ / 🚧 的 Step，两者合并为本窗口目标。
2. 每个窗口结束时：更新总览表状态 + 追加"完成记录"小节（含验证结果与产出文件）；
   本窗口回填掉的清单条目标记为 ✅ 并写明落地位置；本窗口新增的延后项追加到清单末尾。
3. Commit 建议：每个 ✅ / ⚠️ 的 Step 可作为一次独立 commit，建议信息格式
   `Step <N>: <Step 名称>`。
4. `市场部署与运维指南.md` 对应的部署/运维工作放在最后（Step 40 之后）执行，前期不涉及。

---

## 执行顺序调整 — 2026-08-12

**背景：** Step 10 之后复盘发现，`up` / 迁移 / 环境变量注入这些 Step 只能"自证"
（断言生成的文本），没法"他证"（真的把容器跑起来），因为**还没有一个能运行的组件镜像**；
同时市场端点契约（P11）也只被 Mock 验证过。

**调整后的顺序：**

```
插入 A（平台自测组件）→ Step 18（市场后端）→ Step 19（登录发布）→ Step 11（注入引擎）→
**Step 21/22（真实组件）→ Step 12 → 25/26 → 13/14/15** → …

> 2026-08-14 调整：先做两个真实组件再回 Step 12。理由是 CLI 的验证不能停在
> "生成的文件长得对"，得能"跑起来是对的"；而 Phase 5 绝大多数验证项不依赖剩余 CLI 工作，
> 现在就能做透。（Step 18/19 已完成，市场与发布链路可用，组件做完随时能发上去。）
```

| 决策 | 理由 |
| --- | --- |
| 先做自测组件，而不是先做市场 | 安装链路已经通了（local / git 两种安装源都实测过），市场只是第三种安装源；而**没有镜像就没法 up**，这是硬阻塞。开发计划 Step 4 的备注也写明"可并行启动 Step 21" |
| 自测组件不占用 Phase 5 的组件 ID | Phase 5（Step 21–28）是业务组件，服务于 E2E 验收；插入 A 是平台自己的测试夹具，长期供 Step 11–17、32、33、37 复用，两者不混淆 |
| 市场（18/19）提前到 Step 11 之前 | 你的要求：把两条路都先铺好，之后每个 Step 都能全量验证。客户端已有的 Mock 契约（D47/D48）正好翻译成服务端的验收测试，回填 P11 |

## 插入工作（不占用开发计划的 Step 编号）

| 编号 | 名称 | 状态 | 完成日期 | 备注 |
| --- | --- | --- | --- | --- |
| A | 平台自测组件（demo/hello、demo/caller） | ✅ | 2026-08-12 | 镜像已构建，四条平台承诺已用真容器验证 |

## Step 18 分段推进（市场后端体量大，拆成 5 段）

> 市场后端有 25 项验证 + 库表 + 对象存储 + 认证，还牵连《市场部署与运维指南》。
> 拆段推进，每段单独可验证、单独登记；**5 段全部完成后 Step 18 才置为完成**。

| 段 | 内容 | 覆盖的验证项 | 状态 |
| --- | --- | --- | --- |
| 18-A | 领域模型 + 发布校验器（保留变量、闭源契约、migration、resources、Manifest 规则） | 18.6–18.11 | ✅ 2026-08-12 |
| 18-B | 仓储层（Repository 接口 + 内存实现 + PostgreSQL 实现）与产物存储（RustFS + 内存） | 18.22 | ✅ 2026-08-12 |
| 18-C | 服务层（发布/查询/下载/可见性/状态/软删除/审计）+ 认证（注册、登录、Token） | 18.12–18.14、18.17–18.21、18.24、18.25 | ✅ 2026-08-12 |
| 18-D | HTTP 层（路由与响应契约，**回填 P11**）+ main 装配 + 启动配置 | 18.1–18.5、18.15、18.16、18.23 | ✅ 2026-08-13 |
| 18-E | 部署形态交付（Dockerfile + compose + 文档）与真实部署复验 | 全量复验 | ✅ 2026-08-13 |

## 总览进度表

### Phase 1：基础设施

| Step | 名称 | 状态 | 完成日期 | 备注 |
| --- | --- | --- | --- | --- |
| 1 | 项目仓库初始化 | ✅ | 2026-08-12 | Monorepo 骨架 + 工具链 |
| 2 | CLI 框架搭建 | ✅ | 2026-08-12 | cobra 命令树 + clierr + JSON 日志 |
| 3 | `brickkit init` 命令 | ✅ | 2026-08-12 | TDD：先行为测试后实现 |

### Phase 2：核心解析引擎

| Step | 名称 | 状态 | 完成日期 | 备注 |
| --- | --- | --- | --- | --- |
| 4 | Manifest 解析器（component.yaml） | ✅ | 2026-08-12 | 30 项验证全通过；**可并行启动 Step 21** |
| 5 | brickkit.yaml 解析器 | ⚠️ | 2026-08-12 | 22 项验证全通过；延后 P1–P5（见清单） |
| 6 | 安装源实现（local/market/git） | ⚠️ | 2026-08-12 | 13 项验证全通过；延后 P6–P12（见清单） |
| 7 | 依赖解析引擎 | ⚠️ | 2026-08-12 | 15 项验证全通过；延后 P13–P15（见清单） |

### Phase 3：CLI 命令实现

| Step | 名称 | 状态 | 完成日期 | 备注 |
| --- | --- | --- | --- | --- |
| 8 | 配置备份与恢复（reset） | ⚠️ | 2026-08-12 | 7 项验证全通过（8.2/8.3 为库层验证）；延后 P16 |
| 9 | `brickkit add` / `remove` | ✅ | 2026-08-12 | 25 项验证全通过；回填 P6/P7/P9/P13/P16 |
| 10 | 拓扑排序与 `brickkit order` | ⚠️ | 2026-08-12 | 7 项验证全通过；延后 P17（级联过滤） |
| 11 | 环境变量注入引擎 | ✅ | 2026-08-13 | 注入引擎 + 级联启停；回填 P1 / P14 / P17，P2 完成合并逻辑 |
| 12 | docker-compose.yaml 生成 | ✅ | 2026-08-14 | 真实两组件生成并跑起来；回填 P2/P4/P20/P21 |
| 13 | 本地调试（local: true）与 local-debug.env | ✅ | 2026-08-14 | 14 项验证全通过（13.14 仅单测）；回填 P3；真跑抓到 localPort 默认值的设计缺陷 |
| 14 | 数据库迁移执行（Docker） | ✅ | 2026-08-14 | 生成侧 Step 12 已覆盖，本次核对补齐；14.3/14.4 真跑验证；抓到组件入口与本地源缓存两个真问题 |
| 15 | 引擎调用与生命周期命令（up/down/status） | ⬜ | | |
| 16 | K8s 部署文件生成 | ⬜ | | |
| 17 | `brickkit sync` 命令 | ⬜ | | |

### Phase 4：市场后端

| Step | 名称 | 状态 | 完成日期 | 备注 |
| --- | --- | --- | --- | --- |
| 18 | 市场后端实现（RustFS + 高级校验） | ✅ | 2026-08-13 | 分 5 段推进全部完成；25 项验证全通过；对象存储改用 RustFS，见 D39 |
| 19 | CLI 发布与登录功能 | ✅ | 2026-08-13 | login / publish 全实现；真实市场端到端验证过发布→安装闭环 |
| 20 | 签名与安全（cosign） | ⬜ | | |

### Phase 5：测试组件开发

| Step | 名称 | 状态 | 完成日期 | 备注 |
| --- | --- | --- | --- | --- |
| 21 | department/tree（Go，单端口双协议） | ✅ | 2026-08-14 | 叶子组件；HTTP+gRPC 共用一端口，真容器验证 10 项 |
| 22 | people/basic（Python，extraPorts 双端口） | ✅ | 2026-08-14 | 跨语言：Python 客户端调 Go 服务端；强/弱依赖差异已用真容器验证 |
| 23 | auth/password-login（Go，HTTP） | ⬜ | | |
| 24 | authorization/rbac（Go，HTTP + gRPC） | ⬜ | | |
| 25 | erp/backend（Go，连接组件） | ⬜ | | |
| 26 | portal/user-frontend（nginx） | ⬜ | | |
| 27 | infra/redis-event-bus（Python） | ⬜ | | |
| 28 | infra/api-docs（Go） | ⬜ | | |
| 29 | 多版本默认共存验证 | ⬜ | | |

### Phase 6：集成与 E2E

| Step | 名称 | 状态 | 完成日期 | 备注 |
| --- | --- | --- | --- | --- |
| 30 | 渐进式拼装与端到端验证 | ⬜ | | |
| 31 | 完整功能点验证清单（50 项） | ⬜ | | |

### Phase 7：专项测试

| Step | 名称 | 状态 | 完成日期 | 备注 |
| --- | --- | --- | --- | --- |
| 32 | 边界测试 | ⬜ | | |
| 33 | 错误处理测试 | ⬜ | | |
| 34 | 兼容性测试（Docker / Podman） | ⬜ | | 需安装 podman |
| 35 | 安全测试 | ⬜ | | |
| 36 | 性能基准测试 | ⬜ | | |
| 37 | 回归测试 | ⬜ | | |
| 38 | 升级测试 | ⬜ | | |

### Phase 8：验收与发布

| Step | 名称 | 状态 | 完成日期 | 备注 |
| --- | --- | --- | --- | --- |
| 39 | 完整功能点验收 | ⬜ | | |
| 40 | 文档验证 | ⬜ | | |
| 41 | 市场部署与运维（对应《市场部署与运维指南》） | ⬜ | | 最后执行 |

---

## 延后实现清单（跨 Step 回填）

> **这张表是"⚠️ 步骤到底缺什么"的唯一权威来源。** 每条都是当前 Step 有意不做的部分——
> 因为它依赖后续 Step 才有的信息，现在做只能做半个、也无法完整测试。
> 进入某个 Step 前，先在这里筛出"回填 Step = 该 Step"的条目，与该 Step 自身的任务合并执行。
> 回填完成后把状态改为 ✅ 并写明落地位置，不要删行（保留可追溯性）。

| # | 来源 Step | 延后的内容 | 为什么现在不做 | 回填 Step | 状态 |
| --- | --- | --- | --- | --- | --- |
| P1 | 5 | 级联计算（`enabled` 三态 → 实际启停集合） | 需要 Manifest 的依赖图，Step 6/7 之后才具备。**Step 7 的 `resolver.ResolveConfig` 目前不过滤 `enabled`**，级联在这里落地 | 11 | ✅ Step 11：`internal/cascade`，与 003 §4.3 的示例逐行对齐 |
| P2 | 5 | 资源配额三层合并（brickkit.yaml > component.yaml > CLI 默认值） | 需要 Manifest 才能合并；生成部署文件时才用得到 | 11 / 12 | ✅ Step 11 合并 + Step 12 写进 `deploy.resources`（含 K8s→compose 单位转换） |
| P3 | 5 | `localPort` 自动分配 | 需要全项目端口视图与运行期信息 | 13 | ✅ Step 13：统一的宿主机端口台账（localPort / exposePort / 自动映射共用一张表）；默认值改为组件声明的 `deployment.port`，见 D178 |
| P4 | 5 | `exposePort` 与 `deployment.port` 的冲突判定 | 需要 Manifest 中的 `deployment.port` | 12 | ✅ Step 12：生成阶段就报冲突，并指出改哪个字段 |
| P5 | 5 | 资源密码硬编码告警 | 属于 up 前的体检输出，需命令层承载 | 15 | ⬜ |
| P6 | 6 | 组件的 `sourceType`（git/registry）与 `gitUrl` | 只有 `--repo` / `--repo-all` 需要；且命中 Manifest 缓存时这两个字段不可得 | 9 | ✅ 落在 `source.Client.Origin`（绕开 Manifest 缓存直接问安装源） |
| P7 | 6 | 把 `internal/source` 接线到命令（`add` / `up` 调用、终端输出 📦/⚠️ 渲染） | Step 6 只交付库 | 9 | ✅ `add` 已接线并渲染；`up` 的接线属 Step 15 |
| P8 | 6 | 签名校验（`installer.requireSignature` + cosign）接入下载路径 | cosign 未安装、签名格式由 Step 20 定义。**当前下载路径完全不验签** | 20 | ⬜ |
| P9 | 6 | 缓存清理（remove 时删除 `.brickkit/manifests` 与 `.brickkit/artifacts` 中的对应条目） | 属于 `brickkit remove` 的职责（9.12 / 9.13） | 9 | ✅ 落在 `cli.cleanupComponent` |
| P10 | 6 | 升级路径：`up` 检测到版本变化时拉取新版本 Manifest + 产物（004 §4.6） | 需要 up 的执行流与旧/新版本对比 | 15 | ⬜ |
| P11 | 6 | 市场端点契约落地：`GET …/manifest` 的 JSON 信封、`GET …/artifacts/{id}/download?file=` | 客户端已按 D47/D48 实现并有 Mock 覆盖，但**服务端还不存在**，真实市场未验证过 | 18 | ✅ 18-D：服务端已实现，且用**真实 CLI 二进制**对**真实市场进程**跑通了 `add`（见 18-D 完成记录的端到端验证） |
| P12 | 6 | git 安装源指定分支 / tag / commit | 003 §6.3 的 git 源只有 `url` 字段，当前固定 `--depth 1` 拉默认分支。若后续设计书增加 `ref` 字段再补 | 待定（设计书先行） | ⬜ |
| P13 | 7 | 解析结果写回 brickkit.yaml（递归依赖入库、多版本自动写两个条目） | 解析器只产出依赖图；写文件是 `brickkit add` 的职责 | 9 | ✅ 落在 `config.Edit`（保注释、保 `${ENV_VAR}`） |
| P14 | 7 | 强依赖**被禁用**（`enabled: false`）时报错（004 §10.3） | 与"取不到"是两回事，需要级联启停结果（P1）才能判定 | 11 | ✅ Step 11：只在**钉住**的组件上报错，并打印完整依赖链 |
| P15 | 7 | `CheckUpgrade` 接线到 `brickkit up` 的升级流程（004 §4.6） | 需要 up 的执行流与"版本号变化"检测 | 15 | ⬜ |
| P16 | 8 | **在 `add` / `remove` 执行前自动调用 `backup.SaveLast`**（8.2 / 8.3） | 备份能力已实现并测全，但两个命令当时还没实现 | 9 | ✅ 两个命令均在改配置前备份，`reset --last` 可撤销 |
| P21 | 22 | **22.3 `PEOPLE_BASIC_GRPC_ENDPOINT` 只验证到注入引擎层**：`inject` 已能算出这个变量（Step 11 有测试），但还没有生成的 compose 可以拿去看 | 需要 Step 12 把注入结果写进 docker-compose.yaml，才能按验证项要求"检查 env" | 12 | ✅ Step 12 |
| P20 | 11 | 把注入引擎接线到命令：`internal/inject` 已算出每个组件的环境变量与资源配额，但**还没有任何命令消费它** | 消费方是生成 docker-compose.yaml（Step 12）与 K8s 清单（Step 13）。Step 11 只交付引擎本身，接线是下一步的事 | 12 | ✅ Step 12：`up --dry-run` 消费注入结果生成 compose |
| P22 | 13 | **宿主机端口冲突只查项目内**：CLI 能发现两个组件抢同一个端口，但发现不了宿主机上**别的进程**已经占着这个端口 | 需要真的去 bind 一下才知道，属于启动前的体检；真实验证时就撞上了——`localPort: 9001` 被本机无关进程占用，生成一切正常，跑起来才 503 | 15（随 `--check-resources` 一起做） | ⬜ |
| P23 | 13 | **13.14 Podman 只到单元测试**：断言生成的 `extra_hosts` 是 `host.containers.internal`，没在真 Podman 上跑过 | 本机未装 Podman（见 L1） | 34 | ⬜ |
| P19 | 18-E | 市场的 Kubernetes 部署清单（Deployment / Service / Ingress / Secret）与 Helm chart | 云端模式目前只给了"把环境变量指向云托管数据库与对象存储"的方案，够用但不是一键。K8s 编排与 CLI 的 `deploy.target: k8s` 是同一套知识，一起做更省事 | 34 前后 | ⬜ |
| P18 | 18-D | **CLI 未解析市场错误信封**：拿到 4xx 时只看状态码，把 403 一律说成"市场认证失败，请执行 brickkit login" | 端到端验证时实测：安装一个 blocked 组件，服务端正确返回 403 + `COMPONENT_BLOCKED` + "该版本已被市场下架"，但 CLI 显示的是"认证失败 → 去登录"，把使用者引向完全错误的方向。修法是 `internal/source/market.go` 的 `get()` 解析信封里的 code/message 再决定文案 | 19 | ✅ Step 19 修复：错误信封解析抽到 `internal/market`，read/write 两侧共用；已用真实 blocked 组件复验 |
| P17 | 10 | `brickkit order` 按级联结果过滤组件（`enabled: false` 与被级联跳过的组件不该出现在启动顺序里） | 级联计算是 Step 11 的职责（P1）。当前 order 列出 brickkit.yaml 中的**全部**组件；`TestOrderCurrentlyIgnoresEnabledFalse` 锁定了这一现状，Step 11 实现后该用例会失败，提醒回来改 | 11 | ✅ Step 11：order 先算级联再排序，并输出「组件状态计算」一节；那条自曝用例已按新行为重写 |

---

## 完成记录

### Step 1：项目仓库初始化 — ✅ 2026-08-12

**产出文件：**

| 文件 / 目录 | 说明 |
| --- | --- |
| `go.mod` / `go.sum` | 主 module `github.com/brickkit/brickkit`，`go 1.22` |
| `cmd/brickkit/main.go` | CLI 入口占位（Step 2 替换为 cobra root 命令） |
| `internal/version/` | 版本与能力常量（ldflags 注入）+ 3 个单元测试 |
| `internal/*` | 13 个内部包目录骨架：cli / manifest / config / source / resolver / generator{compose,k8s} / engine / security / market / workspace / upgrade / backup |
| `market-server/go.mod` | 市场后端独立 module `github.com/brickkit/market-server`，`go 1.22` |
| `market-server/cmd/server/main.go` | 市场后端入口占位（Step 18 实现） |
| `market-server/internal/storage/store.go` | `ArtifactStore` 接口 + 对象存储客户端构造（Step 18 实现细节；2026-08-12 由 MinIO 改为 RustFS，见 D39） |
| `tests/*` | 9 类测试目录骨架（unit/integration/e2e/boundary/error/compat/security/perf/regression） |
| `Makefile` | 30 个目标：build/build-cli/build-market/install/clean/fmt/vet/lint/tidy/test/test-unit/test-integration/test-e2e/test-boundary/test-error/test-compat/test-security/test-perf/test-regression/test-upgrade/test-market/test-all/cover/proto-gen/proto-example/tools/tools-protoc/tools-plugins/tools-lint/tools-proto-include/env/help |
| `.gitignore` | 追加 BrickKit 专属规则（generated/credentials/manifests/artifacts/components/bin/.tools/local-debug） |
| `proto/include/` | `google/api/{annotations,http,field_behavior}.proto`、`protoc-gen-openapiv2/options/{annotations,openapiv2}.proto` |
| `proto/example/ping.proto` | protoc 工具链自检用示例契约（含 `google.api.http` 注解） |
| `.tools/bin/` | 本地工具链二进制（已 gitignore，`make tools` 可重建） |

**依赖引入：**

| module | 依赖 | 版本 | 状态 |
| --- | --- | --- | --- |
| 主 module | testify | v1.11.1 | 直接依赖（已被 `internal/version` 测试使用） |
| 主 module | cobra | v1.10.2 | 已声明，Step 2 起使用 |
| 主 module | google/uuid | v1.6.0 | 已声明，后续使用 |
| 主 module | gopkg.in/yaml.v3 | v3.0.1 | 已在依赖图中，Step 4/5 解析器起直接使用 |
| market-server | ~~minio-go/v7~~ → aws-sdk-go-v2 | s3 v1.66.0 / credentials v1.17.42 / core v1.32.3 | 直接依赖（`internal/storage`），见 D39 |

**工具链版本（全部装在 `.tools/bin`，不污染系统环境）：** protoc 35.1、protoc-gen-go v1.36.12、protoc-gen-go-grpc v1.6.2、protoc-gen-grpc-gateway v2、protoc-gen-openapiv2 v2。

**测试验证结果（16 项检查全通过）：**

| # | 验证项 | 结果 |
| --- | --- | --- |
| 1.1 | `go build ./...` 编译通过 | ✅ 主 module 与 market-server 均通过 |
| 1.2 | `go vet ./...` 无警告 | ✅ 两个 module 均零输出 |
| 1.3 | Makefile 各 target 可执行 | ✅ `build`/`test`/`lint`/`proto-gen`/`test-all`/`help`/`env` 全部成功 |
| 1.4 | `.gitignore` 包含所有必要条目 | ✅ `.brickkit/generated/`、`.brickkit/credentials`、`components/`、`.env` 四项齐备 |
| 1.5 | protoc 可编译测试 proto | ✅ `make proto-gen` 由 `ping.proto` 生成 `ping.pb.go` + `ping_grpc.pb.go` |

**关键实现决策（与开发计划的偏差与理由）：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D1 | `go.mod` 固定 `go 1.22`（而非 Go 1.26 默认写入的 1.25） | 附录 G 要求 Go 1.22+；因此对象存储 SDK 也钉在兼容 Go 1.22 的版本（见 D39） |
| D2 | 对象存储客户端只放 `market-server/go.mod`，不放主 module | 产物存储只有市场用得到；CLI 是单二进制分发（001 §4.1），保持精简。~~minio-go~~ → 见 D39：已于 2026-08-12 换成 aws-sdk-go-v2（服务端换用 RustFS） |
| D3 | proto 自检生成物输出到 `.tools/proto-check/` | Go 工具链忽略以 `.` 开头的目录，避免生成的 `.pb.go` 把 grpc/protobuf 拖进 CLI 的依赖图 |
| D4 | `make lint` 在缺少 golangci-lint 时回退 `go vet` | 保证验证项 1.3 在任何机器上可执行；`make tools-lint` 可随时装上真 linter |
| D5 | 各 `test-*` 目标在目录无 `*_test.go` 时跳过而非报错 | 骨架阶段 9 类测试目录为空，`make test-all` 仍应绿灯 |
| D6 | 暂不执行 `make tidy` | tidy 会删掉尚未 import 的 cobra/uuid；Step 2/4 用上后即可正常 tidy |

---

### Step 2：CLI 框架搭建 — ✅ 2026-08-12

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `internal/clierr/clierr.go` | 统一错误类型 `Error{Code, Message, Details, Hints, Tips, Exit, Cause, Warning}` + 30 个错误码目录 + 004 §10.2 渲染格式 |
| `internal/logging/logging.go` | 基于 `log/slog` 的 JSON 日志，输出 stderr，字段名对齐 time/level/message |
| `internal/cli/root.go` | cobra root 命令、全局 flag、中文 usage 模板、错误翻译与退出码 |
| `internal/cli/{init,add,remove,up,down,status,order,sync,reset,login,publish,version}.go` | 11 个命令骨架 + version 完整实现 |
| `internal/version/version.go` | 新增 `Display()`（004 §11.3 的 `v` 前缀格式） |
| `cmd/brickkit/main.go` | 精简为 `os.Exit(cli.Execute())` |
| `internal/{cli,clierr,logging,version}/*_test.go` | 72 个单元测试，internal 语句覆盖率 **100.0%**（234/234） |
| `Makefile` | 新增 `cover-html`、`cover-check`（COVER_MIN=95 门槛），`cover` 改为按函数展开 |

**关键设计：**

| 主题 | 实现 |
| --- | --- |
| 输出分流 | 人类可读输出（✅/📦/表格）走 **stdout**；JSON 日志与 ❌ 错误块走 **stderr**。`brickkit order > x.txt` 不会混入日志 |
| 错误格式 | `❌ <标题>` + `   <键>：<值>` 明细行 + `建议：`（1 条内联、多条自动编号）+ `💡` 提示，逐字对齐 004 §10.2 的 12 个错误样例 |
| 退出码约定 | 0 成功/警告（33.15）、1 运行期错误、2 用法错误（未知命令、缺参、非法 flag）。004 未规定数值，此处显式约定 |
| 警告渲染 | `clierr.Warn()` 用 ⚠️ 渲染且退出码 0，供 Step 11 保留变量冲突复用 |
| 骨架占位 | 未实现命令返回 `NOT_IMPLEMENTED` 并明确指出"开发计划 Step N"，避免静默无响应 |
| 命令分组 | `--help` 按 项目 / 组件 / 生命周期 / 市场 四组展示，含中文 usage 模板与中文化的 help/completion |
| 全局 flag | `--config/-c`（多环境配置，004 §3.5）、`--log-level`（含 `BRICKKIT_LOG_LEVEL` 环境变量，支持 `off` 静音） |
| 参数完整性 | 各命令 flag 与 004 一一对应：add 的 `--yes/--refresh/--repo/--repo-all`、up 的 `--only/--dry-run/--check-resources`、down 的 `--only`、reset 的 `--last`、publish 的 `--path/--visibility` |

**测试验证结果（35 项二进制端到端检查 + 63 个单元测试全通过）：**

| # | 验证项 | 结果 |
| --- | --- | --- |
| 2.1 | `brickkit version` 输出版本信息 | ✅ 含版本号、`brickkit/v1`、`docker, k8s` 三行，格式对齐 004 §11.3 |
| 2.2 | `brickkit --help` 列出所有子命令 | ✅ init/add/remove/up/down/status/order/sync/reset/login/publish/version 全部在列 |
| 2.3 | 不存在的命令报错 | ✅ 输出 `❌ 错误：未知命令 nosuchcmd` + 建议，退出码 2 |
| 2.4 | 每个子命令 `--help` 有输出且退出码 0 | ✅ 12/12 通过 |
| 2.5 | 错误输出格式正确 | ✅ 含 ❌ 符号、错误描述、建议行 |
| 2.6 | 日志为 JSON 格式 | ✅ stderr 每行均为合法 JSON 且含 time/level/message；stdout 无日志 |

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D7 | 日志默认级别 info，每次执行输出"命令开始执行/执行完成"两条 | 验证项 2.6 要求"执行任意命令即可在 stderr 看到 JSON 日志"；不需要时可 `--log-level off` |
| D8 | `msg` 键改名为 `message` | slog 默认键是 `msg`，与验证项 2.6 要求的 `message` 不一致 |
| D9 | 非 `*clierr.Error` 的错误一律按用法错误（退出码 2）翻译 | 所有命令的 RunE 只返回 clierr 错误，因此剩余的原生错误必来自 cobra 的解析阶段 |
| D10 | 用 `ExecuteC()` 而非 `Execute()` | 日志里要记录真实执行的子命令名（`brickkit version`）而非 root |
| D11 | 中文化 cobra 的 usage 模板与内置 help/completion 命令 | 设计书全部输出为中文，混入 `Usage:`/`Available Commands:` 会破坏一致性 |
| D12 | `--config` 放在 root 的 persistent flag | 开发计划 Step 2 要求全局 flag；004 §3.5 只在 up 中举例，全局化是超集，不冲突 |
| D13 | 覆盖率政策：`make cover-check` 门槛 95%，当前实际 100% | 不追求"永远 100%"：进程入口（main）与对外部引擎的调用（docker / kubectl / 对象存储）不强求覆盖，硬凑只会写出没有意义的测试。同时不使用 `-coverpkg`——那会让 cli 测试"顺带"覆盖 version 包，掩盖各包自身的测试缺口 |

**覆盖率补齐记录（94.0% → 100.0%）：** Step 2 初版有 14 条未覆盖语句，逐条归因后全部补齐：

| 类别 | 位置 | 语句 | 处理 |
| --- | --- | --- | --- |
| 跨包归因（cli 测试实际执行了它，但 Go 只统计被测包自身测试） | `version.Display` | 3 | 在 version 包内加 `TestDisplay`，覆盖"补 v 前缀 / 不重复补"两分支 |
| 进程入口 | `cli.Execute` | 3 | `TestExecuteReadsOSArgs`：替换 `os.Args` + `os.Pipe` 接管 stdout 走真实入口 |
| 默认构造未走到 | `NewOptions` / `envOr` | 4 | `options_test.go`：默认值、`BRICKKIT_LOG_LEVEL` 覆盖、空白字符串回落三种情形 |
| 暂无调用点 | `Options.Println` | 1 | 加测试；Step 3 的 init 输出会真正用到 |
| 防御分支 | `NewRootCommand(nil)` | 1 | `TestNewRootCommandNilOptionsUsesDefaults`（只构造不执行） |
| 未来功能 | `Run` 的 `e.Warning` 分支 | 1 | `TestRunRendersWarningWithZeroExit`：提前锁定"警告 ⚠️ 不阻断、退出码 0、日志 WARN"契约（Step 11 复用） |
| **真实测试缺口** | `translate` 的 `default` 分支 | 1 | `TestTooManyArgsUsesFallbackTranslation`：`brickkit init a b c` → 退出码 2 + "命令用法不正确" |

---

### Step 3：`brickkit init` 命令 — ✅ 2026-08-12

**开发方式：TDD（先逻辑层测试，后实现，最后补代码层单测）**

1. **RED**：先写 `internal/cli/init_test.go`（21 个行为测试，覆盖开发计划 3.1–3.12），运行确认**全部因"功能缺失"失败**（而非笔误/编译错误）。
2. **GREEN**：实现 `internal/config` 三个文件 + 接线 `init` 命令，直到行为测试全绿。
3. **代码层单测**：再补 `internal/config` 的 44 个单元测试（Layout 路径推导、命名校验表、gitignore 合并、IO 错误路径）。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `internal/config/layout.go` | `Layout` 目录布局：由 Root + 配置文件名推导全部路径（config/backup/manifests/artifacts/generated/components/.archived/credentials/gitignore/initial/last），不依赖进程 cwd |
| `internal/config/project.go` | 项目名校验（003 §3.1）+ `SuggestProjectName` 规范化建议 + `Skeleton` 骨架生成（004 §3.2） |
| `internal/config/scaffold.go` | `InitProject` 编排 + `checkNotInitialized` + `EnsureGitignore`（幂等追加，003 §11） |
| `internal/cli/init.go` | 命令接线 + 输出渲染 |
| `internal/cli/root.go` | `Options` 新增 `WorkDir` 注入点 |
| `internal/cli/init_test.go`、`internal/config/*_test.go` | 65 个新测试（总计 154 个，internal 覆盖率 99.2%） |

**测试验证结果（31 项二进制端到端 + 154 个单元/行为测试全通过）：**

| # | 验证项 | 结果 |
| --- | --- | --- |
| 3.1 | 不传参数报错 | ✅ `❌ 请指定项目名称：brickkit init <项目名称>`，退出码 2，且不创建任何文件 |
| 3.2 | 成功创建目录结构 | ✅ brickkit.yaml + .brickkit/{backup,manifests,artifacts,generated} + components/ |
| 3.3 | 骨架内容正确 | ✅ project / deploy.target / components: [] / resources: []，且是合法 YAML |
| 3.4 | 初始备份存在且一致 | ✅ `.brickkit/backup/brickkit.yaml.initial` 与配置逐字节相同 |
| 3.5 | `components/.archived/` 存在 | ✅ |
| 3.6 | `.gitignore` 含 `components/` | ✅ 003 §11 全部条目；已有 .gitignore 时只追加缺失项 |
| 3.7 | 重复 init 报错 | ✅ `已存在：brickkit.yaml、.brickkit/`，原配置不被覆盖 |
| 3.8 | 名称含非法字符报错 | ✅ 给出原因 + 命名规则 |
| 3.9 | 名称含大写报错 | ✅ 提示"必须全部小写"并建议 `myproject` |
| 3.10 | 空目录 init 成功 | ✅ |
| 3.11 | 非空目录（无配置）init 成功 | ✅ 已有文件不受影响 |
| 3.12 | 已有 brickkit.yaml 时报错 | ✅ 退出码 1，文件内容不变 |

**额外覆盖（提前满足后续 Step 的验证项）：** 32.20–32.22 项目名（中划线/数字/纯数字）、`--config brickkit.prod.yaml` 多环境初始化（配置与备份名同步变化）、超长名称（K8s namespace 上限）、`init a b` 参数过多。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D14 | `init` 在**当前目录**初始化，不创建以项目名命名的子目录 | 004 §3.2「在当前目录初始化」；3.10/3.11 也明确验证"在（空/非空）目录中执行 init" |
| D15 | 3.1 的文案采用 004 §3.2 的单行形式 `❌ 请指定项目名称：brickkit init <项目名称>` | 004 §10.2 另有一处两行形式（`❌ 错误：请指定项目名称` + `用法：…`）。开发计划 3.1 的预期结果是单行形式，以它为准 |
| D16 | 3.7 的错误文案用「项目已初始化，无需重复执行 init」+ 列出已存在的 `brickkit.yaml`/`.brickkit/` | 开发计划 3.7 写的是"目录已存在"错误，但 init 是在当前目录初始化，说"目录已存在"会误导；改为列出真正冲突的文件，并给出删除/reset 两条出路 |
| D17 | 项目名长度上限 54 | 项目名用于 K8s namespace `brickkit-<项目名>`（RFC1123 上限 63）与 Docker Network 命名，超长会在部署阶段才炸，提前拦住 |
| D18 | `Options.WorkDir` 注入项目根目录，命令层不依赖进程 cwd | 让 add/up/sync 等后续命令都能在测试里并行、隔离地跑；`os.Chdir` 是进程级全局状态，不可并行 |
| D19 | 名称非法时给出「建议：改用 xxx」 | 004 要求错误必须带建议；规范化后的名称是最有用的建议（`MyProject` → `myproject`） |
| D20 | 覆盖率 99.2%：余下 3 条语句豁免 | 分别是 `f.Write` 部分写失败、备份文件写入失败、`filepath.Abs` 失败——都只能靠文件系统级故障注入触发，按 D13 政策不为它们在生产代码里加抽象层 |

**过程中发现并修复的隐患：** Step 2 的测试辅助函数 `run()` 没有隔离 `WorkDir`，`init` 一实现，测试就把 `brickkit.yaml`、`.brickkit/`、`components/`、`.gitignore` 真的写进了 `internal/cli/` 源码目录。已清理并给 `run()` 加上 `t.TempDir()`，同时用 `git status` 确认仓库干净。

---

### Step 4：Manifest 解析器（component.yaml） — ✅ 2026-08-12

**开发方式：TDD。** 先写 64 个行为测试（覆盖 4.1–4.30）→ 运行确认全部因功能缺失失败 → 实现 → 再补 44 个代码层单测。manifest 包覆盖率 **100%**。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `internal/manifest/types.go` | Manifest 全部结构体（Metadata / Artifact / Dependencies / ComponentDep / ResourceDep / ConfigSchema / Deployment / ExtraPort / Resources / Migration / HealthCheck / Observability / Compatibility），字段与 002 §2.2 + 附录 B.1 一一对应 |
| `internal/manifest/parse.go` | 三段式解析：YAML 语法（带行号）→ 结构形状检查 → 语义校验；`ComponentDep.UnmarshalYAML` 支持依赖的标量/映射两种写法 |
| `internal/manifest/validate.go` | 全部字段级校验规则 + 组件 ID / 精确版本 / K8s 端口名正则 |
| `internal/manifest/problems.go` | `Problem{Field, Reason}` 收集器，一次报出所有问题 |
| `internal/manifest/testdata/{department-tree,people-basic}.yaml` | 取自 002 §12.5 / §2.2 的真实 Manifest，Step 21/22 直接复用 |

**关键设计：**

| 主题 | 实现 |
| --- | --- |
| 一次报全部问题 | 校验不"遇错即返回"，而是收集所有 `Problem` 一次渲染，使用者改一轮就能改完；错误块形如 `metadata.id：格式必须为 scope/name…` |
| 精确字段名 | 解码前先做**节点形状检查**（`migration.command`、`artifacts[].files`、`tags`、`dependencies.*`、`configSchema.required` 必须是数组），否则用户只会看到 yaml 库的 `cannot unmarshal !!str into []string` 而不知道是哪个字段 |
| 依赖两种写法 | `- department/tree@1.0.0`（强）与 `- {id: …, optional: true}`（弱）由自定义 `UnmarshalYAML` 统一解析为 `{ID, Version, Optional, Ref}` |
| 前向兼容 | 不启用 yaml 严格模式：新版本组件多写字段时，老 CLI 忽略而不是报错 |
| 错误可追溯 | `Manifest.Source` 记录来源（文件路径/安装源），错误块首行即 `文件：…` |

**测试验证结果（30/30 项验证 + 108 个测试全通过）：**

| # | 验证项 | 对应测试 |
| --- | --- | --- |
| 4.1 | 合法 Manifest 解析成功（department/tree） | `TestParseFileDepartmentTree`（逐字段断言，含 artifacts/资源依赖/configSchema/resources/migration） |
| 4.2–4.6 | 缺少 id / version / image / port / healthCheck 报错 | `TestValidationErrors/4.2…4.6`，均指出具体字段 |
| 4.7–4.8 | 版本号 `abc` / `1.0` 报错 | 提示 `必须是精确版本 major.minor.patch` |
| 4.9–4.10 | 依赖 `^1.0.0` / `~1.0.0` 报错 | 提示"不接受 ^ 或 ~ 等范围约束" |
| 4.11–4.12 | artifacts 缺少 type / files 报错 | `artifacts[0].type`、`artifacts[0].files` |
| 4.13–4.15 | extraPorts name 重复 / 缺 name / 缺 port | `TestExtraPortsDuplicateName` + `TestValidationErrors/4.14、4.15` |
| 4.16 | `deployment.type: static` 报错 | 提示"所有组件都是 container，包括前端组件" |
| 4.17 | `migration.command` 非数组报错 | 形状检查给出 `migration.command：必须是数组格式（当前是标量）` |
| 4.18 | configSchema 格式不合法报错 | 三个用例：type 非 object、属性类型非法、required 项未声明 |
| 4.19 | 弱依赖 `optional: true` 正确识别 | `TestParseFilePeopleBasic` |
| 4.20–4.25 | 空/无 dependencies、无 artifacts / extraPorts / migration / configSchema 均解析成功 | `TestParseEmptyDependencies`、`TestParseMinimalManifest` |
| 4.26–4.27 | apiVersion / kind 非法报错 | 指出应为 `brickkit/v1`、`Component` |
| 4.28–4.29 | 组件 ID 含非法字符 / 含大写报错 | 分别提示 `scope/name` 格式与"必须全部小写" |
| 4.30 | resources 格式不合法报错 | 两个用例：既无 requests 又无 limits；requests 既无 cpu 又无 memory |

**额外提前覆盖（Step 32 边界项）：** 32.1（ID 超长）、32.2（ID 含 @）、32.3（版本号超长仍正常）、32.5（100 个依赖）、32.10（非法 YAML 带行号）、32.11（空文件）、32.12–32.13（只有 scope / 只有 name）、32.14–32.16（port 0 / 负数 / >65535）、32.17（extraPort 与主端口相同）、32.23（files 空列表）、32.24（路径含空格合法）、32.25（properties 为空合法）。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D21 | `metadata.name` 与 `metadata.description` 也强制必填 | 002 §2.3 明确标为必须；4.x 未列举但规范如此，测试单独覆盖 |
| D22 | 版本号严格 `major.minor.patch`，拒绝 `1.0.0-beta` / `+build` / `v` 前缀 | 002 §7.1 与全书一致地只说精确 major.minor.patch；预发布语义由市场的版本状态（draft/stable）承担 |
| D23 | 组件 ID 上限 63 字符 | ID 转换后要拼成版本化服务名（002 §10.4），必须符合 DNS 标签规则 |
| D24 | `extraPorts.name` 按 K8s Service 端口名规则校验（≤15 字符、小写、首尾字母数字） | 附录 B.7 明确该值直接用作 K8s Service 端口名；不提前拦会在 Step 16 生成 YAML 时才炸 |
| D25 | `extraPorts.port` 与主端口相同判为**错误**（32.17 允许"报错或警告"） | 同端口不可能同时提供两个协议，属于写错而非可容忍配置 |
| D26 | artifacts 文件路径必须是相对路径且不得用 `..` 越出仓库根 | 008 安全边界：产物文件会被下载到使用者机器，绝对路径与路径穿越是明确的攻击面 |
| D27 | 组件不能依赖自己；同一依赖不得重复声明 | 前者提前满足 7.9（自依赖报错），后者是明确的书写错误 |
| D28 | 不启用 yaml 严格模式（未知字段不报错） | 保持前向兼容：新组件写了新字段时，旧版 CLI 应能忽略而不是拒绝安装 |
| D29 | Step 4 不实现"版本化服务名"转换 | 那是环境变量注入与部署文件生成（Step 11/12）的职责，届时连同 002 §10.4 的转换规则一起测 |

---

### Step 5：brickkit.yaml 解析器 — ✅ 2026-08-12

**开发方式：TDD。** 先写 45 个业务行为测试（覆盖 5.1–5.22）→ 确认全部失败 → 实现 → 再补 40 个代码层单测。internal 覆盖率 99.5%，累计 347 个测试。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `internal/config/config.go` | `Config` / `Deploy` / `Source` / `Component` / `Resource` / `Binding` / `Installer` 结构 + 访问器（`EnabledState`、`IsPinned`、`IsDisabled`、`Ref`、`ComponentsByID`、`HasMultipleVersions`、`EnabledSources`、`RequireSignature`） |
| `internal/config/parse.go` | 四段式解析：YAML 语法（带行号）→ `${ENV_VAR}` 展开 → 结构形状检查 → 语义校验；`ExpandEnv` 导出供后续 Step 复用 |
| `internal/config/validate.go` | project / deploy / sources / components / resources 全量校验 |
| `internal/clierr/problems.go` | **重构**：把 Step 4 的问题收集器提升为共享的 `clierr.ProblemSet`，manifest 与 config 共用（后续 resolver、市场校验器也会用） |
| `internal/manifest/validate.go` | 导出 `ComponentIDProblem` / `IsExactVersion`，让 brickkit.yaml 与 Manifest 共用同一套组件 ID / 精确版本规则，不重复正则 |
| `internal/config/testdata/erp-project.yaml` | 取自 003 §2 的完整 ERP 配置（5.1 的验证输入） |

**关键设计：**

| 主题 | 实现 |
| --- | --- |
| `enabled` 三态 | 用 `*bool` 承载：`nil`=默认开启可被级联 / `true`=钉住 / `false`=显式关闭，通过 `EnabledState` 枚举暴露，`String()` 给出"钉住/默认开启/显式禁用"用于 up/status 输出 |
| `${ENV_VAR}` | 在 YAML **节点层**展开，只作用于值、不动 key（否则环境变量能改配置结构）；变量不存在时**保留原样**而非注入空串，使用者能一眼看出漏配 |
| 资源配额结构复用 | `components[].resources` 直接复用 `manifest.Resources`，Step 11 的三层合并（brickkit.yaml > component.yaml > CLI 默认值）不需要类型转换 |
| 多版本共存 | 同 ID 不同版本是**合法**的两个条目；只有同 ID 同版本重复才报错 |
| 跨条目一致性检查 | localPort 冲突、exposePort 冲突、binding 指向未声明的组件——这些只用配置本身就能判定，因此在解析阶段就拦住 |

**测试验证结果（22/22 项验证 + 85 个 config 测试全通过）：**

| # | 验证项 | 结果 |
| --- | --- | --- |
| 5.1 | 完整 ERP 项目配置解析成功 | ✅ `TestParseConfigFileFullProject` 对 003 §2 的完整配置逐字段断言 |
| 5.2 | `${ENV_VAR}` 解析为环境变量值 | ✅ MARKET_TOKEN / POSTGRES_PASSWORD / REDIS_PASSWORD 均正确替换 |
| 5.3 | 环境变量不存在时保留原样 | ✅ 保留 `${DEFINITELY_NOT_SET}` 字面量 |
| 5.4 | `enabled: true` → 钉住 | ✅ `EnabledPinned` |
| 5.5 | 不写 `enabled` → 默认开启可被级联 | ✅ `EnabledDefault` |
| 5.6 | `enabled: false` → 显式关闭 | ✅ `EnabledDisabled` |
| 5.7 | `components[].resources` 正确解析 | ✅ 只覆盖 limits.memory 时 requests 保持为空（交给 Step 11 合并） |
| 5.8–5.10 | 缺 project / 缺 deploy.target / target 非法 | ✅ 均给出字段名与合法值 |
| 5.11–5.13 | 组件缺 id / 缺 version / 非精确版本 | ✅ `components[0].xxx` 精确定位 |
| 5.14 | 同 ID 多版本解析为两个条目 | ✅ 并提供 `HasMultipleVersions` 供 Step 9 的 remove 校验使用 |
| 5.15 | sources 三种类型正确解析 | ✅ market/git/local + `enabled` 缺省为 true |
| 5.16 | resources + bindings 正确解析 | ✅ 含 database 名与 envPrefix |
| 5.17 | `installer.requireSignature` 正确解析 | ✅ 缺省为 **true**（附录 D.1） |
| 5.18–5.19 | 空 components / resources 列表 | ✅ 字段整体缺失也等价于空列表 |
| 5.20 | config 字段正确解析 | ✅ 空串/null/特殊字符/数字/布尔/数组/嵌套映射全部原样保留（CLI 不校验类型） |
| 5.21 | expose / exposePort / hostname | ✅ 并校验 K8s 环境下 expose 必须有 hostname |
| 5.22 | local / localPort | ✅ 并校验 localPort 必须与 local: true 同时出现 |

**额外提前覆盖：** 32.6–32.8（config 值边界）、32.9（空文件）、32.10（非法 YAML 带行号）、32.19（50 个组件）、13.12（localPort 冲突报错）。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D30 | `${ENV_VAR}` 只展开值、不展开 key | 否则环境变量可以改变配置结构（如 `${KEYNAME}: value`），这是注入面 |
| D31 | 变量缺失时保留 `${VAR}` 原样 | 与 Step 11「弱依赖缺失时完全不注入」同一哲学：宁可让人看见问题，也不要静默变成空字符串 |
| D32 | 把问题收集器提到 `clierr.ProblemSet` 共享 | Step 4 已有一份，Step 5 需要第二份；后面 resolver / 市场校验器还要用。重构后 manifest 的 108 个测试全绿，证明输出格式未变 |
| D33 | 从 manifest 导出 `ComponentIDProblem` / `IsExactVersion` | 组件 ID 与精确版本是 002 的规则，brickkit.yaml 与 component.yaml 必须用同一套；复制正则迟早会漂移 |
| D34 | `installer.requireSignature` 缺省 true | 附录 D.1 明确默认 true；安全默认优先（008），本地开发需要关就显式写 false |
| D35 | localPort / exposePort 写了但没写 local / expose 时**报错** | 静默无效是最难查的配置错误；003 明确这两个字段只在对应开关打开时生效 |
| D36 | K8s + expose 缺 hostname 在解析阶段就报错 | 该规则只依赖 brickkit.yaml 本身（deploy.target 与 expose 都在同一文件），无需等到 Step 16 生成 Ingress |
| D37 | 资源 `kind` 不做枚举限制 | 003 §5.2 只是"常见"清单；kind→环境变量前缀的映射是 Step 11（006）的职责，那时才需要判定具体 kind |
| D38 | **不**实现的部分：级联计算、资源配额三层合并、localPort 自动分配、exposePort 与 deployment.port 的冲突判定、密码硬编码告警 | 它们都需要 Manifest 或运行期信息（Step 11/12/13/15）。按你的要求：现在无法完整测试的业务逻辑不写半成品，留到对应 Step 一次做完并测全 |

**⚠️ 本 Step 延后未实现（须回填）：** D38 的五项已登记为《延后实现清单》 **P1–P5**，
回填 Step 分别为 11 / 11–12 / 13 / 12 / 15。因此 Step 5 状态为 ⚠️ 而非 ✅。

---

### Step 6：安装源实现（local / market / git） — ✅ 2026-08-12

**开发方式：TDD。** 先写 27 个业务行为测试（覆盖 6.1–6.13）+ 2 个版本化服务名测试 → 运行确认
**全部因功能缺失失败** → 实现 → 再补 67 个代码层单测。source 包 98.9%，internal 覆盖率 **99.4%**，
累计 443 个测试（Step 5 时为 347）。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `internal/manifest/servicename.go` | `ServiceName(id, version)`：002 §5.3 版本化服务名转换（`people/basic`+`1.0.0` → `people-basic-1-0-0`） |
| `internal/source/source.go` | `Client`：安装源优先级链 + Manifest/artifacts 缓存 + `--refresh` + 错误聚合；`ManifestCachePath` / `ArtifactDir` |
| `internal/source/fetcher.go` | `fetcher` 接口（local/git/market 统一抽象）+ `errNotFound` 语义 + `manifestMatches` |
| `internal/source/local.go` | 本地目录源：`<path>/<scope>/<name>/component.yaml`；路径不存在是配置错误 |
| `internal/source/git.go` | Git 源：懒 clone 到临时目录（本次运行内复用，Close 清理）；支持组件集合仓库与单组件仓库两种布局 |
| `internal/source/market.go` | 市场源：三个端点 + Bearer 认证 + JSON 信封/裸 YAML 双兼容 + 产物列表缓存 |
| `internal/source/credentials.go` | `.brickkit/credentials` 读取、过期判定、市场地址匹配（004 §5.3） |
| `internal/source/{source,client,fetchers,market,credentials,failures,testsupport}_test.go` | 94 个测试 + 市场 API Mock（httptest）与 git 仓库辅助 |

**关键设计：**

| 主题 | 实现 |
| --- | --- |
| 优先级链 | 按 `sources` 顺序依次尝试，**首个命中即返回**，靠后的源完全不访问（003 §6.5） |
| "没有" vs "失败" | 该源没有该组件 → 继续下一个源；路径不存在/clone 失败/市场不可达 → 记录为真失败。全部只是"没有"时报 004 §10.2 的组件未找到（含"已尝试的安装源"） |
| 缓存 | Manifest → `.brickkit/manifests/<id 转换>-<版本>.yaml`（版本保留点号，对齐 003 §7.3）；产物 → `.brickkit/artifacts/<版本化服务名>/<type>/<文件路径>` |
| 缓存健壮性 | 缓存损坏、内容与请求对不上 → 忽略缓存重新拉取，而不是报错。缓存不该成为故障源 |
| 产物失败不阻断 | 单个产物下载失败只记入 `ArtifactResult.Warnings`（⚠️、退出码 0），继续下一个文件（004 §10.1） |
| 认证 | `.brickkit/credentials` > `sources.authToken`；过期直接报错提示重新登录；凭据的 `marketUrl` 与源地址不符时不使用 |
| 并发安全 | git clone 与市场产物列表都用 `sync.Once` / 互斥保护，`go test -race` 全绿 |

**测试验证结果（13/13 项验证 + 94 个 source 测试全通过）：**

| # | 验证项 | 对应测试 / 结果 |
| --- | --- | --- |
| 6.1 | local 读取本地 component.yaml | `TestLocalSourceReadsComponentYAML`（逐字段断言 + 记录来源 id） |
| 6.2 | local 路径不存在时报错 | `TestLocalSourcePathMissing`：指出安装源 id、配置路径与解析后的绝对路径 |
| 6.3 | git clone 仓库并读取 Manifest | `TestGitSourceClonesRepoAndReadsManifest`（真实 `git init` 测试仓库）+ 单组件仓库变体 |
| 6.4 | git URL 不可达时报错 | `TestGitSourceUnreachableURL`：`CLONE_FAILED` + git 首行报错 + 检查网络建议 |
| 6.5 | market 调用 API 获取 Manifest | `TestMarketSourceFetchesManifest`：断言请求路径 `/api/v1/components/people/basic/versions/1.2.0/manifest` 与 `Bearer` 头 |
| 6.6 | market API 不可达时报错 | `TestMarketSourceUnreachable`：`NETWORK_UNREACHABLE` + 建议检查网络或改用本地源 |
| 6.7 | Manifest 缓存到 `.brickkit/manifests/` | `TestManifestCachedToDisk`（缓存文件可再次解析）+ `TestManifestServedFromCache`（第二次不再请求市场） |
| 6.8 | artifacts 缓存到 `.brickkit/artifacts/` | `TestArtifactsCachedByServiceName`、`TestArtifactsDownloadedFromMarket`（内容逐字节一致） |
| 6.9 | `--refresh` 强制重新拉取 | `TestRefreshForcesRefetch`：篡改缓存 → 不带 refresh 仍用缓存；带 refresh 覆盖回安装源内容 |
| 6.10 | 安装源优先级：靠前的优先 | `TestSourcePriorityFirstWins`（并断言靠后的市场零请求）+ `TestSourceFallsBackToNextSource` |
| 6.11 | `enabled: false` 时跳过 | `TestDisabledSourceIsSkipped` + `TestDisabledBrokenSourceDoesNotFail`（被禁用的源即使配置有误也不影响） |
| 6.12 | artifacts 按版本化服务名组织 | `TestArtifactsCachedByServiceName`：`department-tree-1-0-0/api-contract/proto/department/v1/department.proto` |
| 6.13 | 多版本 artifacts 独立存储 | `TestArtifactsOfMultipleVersionsAreIndependent`：两个目录内容互不覆盖，Manifest 缓存同样按版本区分 |

**额外覆盖：** 市场 401 → 提示 `brickkit login`；Token 过期（注入时钟）；凭据优先于 authToken；
凭据属于别的市场时不使用；组件在所有源中都没有；版本不匹配视为"该源没有"；缓存损坏自动重取；
产物路径穿越拒绝写入；产物下载/写入失败只警告；git 仓库版本不匹配不取产物。

**过程中被测试抓到的缺陷：** 初版 local 源取产物时只按 `<scope>/<name>` 定位、不校验版本，
6.13 的用例当场暴露"2.0.0 的产物目录里写进了 1.0.0 的文件"。修复为：目录型安装源（local/git）
取产物前必须先确认该目录的 `component.yaml` 正是该组件的该版本（见 D44）。

**⚠️ 本 Step 延后未实现（须回填，对应《延后实现清单》 P6–P12）：**

| # | 未实现的内容 | 现状 | 回填 Step |
| --- | --- | --- | --- |
| P6 | 组件 `sourceType`（开源 git / 闭源 registry）与 `gitUrl` | 市场返回也不解析；`--repo` 无从判断闭源组件 | 9 |
| P7 | 把 `internal/source` 接线到 CLI 命令 | 库已完备但**没有任何命令调用它**；产物警告只返回结构体，无人渲染到终端 | 9 |
| P8 | 签名校验接入下载路径 | `installer.requireSignature` 只在 Step 5 被解析，**当前下载链路完全不验签** | 20 |
| P9 | remove 时清理 Manifest / artifacts 缓存 | 缓存只增不减 | 9 |
| P10 | up 检测版本变化后自动拉取新版本 Manifest 与产物 | 库能力具备（`Manifest` + `DownloadArtifacts`），编排未做 | 15 |
| P11 | 市场服务端按 D47 / D48 的契约实现 | 客户端有 Mock 覆盖，**真实市场未验证过一次** | 18 |
| P12 | git 源指定分支 / tag / commit | 固定 `--depth 1` 拉默认分支 | 待设计书补 `ref` 字段 |

**已完整交付、不需要回填的部分：** 三种安装源的 Manifest 获取、优先级与 `enabled` 开关、
产物下载与按版本化服务名归档、Manifest/产物缓存与 `--refresh`、登录态与 authToken 的 Token 优先级、
全部错误路径（6.1–6.13 逐项有测试）。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D44 | 目录型安装源取产物前先比对 `component.yaml` 的 id + version | local/git 按 `<scope>/<name>` 定位，目录里放的是哪个版本只有 Manifest 知道；不校验就会跨版本串味（6.13 实测抓到） |
| D45 | git 源 clone 到**临时目录**，命令结束即删 | 持久层是 `.brickkit/manifests` 与 `.brickkit/artifacts`；再维护一份长期仓库副本会带来"何时更新"的新问题。`components/` 下的源码是 `--repo` 的职责（Step 9），与安装源无关 |
| D46 | git 仓库支持两种布局：`<scope>/<name>/component.yaml` 与根目录 `component.yaml` | 003 §6.3 的示例是组件集合仓库，而 007 §11.2 开源组件发布的是单组件仓库；两者都要能用 |
| D47 | 市场 Manifest 端点同时接受 JSON 信封 `{"success","data":{"manifest":…}}` 与裸 YAML 正文 | 007 §4.5 只给了端点没规定正文格式。Step 18 服务端按信封实现；裸 YAML 兼容自建/静态托管的简易市场 |
| D48 | 产物下载定为两步：`GET …/artifacts` 取列表 → `GET …/artifacts/{artifactId}/download?file=<路径>` | 007 §9.1 只列了端点。一个 artifact 可含多个文件，用 `?file=` 精确到文件，与 002 的 artifacts 模型一致。Step 18 按此实现服务端 |
| D49 | 凭据只在 `marketUrl` 与安装源 url 一致（或缺失）时使用 | 008 安全边界：绝不把 A 市场的 Token 发给 B 市场 |
| D50 | 任一安装源"真失败"就报该错误；全部只是"没有"才报组件未找到 | 使用者要修的通常是那个真失败（路径写错、仓库拉不下来、市场挂了）；把它淹没在"组件未找到"里最难排查 |
| D51 | 缓存损坏或内容对不上时忽略缓存重新拉取，不报错 | 缓存是加速手段，不该成为故障源；报错会逼使用者手工删文件 |
| D52 | 产物下载失败只警告，Manifest 获取失败才阻断 | 004 §10.1 明确：产物文件是开发时辅助，不影响运行时 |
| D53 | **不**实现 `sourceType` / `gitUrl`（开源/闭源标记） | 只有 Step 9 的 `--repo` 需要它，且命中 Manifest 缓存时该信息不可得，现在做只能做半个。市场返回该字段时客户端安全忽略（延续 D38）。已登记为清单 **P6** |
| D54 | `ServiceName` 放在 `manifest` 包 | 002 §5.3 是组件规范的一部分，Step 11/12/16 都要用同一份转换规则。D29 原计划推到 Step 11，但 6.12 的产物目录名提前需要它 |
| D55 | 覆盖率 99.4%：余下 3 条语句豁免 | HTTP 响应体读取中断、`yaml.Marshal` 失败、`filepath.Rel` 失败——都只能靠底层故障注入触发，按 D13 政策不为它们在生产代码里加抽象层 |

---

### Step 7：依赖解析引擎 — ⚠️ 2026-08-12

**开发方式：TDD。** 先写 28 个业务行为测试（覆盖 7.1–7.15）+ 2 个环境变量命名测试 →
运行确认**全部因功能缺失失败** → 实现 → 再补 19 个代码层单测。
resolver 包覆盖率 **100%**，internal 覆盖率 **99.5%**，累计 490 个测试（Step 6 时为 443）。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `internal/resolver/resolver.go` | `Resolver`：递归解析 + 去重 + 循环检测 + 强/弱依赖处理；`Graph`/`Node` 依赖图；`CheckUpgrade`（002 §7.7 五项检查）；`CheckResourceBindings`（Step 15 复用）；`FromSource` 适配 internal/source |
| `internal/manifest/envvar.go` | `EnvPrefix` / `EndpointEnvVar`：004 §5.6 的依赖地址变量命名（弱依赖警告要指名道姓说"哪个变量不会被注入"） |
| `internal/resolver/{resolver,resolver_internal,testsupport}_test.go` | 47 个测试：行为测试跑**真实的 internal/source + 本地安装源**，只有 Manifest 校验器不允许写出来的图（自依赖）才用构造的 Manifest |

**关键设计：**

| 主题 | 实现 |
| --- | --- |
| 三种"取不到"分流 | 根组件取不到 → 原样上抛安装源错误；**强**依赖取不到 → `强依赖缺失` 报错并指出**直接**依赖方；**弱**依赖取不到 → ⚠️ 警告（退出码 0）并说明 `XXX_ENDPOINT` 不会被注入 |
| 循环检测 | DFS 三色标记，环上节点才进路径（`x/root → x/a → x/b → x/a` 只打印 `x/a → x/b → x/a`） |
| 去重 | 按 **ID + 版本**去重，同 ID 不同版本是两个独立节点 → 多版本共存天然成立，不报错不阻断 |
| 依赖图信息 | 每个节点带 `Requires` / `Optional` / `MissingOptional` / `Dependents`。`Dependents` 供 002 §3.9 卸载检查（Step 9）直接复用 |
| 输出顺序 | `Nodes` 为后序遍历结果：**依赖先于依赖方**，Step 10 的拓扑排序与 `brickkit order` 在此之上做输出与分组 |
| 升级检查 | 002 §7.7 的检查 1/2/4/5 由一次递归解析全部覆盖，检查 3（资源绑定）单独实现且导出，Step 15 的 up 可直接复用 |

**测试验证结果（15/15 项验证 + 47 个 resolver 测试全通过）：**

| # | 验证项 | 对应测试 / 结果 |
| --- | --- | --- |
| 7.1 | 强依赖缺失时报错阻断 | `TestStrongDependencyMissingBlocks`：逐字对齐 004 §10.2 错误块；传递依赖缺失时指出**直接**依赖方 |
| 7.2 | 弱依赖缺失时警告但继续 | `TestOptionalDependencyMissingWarnsAndContinues`：⚠️、退出码 0、点名 `INFRA_REDIS_EVENT_BUS_ENDPOINT` 不会被注入 |
| 7.3 | 循环依赖时报错 | `TestDependencyCycleIsReported`：打印 `a/one@1.0.0 → b/two@1.0.0 → c/three@1.0.0 → a/one@1.0.0`；弱依赖同样参与成环判定 |
| 7.4 | 递归解析去重 | `TestSharedDependencyResolvedOnce`：断言 `department/tree` 只被**获取一次**（计数 Provider），并校验 `Dependents` |
| 7.5 | 多版本共存 | `TestMultipleVersionsCoexist`：A→X@1.0.0、B→X@2.0.0 两个节点共存，`Versions()` 可查出供 Step 9 写入 |
| 7.6 | 弱依赖 `optional: true` 正确识别 | `TestOptionalDependencyPresentIsResolved`：能取到时进图且**不产生**警告 |
| 7.7 | 深层递归（3 层以上） | `TestDeepRecursion`：A→B→C→D 全解析，且顺序为 D、C、B、A |
| 7.8 | 菱形依赖去重 | `TestDiamondDependencyDeduplicates`：D 只解析一次 |
| 7.9 | 自依赖报错 | `TestSelfDependencyIsReported`：解析器自身判环（`a/one@1.0.0 → a/one@1.0.0`）；另有测试锁定"component.yaml 写自依赖会被 Manifest 校验更早拒掉"的分层边界 |
| 7.10 | 升级时新版本 Manifest 不可获取 | `TestUpgradeNewVersionManifestUnavailable` |
| 7.11 | 升级时新版本强依赖检查 | 可满足 → 通过并列出 `NewDependencies`；不可满足 → `DEPENDENCY_MISSING` 阻断 |
| 7.12 | 升级时资源依赖已绑定检查 | 三个用例：已绑定通过 / 未声明报错 / 已声明但未绑定给该组件报错 |
| 7.13 | 升级时新版本弱依赖可获取 | `TestUpgradeOptionalDependencyMissingWarns`：警告但继续 |
| 7.14 | 升级时新版本引入循环 | `TestUpgradeIntroducesCycle`：打印循环路径并阻断 |
| 7.15 | 空依赖列表解析成功 | `TestEmptyDependencies`：`components: []` 与完全不写 `dependencies` 都通过 |

**⚠️ 本 Step 延后未实现（须回填，对应《延后实现清单》 P13–P15）：**

| # | 未实现的内容 | 现状 | 回填 Step |
| --- | --- | --- | --- |
| P13 | 解析结果写回 brickkit.yaml | 解析器只产出依赖图，**没有任何东西写文件** | 9 |
| P14 | 强依赖**被禁用**时报错（004 §10.3） | 目前只处理"取不到"；`ResolveConfig` 不看 `enabled`（级联属 P1） | 11 |
| P15 | `CheckUpgrade` 接线到 `up` | 检查逻辑齐备且有测试，但没有命令调用它 | 15 |

**不在本 Step 职责内（开发计划已分配给别的 Step，不是遗留）：** 拓扑排序与 `brickkit order`（Step 10）、
环境变量注入（Step 11）、级联启停计算（Step 11）。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D56 | 行为测试跑**真实的 internal/source + 本地安装源**，只有"Manifest 校验器不允许写出来的图"才用构造的 Manifest | 依赖解析的输入是真实 component.yaml，用假 Provider 测出来的绿灯说明不了解析链路是通的。自依赖（002 禁止）无法写成合法 component.yaml，只能构造 |
| D57 | 强依赖缺失的错误指出**直接**依赖方，而不是根组件 | 使用者要改的是那个直接依赖方的 Manifest 或安装源；报根组件会把人引到错误的位置 |
| D58 | 弱依赖也参与循环检测 | 环就是环：即使它是可选的，一旦两端都在图里，启动顺序仍然无解 |
| D59 | 缺失依赖的"原因"如实透传底层错误（市场不可达 ≠ 组件不存在） | 004 §10.2 的样例原因是"在所有安装源中均未找到"，但市场挂掉时照抄这句会误导使用者去查版本号 |
| D60 | 循环路径只打印环上的节点 | `x/root → x/a → x/b → x/a` 里 `x/root` 不在环上，打出来会让人误以为要改 root |
| D61 | `Node.Dependents` 在解析时顺手记录 | 002 §3.9 的卸载检查（Step 9）与"谁依赖了缺失组件"的提示都要用；解析时记录零成本，事后重算要再遍历一遍图 |
| D62 | 资源绑定检查一次报出全部未满足项，并区分"未声明"与"已声明但未绑定" | 延续 Step 4/5 的一次报全部风格；两种情况的修法不同（加 resources 条目 vs 加 bindings 条目） |
| D63 | `CheckResourceBindings` 导出 | 002 §7.7 只在升级时要求，但 Step 15 的 `up` 前置体检要用同一套规则；两份实现迟早会漂移 |
| D64 | `EnvPrefix` / `EndpointEnvVar` 放在 `manifest` 包 | 004 §4.5 的弱依赖警告必须点名具体变量名，否则使用者不知道自己该在代码里 `os.environ.get()` 哪个键。Step 11 的注入复用同一份实现（同 D54 的处理） |
| D65 | `ResolveConfig` 不过滤 `enabled` | 级联启停是 Step 11（P1）的职责；解析阶段先把依赖图建全，否则级联自己也没有图可算 |

---

### Step 8：配置备份与恢复（reset） — ⚠️ 2026-08-12

**开发方式：TDD。** 先写 21 个业务行为测试（库层 12 + 命令层 9，覆盖 8.1–8.7）→
运行确认**全部因功能缺失失败** → 实现 → 再补 16 个代码层单测。
backup 包覆盖率 **100%**，internal 覆盖率 **99.5%**，累计 527 个测试（Step 7 时为 490）。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `internal/backup/backup.go` | `Kind`（Initial / Last）+ `Path` / `Exists` / `SaveLast` / `Restore` / `Record`；备份缺失、IO 失败的完整错误文案 |
| `internal/cli/reset.go` | `brickkit reset` / `reset --last` 完整实现，输出逐字对齐 003 §7.2 与 004 §3.10 |
| `internal/cli/root.go` | `Options` 新增 `Now`（时钟注入点）+ `now()` 兜底，用于锁定"恢复时间"输出 |
| `internal/cli/cli_test.go` | `TestNotImplementedCommands` 移除 reset（已实现，命令清单同步） |
| `internal/backup/*_test.go`、`internal/cli/reset_test.go` | 37 个测试 |

**关键设计：**

| 主题 | 实现 |
| --- | --- |
| 两个快照互不干扰 | `.initial` 永远是 init 时的骨架；`.last` 只表示"上一步操作前"，每次覆盖，不是历史堆栈。`reset` 不会破坏 `.last`，`reset --last` 也不会动 `.initial` |
| 备份可反复使用 | 恢复不消耗备份文件，可以连续 reset |
| 误删也能救 | `brickkit.yaml` 被删掉后 `reset` 仍能把它写回来——这正是备份的意义 |
| 失败不动现场 | 备份缺失 / 不可读时报错，当前配置一个字节都不改 |
| 多环境隔离 | `--config brickkit.prod.yaml` 时备份名与恢复目标同步变化，与默认配置的备份互不串味 |
| 恢复不校验内容 | reset 正是"配置被改坏了"时的救急手段，在这里加 YAML 校验只会把唯一的退路也堵上 |

**测试验证结果（7/7 项验证 + 37 个测试全通过）：**

| # | 验证项 | 对应测试 / 结果 |
| --- | --- | --- |
| 8.1 | `brickkit init` 后 `.initial` 存在 | `TestInitialBackupCreatedByInit`：文件存在且与配置逐字节相同 |
| 8.2 | `brickkit add` 前自动创建 `.last` | ⚠️ **库层验证**：`TestSaveLastCopiesCurrentConfig` 等 5 个用例覆盖 `backup.SaveLast` 的全部行为（复制、覆盖、不动 `.initial`、目录重建、未初始化报错）。命令接线待 Step 9（P16） |
| 8.3 | `brickkit remove` 前自动创建 `.last` | ⚠️ 同上：`remove` 与 `add` 调用的是同一个 `SaveLast`，接线同属 P16 |
| 8.4 | `brickkit reset` 恢复到初始状态 | `TestResetRestoresInitial`：内容还原 + 输出三行逐字对齐样例；另有可重复 reset、配置被删后恢复两个用例 |
| 8.5 | `brickkit reset --last` 恢复到上一次 | `TestResetLastRestoresPreviousState`、`TestResetAndResetLastAreIndependent` |
| 8.6 | reset 后提示需要重新 `brickkit up` | `TestResetPrintsUpReminder`：`⚠️ 注意：恢复后需要重新执行 brickkit up 使变更生效` |
| 8.7 | 备份文件不存在时报错 | `TestResetWithoutInitialBackup`（退出码 1、现场不变）、`TestResetLastWithoutBackup`（提示 `.last` 的来历与改用 reset 的出路） |

**额外覆盖：** 多环境 `--config` 的 reset、`reset extra` 用法错误（退出码 2）、未初始化目录 reset、
注入时钟锁定 `恢复时间：2026-08-08T10:00:00Z`、权限与占位文件导致的 5 类 IO 失败路径。
另用真实二进制端到端跑通 init → 改坏配置 → reset → `reset --last`（无备份）四步，输出与设计书样例一致。

**⚠️ 本 Step 延后未实现（须回填，对应《延后实现清单》 P16）：**

| # | 未实现的内容 | 现状 | 回填 Step |
| --- | --- | --- | --- |
| P16 | `add` / `remove` 执行前自动调用 `backup.SaveLast`（8.2 / 8.3） | 备份能力已实现并测全；两个命令本身仍返回 NOT_IMPLEMENTED，`TestAddRemoveStillNotImplemented` 锁定了这一现状 | 9 |

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D66 | 8.2 / 8.3 拆成"库层现在测全 + 命令接线延后" | 按你的要求：现在测不了的（add/remove 未实现）不硬写半成品。`SaveLast` 的行为与失败路径此刻就能测全，Step 9 接线时只剩一行调用 |
| D67 | `reset` **不**为当前状态创建备份 | 003 §7.1 的备份时机只有 init / add / remove。若 reset 也写 `.last`，`reset --last` 的语义就从"回到上一次 add/remove 前"变成"撤销上一次 reset"，与设计书冲突 |
| D68 | 恢复时不校验备份内容是否为合法 YAML | reset 是配置被改坏时的救急路径；在这里加校验等于把唯一的退路也堵上。备份本身坏了属于极端情形，交给下一条命令的解析器报错 |
| D69 | `.last` 缺失与 `.initial` 缺失给不同的建议 | 前者是"还没执行过 add/remove"（正常状态，应提示改用 `brickkit reset`），后者是"备份被误删或走错目录"（异常状态，应提示从版本库恢复） |
| D70 | `Options.Now` 作为时钟注入点 | "恢复时间"要进输出；不注入就只能用正则模糊断言，锁不住格式。与 D18 的 `WorkDir` 同样是"不依赖进程全局状态"的处理 |
| D71 | 输出里的备份路径用相对项目根的形式 | 003 §7.2 的样例就是 `.brickkit/backup/brickkit.yaml.initial`；打印绝对路径在测试临时目录里会变成一长串噪音 |

---

### Step 9：`brickkit add` / `remove` 命令 — ✅ 2026-08-12

**开发方式：TDD。** 先写 36 个业务行为测试（add 23 + remove 13，覆盖 9.1–9.25）→
运行确认**全部因功能缺失失败** → 实现 → 再补 40 个代码层单测。
internal 覆盖率 **98.6%**，累计 624 个测试（Step 8 时为 527）。

**本 Step 同时回填了 5 条延后项：P6、P7、P9、P13、P16**（清单中已标 ✅ 并注明落地位置）。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `internal/cli/add.go` | add 全流程：解析引用 → 已存在确认 → 备份 → 递归解析 → clone 资格检查 → 下载产物 → 写配置 → clone；输出对齐 004 §3.3 |
| `internal/cli/remove.go` | remove 全流程：定位版本 → 依赖方检查 → 备份 → 删条目 → 清缓存 → 删源码目录；输出对齐 004 §3.4 |
| `internal/cli/component.go` | `parseComponentRef`（`id@version` 解析与校验）、`confirm`（交互确认）、依赖强弱分类 |
| `internal/config/edit.go` | **brickkit.yaml 原地编辑器**：在 YAML 节点层增删组件，保留注释、顺序与 `${ENV_VAR}`；`restoreBlankLines` 还原顶层块之间的空行 |
| `internal/workspace/workspace.go` | 组件源码工作区：`Clone`（完整仓库）/ `RemoveSource` / `SourceDir`，空 scope 目录自动收走 |
| `internal/source/{fetcher,local,git,market,source}.go` | 新增 `Origin`（开源 git / 闭源 registry / 本地）与 `Client.Origin`，供 `--repo` 判断 |
| `internal/cli/root.go` | `Options` 新增 `Stdin`（交互确认的输入注入点） |
| 测试文件 | `cli/{add,remove,component,testsupport_component}_test.go`、`config/edit_test.go`、`workspace/workspace_test.go`、`source/origin_test.go` 共 76 个测试 |

**关键设计：**

| 主题 | 实现 |
| --- | --- |
| 配置写入 | 走 YAML 节点层，**不经过 Config 结构体**——否则 `${POSTGRES_PASSWORD}` 会被展开后的明文密钥写回文件。注释、顺序、空行排版都保留 |
| 不写 enabled | add 自动添加的条目只写 `id` + `version`（004 §3.3 关键规则），`enabled: true` 只能是用户手写的显式意图 |
| 备份即退路 | add / remove 改配置前一律 `backup.SaveLast`，`brickkit reset --last` 可完整撤销一次操作 |
| 失败不留半场 | 备份失败、强依赖缺失、`--repo` 资格不符都在**写配置之前**中止，配置一个字节不动 |
| 依赖方检查 | remove 只读"配置里已有组件"的 Manifest，**不做递归解析**：递归会因为某个无关组件的依赖缺失而整体失败，把"移除"这条退路也堵死 |
| 源码目录按 ID | `components/<scope>/<name>/` 与版本无关，同 ID 还有其他版本时不删 |
| 产物不阻断 | 下载失败只警告（004 §10.1），安装照常完成 |

**测试验证结果（25/25 项验证 + 76 个测试全通过）：**

| # | 验证项 | 对应测试 / 结果 |
| --- | --- | --- |
| 9.1 | add 正常添加组件 | `TestAddWritesComponentToConfig`：条目写入 + `📦 添加` / `✅ 已写入 brickkit.yaml（1 个组件）` |
| 9.2 | add 递归拉取依赖 | `TestAddPullsDependenciesRecursively`：erp/backend 及 3 个依赖全部入库，输出区分 `依赖` / `弱依赖` |
| 9.3 | add 自动下载 artifacts | `TestAddDownloadsArtifacts`：`.brickkit/artifacts/<版本化服务名>/<type>/…` + Manifest 缓存 |
| 9.4 | `--yes` 非交互 | `TestAddYesOnFreshComponentHasNoPrompt`、`TestAddSameVersionWithYesRefreshesWithoutPrompt`（空 stdin 不阻塞） |
| 9.5 | 同 ID 不同版本共存 | `TestAddSecondVersionCoexists`：两个条目 + `多版本共存` 提示 |
| 9.6 | 同版本提示已存在 | `TestAddSameVersionPromptsWhenExisting`：回答 n → 配置不变、无重复条目 |
| 9.7 | 同版本 + `--yes` 直接刷新 | `TestAddSameVersionWithYesRefreshesWithoutPrompt`：篡改缓存后被重新拉取 |
| 9.8 | remove 有强依赖方报错 | `TestRemoveBlockedByStrongDependent`：列出全部依赖方 + `请先移除依赖方`，配置不变 |
| 9.9 | 多版本未指定版本报错 | `TestRemoveMultiVersionRequiresVersion`：逐字对齐 004 §3.4 样例 |
| 9.10 | 指定版本移除成功 | `TestRemoveWithVersionKeepsOtherVersion`：另一版本的产物与缓存完好 |
| 9.11 | 自动删除源码目录 | `TestRemoveDeletesSourceDirectory` + `TestRemoveKeepsSourceDirWhenAnotherVersionRemains` |
| 9.12 | 清理 artifacts 缓存 | `TestRemoveDeletesEntryAndCaches` |
| 9.13 | 清理 Manifest 缓存 | 同上 |
| 9.14 | `--repo` clone 开源组件 | `TestAddRepoClonesOpenSourceComponent`：`components/people/basic/.git` 存在（完整仓库） |
| 9.15 | `--repo` 闭源组件报错 | `TestAddRepoClosedSourceComponentFails`：`clone 失败：该组件为闭源组件` + registry 说明 |
| 9.16 | `--repo` 目录已存在报错 | `TestAddRepoExistingDirectoryFails`：已有源码一个字节不动 |
| 9.17 | `--repo-all` clone 全部开源依赖 | `TestAddRepoAllClonesOpenSourceAndSkipsClosed` |
| 9.18 | `--repo-all` 跳过闭源并提示 | 同上：`⏭️ authorization/rbac … 闭源组件，跳过 clone` + 汇总行 |
| 9.19 | 已存在组件 + `--repo` 只 clone | `TestAddRepoOnConfiguredComponentOnlyClones`：配置逐字节不变 |
| 9.20 | add 不写 enabled | `TestAddDoesNotWriteEnabledField` |
| 9.21 | add 不存在的组件报错 | `TestAddUnknownComponent`：配置不变 |
| 9.22 | 版本号格式错误报错 | `TestAddInvalidVersion`（退出码 2，用法错误） |
| 9.23 | remove 不存在的组件报错 | `TestRemoveUnknownComponent`、`TestRemoveUnknownVersion`（提示现有版本） |
| 9.24 | remove 弱依赖方不阻止 | `TestRemoveWeakDependentWarnsButProceeds`：⚠️ 指出受影响组件与环境变量 |
| 9.25 | 产物下载失败警告但继续 | `TestAddArtifactDownloadFailureWarnsButContinues`：退出码 0，条目照常写入 |

**额外覆盖：** 注释与 `${ENV_VAR}` 保留（且不写回展开后的密钥）、`reset --last` 撤销 add / remove、
强依赖缺失时配置不变、弱依赖缺失的 add 警告、`--repo` clone 失败不留残目录、
`--repo-all` 跳过已有目录与本地源组件、缓存命中时的 artifacts 提示、
备份/缓存/源码目录的 5 类 IO 失败路径。
另用真实二进制端到端跑通 init → add（含弱依赖警告）→ 被依赖时 remove 阻断 → remove 成功。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D72 | 配置写入走 YAML 节点层，不经过 `Config` 结构体 | `Config` 里的值是 `${ENV_VAR}` **展开后**的结果（Step 5 的 ExpandEnv）。用它回写等于把 `.env` 里的密钥明文落盘，是严重的安全事故 |
| D73 | `Save` 后按原文还原顶层块之间的空行 | yaml.v3 的节点模型没有空行，往返一次就把 project / deploy / components 挤成一坨。brickkit.yaml 是人要读、要手改的文件，排版不能越编辑越糟。（行尾注释前的对齐空格仍会被规范化，属 yaml 库行为，不影响内容） |
| D74 | `--repo` 的资格检查（闭源 / 目录已存在 / 无仓库地址）放在**写配置之前** | 004 §3.3 的闭源提示是"你仍然可以正常安装（不含 --repo）"，说明此时不该已经装了一半。先检查后写，命令要么全做要么不做 |
| D75 | `--repo-all` 遇到"目录已存在"**跳过**而不是报错（`--repo` 仍报错） | 004 的行为表把这行写在 `--repo` / `--repo-all` 之下未区分，但批量 clone 若因为一个已存在目录整体失败，增量工作流就没法用了。单点操作（`--repo`）报错、批量操作跳过并提示，是更符合意图的读法 |
| D76 | `Origin` 绕开 Manifest 缓存直接问安装源 | 缓存里存的是 component.yaml，本来就不含 `sourceType` / `gitUrl`。`--repo` 是低频操作，多一次请求换信息准确，好过为它引入一套缓存元数据文件（P6 当初推迟的正是这个设计） |
| D77 | remove 的依赖方检查不做递归解析 | 递归解析会因为**任何一个**无关组件的依赖缺失而整体失败，导致"配置坏了就没法 remove"。只读配置内已有组件的 Manifest，取不到就警告"无法确认"，不假装没有依赖方 |
| D78 | 源码目录只在同 ID 最后一个版本被移除时删除 | `components/<scope>/<name>/` 按组件 ID 组织、与版本无关；`remove X@1.0.0` 时若 X@2.0.0 还在，删掉源码会连带毁掉另一个版本的工作区 |
| D79 | 参数值写错（版本号非法、ID 非法）统一按用法错误（退出码 2） | 与 D9 的"缺参属用法错误"一致；`add X@abc` 和 `add`（缺参）本质都是命令行写错，退出码不该不同 |
| D80 | add 一律先 `SaveLast` 再动配置，即使最终没有改动 | 003 §7.1 写的是"每次 add / remove 前"。多一次等价备份无害，漏一次就没有退路 |

---

### Step 10：拓扑排序与 `brickkit order` — ⚠️ 2026-08-12

**开发方式：TDD。** 先写 16 个业务行为测试（resolver 10 + cli 6，覆盖 10.1–10.7）→
运行确认**全部因功能缺失失败** → 实现 → 再补 14 个代码层单测。
`resolver.Order` 覆盖率 **100%**，internal 覆盖率 **98.3%**，累计 654 个测试（Step 9 时为 624）。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `internal/resolver/order.go` | Kahn 算法拓扑排序：`Order(*Graph) (*Plan, error)`、`Plan`/`PlanStep`、`Independent()`/`Last()`、环检测与 `sortRefs` |
| `internal/cli/order.go` | `brickkit order`：启动顺序表 + 要点 + 依赖图，输出对齐 004 §3.8 |
| `internal/resolver/order_test.go`、`order_internal_test.go`、`internal/cli/order_test.go` | 30 个测试 |

**关键设计：**

| 主题 | 实现 |
| --- | --- |
| 只有强依赖成边 | 弱依赖可能根本不启动（004 §4.5），让它约束顺序等于把"可选"偷偷变成"必选"；弱依赖只在"可跳过"一节与依赖图里标注 |
| 顺序可复现 | 同层内按组件 ID + 版本号（数字序）排序。部署文件要能复现，不能因 map 遍历顺序而变 |
| 环检测是最后一道防线 | Manifest 校验（自依赖）与递归解析（成环）都放过的图，排序阶段仍会拦下，并用 DFS 找出**真实的环**打印路径（环外的尾巴不打印） |
| 多版本各自独立 | 排序单位是 `ID + 版本`，`people-basic-1-0-0` 与 `people-basic-2-0-0` 是两个独立节点 |
| 只读命令 | 不改配置、不启动容器；Manifest 走缓存，装好之后离线可用 |

**测试验证结果（7/7 项验证 + 30 个测试全通过）：**

| # | 验证项 | 对应测试 / 结果 |
| --- | --- | --- |
| 10.1 | 正确的拓扑排序结果 | `TestOrderERPChain`（004 §4.3 的 ERP 依赖链，逐条断言先后关系）+ 命令层 `TestOrderPutsDependenciesFirst` |
| 10.2 | 弱依赖不参与排序约束 | `TestOptionalDependencyDoesNotConstrainOrder`：弱依赖的 ID 特意排在字母序后面，若参与约束就会被拉到前面；另有"同时被强/弱依赖引用时按强算" |
| 10.3 | 输出格式正确 | `TestOrderOutputFormat`：编号、版本化服务名、`← 依赖 1`、可独立启动、必须最后启动、依赖图、`→` |
| 10.4 | 循环依赖报错 | `TestOrderDetectsCycle`（排序层）+ `TestOrderReportsCycle`（命令层）+ 自环、多个环、环外尾巴三个补充用例 |
| 10.5 | 无依赖组件排第一 | `TestOrderERPChain`：`department/tree` 位次为 1；`Plan.Independent()` 列出全部无强依赖组件 |
| 10.6 | 多版本各自独立排序 | `TestOrderMultipleVersionsIndependently` + 命令层 `TestOrderShowsEachVersionSeparately` |
| 10.7 | 空组件列表时提示 | `TestOrderOnEmptyProject`：`📋 当前项目没有组件` + `brickkit add` 引导，退出码 0 |

**额外覆盖：** 排序确定性（连排 6 次结果一致）、依赖序号升序、图外依赖被跳过、
单组件项目不打印"必须最后启动"、弱依赖缺失时的 ⚠️ 与依赖图标注、
Manifest 不可用时报错、未初始化目录、多余参数。
另用真实二进制端到端跑通 5 组件的 ERP 依赖链与空项目两种情形。

**⚠️ 本 Step 延后未实现（须回填，对应《延后实现清单》 P17）：**

| # | 未实现的内容 | 现状 | 回填 Step |
| --- | --- | --- | --- |
| P17 | order 按级联结果过滤组件 | 当前列出 brickkit.yaml 中的**全部**组件，`enabled: false` 的也在内。`TestOrderCurrentlyIgnoresEnabledFalse` 锁定了这一现状，Step 11 实现级联后该用例会失败，提醒回来改 | 11 |

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D81 | 排序只用强依赖成边 | 开发计划 Step 10 目标明确"弱依赖不参与排序约束"；且弱依赖缺失时若成边，排序会因为一个可选组件而失败 |
| D82 | 同层内按 ID + 版本号排序（版本按数字比较） | Kahn 算法在同层有多个候选时顺序不确定，直接影响生成的 compose/K8s 文件；固定 tie-break 让输出可复现、可 diff |
| D83 | `← 依赖 N` 列的是**直接强依赖**的序号 | 004 §3.8 样例里 `auth-rbac ← 依赖 1, 2` 与同书 §4.3 的依赖关系（auth/rbac 只依赖 department/tree）对不上，样例自身不自洽；取"直接依赖"这一可验证的定义 |
| D84 | "必须最后启动"= 启动顺序中的最后一个组件 | 004 §3.8 样例写的是 `erp-backend（依赖最多）`，但同一段的编号列表里 `portal-user-frontend` 才是第 5 个（最后）。样例前后矛盾，取"顺序上的最后一个"这一无歧义定义，并把理由改成"需等前 N 个组件就绪" |
| D85 | 排序阶段独立做环检测，而不是信任解析阶段 | 解析器的环检测走的是 DFS 路径，排序走的是入度；两者独立成立才说明依赖图真的无环。且 `Order` 是导出 API，Step 12/15 可能传入手工构造的图 |
| D86 | order 不做级联过滤（登记为 P17） | 级联计算需要 `enabled` 三态 + 依赖图，是 Step 11 的完整职责；在这里做半个级联，两处逻辑迟早漂移 |

---

### 插入 A：平台自测组件（demo/hello、demo/caller） — ✅ 2026-08-12

**为什么做这件事：** Step 11–15（环境变量注入、compose 生成、迁移、up/down/status）
在没有真实镜像时只能"自证"——断言生成出来的文本长什么样，却没法回答
"这份文件真的能把容器跑起来吗"。这两个组件就是给平台自己用的测试夹具。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `tests/components/demo-hello/` | 最小 HTTP 组件：`/healthz`、`/api/v1/hello`（读 configSchema 注入的 GREETING）、`/api/v1/env`（回显平台注入的变量）；含 component.yaml、openapi.json 产物、Dockerfile、6 个单测 |
| `tests/components/demo-caller/` | 调用方组件：强依赖 `demo/hello@1.0.0`、弱依赖 `demo/bus@1.0.0`（故意不存在）、资源依赖 database/postgresql、`migration.command`；含 9 个单测 |
| `Makefile` | 新增 `make test-components`（各自独立 module 的单测，已并入 `test-all`）与 `make demo-images`（构建三个镜像） |
| `.gitignore` | `components/` → `/components/`：原规则把 `tests/components/` 也忽略了，自测组件根本进不了库 |

**这两个组件覆盖的平台承诺：**

| 承诺 | 由谁验证 | 真容器实测结果 |
| --- | --- | --- |
| 依赖地址注入 + DNS 即注册中心（002 §5） | caller 读 `DEMO_HELLO_ENDPOINT` 调 hello | ✅ 返回上游 `demo/hello@1.0.0` 的响应 |
| 弱依赖缺失完全不注入变量、组件自行降级（002 §3.4） | caller 的 `/api/v1/status` | ✅ `eventBus: degraded`，`DEMO_BUS_ENDPOINT` 为空 |
| configSchema 配置项注入（004 §5.6） | hello 的 `GREETING` | ✅ 响应中的问候语随注入变化 |
| 资源连接信息注入（006） | caller 的 `DATABASE_*` 回显 | ✅ 变量到位 |
| 迁移在主服务前执行、失败以非 0 退出码阻断（005） | `caller migrate` | ✅ 数据库可达→0；不可达→1；`MIGRATION_SHOULD_FAIL=1`→1 |
| 健康检查只看本进程（002 §9.4） | hello / caller 的 `/healthz` | ✅ 上游不可达时仍 200 |
| 容器非 root 运行（开发计划 21.10） | 镜像 | ✅ `uid=10001(brickkit)` |
| 多版本共存（002 §3.6） | hello 的 1.0.0 / 2.0.0 两个 tag | ✅ 两个容器同时在跑 |

**验证方式：** `make test-components`（15 个单测）+ `make demo-images` 构建镜像 +
手写一份最小 compose 真起容器，逐条 curl 验证上表。compose 中 `demo-hello-1-0-0`
等服务名即版本化服务名（002 §5.3），Step 12 生成的文件应当与这份手写文件形状一致。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D87 | 自测组件放 `tests/components/<组件>/`，各自独立 Go module | 002 §1.6 要求"一个组件一个仓库、不支持 monorepo 子目录"。这里放的是**源码**，需要时 `git init` 就是一个独立组件仓库（git 安装源与 `--repo` 测试正需要）；独立 module 也保证它们不被编进 CLI 的依赖图 |
| D88 | 不占用 Phase 5（Step 21–28）的组件 ID，另起 `demo/` scope | Phase 5 是业务组件，服务于 E2E 验收；这两个是平台夹具，长期供 Step 11–17、32、33、37 复用。混用会让"验收"与"自测"互相污染 |
| D89 | 迁移命令不做真实建表，而是"可达性检查 + 可控失败开关" | 平台要验证的是**编排**（迁移先于主服务、失败能阻断），不是某张业务表长什么样。`MIGRATION_SHOULD_FAIL` 让 Step 14 的失败路径可以确定性复现；同时真连一次数据库，证明资源注入不是摆设 |
| D90 | 组件零外部依赖（只用标准库） | 夹具要能在离线、无 GOPROXY 的机器上构建；也避免夹具自身的依赖问题干扰平台测试 |
| D91 | 仓库根 `.gitignore` 的 `components/` 改为 `/components/` | 原规则未锚定，连 `tests/components/` 一起忽略了。用户项目的 `.gitignore`（Step 3 生成）仍按 003 §11 原文写 `components/`，不改 |

---

### Step 18-A：市场领域模型与发布校验器 — ✅ 2026-08-12

**开发方式：TDD。** 先写 55 个业务行为测试（覆盖 18.6–18.11 与 007 §18 的全部 Manifest 规则）→
运行确认全部失败 → 实现 → 再补 44 个代码层单测。market-server 覆盖率 **98.5%**，99 个测试。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `market-server/internal/model/model.go` | 领域常量（来源类型、可见性、版本状态、错误码、审计动作）+ `APIError`（007 §18 的错误结构）+ Component / Version / ArtifactRecord / User / Token / AccessPolicy / AuditEntry |
| `market-server/internal/model/manifest.go` | 市场侧的 Manifest 结构（独立于 CLI 实现） |
| `market-server/internal/model/dependency.go` | 依赖的三种写法解析：标量、`{id: "x@ver"}`、`{id, version}`（007 §3.7 用的是第三种） |
| `market-server/internal/validator/validator.go` | 发布校验：形状检查 → 字段校验（一次报全部）→ 保留变量 → 闭源 API 契约 |
| `market-server/internal/validator/reserved.go` | 保留变量规则与 `EnvVarName` 转换（004 §5.6.1 / 007 §18.1） |

**测试验证结果（18.6–18.11 全部通过）：**

| # | 验证项 | 对应测试 |
| --- | --- | --- |
| 18.6 | 保留变量校验（源头防御） | `TestReservedVariableConflictIsRejected`：9 类保留模式逐个验证（精确 / 后缀 / 前缀） |
| 18.7 | 错误信息包含冲突详情 | `TestReservedVariableConflictDetails`：configKey、envVarName、conflictPattern、suggestion + componentId/version，多个冲突一次全报 |
| 18.8 | 闭源无 API 契约拒绝 | `TestClosedSourceWithoutAPIContractIsRejected` |
| 18.9 | 闭源有 API 契约成功 | `TestClosedSourceWithAPIContractPasses` |
| 18.10 | 开源无契约不强制 | `TestOpenSourceWithoutAPIContractPasses`；另有"闭源但不提供 API（无 port）也不强制" |
| 18.11 | migration 格式校验 | `TestMigrationCommandMustBeArray`（形状检查给出精确字段名）+ 空数组 / 空参数 |

**额外覆盖：** deployment.resources 格式、Manifest 全部必填字段（12 个用例）、artifacts 格式
（container 用 reference、其余用 files）、type/format 自由字符串不校验取值、依赖必须精确版本、
资源依赖 kind+engine、sourceType 合法性、开源必须给 gitUrl、请求版本与 Manifest 版本一致、
一次报出全部问题、未知字段忽略（前向兼容）。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D92 | 市场**独立实现** Manifest 结构与校验，不复用 CLI 的 `internal/manifest` | 一是 Go 的 internal 包本就跨不了 module；二是 007 §18 的定位就是"源头防御"，两侧各自成立才叫双保险——有人直接调 API 绕过 CLI 时，市场仍拦得住 |
| D93 | 先做形状检查再解码结构体 | 否则 `migration.command: "python x"` 只会得到 json 库的类型错误，用户不知道是哪个字段。与 CLI 侧 Step 4 的做法一致 |
| D94 | `{envPrefix}_*` 保留模式在发布时**不校验** | envPrefix 是使用者在 brickkit.yaml 里定的，发布时市场无从知道。那一层交给 CLI 注入时的运行期防御（004 §5.6.1：警告但跳过） |
| D95 | 依赖解析同时兼容三种写法 | CLI 发布时原样转换 component.yaml（标量/映射两种），而 007 §3.7 的发布示例用的是 `{id, version}` 分列写法。收下三种，输出统一成映射 |
| D96 | 请求级问题与 Manifest 问题合并成一个错误上报，错误码以 Manifest 优先 | 发布者一次就能看到所有要改的地方；Manifest 不合规是更根本的问题，错误码指向它更有指导性 |

---

### Step 18-B：仓储层与产物存储 — ✅ 2026-08-12

**开发方式：TDD + 行为契约。** 先写一份**共用的行为契约测试**（12 组场景），
让内存实现与 PostgreSQL 实现跑同一套断言 → 内存实现转绿 → PostgreSQL 实现转绿
（对着本机真实 PG 跑）。产物存储同样一套契约跑内存与真实 RustFS 两个实现。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `market-server/internal/repo/repo.go` | `Repository` 接口（组件 / 版本 / 产物 / 访问策略 / 用户令牌 / 审计）+ `ErrNotFound`、`ErrConflict` + 查询条件 |
| `market-server/internal/repo/memory.go` | 进程内实现：单测与本地模式用 |
| `market-server/internal/repo/postgres.go` | PostgreSQL 实现（lib/pq），含 `Migrate`（幂等建表）与仅供测试的 `TruncateAll` |
| `market-server/internal/repo/sort.go` | 组件按 ID 升序、版本按**数字**倒序（1.10.0 必须排在 1.2.0 之前） |
| `market-server/migrations/001_init.sql`、`internal/repo/schema.sql` | 007 §10 的 9 张表 + 索引；`go:embed` 进二进制，部署时不用单独分发 SQL |
| `market-server/internal/storage/artifacts.go` | `ObjectKey` / `VersionPrefix` + 内存实现 + `S3Store`（RustFS，含 `EnsureBucket`） |
| `Makefile` | 新增 `make test-market-integration`：带 `.env` 跑真实 PostgreSQL + RustFS 的集成测试 |

**测试验证结果：**

| # | 验证项 | 结果 |
| --- | --- | --- |
| 18.22 | artifacts 按版本独立存储 | ✅ 两处保证：仓储层产物记录以 `<组件,版本>` 为主键；对象键为 `components/<组件ID>/<版本>/<类型>/<文件>`，版本在路径里，升级不覆盖旧版本契约（002 §7.10） |
| — | 版本不可重复（18.14 的仓储层保证） | ✅ 同 ID 同版本 `CreateVersion` 返回 `ErrConflict`（PG 靠唯一键，内存靠 map 检查） |
| — | 两个实现语义一致 | ✅ 12 组契约场景在 memory 与 postgres 上**全部跑通**：组件 CRUD、搜索（关键字/标签交集/可见性/额外放行/blocked/分页）、可见性与状态、下载计数、版本创建与列表排序、软删除、产物按版本独立、上传标记幂等、访问策略覆盖写、用户唯一与令牌、审计只追加、ErrNotFound |
| — | 产物存储两个实现语义一致 | ✅ 内存与**真实 RustFS** 跑同一份契约：写读、覆盖写、按版本前缀列举、不存在对象报 `ErrObjectNotFound`、bucket 自动创建 |
| — | 路径穿越防御 | ✅ `ObjectKey` 把 `../../../etc/passwd` 规整回版本前缀之内（008） |

真实环境实测：`make test-market-integration` 对 `my-postgres`（PostgreSQL 15.18）
与 `my-rustfs` 全绿；库表已在 `brickkit_market` 中建好（9 张表）。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D97 | 仓储层写一份**行为契约测试**，内存与 PostgreSQL 共用 | 两个实现最怕语义漂移：内存版全绿、上了真库才炸。共用一份断言，PG 那一份只在设置 `MARKET_TEST_DATABASE_URL` 时跑，其余环境自动跳过，离线开发不受影响 |
| D98 | 版本排序在 Go 里做，不在 SQL 里做 | SQL 的字符串序会把 `1.10.0` 排在 `1.2.0` 前面，"最新版本"就错了。两个实现共用同一个 `CompareVersions`，排序结果必然一致 |
| D99 | 可见性作为**查询条件**下推到仓储层 | 鉴权逻辑仍在服务层，但"能看到哪些"必须参与 SQL 过滤——否则先分页再过滤，翻页会缺条目 |
| D100 | 建表 SQL 用 `go:embed` 打进二进制，启动时幂等执行 | 部署只需要一个二进制 + 环境变量，不用额外分发/挂载 SQL 文件；与《市场部署与运维指南》的容器化部署一致 |
| D101 | 对象键把版本放在路径里 | 18.22 的要求由**路径结构**保证，而不是靠代码里记得加版本；也让"按版本清理/列举"变成一次前缀操作 |
| D102 | `TruncateAll` 只给测试用，并在注释里写明 | 契约测试要求每个用例从干净的库开始；生产代码里没有任何调用点 |

---

### Step 18-C：服务层与认证 — ✅ 2026-08-12

**开发方式：TDD。** 先写 38 个业务行为测试（覆盖 18.12–18.14、18.17–18.21、18.24、18.25）→
运行确认全部失败 → 实现 → 再补 33 个代码层单测（含故障注入）。
market-server 集成覆盖率 **91.6%**，累计 204 个测试。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `market-server/internal/service/service.go` | 发布编排、产物上传下载、查询（Manifest/版本/详情/搜索）、状态与可见性变更、软删除、访问策略、审计 |
| `market-server/internal/service/auth.go` | 注册（bcrypt）、登录签发令牌、令牌认证与过期、鉴权规则（匿名/所有者/管理员/访问策略） |

**分层约定（写在包注释里）：** validator 只判断"Manifest 合不合规"；repo 只管存取；
**service 是唯一知道"谁能做什么"的地方**；handler（18-D）只做协议转换。

**测试验证结果：**

| # | 验证项 | 对应测试 / 结果 |
| --- | --- | --- |
| 18.12 | 可见性控制（private） | `TestPrivateComponentAccessControl`：所有者 ✅ / 被授权用户 ✅ / 未授权用户 403；组织级授权单独一例；搜索结果同样按权限过滤 |
| 18.13 | 审计日志记录 | `TestAuditRecordsPublishAndChanges`：发布、可见性变更、状态变更、删除四类动作都有记录且带操作者；注册/登录/下载也有 |
| 18.14 | 版本不可重复 | `TestPublishRejectsDuplicateVersion`：返回 `VERSION_ALREADY_EXISTS` + componentId/version 详情 |
| 18.17 | 版本状态变更 | `TestSetVersionStatus`；`TestOnlyAdminCanBlockVersion`：deprecated 归所有者，blocked 只有管理员（007 §6.3） |
| 18.18 | 可见性变更 | `TestSetVisibilityRequiresOwner`：非所有者 403，非法值 400 |
| 18.19 | 用户注册 | `TestRegisterAndLogin`：bcrypt 哈希落库、对外不返回哈希；重名冲突、密码长度等 4 类入参校验 |
| 18.20 | 用户登录 | 同上：签发带有效期的令牌；密码错与用户不存在返回**同样**的错误（避免枚举用户名） |
| 18.21 | 未认证访问 private 拒绝 | `TestPrivateComponentDeniedForAnonymous`：Manifest / 详情 403，搜索结果为空 |
| 18.24 | 已发布版本不可物理删除 | `TestDeleteVersionIsSoftDelete`：库里记录仍在（status=deleted），对外 404，且版本号**不可回收**（再次发布仍报重复） |
| 18.25 | blocked 不可安装 | `TestBlockedVersionCannotBeInstalled` / `TestBlockedComponentCannotBeInstalled`：返回 `COMPONENT_BLOCKED` 而不是含糊的 404 |

**额外覆盖：** 发布必须认证、非所有者不能发布、Manifest 校验接入、组件 ID 与 Manifest 不一致、
产物元数据落库、上传文件必须在 files 列表内、**文件没传齐不能转 stable**、下载累加计数与审计、
draft 版本只有所有者可见、令牌过期/孤儿令牌/空白令牌、管理员代操作、发布不改变已有可见性、
以及用故障注入仓储验证"内部错误统一转 INTERNAL、对外不泄漏实现细节"。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D103 | 发布分两步：`POST 版本`（draft）→ 上传产物 → `PUT 状态=stable` | 007 §18.2 要求"上传的文件必须与 files 列表一致"，但发布请求里只有元数据、文件还没上传。转 stable 时校验齐全，既满足规范又让 CLI 的 publish 有明确的三步流程（Step 19 用） |
| D104 | 版本号**不可回收**：软删除的版本仍占位 | 若允许回收，同一个 `people/basic@1.0.0` 在不同时间可能指向不同内容——所有基于精确版本的信任（002 §7.1）都会崩塌 |
| D105 | blocked 返回 `COMPONENT_BLOCKED` 而不是 404 | 使用者需要知道"这个组件被下架了"才能去找替代方案；含糊的 404 只会让人反复重试 |
| D106 | 密码错误与用户不存在返回**同样**的错误 | 否则登录接口就成了用户名枚举器（008 安全） |
| D107 | 审计写失败不阻断主流程 | 主操作已经成功，为了记日志失败而回滚反而更糟；审计缺条记录是可接受的降级 |
| D108 | 匿名不是错误 | `Authenticate("")` 返回匿名身份而不是报错——public 组件的查询本来就不需要登录（007 §9.6），把匿名当错误会让 CLI 的无 Token 安装路径全断 |
| D109 | `Options.BcryptCost` 作为测试注入点 | bcrypt 默认成本让整套服务测试从 0.08s 涨到 4.7s。测试用最低成本，生产用默认值 |
| D110 | 内部错误统一包成 `INTERNAL`，原始错误放 `details.cause` | 004 §10 的原则：错误信息不暴露内部实现细节。details 供服务端日志与排障，message 面向调用方 |

---

### Step 18-D：HTTP 层与服务装配 — ✅ 2026-08-13

**开发方式：TDD。** 先写业务行为测试（HTTP 层 38 例 + 启动配置 13 例 + 管理员引导 5 例，
覆盖 18.1–18.5、18.15、18.16、18.23）→ 运行确认全部失败 → 实现 → 再补 40 个代码层测试。
market-server 集成覆盖率 **91.9%**，累计 **318 个测试**。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `market-server/internal/handler/router.go` | 极简路由器（路径参数 + JSON 化的 404/405） |
| `market-server/internal/handler/response.go` | 统一响应信封、错误码到 HTTP 状态码的映射 |
| `market-server/internal/handler/handler.go` | 路由表、Bearer Token 解析、健康检查 |
| `market-server/internal/handler/{components,versions,artifacts,auth}.go` | 18 个端点的协议转换 |
| `market-server/internal/middleware/middleware.go` | panic 兜底、访问日志 |
| `market-server/internal/config/config.go` | 启动配置（运维指南 §5.1 的环境变量、默认值、密钥脱敏） |
| `market-server/internal/service/admin.go` | 管理员引导（运维指南 §6.5） |
| `market-server/cmd/server/main.go` | 装配：读配置 → 连库建表 → 连对象存储建桶 → 引导管理员 → 监听 → 优雅停机 |

**测试验证结果：**

| # | 验证项 | 对应测试 / 结果 |
| --- | --- | --- |
| 18.1 | 组件发布 API | `TestPublishReturns201`：201 + 版本信息；另覆盖未认证 401、坏令牌 401、ID 不一致 400、重复版本 409、坏 JSON 400 |
| 18.2 | 版本查询 API | `TestListVersionsReturnsNewestFirst`：语义化版本倒序（1.10.0 > 1.2.0，不是字符串序） |
| 18.3 | Manifest 获取 API | `TestManifestEndpointMatchesCLIEnvelopeContract`：逐层断言 `data.manifest` / `data.sourceType` / `data.gitUrl`；未知版本 404 |
| 18.4 | 产物上传到 RustFS | `TestUploadArtifactStoresObjectUnderVersionPrefix`：对象键落在 `components/<id>/<版本>/<type>/` 下；非所有者 403；缺 `?file=` 400 |
| 18.5 | 产物下载 | `TestDownloadArtifactReturnsRawFileContent`：返回文件原文（非信封）+ Content-Disposition；`?file=` 选择文件；未声明的文件 404 |
| 18.15 | 组件搜索 API | `TestSearchComponentsReturnsItemsAndTotal` 等 5 例：关键字、标签、分页、`total` 是总数而非本页条数、空结果是 `[]` 而非 `null` |
| 18.16 | 组件详情 API | `TestComponentDetailReturnsMetadataAndVersions`：007 §4.3 的平铺形状 + 版本列表 + 最新版本 |
| 18.23 | 健康检查 API | `TestHealthEndpointIsPublic`：200、匿名可访问、回显构建版本；**不查库**（探针只回答"进程活着吗"） |

**端到端验证（P11 回填）：** 编译出真实的 `market-server` 与 `brickkit` 二进制，
连**真实 PostgreSQL + RustFS** 跑完整条链路：

1. 启动服务 → 自动建表、自动建桶、按 `ADMIN_USERNAME/PASSWORD` 引导管理员；
2. `curl` 健康检查 → 登录管理员 → 发布 draft → 上传产物 → 转 stable；
3. `brickkit init` 造项目，`sources` 指向该市场，执行 **`brickkit add smoke/hello@1.0.0`**
   → Manifest ✅ / artifacts ✅，产物落在 `.brickkit/artifacts/smoke-hello-1-0-0/api-docs/openapi.json`，
   内容与上传的完全一致，`brickkit order` 能正确算出服务名；
4. `SIGTERM` → 优雅停机日志正常；数据与对象已清理。

**这是 D47/D48 第一次被真实服务端验证**（此前只有 Mock），P11 就此关闭。

**同时发现一个真实缺陷（记为 P18）：** 把该版本标成 blocked 后再装，服务端返回的是
403 + `COMPONENT_BLOCKED` + "该版本已被市场下架"，但 CLI 只看状态码，
显示成"市场认证失败 → 执行 brickkit login"——把使用者引向完全错误的方向。
服务端是对的，问题在 CLI 没解析错误信封，回填到 Step 19。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D111 | 自己写路由器，不用 `http.ServeMux` | 组件 ID 自带一个 `/`（`scope/name`），且 ServeMux 未命中时写的是纯文本 404/405——CLI 只解析 JSON 信封，拿到纯文本会报"响应无法解析"，把真正的原因盖掉 |
| D112 | 错误码 → 状态码的映射是对外契约 | CLI 靠 404 判断"这个源没有该组件"从而继续试下一个安装源（D40），靠 401/403 提示登录。映射错了整条安装链都会跑偏。`COMPONENT_BLOCKED` 映射为 403（策略性拒绝），CLI 侧的文案问题记为 P18 |
| D113 | 上传端点是 `POST …/artifacts/{artifactId}/upload?file=` | 007 §9.3 写的是 `POST …/artifacts`，但一个产物可声明多个文件（多份 proto），不指明是哪个文件就落不到正确的对象键上；一次请求传一个文件，因此也不需要 multipart |
| D114 | 大小上限用自己的 `limitReader` 而非 `http.MaxBytesReader` | 后者的错误会穿过服务层被统一包成 INTERNAL（500），而"文件太大"是请求本身的问题、应该是 400。用标志位把这件事留在协议层 |
| D115 | `total` 下推到仓储层（新增 `CountComponents`） | 用 `len(items)` 冒充总数，翻页时前端永远算不出还有几页。`ListComponents` 与 `CountComponents` 共用同一份 WHERE 构造，防止两者过滤条件分叉 |
| D116 | 认证在 handler、鉴权在 service，中间件里一条权限规则都不写 | 匿名可以读 public 组件，"要不要认证"取决于被访问的资源，中间件没法一刀切；权限规则只有一份实现才不会分叉 |
| D117 | 令牌用不透明随机串存库，不用 JWT | 运维指南 §5.1 列了 `JWT_SECRET`，但存库的令牌可**即时吊销**（登出即失效），JWT 做不到。`JWT_EXPIRY_HOURS` 仍被采纳为有效期，`JWT_SECRET` 不再需要——**待 18-E 修订运维指南** |
| D118 | 管理员引导只补权限、不覆盖口令 | 运维可能已在市场里改过密码，每次重启按 `.env` 把它改回去会非常难排查 |
| D119 | 启动时自动建表、自动建桶 | 表结构全是 `CREATE TABLE IF NOT EXISTS`，重启幂等；省掉部署时"先跑迁移、再手工建桶"两步易漏的操作 |
| D120 | 配置缺失项一次全报，端口/有效期写错当场失败 | 让运维改一遍 `.env` 就能起来；默默退回默认端口会让人对着一个"起来了但访问不到"的服务查半天 |

**未做（留给 18-E）：** compose 编排文件与 Dockerfile、运维指南按 D117/D119 的修订、
真实部署形态下的复验。

---

### Step 18-E：部署形态交付与真实部署复验 — ✅ 2026-08-13

**开发方式：** 先补上"文档承诺了但代码做不到"的那部分能力（TDD，11 个业务测试），
再交付部署产物，最后**用真实部署把整条链路跑一遍**——这一段的验收不是单测，是能不能一把起来。
market-server 累计 **340 个测试**。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `market-server/Dockerfile` | 多阶段构建，非 root（uid 10001），`-X main.version` 注入版本 |
| `market-server/.dockerignore` | 构建时排除测试与本地环境文件 |
| `deploy/market/docker-compose.yaml` | 四个服务的完整编排，端口全部可用 `.env` 覆盖 |
| `deploy/market/.env.example` | 环境变量模板 |
| `部署模式.md` | **本地 / 单机 / 云端三种部署模式 + 怎么用**，并区分"市场"与"你拼装出来的项目"两件事 |
| `市场部署与运维指南.md` | 按实测结果修订到 v1.2.0 |
| `Makefile` | `market-up` / `market-down` / `market-logs` / `market-image` |
| `internal/service/admin.go` | 新增 `ResetAdminPassword`（运维指南 §9 Q5 的落地） |

**真实部署复验（全新环境，一把起）：**

| # | 验证 | 结果 |
| --- | --- | --- |
| 1 | `docker compose up -d --build` 从零起 | 三个服务全部 healthy，无需任何手工步骤 |
| 2 | 自动建库表、自动建 bucket、自动引导管理员 | ✅ 日志确认，重启幂等 |
| 3 | 健康检查 | `{"success":true,"data":{"status":"ok","version":"dev"}}` |
| 4 | 管理员登录 → 发布 draft → 上传产物 → 转 stable | 201 / 200 / 200 |
| 5 | 匿名下载产物 | 内容与上传完全一致 |
| 6 | **真实 `brickkit add` 从 compose 部署的市场安装** | Manifest ✅ / artifacts ✅，落盘内容一致 |
| 7 | `down` 后再 `up`，数据是否还在 | 组件与产物都在（数据在 PG 与对象存储，进程无状态） |
| 8 | 管理员口令重置流程（Q5） | 旧口令被拒、新口令可用、**旧 Token 立即 401** |
| 9 | 优雅停机 | SIGTERM 后等待在途请求，日志正常 |

**真实部署逼出来的三个问题（单测发现不了）：**

| 问题 | 现象 | 处理 |
| --- | --- | --- |
| **HEAD 被 405** | compose 的 healthcheck 用 `wget --spider`，发的是 HEAD，容器一直 unhealthy | 真正的 bug：**凡是能 GET 的地址都该能 HEAD**。补了 3 个测试再改路由（D121） |
| **RustFS 起不来** | `Permission denied (os error 13)` 反复重启 | 容器内以 uid 10001 运行，bind mount 目录属 root。编排里加 `rustfs-init` 一次性容器改属主（D122） |
| **`.env` 被静默覆盖** | 容器里的凭据不是 `.env` 里的值 | docker compose 中宿主机 shell 已导出的同名变量优先级更高。写进指南 §4.3 与《部署模式》§5 |

**顺带修正：** `market-server/go.mod` 的 go 指令不知何时被 `go mod tidy` 抬到了 1.25.0
（`golang.org/x/crypto` 升到了 v0.55.0 带上去的），与 D1「Go 1.22」和 CLI 模块不一致，
镜像构建直接失败。已把 x/crypto 钉回 v0.21.0、go 指令钉回 1.22（D123）。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D121 | HEAD 路由到 GET 的处理器 | HEAD 就是"只要响应头的 GET"，探针与负载均衡默认发它；响应体由 net/http 自动丢弃。同时验证了 HEAD 不能绕过鉴权变成私有组件的存在性探针 |
| D122 | 编排里放一个 `rustfs-init` 一次性容器改属主 | 让 `docker compose up -d` 在全新机器上一把起来。写文档要求运维手工 chown 是把已知问题外包给使用者 |
| D123 | 依赖版本服从最低 Go 版本要求，而不是反过来 | 依赖升级悄悄抬高语言版本，代价是镜像构建失败——而这类问题只有真正构建时才暴露 |
| D124 | 令牌重置连带吊销该账号全部令牌 | 会走到"重置口令"这一步通常意味着凭据已不可信，留着旧 Token 等于没改 |
| D125 | 管理员提权只能改库，不提供 API | 授予管理员是低频且高风险的操作，做成接口等于给自己开一个提权面 |
| D126 | 部署文档分成两份：**《部署模式》讲"跑在哪、怎么用"，《运维指南》讲单机部署的每一步** | 两个读者不同：前者是要选型和上手的人，后者是已经决定单机自托管、正在敲命令的人 |

**未做（明确记录）：** Kubernetes 清单与 Helm chart（云端模式目前只给了环境变量方案，
K8s 编排随 CLI 的 k8s 目标一起做，见 P19）；市场的 HTTPS 由入口层负责，本身不做 TLS。

---

### Step 19：CLI 发布与登录 — ✅ 2026-08-13

**开发方式：TDD。** 先写 33 个业务行为测试（login 12 + publish 21，覆盖 19.1–19.15）→
确认全部失败 → 实现 → 再补 22 个代码层测试。CLI 模块 **724 个测试**，`internal` 覆盖率 **96.0%**。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `internal/market/client.go` | 市场写入侧客户端：登录、建版本、列产物、上传、改状态、改可见性 |
| `internal/market/errors.go` | 响应信封与错误码翻译（**read / write 两侧共用**） |
| `internal/cli/login.go` | `brickkit login`：市场地址解析、交互/非交互取凭据、写 credentials |
| `internal/cli/publish.go` | `brickkit publish`：本地校验 → draft → 上传产物 → stable → 可见性 |
| `internal/cli/imageref.go` | 镜像引用校验（19.13） |
| `internal/source/credentials.go` | 新增 `SaveCredentials`（0600、先写临时文件再改名） |

**测试验证结果：**

| # | 验证项 | 结果 |
| --- | --- | --- |
| 19.1–19.5 | login 成功、失败、credentials 位置与格式 | `type=password`、`marketUrl`、`expiresAt`(RFC3339)、`createdAt` 齐全 |
| 19.6 | Token 过期 | 明确说"已过期 → 重新登录"，而不是笼统的认证失败 |
| 19.7 | publish 正常发布 | 调用序列锁定为 draft → 列产物 → 上传 → stable |
| 19.8 | 未登录 publish | 报错且**一个请求都不发** |
| 19.9 | 上传 artifacts | 上传内容与磁盘文件逐字节一致，`?file=` 路径正确 |
| 19.10 | 从 `.archived/` 发布 | 成功 |
| 19.11 | 输出含校验结果 | Manifest ✅ / 镜像引用 ✅ / artifacts ✅ |
| 19.12 | Manifest 非法 | 报错，且不在市场留下痕迹 |
| 19.13 | 镜像引用无效 | 无标签 / latest / 含空格 / 大写全部拒绝；registry 带端口与 digest 正常放行 |
| 19.14 / 19.15 | Token 优先级 | credentials > authToken；无登录态时回落；**另一个市场的登录态不会被误用** |

**真实端到端验证：** compose 起真实市场 → `brickkit login --password-stdin` 写出 0600 的凭据
→ `brickkit publish` 发布带 proto + openapi 的组件 → 在**另一个项目**里 `brickkit add` 装回来，
两个产物文件落盘且内容一致。**这是发布与安装第一次在真实服务上闭环。**

**P18 已修复并复验：** 把版本标成 blocked 后再安装，现在给的是
"该版本已被市场阻止，不能安装 → 联系市场管理员"，而不是修复前的"认证失败 → 去登录"。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D127 | 新建 `internal/market` 承载市场协议，与 `internal/source` 分工 | source 关心的是"多安装源怎么回退"，market 关心的是"市场协议长什么样"。信封与错误码翻译放在 market，两侧共用——P18 的根因正是这段逻辑此前只存在于 read 侧且只看状态码 |
| D128 | 所有本地校验都在联网之前做完 | Manifest、镜像引用、产物文件缺失全部先查。等传到一半才失败，市场里会留下一个永远转不了 stable 的 draft |
| D129 | Manifest 以 **component.yaml 原文转 JSON** 上传，不走结构体 | 过一遍 CLI 的 Manifest 结构体会把"市场认识、CLI 还没建模"的字段悄悄丢掉。发布必须是保真的 |
| D130 | 可见性放在转 stable **之后**设置 | 反过来的话，中间那段时间组件处于"已存在但可见性未定"的状态 |
| D131 | 来源类型按组件目录的 git remote 推断，闭源时不外泄仓库地址 | 开源组件不用每次手打 `--git-url`；显式 `--source-type registry` 时即使目录里有 remote 也不发出去——那可能是内网地址 |
| D132 | `--sign` 直接报"Step 20 未实现"，不静默忽略 | 忽略会让人以为组件带了签名，而市场里其实什么都没有——这比不支持更危险 |
| D133 | 密码在任何路径下都不回显 | 终端里回显会被旁人看到，管道里回显会被写进 CI 日志。真终端用 `x/term` 关回显，非终端按行读 |
| D134 | 用户名与密码共用一个 `bufio.Reader` | 每次新建读取器会把上一行之后已经读进缓冲区的内容丢掉，密码就再也读不到了 |

**未做（明确记录）：** cosign 签名（`--sign`，Step 20）；`brickkit publish` 的 `--dry-run`
（设计书未要求，暂不加）。

---

### 修订：把建表 SQL 从代码里搬进真正的 migration 文件 — 2026-08-14

**起因：** 你问"migration 文件在哪、每个组件的库结构在哪生成"。答案暴露了一个我做错的地方：
我把建表 SQL 埋在了 Go 的 `ensureSchema()` 与 Python 的 `ensure_schema()` 里。
设计书（002 §8）确实只规定 `migration.command` 是一条命令、不规定形式，
但**把 DDL 藏在代码里，"这一版相对上一版改了什么"就看不见了** ——
而这正是 Step 38 升级测试要验证的东西。

**改法：两个组件都改成有版本的 SQL 文件 + 执行器。**

```
migrations/
├── 0001_init.sql              建表与索引
└── 0002_seed_*.sql            初始数据
```

| 组件 | 执行器 | 打包方式 |
| --- | --- | --- |
| department/tree（Go） | `migrate.go` | `go:embed migrations/*.sql`，直接进二进制 |
| people/basic（Python） | `app/migrate.py` | `COPY migrations ./migrations` 进镜像 |

执行器的三条不变量（两种语言各自实现，行为一致，各有 6 个对着真库跑的测试）：

| 不变量 | 为什么 |
| --- | --- |
| **幂等** | 容器每次重启都会再跑一遍迁移，第二次必须什么都不做 |
| **原子** | 迁移与它的版本记录在**同一个事务**里。先执行后记录会留下一个窗口，进程正好挂在那里就会重复执行 |
| **有序** | 按文件名递增执行，失败即中止；失败的迁移**不记录版本**——记了下次就跳过，数据库会永远停在半吊子状态 |

**真容器复验：** 两个组件各跑两次 `migrate`，第二次都是"无新增迁移"；
`schema_migrations` 表里两条记录、带执行时间；中途改过的数据不会被第二次迁移盖回去。

**顺带澄清的边界（你的原话）：**

| 事情 | 归属 |
| --- | --- |
| database 本身 | **由人建**（运维/你）。库名通过 `DATABASE_NAME` 注入，组件不猜 |
| 库里的表结构与初始数据 | **组件的 migrations**，平台按 `migration.command` 执行，语言无关 |

**追加（同日）：迁移记录按组件隔离 + 回退（undo）**

真容器又测出一个更严重的问题：把两个组件迁移到**同一个数据库**时，
`schema_migrations` 只按 `version` 去重，而两个组件的版本号都叫 `0001_init` ——
后跑的 people/basic 的 0001 被当成"已执行"跳过，然后 0002 往一张
根本没建出来的 people 表里插数据，**直接失败**。

| 改动 | 说明 |
| --- | --- |
| 主键改为 `(component_id, version)` | 版本号是**每个组件各自的**，不是全局的 |
| 共用库时打警告 | 不阻断（本地调试确实方便），但提示 002 §2.2 数据自治要求各用各的库 |
| 新增 `migrate down [n]` / `migrate reset` | 回退最近 n 个 / 全部回退，按版本**倒序**执行 |
| 迁移文件改为 `*.up.sql` + `*.down.sql` 成对 | 没有 down 脚本时回退**报错**，不假装成功 |
| 每个组件补 README | 写清预设库名与 `CREATE DATABASE` 命令，执行一次即可 |

真容器复验（两个组件各跑一遍 `migrate → down → reset → migrate`）：
数据 4 条 → 0 条（表还在）→ 表没了 → 重新回到 4 条。

**沉淀为设计书标准（2026-08-14）：** 这些规则原本只活在两个组件的 README 里，
下一个写组件的人无从知道。已写进设计书：

| 文档 | 新增内容 |
| --- | --- |
| **002 §8.10** | 迁移脚本组织：`NNNN_name.up.sql` / `.down.sql` 成对，版本在组件内唯一递增，**已执行的迁移不得修改** |
| **002 §8.11** | 迁移状态表：`schema_migrations` 的表结构，**主键必须含组件标识**（附共用库会怎么坏的说明） |
| **002 §8.12** | 迁移执行的三条不变量：幂等 / 原子 / 有序，以及各自不满足会怎样 |
| **002 §8.13** | 回退（down）的定位：开发测试用；生产用新的 up 迁移修；缺 down 脚本必须报错 |
| **006 §9.5** | 组件数据库的创建与命名：三层职责划分、为什么建库不交给组件 migration（权限/连接对象/归属）、一组件一库、命名约定、组件必须在文档里声明所需库 |
| **009 §11.10–11.12** | 组件开发者视角的"怎么做"：先建库、Go/Python 的脚本组织与打包、migrate/down/reset 子命令 |
| **011 §2.5** | 使用者视角：安装前为每个组件建库，附命令 |
| **000 导航** | 故障速查加两条（`database does not exist`、共用库迁移互顶）；概念索引加三条 |

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D165 | 这些规则写进设计书而不是只留在组件 README | README 只对这两个组件有效。下一个人写组件时会重新踩一遍共用库、缺 down 脚本、把建表埋进代码这些坑 |
| D160 | `schema_migrations` 主键是 `(component_id, version)` | 版本号是组件各自的。共用库时先跑的组件会把后跑的顶掉——真容器实测直接失败 |
| D161 | 共用数据库只警告不阻断 | 本地调试共用一个库确实方便；按组件隔离之后也不会再互相顶掉。但要提醒它违反数据自治 |
| D162 | down 脚本缺失时回退**报错** | 假装回退成功会让迁移记录与真实结构对不上，比报错危险得多 |
| D163 | `down` / `reset` 只面向开发与测试 | 生产环境的结构问题应当用一个新的 up 迁移去修（002 §8.9：不做破坏性操作） |
| D164 | 建库命令写进组件 README，并预设库名 | 006 §9.1 明确 CLI 不建库。既然是人来做，就必须在组件文档里说清楚要建哪个库、执行一次即可 |
| D155 | 建表 SQL 必须是独立的、有版本号的文件，不写在代码里 | 迁移文件是"结构变更"的可评审记录。埋在代码里的 `CREATE TABLE IF NOT EXISTS` 只能从无到有，表达不了加列、改索引 |
| D156 | 迁移与版本记录同事务 | 两者分开就存在"执行成功但没记上"的窗口，重启后会重复执行 |
| D157 | 失败的迁移不记录版本 | 记了下次跳过，数据库停在半吊子状态且再也不会被修复 |
| D158 | 内存实现不再参与迁移 | 迁移是 SQL，只能对着真库跑。内存实现是测试替身，直接给种子数据即可；两边数据漂移会被 PostgreSQL 那组契约测试抓到 |
| D159 | `CREATE DATABASE` 不进组件的 migration | 建库要 `CREATEDB` 权限、且必须先连到另一个库；云数据库上应用账号通常无权 —— 会造成"开发能跑、生产必炸"。库由人建，库名注入 |

---

### Step 14：数据库迁移执行（Docker） — ✅ 2026-08-14

**开发方式：先核对，再补缺。** 迁移 service 的生成在 Step 12 就做完了，
本 Step 的做法是逐条核对 14.1–14.9 已经被什么锁定、缺什么，然后只补真正缺的部分。
CLI 模块 **886 个测试**，`internal/compose` 覆盖率 **92.5%**、`internal/source` **94.3%**。

**核对结果：**

| # | 验证项 | 状态 |
| --- | --- | --- |
| 14.1 / 14.5 / 14.7 | migration service 生成、用组件自己的镜像、`restart: "no"` | Step 12 已有测试锁定 |
| 14.2 | 主 service 等 `service_completed_successfully` | Step 12 已有测试锁定 |
| 14.6 | 迁移容器环境变量与主容器一致 | Step 12 已有测试；**本次补**：依赖被搬去本地调试、地址被改写之后两边仍必须一致 |
| 14.8 | 无 migration 字段不生成 | Step 12 已有测试锁定 |
| 14.9 | migration 等基础资源先启动 | Step 12 已有测试锁定 |
| 14.3 | **迁移失败时主 service 不启动** | **本次真跑验证** |
| 14.4 | **迁移成功时主 service 正常启动** | **本次真跑验证** |

**本次新增的 7 个测试：**

| 测试 | 盯住什么 |
| --- | --- |
| 迁移容器继承**改写后**的环境变量 | local: true 把依赖地址改成 localPort 时，迁移容器不能还拿着旧地址 |
| 迁移容器拿到同样的 `extra_hosts` | 环境变量一致，寻址方式也得一致（**核对时发现的漏洞**，见下） |
| 迁移只等资源、不等别的组件 | 把强依赖写进去会把一条链上的迁移串成串行；弱依赖更可能根本不启动 |
| 迁移容器不占宿主机端口 | 它和主容器同镜像同环境，映射一份 expose 端口就会两个容器抢同一个宿主机端口 |
| 迁移容器不做健康检查 | 它跑完就退出，healthcheck 只会给出"unhealthy 然后消失"的假象 |
| 组件入口：未知参数必须报错（Go / Python 各一组） | 见下文① |

**核对时发现的漏洞：迁移容器拿到了主容器的环境变量，却没有对应的 `extra_hosts`。**
002 §8.5 承诺"环境变量原封不动复制"，但项目里有 `local: true` 组件时，
环境变量里是一个要靠 `extra_hosts` 才解析得了的主机名——迁移容器解析不了它。
已修（两者一起给），并把这条写进 002 §8.5。

**真跑起来抓到的两个真问题：**

**① 组件入口把未知参数当成"启动服务"，一个拼错的迁移命令就能卡死整个项目。**
构造 14.3 时，我把迁移命令改成 `migrate-does-not-exist`，预期是迁移容器失败退出。
实际是：`department/tree` 的 `run()` 只认 `args[0] == "migrate"`，**其余一律回落到 `serve()`**，
于是迁移容器把服务起了起来，永不退出，两个主服务永远卡在 `Created`——
而容器日志里写着"组件已就绪"，看上去一切正常。
这与 D167 的 entrypoint 参数错位是**同一种事故的两个来源**（一个来自平台生成，一个来自组件入口）。
`people/basic`（Python）有同样的写法，也一并修了。
两个组件都改成**先认参数、不认识就报错**，且校验发生在读环境变量、连数据库**之前**——
为了说一句"参数写错了"，不该先去连一个可能根本连不上的库。
规则写进 **002 §8.5.1**（硬性要求 + 正误对照）与 **009 §11.13**（组件开发者须知）。

**② 本地安装源的 Manifest 与产物被缓存，改了不生效且一声不吭。**
修好①之后重跑，迁移容器报的还是旧命令——`.brickkit/manifests/` 里是一份旧快照。
004 §7.5 原文就是"`up` 时优先使用缓存"，所以这不是实现走样，是**设计没区分本地源**。
缓存是为了省网络往返，本地源根本没有网络往返；它的语义恰恰是"这份源码正在被我改"。
后果是改了端口 / 迁移命令 / 配额之后 `brickkit up` 依旧按旧的生成，且没有任何提示。
已修：本地源不吃 Manifest 缓存，**产物（.proto / openapi.json）同理**——
缓存住会让调用方按旧契约生成客户端。远程源（market / git）的同一个版本内容不可变，缓存照旧。

**真实运行验证：**

```
迁移命令写错 → 迁移容器 exit 1，报"未知的参数：migrate-does-not-exist（可用：…）"
             → department-tree-1-0-0 停在 Created，从未启动          ✅ 14.3
             → 强依赖它的 people-basic-1-0-0 也停在 Created
             → compose 明确报 didn't complete successfully: exit 1
改回正确命令 → 两个迁移容器 Exited (0) → 两个主服务 healthy          ✅ 14.4
             → curl :18092/api/v1/people → 张三 → 技术中心
```

**设计书修正：**

| 位置 | 改动 |
| --- | --- |
| 002 §8.5 | 补：`extra_hosts` 与环境变量一并保持一致 |
| 002 §8.5.1（新增） | 组件入口必须对未知参数 fail fast：正误对照、后果、以及"校验要在连库之前" |
| 009 §11.13（新增） | 同一条规则的组件开发者视角，含常见错误写法 |
| 004 §7.5 | 缓存策略补本地源例外，并说明为什么远程源该缓存、本地源不该 |
| 000 导航 | 故障速查两条：迁移容器不退出的**第二种**成因；改了本地源却没反应 |

版本号：002→1.7.0、004→1.10.0、009→1.8.0。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D183 | 迁移容器与主容器共享 `extra_hosts` | 002 §8.5 承诺环境变量一致；给了一个解析不了的主机名不算"一致" |
| D184 | 组件入口必须对未知参数非零退出（写进 002，成为组件的硬性要求） | 迁移容器与主容器是同一个镜像，只靠参数区分。回落到"启动服务"会让迁移容器永不退出、项目卡死，而日志显示一切正常 |
| D185 | 参数校验放在读环境变量、连数据库之前 | 否则想说"参数写错了"，得先连上一个可能连不上的库，最后给出误导的"连接数据库失败" |
| D186 | 本地安装源不吃 Manifest 与产物缓存 | 缓存省的是网络往返，本地源没有；本地源的语义就是"正在改"。远程源同一版本内容不可变，缓存仍然正确 |
| D187 | 只扫到第一个非本地源为止来判定"是否来自本地源" | 优先级更高的远程源可能才是真正的提供方，而"远程源有没有这个组件"问不起——那正是缓存存在的原因 |

**未做（明确记录）：** 迁移的**执行**本身仍由 compose 驱动（`up` 真启动属 Step 15）；
本 Step 交付的是"生成正确 + 失败语义正确"，两者都已真跑验证。

---

### Step 13：本地调试（local: true）与 local-debug.env — ✅ 2026-08-14

**开发方式：TDD。** 先写 30 个业务行为测试（覆盖 13.1–13.14 与延后项 P3）→ 确认编译不过（RED）
→ 实现 → 补 8 个代码层测试 → **用真实组件把两个方向都跑通**。
CLI 模块 **876 个测试**，`internal/compose` 覆盖率 **91.8%**。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `internal/compose/local.go` | local: true 的全部逻辑：宿主机端口台账、extra_hosts、端口映射、local-debug env 文件 |
| `internal/compose/compose.go` | plan 拆出 `locals`；组件 service 增加 `ports` / `extra_hosts`；资源容器按需开端口 |
| `internal/cli/up.go` | 写出 env 文件、渲染本地调试指引、容器引擎检测 |
| `internal/clierr/clierr.go` | 新增 `CodeMigrationSkipped`（local 组件的迁移不代跑，仅警告） |

**核心思路：local 组件让两个方向同时断了，CLI 各补一条路。**

| 方向 | 断在哪 | 补法 |
| --- | --- | --- |
| 容器 → 宿主机 | 依赖方在容器网络里找不到 `people-basic-1-0-0` 了 | `extra_hosts` 把服务名指到宿主机网关，端口换成 `localPort`。**调用方代码零修改** |
| 宿主机 → 容器 | IDE 里的进程进不去容器网络，连不上依赖与数据库 | 把这些容器的端口映射到宿主机，地址写进 `local-debug.<版本化服务名>.env` |

两个方向共用**一张宿主机端口台账**：`localPort`、`exposePort`、自动映射的端口
抢的是同一台机器上的同一批端口号，分开算迟早撞车。

**测试验证结果：**

| # | 验证项 | 结果 |
| --- | --- | --- |
| 13.1 | local 组件不生成容器 | service 与**迁移 service** 都不生成；迁移不代跑时给出警告（否则只能对着 `relation does not exist` 猜） |
| 13.2 | extra_hosts 映射 | 只有真正依赖它的容器才加；多个 local 组件各加一条；没有 local 组件时文件里不出现 extra_hosts |
| 13.3 / 13.13 | 依赖端口自动映射 | 优先 `10000 + 容器端口`（8080→18080、5432→15432），已 `expose` 的复用现成端口不重复映射 |
| 13.4 / 13.6 / 13.7 | env 文件 | 按版本化服务名命名，两个版本各一份；含 COMPONENT_ID / COMPONENT_VERSION |
| 13.5 | 依赖地址指向 localhost | `http://localhost:18080`；依赖也是 local 时直接走它的 localPort |
| 13.8 | 资源连接改写 | 托管资源 host→localhost、port→映射端口；**外部资源原样保留**（改写会连到一个不存在的服务） |
| 13.9 | config 覆盖值 | 与容器里注入的完全一致——本地调试出来的行为要能说明容器里也对 |
| 13.10 / 13.11 / 13.12 | localPort | 显式值优先；未写时默认取组件声明的端口（见下）；冲突（含与 exposePort、与另一个 local 组件的额外端口）在生成阶段报错 |
| 13.14 | Podman | 生成 `host.containers.internal`，且文件里不再出现 `host-gateway`。**只到单元测试**（本机没装 Podman，登记为 P23） |

**真实运行验证（department/tree 在宿主机、people/basic 在容器）：**

```
brickkit up --dry-run                    → 生成 compose + local-debug env
docker compose up -d                     → 只起 postgres 与 people/basic
宿主机 ./department-tree migrate         → 经 localhost:15432 连上容器里的 postgres ✅
宿主机 ./department-tree                 → 监听 8080
curl :18092/api/v1/people                → 张三 → 技术中心（部门名来自宿主机进程）✅
改宿主机进程读的那个库                    → 容器返回的部门名立刻跟着变 ✅
kill 宿主机进程                          → 容器返回 503 ✅（证明这条路是真的活的）
```

**真跑起来抓到的两个真问题：**

**① `localPort` 的默认值规则本身是坏的（设计书缺陷）。** 005 §4.6 原文是"用户未指定 →
CLI 自动分配（从 8081 开始递增）"。照做的结果：CLI 对所有依赖方宣告 8081，
而宿主机上的进程监听的是它自己代码里的 8080（`listenAddr = ":8080"`），
调用方稳定 503，且**日志里没有任何一行提到端口**。
根因是平台**从不注入"我该监听哪个端口"**——环境变量表里只有"别人在哪"（`*_ENDPOINT`）。
修法：默认取组件声明的 `deployment.port`，该端口被占用时才退到 8081 起递增。
写了 `localPort` 就要自己让进程监听它，这一点现在写进了 009 §12.6 的开发者须知。

**② local 组件的数据库从"需要预先创建"的清单里漏了。** `plan.databases()` 只扫容器组件，
于是 `brickkit_department` 一个字都没提。使用者照着清单建完库，一在 IDE 里启动就撞上
`database "brickkit_department" does not exist`。已修（local 组件同样算数）。

**设计书修正：**

| 位置 | 改动 |
| --- | --- |
| 005 §4.5 | "组件代码零修改"→"**调用方**代码零修改"，并写明平台不注入"我该监听哪个端口" |
| 005 §4.6 | 默认值改为组件声明的 `deployment.port`；补失败现象（持续 503 且日志不提端口）与"显式写了就自己负责监听" |
| 005 §4.8 | 补宿主机端口分配规则：优先 `10000 + 容器端口`（这才解释得了文档里的 15432 与 18080）；说明基础资源同规则、local 组件的额外端口不重分配 |
| 003 §4.5 / 004 §6.4 / 009 §12.6 | 同步默认值规则；009 加"写了 localPort 你要做什么"的开发者对照表 |
| 003 / 009 / 004 的 18080 规则 | 统一补上"优先 10000 + 容器端口" |
| 000 导航 | 故障速查加两条：503 且日志不提端口、local 组件 `relation does not exist` |

版本号：003→1.6.0、004→1.9.0、005→1.7.0、009→1.7.0。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D173 | local 组件的 env 文件从**容器版的注入结果**出发，只改"怎么连过去" | 库名、配置项、密码引用都与容器里跑的完全一致，本地调试出来的行为才能说明容器里也对 |
| D174 | 宿主机端口优先 `10000 + 容器端口`，撞了才顺序扫描 | 端口号自带含义（15432 一眼是 postgres、19090 一眼是 gRPC），排障不用回头查文件。这条规则同时解释了设计书里 18080 与 15432 两个样例数字 |
| D175 | `localPort` / `exposePort` / 自动映射共用一张端口台账，使用者写死的先占位 | 它们抢的是同一台机器上的同一批端口。反过来会出现"自动分配抢走了使用者钦定的端口"这种莫名其妙的失败 |
| D176 | 外部资源（host 是 IP/域名）**不**改写成 localhost | 它本来就在容器网络之外，宿主机按原地址就能连上；改写反而会连到一个不存在的服务 |
| D177 | local 组件的额外端口不重新分配，保持声明值 | 宿主机上跑的是同一份代码，额外端口监听的还是声明值。两个 local 组件声明了同一个额外端口时它们会真的抢端口，因此在生成阶段报错 |
| D178 | `localPort` 默认取组件声明的 `deployment.port` | 见上文①：直接从 8081 起会得到一个没人监听的端口。8081 只是**退路** |
| D179 | local 组件的迁移不代跑，但必须警告 | 容器里那份代码不是开发者正在调试的代码，代跑没有意义；不说一声则会对着 `relation does not exist` 发懵 |
| D180 | `setVar` 只改已存在的变量，不补新的 | 变量不存在意味着注入引擎判定"这条不该注入"（如弱依赖没启动），本地调试没有理由把它凭空补回来 |
| D181 | 引擎检测放在命令层，`BRICKKIT_ENGINE` 可覆盖 | Step 13 只需要它决定 extra_hosts 的别名；"两个都没装"要到真启动（Step 15）才该报错。同时装了两个引擎的机器需要一个说得清的开关 |
| D182 | env 文件权限 0600 | 里面有资源连接信息；compose 文件是 0644，但那个文件本来就要交给 docker 读 |

**未做（明确记录）：** 宿主机端口冲突只查项目内（**P22**，随 Step 15 的启动前体检做）；
13.14 的 Podman 只到单元测试（**P23**，Step 34）。

---

### Step 12：docker-compose.yaml 生成 — ✅ 2026-08-14

**开发方式：TDD。** 先写 46 个业务行为测试（覆盖 12.1–12.16 与 P2/P4/P20/P21）→
确认失败 → 实现 → 用**真实的两个组件**生成并**真的跑起来**。
CLI 模块 **834 个测试**，`internal` 覆盖率 **95.1%**。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `internal/compose/compose.go` | 纯函数：配置 + 三份计算结果 → compose 文件内容 + "使用者还需要做什么" |
| `internal/compose/quota.go` | K8s 写法（`100m` / `128Mi`）转 compose 写法（`0.10` / `128M`） |
| `internal/cli/up.go` | `brickkit up --dry-run`：生成部署文件但不启动（回填 P20） |

**测试验证结果：**

| # | 验证项 | 结果 |
| --- | --- | --- |
| 12.1 | compose 语法合法 | **真的调 `docker compose config`**，且断言无警告（因此没写已废弃的 `version` 字段） |
| 12.2 / 12.12 | depends_on | 强依赖 `service_healthy`；迁移 `service_completed_successfully`；**弱依赖不写**（它可能不启动，写了会卡死整个项目） |
| 12.3 | healthcheck | 由 Manifest 生成；探**主端口**；`type: none` 不生成（否则依赖方永远等不到 healthy） |
| 12.4 | deploy.resources | limits / reservations，单位转换（回填 P2 后半） |
| 12.5 | exposePort | 端口映射；**冲突在生成阶段报错**（回填 P4） |
| 12.6 / 12.11 | migration service | 一次性 service，`restart: "no"`，环境变量与主容器一致 |
| 12.7 | local: true | 不生成 service，且不写进别人的 depends_on |
| 12.8 / 12.9 / 12.10 | 网络 / 卷 / 重启策略 | `brickkit-<项目>-net`、资源数据卷、`unless-stopped` |
| 12.13 / 12.14 | 基础资源容器 | host 是服务名→生成；是 IP/域名→不生成（006 §10.4） |
| 12.15 / 12.16 | 多版本 / 文件头 | 各自独立 service；头部写明"由 CLI 自动生成、请勿手动编辑" |

**真实运行验证（两个真组件 + 生成的文件）：**

```
brickkit up --dry-run → 生成 + 提示要建哪两个库
docker compose up -d  → migration 退出码 0 → 组件按依赖顺序 healthy
curl :18092/api/v1/people → 张三 → 技术中心（部门名来自容器网络内的 department/tree）
```

**真跑起来抓到的一个真 bug（设计书样例同样有）：** 迁移容器没在做迁移，
而是**把服务启动起来了**——组件镜像带 `ENTRYPOINT`，而 compose 的 `command` 只覆盖 `CMD`，
于是实际执行的是 `/app/department-tree /app/department-tree migrate`，参数错位。
后果很隐蔽：迁移永远不"完成"，主服务卡在 `service_completed_successfully` 上，整个项目起不来。
修法是把 `migration.command` 拆成 `entrypoint`（第一段）+ `command`（其余）。
**设计书已全面修正**（不只是发现问题的那两处）：

| 位置 | 处理 |
| --- | --- |
| 002 §8.4 | 加入正确/错误写法对照、失败后果说明，并写明 **K8s 不需要拆**（`container.command` 本身整体替换 ENTRYPOINT） |
| 005 §5、§6.2；004 §5.x；006 §12.2；附录合集 | 4 处生成的 compose 迁移 service 全部改为 `entrypoint` + `command` |
| 005、004、附录合集的 K8s Job 样例 | **不改**，但加注释说明为什么这里整条命令写 `command` 就对——防止有人照着 compose 的写法去"修" |
| 009 §11.4 | 从组件开发者视角说明：`migration.command` 照常写完整命令，CLI 会自动拆；手写 compose 调试时才要注意 |
| 000 导航 | 故障速查加入"迁移容器一直不退出、主服务卡在 Created" |

`component.yaml` 里 `migration.command` 的写法**不变**（完整命令），
拆分是 CLI 生成 compose 时做的事——两种部署目标的差异只在这一处。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D166 | 不写 `version: "3.8"` | Compose V2 已废弃该字段，写了会产生警告。设计书样例成文于 V1 时代；测试直接断言 `docker compose config` 无警告 |
| D167 | 迁移 service 同时覆盖 `entrypoint` 与 `command` | 只写 command 在带 ENTRYPOINT 的镜像上参数错位，且失败方式极其隐蔽（迁移容器变成服务，项目卡死） |
| D168 | 弱依赖不进 `depends_on` | 它可能根本不启动，写进去 compose 会一直等，把"可选"变成"必须" |
| D169 | 依赖没有健康检查的组件时用 `service_started` | 等 `service_healthy` 会永远等下去 |
| D170 | environment 用 `KEY=value` 列表而非 map | 列表保序，生成文件可比对；map 在 YAML 里会被重排，每次 diff 都是噪音 |
| D171 | Step 12 只实现 `up --dry-run` | `--dry-run` 就是"只生成不启动"，正是 Step 12 的交付物；镜像权限检测、执行迁移、调引擎启动属 Step 15。`up` 不带 `--dry-run` 仍报"尚未实现（Step 15）"，并有用例锁定 |
| D172 | 生成时输出"需要预先创建哪些数据库" | 006 §9.5：CLI 不建库，但有责任说清楚。否则组件在迁移阶段抛 `database "xxx" does not exist`，使用者以为是组件的问题 |

---

### Step 22：people/basic（Python，extraPorts 双端口） — ✅ 2026-08-14

**开发方式：TDD。** 先写 33 个业务行为测试 → 实现 → 真容器验证。
技术栈按设计书原样：Python + FastAPI + grpcio + PostgreSQL。

**宿主机没有可用的 Python 包管理环境**（`python3-venv` 未安装，装它要 sudo），
因此这个组件的**测试跑在容器里**（Dockerfile 的 `test` 层）：版本固定、可复现，
不依赖开发机装了什么。`make test-components` 会自动分流 Go 组件与 Python 组件。

**产出文件：** `tests/components/people-basic/`

| 文件 | 说明 |
| --- | --- |
| `proto/people/v1/people.proto` | 自己的 gRPC 契约 |
| `proto/vendor/department/v1/department.proto` | **从 department/tree 的 api-contract 产物 vendored 过来**——契约即产物的实际用法 |
| `app/service.py` | 业务逻辑；HTTP 与 gRPC 都只是薄壳 |
| `app/http_api.py` / `app/grpc_api.py` | FastAPI（8080）+ grpcio（9090，extraPorts） |
| `app/department.py` | 强依赖客户端（**Python 调 Go**，跨语言） |
| `app/events.py` | 弱依赖客户端：地址没注入就用空实现 |
| `app/store.py` / `app/config.py` | 人员数据（只存自己的）+ 环境变量配置 + JSON 日志 |
| `component.yaml` / `openapi.json` / `Dockerfile` | Manifest（含 extraPorts）、FastAPI 导出的文档、非 root 镜像 |

**测试验证结果：**

| # | 验证项 | 结果 |
| --- | --- | --- |
| 22.1 | HTTP API（8080） | 列表 / 过滤 / 单查 / 404 |
| 22.2 | gRPC（9090） | `grpcurl -plaintext localhost:19091 people.v1.PeopleService/ListPeople` 真容器跑通，含反射 |
| 22.3 | `PEOPLE_BASIC_GRPC_ENDPOINT` 注入 | ⚠️ 注入引擎侧已验证（Step 11 的 `TestExtraPortEndpointInjection`）；**compose 层面待 Step 12** |
| 22.4 | `/healthz` | 200 |
| 22.5 | migration | 容器内执行成功，幂等 |
| 22.6 | OpenAPI 自动生成 | FastAPI 生成，`make openapi-people` 导出为产物；3 个路径 |
| 22.7 | 强依赖 department/tree | **Python 组件通过 gRPC 调 Go 组件**，人员的部门名来自对方，不在本地存副本 |
| 22.8 | 弱依赖安全读取 | 未注入 `INFRA_REDIS_EVENT_BUS_ENDPOINT` 时用空实现降级，日志明说"事件发布将跳过" |
| 22.9 | artifacts 声明完整 | proto + openapi，且声明的文件必须真的存在（测试锁定） |
| 22.10 | extraPorts 声明 | `[{name: grpc, port: 9090}]` |

**强依赖 vs 弱依赖的分界线（真容器验证）：**

| 情形 | 表现 |
| --- | --- |
| 弱依赖（事件总线）没注入 | 接口 200，事件发布静默跳过 |
| 弱依赖调用抛异常 | 接口 200，只记警告 |
| **强依赖（department/tree）停掉** | 业务接口 **503「部门信息暂时不可用」**，`/healthz` 仍 200，依赖恢复后自动好 |

**真容器踩出来的一个真问题：** 最初 `DepartmentClient` 里放了个永不过期的缓存。
结果把 department/tree 停掉之后，接口照样返回 200——**缓存把"强依赖已经挂了"盖住了**；
同时我自己写的 `test_department_rename_is_reflected_immediately` 也成了假测试
（测试替身没有缓存，真实现有）。改法：去掉跨请求缓存，
把 N+1 问题挪到服务层用**单次请求内的去重**解决，并补两个测试锁死这两条性质。

**CLI 端到端：** `brickkit add people/basic@1.0.0` 自动拉起强依赖 department/tree、
对缺失的弱依赖给出警告、下载 4 个产物文件，`brickkit order` 算出正确的启动顺序。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D149 | Python 组件的测试跑在容器里 | 宿主机没有 python3-venv，装它要 sudo。容器里版本固定、可复现，也和组件的实际运行环境一致 |
| D150 | 部门名**不在本组件存副本**，每次去问 department/tree | 002 §2.2 数据自治。存副本就得处理同步，而部门改名后本组件必须立刻跟着变 |
| D151 | 强依赖不可用 → 503，弱依赖不可用 → 静默降级 | 这是强弱依赖唯一有意义的区别。强依赖挂了还假装成功，会让调用方以为"这个人真的没有部门" |
| D152 | 去掉跨请求缓存，改为单次请求内去重 | 缓存会盖住依赖故障、也让"改名立刻生效"落空；请求内去重两头都占 |
| D153 | openapi.json 由 FastAPI 导出而非手写 | 手写的文档迟早和代码对不上，而它是要发布到市场的产物 |
| D154 | 把 department/tree 的 proto vendored 进本组件仓库 | 这正是使用方的真实工作流：`brickkit add` → 从 `.brickkit/artifacts/…/api-contract/` 取到 proto → 生成客户端 |

---

### Step 21：department/tree（Go，单端口双协议） — ✅ 2026-08-14

**为什么先做组件：** 你的判断——CLI 的验证不能停在"生成的文件长得对"，得能"跑起来是对的"。
逐条核对后确认：Phase 5 绝大多数验证项不依赖剩余的 CLI 工作，可以现在就做透；
只有 22.3 / 25.3 / 25.5 / 26.3 / 26.4 / 29.x 反向依赖 Step 12/15。
因此顺序定为：**21 + 22 两个组件 → 回到 Step 12 → 再补 25/26**。

**开发方式：TDD。** 先写 34 个业务行为测试（service / server / component / store 四组）→
确认全部失败 → 实现。技术栈按设计书原样：Go + 真实 gRPC + PostgreSQL。

**产出文件：** `tests/components/department-tree/`

| 文件 | 说明 |
| --- | --- |
| `proto/department/v1/department.proto` | gRPC 契约（发布为 api-contract 产物） |
| `service.go` | 业务逻辑 + HTTP 与 gRPC 两个出口（**一份逻辑、两种协议**） |
| `server.go` | 单端口双协议：h2c 承载明文 HTTP/2，按 Content-Type 分流 |
| `store.go` / `postgres.go` | Store 接口 + 内存实现 + PostgreSQL 实现（递归 CTE 查子树） |
| `config.go` | 环境变量配置 + JSON 日志（口令在日志出口统一打码） |
| `main.go` | `migrate` 子命令与服务启动共用一个二进制 |
| `component.yaml` / `openapi.json` / `Dockerfile` | Manifest、API 文档、非 root 镜像 |

**测试验证结果（10 项全部用真容器验证）：**

| # | 验证项 | 结果 |
| --- | --- | --- |
| 21.1 | HTTP API | `GET /api/v1/departments` 200，支持 `?parentId=` 与 `/{id}/subtree` |
| 21.2 | gRPC 调用（单端口） | `grpcurl -plaintext localhost:18090 …/ListDepartments` 与 HTTP **同一个端口**，结果一致 |
| 21.3 | gRPC Reflection | `grpcurl list` 列出 `department.v1.DepartmentService` 及其三个方法，无需 .proto |
| 21.4 | `/healthz` | 200 |
| 21.5 | `/healthz` 不检查外部依赖 | **把 PostgreSQL 停掉**：healthz 仍 200，业务接口如实 503，库恢复后自动好 |
| 21.6 | migration | 容器内跑 `migrate` 成功；重复执行幂等（测试锁定） |
| 21.7 | artifacts 声明完整 | proto + openapi；`brickkit add` 落盘到 `.brickkit/artifacts/department-tree-1-0-0/`，且**下载到的 proto 能直接 protoc 生成客户端** |
| 21.8 | JSON 日志 | 容器输出为 JSON，含 componentId |
| 21.9 | 环境变量配置 | 无硬编码；缺 DATABASE_* 时**快速失败并列出缺哪几个**，不退化到 localhost |
| 21.10 | 非 root | `docker exec … id` → uid=10001(brickkit) |

**额外覆盖：** HTTP 与 gRPC 返回一致性（两处过滤语义必须相同）、gRPC 的 NOT_FOUND 状态码、
存储故障时错误信息不外泄底层原因、日志级别可调、口令不进日志、
存储契约测试（内存 + 真实 PostgreSQL 共用一份契约）。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D143 | 一份业务逻辑，HTTP 与 gRPC 都只是薄壳 | 两边各写一遍查询，迟早出现"HTTP 说有、gRPC 说没有"。测试里专门锁了两种协议结果一致 |
| D144 | 单端口双协议用 h2c + Content-Type 分流，不用 cmux | Manifest 只声明一个 `deployment.port`，健康检查、`_ENDPOINT` 注入、K8s Service 都只认它。少一个端口就少一处要在 extraPorts 里同步的地方 |
| D145 | 组件直连真实 PostgreSQL，**不做内存回退** | 回退会让"配错了库"看起来一切正常。缺 DATABASE_* 直接启动失败，并说明这些变量由平台按资源绑定注入 |
| D146 | 子树查询用递归 CTE 在库里做 | 组织架构可以很大；更重要的是"子树"的语义只有一份实现 |
| D147 | 敏感字段在日志出口统一打码 | 靠"写日志的人记得别写口令"不可靠——连接失败时最想打印的就是带口令的 DSN |
| D148 | 迁移与服务共用同一个二进制 | 迁移脚本与业务代码永远是同一个版本，不会出现"迁移用的是旧镜像"这种事 |

**顺带补齐：** `.tools/bin/grpcurl`（21.2/21.3 的验证工具）、
`make proto-department`（由 proto 重新生成 Go 代码）、
`department-tree` 已加入 `make test-components` 与 `make demo-images`。

---

### Step 11：环境变量注入引擎 + 级联启停 — ✅ 2026-08-13

**开发方式：TDD。** 先写 51 个业务行为测试（级联 15 + 注入 32 + order 命令层 4，
覆盖 11.1–11.18 与 P1 / P14 / P17）→ 确认全部失败 → 实现 → 再补 15 个代码层测试。
CLI 模块 **783 个测试**，`internal` 覆盖率 **95.9%**。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `internal/cascade/cascade.go` | 级联启停计算（P1）：可行性 → 种子 → 强依赖闭包，并给出每个组件的判定理由 |
| `internal/inject/inject.go` | 注入引擎：平台变量、依赖地址、资源连接、组件配置、资源配额合并 |
| `internal/inject/reserved.go` | 保留变量冲突检测（与市场侧同一套算法） |
| `internal/resolver/resolver.go` | 新增 `Graph.Subgraph`：先算级联、再对启动集合排序 |
| `internal/cli/order.go` | 输出「组件状态计算」一节，启动顺序只含实际启动的组件（P17） |
| `internal/config/config.go` | 补齐 006 §2.2 的六种资源类型常量 |

**测试验证结果：**

| # | 验证项 | 结果 |
| --- | --- | --- |
| 11.1 / 11.2 | 主端口与额外端口 ENDPOINT | `DEPARTMENT_TREE_ENDPOINT` / `PEOPLE_BASIC_GRPC_ENDPOINT`，变量名不带版本、值指向版本化服务名 |
| 11.3 / 11.4 | 弱依赖缺失 | 主端口与额外端口**都不注入**；断言"不残留任何该组件的变量" |
| 11.5 | 保留变量冲突 | 警告并跳过，平台值优先；`{envPrefix}_*` 这类只有读完 brickkit.yaml 才知道的前缀也防住了 |
| 11.6 | 资源配额合并 | 三层优先级 + **逐字段**合并（只调内存不该丢掉组件推荐的 CPU） |
| 11.7 / 11.8 | config 覆盖与默认值 | 覆盖优先；`false` 也是有效覆盖，不当成"没写" |
| 11.9–11.12 | COMPONENT_* 与资源连接 | 六类资源的变量名逐条对齐 006 §5.2；envPrefix 多资源区分 |
| 11.13–11.16 | 升级时 configSchema 变更 | 新增用新默认值、删除静默忽略、改默认值生效、有覆盖则覆盖优先 |
| 11.17 / 11.18 | 命名转换 | `defaultPageSize` → `DEFAULT_PAGE_SIZE`；`infra/redis-event-bus` → `INFRA_REDIS_EVENT_BUS_ENDPOINT` |

**回填的延后项：**

| 项 | 结果 |
| --- | --- |
| **P1** 级联计算 | `internal/cascade`。003 §4.3 那张五组件的表被写成一个测试逐行对齐 |
| **P14** 强依赖被禁用 | 只在**钉住**的组件上报错（未钉住的是"用户关掉了一整条链"的正常操作），错误里打印完整依赖链 |
| **P17** order 按级联过滤 | order 先算级联再排序；那条自曝用例 `TestOrderCurrentlyIgnoresEnabledFalse` 已按新行为重写 |
| **P2** 资源配额合并 | 合并逻辑完成；写进部署文件属 Step 12 |

**真实组件验证：** 用 `tests/components/` 里的 demo/hello 与 demo/caller 跑了三种情形：
正常（两个都启动，弱依赖 demo/bus 缺失时明确说明不注入 `DEMO_BUS_ENDPOINT`）、
关掉强依赖（caller 级联跳过，输出"本次没有组件会启动"）、
钉住 caller 却关掉 hello（报错并打印依赖链）。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D135 | 级联只沿**强依赖**展开，弱依赖不会被拉起 | 003 §4.3 原文就是"至少有一个正在启动的组件**强依赖**它"。弱依赖是"有就用、没有就降级"，让它自动启动等于把可选悄悄变成必启。跳过时输出会写明"需要它时请显式 enabled: true" |
| D136 | 只被弱依赖指向的组件不算根组件 | 否则它会因为"没人强依赖"而自动成为入口启动，与上一条自相矛盾 |
| D137 | 强依赖被禁用：**钉住的报错，没钉住的静默级联跳过** | 钉住 + 关掉它的依赖是两个直接冲突的意图，必须让使用者知道；而没钉住的那种情况恰恰是级联存在的意义 |
| D138 | 注入引擎不产出文件，只产出"注入什么" | Docker 与 K8s 两种目标共用同一套规则，各自渲染。规则写在渲染器里迟早会分叉 |
| D139 | 环境变量按名字排序输出 | map 遍历顺序随机会让生成的部署文件每次都不一样，git diff 全是噪音 |
| D140 | 没有默认值也没有覆盖的配置项**不注入** | 注入空串会让组件以为"配置过了但值是空的"，与弱依赖不注入是同一条原则 |
| D141 | 资源配额逐字段合并，不整块覆盖 | 使用者常常只想调大内存，不该因此丢掉组件推荐的 CPU 配额 |
| D142 | 保留变量表在 CLI 与市场各存一份，但算法必须一致 | 市场看不到 `{envPrefix}_*`（那是使用者在 brickkit.yaml 里定的），CLI 是最后一道闸；两边的 `EnvVarName` 不一致会出现"发布时说没冲突、注入时却冲突" |

**未做（明确记录）：** 注入结果还没有任何命令消费（**P20**，回填 Step 12）——
Step 11 只交付引擎本身，生成 compose 时才用得上。

---

### 变更记录：对象存储由 MinIO 改为 RustFS — 2026-08-12

**背景：** 你已部署 RustFS（S3 兼容对象存储），后续不使用 MinIO。设计书 007（组件市场设计）
本身只要求"对象存储"、不绑定具体产品，因此**核心设计不受影响**，改动只落在技术选型层。

**改动清单：**

| 位置 | 改动 |
| --- | --- |
| `market-server/go.mod` | 移除 `minio/minio-go`；引入 `aws-sdk-go-v2`（core v1.32.3 / credentials v1.17.42 / service/s3 v1.66.0） |
| `market-server/internal/storage/store.go` | 重写为厂商中立的 S3 客户端：`Config`（Endpoint/AccessKey/SecretKey/Bucket/Region）+ `ConfigFromEnv` + `NewS3Client`，`BaseEndpoint` 指向 RustFS、`UsePathStyle: true` |
| `market-server/internal/storage/store_test.go` | 新增 12 个单元测试（配置校验、缺失项一次全报、环境变量读取与默认值、客户端选项断言），market-server 首次有测试 |
| `.env` | 新建（已 gitignore）：`POSTGRES_PASSWORD=q`、`RUSTFS_ENDPOINT=http://localhost:9000`、`RUSTFS_ACCESS_KEY/SECRET_KEY=rustfsadmin`、`RUSTFS_BUCKET=brickkit-artifacts` 等 |
| `.env.example` | 新建（入库）：同名变量的占位模板，不含真实密钥 |
| `开发计划.md` | 9 处 MinIO → RustFS（技术选型、Step 1 依赖、Step 18 标题与任务、18.4、19.9、功能点 12、附录 G 环境要求） |
| `市场部署与运维指南.md` | 57 处 MinIO → RustFS：架构图、组件职责、端口规划、目录结构、`.env`、compose 服务（`rustfs` + `rustfs-init`）、验证步骤、环境变量表、运维命令、FAQ、安全与总结；顺手修正"开发计划 Step 16"→"Step 18" |

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D39 | S3 客户端选 `aws-sdk-go-v2`，而不是继续用 minio-go 指向 RustFS | 厂商中立：代码与依赖里不再出现 minio 字样，换 Ceph / S3 本体也不用改。为兼容 Go 1.22（D1），s3 钉在 v1.66.0（要求 go 1.21），未取最新版（要求 go 1.24） |
| D40 | `RUSTFS_ENDPOINT` 必须带 `http://` / `https://` scheme | S3 SDK 的 `BaseEndpoint` 需要完整 URL；用 scheme 表达是否 TLS，比再加一个 `USE_SSL` 布尔量更少出错 |
| D41 | 对象存储访问强制 `UsePathStyle: true` | 自建 S3 服务（RustFS/Ceph/MinIO）普遍不支持 AWS 的 virtual-hosted 风格域名 |
| D42 | bucket 初始化与健康检查用 `aws-cli` 走标准 S3 协议，不用厂商专用 CLI | 原指南用 `mc`（MinIO 专用）；改用 aws-cli 后这两段在任何 S3 兼容服务上都不用改 |
| D43 | 运维指南中 RustFS 的镜像标签/环境变量/Console 端口标注"按实际部署校对" | 我没有你 RustFS 部署的一手信息，按官方镜像通用约定书写并显式标注，避免把猜测写成事实 |


## 遗留与阻塞

> 本表只登记**环境与外部依赖**类的阻塞。功能上有意延后的实现，一律登记在上面的
> 《延后实现清单》（P 编号），不要分散在两处。

| # | 内容 | 影响的 Step | 处理计划 |
| --- | --- | --- | --- |
| L1 | 本机未安装 podman / podman-compose | Step 34 兼容性测试 | 进入 Step 34 前安装；不影响此前所有 Step |
| L2 | 本机未安装 golangci-lint | 全程 lint | `make tools-lint` 可按需安装；当前 lint 回退 `go vet`，不阻塞 |
| L3 | 本机未安装 cosign / grpcurl | Step 20、21+ | 分别在对应 Step 前安装（RustFS 与 PostgreSQL 已就绪） |
| L4 | ~~brickkit.yaml 解析器尚未被任何命令调用~~ | — | ✅ Step 9 解决：`add` / `remove` 已读写 brickkit.yaml |
| L5 | ~~安装源客户端（internal/source）尚未被任何命令调用~~ | — | ✅ Step 9 解决（P7）；市场端点契约已由 18-D 的端到端验证关闭（P11 ✅） |
| L6 | 依赖解析引擎（internal/resolver）尚未被任何命令调用 | Step 15 | ✅ Step 9 在 `add` 中接线（P13）；Step 11 在 `order` 中接入级联（P14 ✅ / P17 ✅）；升级检查见 **P15** |

---

## 环境基线（2026-08-12 实测）

| 工具 | 版本 | 计划要求 | 状态 |
| --- | --- | --- | --- |
| Go | 1.26.0 | 1.22+ | ✅ |
| Docker | 29.7.1 | 20.10+ | ✅ |
| Docker Compose | v5.3.1 | V2+ | ✅ |
| Python | 3.14.4 | 3.11+ | ✅ |
| Git | 2.53.0 | — | ✅ |
| protoc | 35.1（`.tools/bin`） | 3.20+ | ✅ |
| RustFS | 已部署（http://localhost:9000） | S3 兼容对象存储 | ✅ |
| PostgreSQL | 已部署（localhost:5432） | 15 | ✅ |
| Podman | 未安装 | 4.0+ | ⬜ Step 34 前补 |
