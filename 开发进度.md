---
created: 2026-08-12
updated: 2026-08-12
---
# BrickKit 开发进度追踪

> 本文件是 `开发计划.md` 的执行台账。**每个开发窗口只推进一个 Step**，完成后在此登记，
> 你可以据此查看进度并决定在哪些节点执行 `git commit`。

## 状态图例

| 图例 | 含义 |
| --- | --- |
| ⬜ | 未开始 |
| 🚧 | 进行中 |
| ✅ | 已完成并通过该 Step 的全部测试验证项 |
| ⚠️ | 已完成，但存在遗留项（见"遗留与阻塞"章节） |

## 使用约定

1. 每个窗口开始时：读取本文件，找到第一个 ⬜ 的 Step，作为本窗口目标。
2. 每个窗口结束时：更新总览表状态 + 追加"完成记录"小节（含验证结果与产出文件）。
3. Commit 建议：每个 ✅ 的 Step 可作为一次独立 commit，建议信息格式
   `Step <N>: <Step 名称>`。
4. `市场部署与运维指南.md` 对应的部署/运维工作放在最后（Step 40 之后）执行，前期不涉及。

---

## 总览进度表

### Phase 1：基础设施

| Step | 名称 | 状态 | 完成日期 | 备注 |
| --- | --- | --- | --- | --- |
| 1 | 项目仓库初始化 | ✅ | 2026-08-12 | Monorepo 骨架 + 工具链 |
| 2 | CLI 框架搭建 | ✅ | 2026-08-12 | cobra 命令树 + clierr + JSON 日志 |
| 3 | `brickkit init` 命令 | ✅ | 2026-08-12 | TDD：先行为测试后实现 |

### Phase 2：核心解析引擎

| Step | 名称 | 状态 | 完成日期 | 备注 |
| --- | --- | --- | --- | --- |
| 4 | Manifest 解析器（component.yaml） | ✅ | 2026-08-12 | 30 项验证全通过；**可并行启动 Step 21** |
| 5 | brickkit.yaml 解析器 | ✅ | 2026-08-12 | 22 项验证全通过 |
| 6 | 安装源实现（local/market/git） | ⬜ | | |
| 7 | 依赖解析引擎 | ⬜ | | |

### Phase 3：CLI 命令实现

| Step | 名称 | 状态 | 完成日期 | 备注 |
| --- | --- | --- | --- | --- |
| 8 | 配置备份与恢复（reset） | ⬜ | | |
| 9 | `brickkit add` / `remove` | ⬜ | | |
| 10 | 拓扑排序与 `brickkit order` | ⬜ | | |
| 11 | 环境变量注入引擎 | ⬜ | | |
| 12 | docker-compose.yaml 生成 | ⬜ | | |
| 13 | 本地调试（local: true）与 local-debug.env | ⬜ | | |
| 14 | 数据库迁移执行（Docker） | ⬜ | | |
| 15 | 引擎调用与生命周期命令（up/down/status） | ⬜ | | |
| 16 | K8s 部署文件生成 | ⬜ | | |
| 17 | `brickkit sync` 命令 | ⬜ | | |

### Phase 4：市场后端

| Step | 名称 | 状态 | 完成日期 | 备注 |
| --- | --- | --- | --- | --- |
| 18 | 市场后端实现（RustFS + 高级校验） | ⬜ | | 对象存储改用 RustFS，见 D39 |
| 19 | CLI 发布与登录功能 | ⬜ | | |
| 20 | 签名与安全（cosign） | ⬜ | | |

### Phase 5：测试组件开发

| Step | 名称 | 状态 | 完成日期 | 备注 |
| --- | --- | --- | --- | --- |
| 21 | department/tree（Go，单端口双协议） | ⬜ | | |
| 22 | people/basic（Python，extraPorts 双端口） | ⬜ | | |
| 23 | auth/password-login（Go，HTTP） | ⬜ | | |
| 24 | authorization/rbac（Go，HTTP + gRPC） | ⬜ | | |
| 25 | erp/backend（Go，连接组件） | ⬜ | | |
| 26 | portal/user-frontend（nginx） | ⬜ | | |
| 27 | infra/redis-event-bus（Python） | ⬜ | | |
| 28 | infra/api-docs（Go） | ⬜ | | |
| 29 | 多版本默认共存验证 | ⬜ | | |

### Phase 6：集成与 E2E

| Step | 名称 | 状态 | 完成日期 | 备注 |
| --- | --- | --- | --- | --- |
| 30 | 渐进式拼装与端到端验证 | ⬜ | | |
| 31 | 完整功能点验证清单（50 项） | ⬜ | | |

### Phase 7：专项测试

| Step | 名称 | 状态 | 完成日期 | 备注 |
| --- | --- | --- | --- | --- |
| 32 | 边界测试 | ⬜ | | |
| 33 | 错误处理测试 | ⬜ | | |
| 34 | 兼容性测试（Docker / Podman） | ⬜ | | 需安装 podman |
| 35 | 安全测试 | ⬜ | | |
| 36 | 性能基准测试 | ⬜ | | |
| 37 | 回归测试 | ⬜ | | |
| 38 | 升级测试 | ⬜ | | |

### Phase 8：验收与发布

| Step | 名称 | 状态 | 完成日期 | 备注 |
| --- | --- | --- | --- | --- |
| 39 | 完整功能点验收 | ⬜ | | |
| 40 | 文档验证 | ⬜ | | |
| 41 | 市场部署与运维（对应《市场部署与运维指南》） | ⬜ | | 最后执行 |

---

## 完成记录

### Step 1：项目仓库初始化 — ✅ 2026-08-12

**产出文件：**

| 文件 / 目录 | 说明 |
| --- | --- |
| `go.mod` / `go.sum` | 主 module `github.com/brickkit/brickkit`，`go 1.22` |
| `cmd/brickkit/main.go` | CLI 入口占位（Step 2 替换为 cobra root 命令） |
| `internal/version/` | 版本与能力常量（ldflags 注入）+ 3 个单元测试 |
| `internal/*` | 13 个内部包目录骨架：cli / manifest / config / source / resolver / generator{compose,k8s} / engine / security / market / workspace / upgrade / backup |
| `market-server/go.mod` | 市场后端独立 module `github.com/brickkit/market-server`，`go 1.22` |
| `market-server/cmd/server/main.go` | 市场后端入口占位（Step 18 实现） |
| `market-server/internal/storage/store.go` | `ArtifactStore` 接口 + 对象存储客户端构造（Step 18 实现细节；2026-08-12 由 MinIO 改为 RustFS，见 D39） |
| `tests/*` | 9 类测试目录骨架（unit/integration/e2e/boundary/error/compat/security/perf/regression） |
| `Makefile` | 30 个目标：build/build-cli/build-market/install/clean/fmt/vet/lint/tidy/test/test-unit/test-integration/test-e2e/test-boundary/test-error/test-compat/test-security/test-perf/test-regression/test-upgrade/test-market/test-all/cover/proto-gen/proto-example/tools/tools-protoc/tools-plugins/tools-lint/tools-proto-include/env/help |
| `.gitignore` | 追加 BrickKit 专属规则（generated/credentials/manifests/artifacts/components/bin/.tools/local-debug） |
| `proto/include/` | `google/api/{annotations,http,field_behavior}.proto`、`protoc-gen-openapiv2/options/{annotations,openapiv2}.proto` |
| `proto/example/ping.proto` | protoc 工具链自检用示例契约（含 `google.api.http` 注解） |
| `.tools/bin/` | 本地工具链二进制（已 gitignore，`make tools` 可重建） |

**依赖引入：**

| module | 依赖 | 版本 | 状态 |
| --- | --- | --- | --- |
| 主 module | testify | v1.11.1 | 直接依赖（已被 `internal/version` 测试使用） |
| 主 module | cobra | v1.10.2 | 已声明，Step 2 起使用 |
| 主 module | google/uuid | v1.6.0 | 已声明，后续使用 |
| 主 module | gopkg.in/yaml.v3 | v3.0.1 | 已在依赖图中，Step 4/5 解析器起直接使用 |
| market-server | ~~minio-go/v7~~ → aws-sdk-go-v2 | s3 v1.66.0 / credentials v1.17.42 / core v1.32.3 | 直接依赖（`internal/storage`），见 D39 |

**工具链版本（全部装在 `.tools/bin`，不污染系统环境）：** protoc 35.1、protoc-gen-go v1.36.12、protoc-gen-go-grpc v1.6.2、protoc-gen-grpc-gateway v2、protoc-gen-openapiv2 v2。

**测试验证结果（16 项检查全通过）：**

| # | 验证项 | 结果 |
| --- | --- | --- |
| 1.1 | `go build ./...` 编译通过 | ✅ 主 module 与 market-server 均通过 |
| 1.2 | `go vet ./...` 无警告 | ✅ 两个 module 均零输出 |
| 1.3 | Makefile 各 target 可执行 | ✅ `build`/`test`/`lint`/`proto-gen`/`test-all`/`help`/`env` 全部成功 |
| 1.4 | `.gitignore` 包含所有必要条目 | ✅ `.brickkit/generated/`、`.brickkit/credentials`、`components/`、`.env` 四项齐备 |
| 1.5 | protoc 可编译测试 proto | ✅ `make proto-gen` 由 `ping.proto` 生成 `ping.pb.go` + `ping_grpc.pb.go` |

**关键实现决策（与开发计划的偏差与理由）：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D1 | `go.mod` 固定 `go 1.22`（而非 Go 1.26 默认写入的 1.25） | 附录 G 要求 Go 1.22+；因此对象存储 SDK 也钉在兼容 Go 1.22 的版本（见 D39） |
| D2 | 对象存储客户端只放 `market-server/go.mod`，不放主 module | 产物存储只有市场用得到；CLI 是单二进制分发（001 §4.1），保持精简。~~minio-go~~ → 见 D39：已于 2026-08-12 换成 aws-sdk-go-v2（服务端换用 RustFS） |
| D3 | proto 自检生成物输出到 `.tools/proto-check/` | Go 工具链忽略以 `.` 开头的目录，避免生成的 `.pb.go` 把 grpc/protobuf 拖进 CLI 的依赖图 |
| D4 | `make lint` 在缺少 golangci-lint 时回退 `go vet` | 保证验证项 1.3 在任何机器上可执行；`make tools-lint` 可随时装上真 linter |
| D5 | 各 `test-*` 目标在目录无 `*_test.go` 时跳过而非报错 | 骨架阶段 9 类测试目录为空，`make test-all` 仍应绿灯 |
| D6 | 暂不执行 `make tidy` | tidy 会删掉尚未 import 的 cobra/uuid；Step 2/4 用上后即可正常 tidy |

---

### Step 2：CLI 框架搭建 — ✅ 2026-08-12

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `internal/clierr/clierr.go` | 统一错误类型 `Error{Code, Message, Details, Hints, Tips, Exit, Cause, Warning}` + 30 个错误码目录 + 004 §10.2 渲染格式 |
| `internal/logging/logging.go` | 基于 `log/slog` 的 JSON 日志，输出 stderr，字段名对齐 time/level/message |
| `internal/cli/root.go` | cobra root 命令、全局 flag、中文 usage 模板、错误翻译与退出码 |
| `internal/cli/{init,add,remove,up,down,status,order,sync,reset,login,publish,version}.go` | 11 个命令骨架 + version 完整实现 |
| `internal/version/version.go` | 新增 `Display()`（004 §11.3 的 `v` 前缀格式） |
| `cmd/brickkit/main.go` | 精简为 `os.Exit(cli.Execute())` |
| `internal/{cli,clierr,logging,version}/*_test.go` | 72 个单元测试，internal 语句覆盖率 **100.0%**（234/234） |
| `Makefile` | 新增 `cover-html`、`cover-check`（COVER_MIN=95 门槛），`cover` 改为按函数展开 |

**关键设计：**

| 主题 | 实现 |
| --- | --- |
| 输出分流 | 人类可读输出（✅/📦/表格）走 **stdout**；JSON 日志与 ❌ 错误块走 **stderr**。`brickkit order > x.txt` 不会混入日志 |
| 错误格式 | `❌ <标题>` + `   <键>：<值>` 明细行 + `建议：`（1 条内联、多条自动编号）+ `💡` 提示，逐字对齐 004 §10.2 的 12 个错误样例 |
| 退出码约定 | 0 成功/警告（33.15）、1 运行期错误、2 用法错误（未知命令、缺参、非法 flag）。004 未规定数值，此处显式约定 |
| 警告渲染 | `clierr.Warn()` 用 ⚠️ 渲染且退出码 0，供 Step 11 保留变量冲突复用 |
| 骨架占位 | 未实现命令返回 `NOT_IMPLEMENTED` 并明确指出"开发计划 Step N"，避免静默无响应 |
| 命令分组 | `--help` 按 项目 / 组件 / 生命周期 / 市场 四组展示，含中文 usage 模板与中文化的 help/completion |
| 全局 flag | `--config/-c`（多环境配置，004 §3.5）、`--log-level`（含 `BRICKKIT_LOG_LEVEL` 环境变量，支持 `off` 静音） |
| 参数完整性 | 各命令 flag 与 004 一一对应：add 的 `--yes/--refresh/--repo/--repo-all`、up 的 `--only/--dry-run/--check-resources`、down 的 `--only`、reset 的 `--last`、publish 的 `--path/--visibility` |

**测试验证结果（35 项二进制端到端检查 + 63 个单元测试全通过）：**

| # | 验证项 | 结果 |
| --- | --- | --- |
| 2.1 | `brickkit version` 输出版本信息 | ✅ 含版本号、`brickkit/v1`、`docker, k8s` 三行，格式对齐 004 §11.3 |
| 2.2 | `brickkit --help` 列出所有子命令 | ✅ init/add/remove/up/down/status/order/sync/reset/login/publish/version 全部在列 |
| 2.3 | 不存在的命令报错 | ✅ 输出 `❌ 错误：未知命令 nosuchcmd` + 建议，退出码 2 |
| 2.4 | 每个子命令 `--help` 有输出且退出码 0 | ✅ 12/12 通过 |
| 2.5 | 错误输出格式正确 | ✅ 含 ❌ 符号、错误描述、建议行 |
| 2.6 | 日志为 JSON 格式 | ✅ stderr 每行均为合法 JSON 且含 time/level/message；stdout 无日志 |

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D7 | 日志默认级别 info，每次执行输出"命令开始执行/执行完成"两条 | 验证项 2.6 要求"执行任意命令即可在 stderr 看到 JSON 日志"；不需要时可 `--log-level off` |
| D8 | `msg` 键改名为 `message` | slog 默认键是 `msg`，与验证项 2.6 要求的 `message` 不一致 |
| D9 | 非 `*clierr.Error` 的错误一律按用法错误（退出码 2）翻译 | 所有命令的 RunE 只返回 clierr 错误，因此剩余的原生错误必来自 cobra 的解析阶段 |
| D10 | 用 `ExecuteC()` 而非 `Execute()` | 日志里要记录真实执行的子命令名（`brickkit version`）而非 root |
| D11 | 中文化 cobra 的 usage 模板与内置 help/completion 命令 | 设计书全部输出为中文，混入 `Usage:`/`Available Commands:` 会破坏一致性 |
| D12 | `--config` 放在 root 的 persistent flag | 开发计划 Step 2 要求全局 flag；004 §3.5 只在 up 中举例，全局化是超集，不冲突 |
| D13 | 覆盖率政策：`make cover-check` 门槛 95%，当前实际 100% | 不追求"永远 100%"：进程入口（main）与对外部引擎的调用（docker / kubectl / 对象存储）不强求覆盖，硬凑只会写出没有意义的测试。同时不使用 `-coverpkg`——那会让 cli 测试"顺带"覆盖 version 包，掩盖各包自身的测试缺口 |

**覆盖率补齐记录（94.0% → 100.0%）：** Step 2 初版有 14 条未覆盖语句，逐条归因后全部补齐：

| 类别 | 位置 | 语句 | 处理 |
| --- | --- | --- | --- |
| 跨包归因（cli 测试实际执行了它，但 Go 只统计被测包自身测试） | `version.Display` | 3 | 在 version 包内加 `TestDisplay`，覆盖"补 v 前缀 / 不重复补"两分支 |
| 进程入口 | `cli.Execute` | 3 | `TestExecuteReadsOSArgs`：替换 `os.Args` + `os.Pipe` 接管 stdout 走真实入口 |
| 默认构造未走到 | `NewOptions` / `envOr` | 4 | `options_test.go`：默认值、`BRICKKIT_LOG_LEVEL` 覆盖、空白字符串回落三种情形 |
| 暂无调用点 | `Options.Println` | 1 | 加测试；Step 3 的 init 输出会真正用到 |
| 防御分支 | `NewRootCommand(nil)` | 1 | `TestNewRootCommandNilOptionsUsesDefaults`（只构造不执行） |
| 未来功能 | `Run` 的 `e.Warning` 分支 | 1 | `TestRunRendersWarningWithZeroExit`：提前锁定"警告 ⚠️ 不阻断、退出码 0、日志 WARN"契约（Step 11 复用） |
| **真实测试缺口** | `translate` 的 `default` 分支 | 1 | `TestTooManyArgsUsesFallbackTranslation`：`brickkit init a b c` → 退出码 2 + "命令用法不正确" |

---

### Step 3：`brickkit init` 命令 — ✅ 2026-08-12

**开发方式：TDD（先逻辑层测试，后实现，最后补代码层单测）**

1. **RED**：先写 `internal/cli/init_test.go`（21 个行为测试，覆盖开发计划 3.1–3.12），运行确认**全部因"功能缺失"失败**（而非笔误/编译错误）。
2. **GREEN**：实现 `internal/config` 三个文件 + 接线 `init` 命令，直到行为测试全绿。
3. **代码层单测**：再补 `internal/config` 的 44 个单元测试（Layout 路径推导、命名校验表、gitignore 合并、IO 错误路径）。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `internal/config/layout.go` | `Layout` 目录布局：由 Root + 配置文件名推导全部路径（config/backup/manifests/artifacts/generated/components/.archived/credentials/gitignore/initial/last），不依赖进程 cwd |
| `internal/config/project.go` | 项目名校验（003 §3.1）+ `SuggestProjectName` 规范化建议 + `Skeleton` 骨架生成（004 §3.2） |
| `internal/config/scaffold.go` | `InitProject` 编排 + `checkNotInitialized` + `EnsureGitignore`（幂等追加，003 §11） |
| `internal/cli/init.go` | 命令接线 + 输出渲染 |
| `internal/cli/root.go` | `Options` 新增 `WorkDir` 注入点 |
| `internal/cli/init_test.go`、`internal/config/*_test.go` | 65 个新测试（总计 154 个，internal 覆盖率 99.2%） |

**测试验证结果（31 项二进制端到端 + 154 个单元/行为测试全通过）：**

| # | 验证项 | 结果 |
| --- | --- | --- |
| 3.1 | 不传参数报错 | ✅ `❌ 请指定项目名称：brickkit init <项目名称>`，退出码 2，且不创建任何文件 |
| 3.2 | 成功创建目录结构 | ✅ brickkit.yaml + .brickkit/{backup,manifests,artifacts,generated} + components/ |
| 3.3 | 骨架内容正确 | ✅ project / deploy.target / components: [] / resources: []，且是合法 YAML |
| 3.4 | 初始备份存在且一致 | ✅ `.brickkit/backup/brickkit.yaml.initial` 与配置逐字节相同 |
| 3.5 | `components/.archived/` 存在 | ✅ |
| 3.6 | `.gitignore` 含 `components/` | ✅ 003 §11 全部条目；已有 .gitignore 时只追加缺失项 |
| 3.7 | 重复 init 报错 | ✅ `已存在：brickkit.yaml、.brickkit/`，原配置不被覆盖 |
| 3.8 | 名称含非法字符报错 | ✅ 给出原因 + 命名规则 |
| 3.9 | 名称含大写报错 | ✅ 提示"必须全部小写"并建议 `myproject` |
| 3.10 | 空目录 init 成功 | ✅ |
| 3.11 | 非空目录（无配置）init 成功 | ✅ 已有文件不受影响 |
| 3.12 | 已有 brickkit.yaml 时报错 | ✅ 退出码 1，文件内容不变 |

**额外覆盖（提前满足后续 Step 的验证项）：** 32.20–32.22 项目名（中划线/数字/纯数字）、`--config brickkit.prod.yaml` 多环境初始化（配置与备份名同步变化）、超长名称（K8s namespace 上限）、`init a b` 参数过多。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D14 | `init` 在**当前目录**初始化，不创建以项目名命名的子目录 | 004 §3.2「在当前目录初始化」；3.10/3.11 也明确验证"在（空/非空）目录中执行 init" |
| D15 | 3.1 的文案采用 004 §3.2 的单行形式 `❌ 请指定项目名称：brickkit init <项目名称>` | 004 §10.2 另有一处两行形式（`❌ 错误：请指定项目名称` + `用法：…`）。开发计划 3.1 的预期结果是单行形式，以它为准 |
| D16 | 3.7 的错误文案用「项目已初始化，无需重复执行 init」+ 列出已存在的 `brickkit.yaml`/`.brickkit/` | 开发计划 3.7 写的是"目录已存在"错误，但 init 是在当前目录初始化，说"目录已存在"会误导；改为列出真正冲突的文件，并给出删除/reset 两条出路 |
| D17 | 项目名长度上限 54 | 项目名用于 K8s namespace `brickkit-<项目名>`（RFC1123 上限 63）与 Docker Network 命名，超长会在部署阶段才炸，提前拦住 |
| D18 | `Options.WorkDir` 注入项目根目录，命令层不依赖进程 cwd | 让 add/up/sync 等后续命令都能在测试里并行、隔离地跑；`os.Chdir` 是进程级全局状态，不可并行 |
| D19 | 名称非法时给出「建议：改用 xxx」 | 004 要求错误必须带建议；规范化后的名称是最有用的建议（`MyProject` → `myproject`） |
| D20 | 覆盖率 99.2%：余下 3 条语句豁免 | 分别是 `f.Write` 部分写失败、备份文件写入失败、`filepath.Abs` 失败——都只能靠文件系统级故障注入触发，按 D13 政策不为它们在生产代码里加抽象层 |

**过程中发现并修复的隐患：** Step 2 的测试辅助函数 `run()` 没有隔离 `WorkDir`，`init` 一实现，测试就把 `brickkit.yaml`、`.brickkit/`、`components/`、`.gitignore` 真的写进了 `internal/cli/` 源码目录。已清理并给 `run()` 加上 `t.TempDir()`，同时用 `git status` 确认仓库干净。

---

### Step 4：Manifest 解析器（component.yaml） — ✅ 2026-08-12

**开发方式：TDD。** 先写 64 个行为测试（覆盖 4.1–4.30）→ 运行确认全部因功能缺失失败 → 实现 → 再补 44 个代码层单测。manifest 包覆盖率 **100%**。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `internal/manifest/types.go` | Manifest 全部结构体（Metadata / Artifact / Dependencies / ComponentDep / ResourceDep / ConfigSchema / Deployment / ExtraPort / Resources / Migration / HealthCheck / Observability / Compatibility），字段与 002 §2.2 + 附录 B.1 一一对应 |
| `internal/manifest/parse.go` | 三段式解析：YAML 语法（带行号）→ 结构形状检查 → 语义校验；`ComponentDep.UnmarshalYAML` 支持依赖的标量/映射两种写法 |
| `internal/manifest/validate.go` | 全部字段级校验规则 + 组件 ID / 精确版本 / K8s 端口名正则 |
| `internal/manifest/problems.go` | `Problem{Field, Reason}` 收集器，一次报出所有问题 |
| `internal/manifest/testdata/{department-tree,people-basic}.yaml` | 取自 002 §12.5 / §2.2 的真实 Manifest，Step 21/22 直接复用 |

**关键设计：**

| 主题 | 实现 |
| --- | --- |
| 一次报全部问题 | 校验不"遇错即返回"，而是收集所有 `Problem` 一次渲染，使用者改一轮就能改完；错误块形如 `metadata.id：格式必须为 scope/name…` |
| 精确字段名 | 解码前先做**节点形状检查**（`migration.command`、`artifacts[].files`、`tags`、`dependencies.*`、`configSchema.required` 必须是数组），否则用户只会看到 yaml 库的 `cannot unmarshal !!str into []string` 而不知道是哪个字段 |
| 依赖两种写法 | `- department/tree@1.0.0`（强）与 `- {id: …, optional: true}`（弱）由自定义 `UnmarshalYAML` 统一解析为 `{ID, Version, Optional, Ref}` |
| 前向兼容 | 不启用 yaml 严格模式：新版本组件多写字段时，老 CLI 忽略而不是报错 |
| 错误可追溯 | `Manifest.Source` 记录来源（文件路径/安装源），错误块首行即 `文件：…` |

**测试验证结果（30/30 项验证 + 108 个测试全通过）：**

| # | 验证项 | 对应测试 |
| --- | --- | --- |
| 4.1 | 合法 Manifest 解析成功（department/tree） | `TestParseFileDepartmentTree`（逐字段断言，含 artifacts/资源依赖/configSchema/resources/migration） |
| 4.2–4.6 | 缺少 id / version / image / port / healthCheck 报错 | `TestValidationErrors/4.2…4.6`，均指出具体字段 |
| 4.7–4.8 | 版本号 `abc` / `1.0` 报错 | 提示 `必须是精确版本 major.minor.patch` |
| 4.9–4.10 | 依赖 `^1.0.0` / `~1.0.0` 报错 | 提示"不接受 ^ 或 ~ 等范围约束" |
| 4.11–4.12 | artifacts 缺少 type / files 报错 | `artifacts[0].type`、`artifacts[0].files` |
| 4.13–4.15 | extraPorts name 重复 / 缺 name / 缺 port | `TestExtraPortsDuplicateName` + `TestValidationErrors/4.14、4.15` |
| 4.16 | `deployment.type: static` 报错 | 提示"所有组件都是 container，包括前端组件" |
| 4.17 | `migration.command` 非数组报错 | 形状检查给出 `migration.command：必须是数组格式（当前是标量）` |
| 4.18 | configSchema 格式不合法报错 | 三个用例：type 非 object、属性类型非法、required 项未声明 |
| 4.19 | 弱依赖 `optional: true` 正确识别 | `TestParseFilePeopleBasic` |
| 4.20–4.25 | 空/无 dependencies、无 artifacts / extraPorts / migration / configSchema 均解析成功 | `TestParseEmptyDependencies`、`TestParseMinimalManifest` |
| 4.26–4.27 | apiVersion / kind 非法报错 | 指出应为 `brickkit/v1`、`Component` |
| 4.28–4.29 | 组件 ID 含非法字符 / 含大写报错 | 分别提示 `scope/name` 格式与"必须全部小写" |
| 4.30 | resources 格式不合法报错 | 两个用例：既无 requests 又无 limits；requests 既无 cpu 又无 memory |

**额外提前覆盖（Step 32 边界项）：** 32.1（ID 超长）、32.2（ID 含 @）、32.3（版本号超长仍正常）、32.5（100 个依赖）、32.10（非法 YAML 带行号）、32.11（空文件）、32.12–32.13（只有 scope / 只有 name）、32.14–32.16（port 0 / 负数 / >65535）、32.17（extraPort 与主端口相同）、32.23（files 空列表）、32.24（路径含空格合法）、32.25（properties 为空合法）。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D21 | `metadata.name` 与 `metadata.description` 也强制必填 | 002 §2.3 明确标为必须；4.x 未列举但规范如此，测试单独覆盖 |
| D22 | 版本号严格 `major.minor.patch`，拒绝 `1.0.0-beta` / `+build` / `v` 前缀 | 002 §7.1 与全书一致地只说精确 major.minor.patch；预发布语义由市场的版本状态（draft/stable）承担 |
| D23 | 组件 ID 上限 63 字符 | ID 转换后要拼成版本化服务名（002 §10.4），必须符合 DNS 标签规则 |
| D24 | `extraPorts.name` 按 K8s Service 端口名规则校验（≤15 字符、小写、首尾字母数字） | 附录 B.7 明确该值直接用作 K8s Service 端口名；不提前拦会在 Step 16 生成 YAML 时才炸 |
| D25 | `extraPorts.port` 与主端口相同判为**错误**（32.17 允许"报错或警告"） | 同端口不可能同时提供两个协议，属于写错而非可容忍配置 |
| D26 | artifacts 文件路径必须是相对路径且不得用 `..` 越出仓库根 | 008 安全边界：产物文件会被下载到使用者机器，绝对路径与路径穿越是明确的攻击面 |
| D27 | 组件不能依赖自己；同一依赖不得重复声明 | 前者提前满足 7.9（自依赖报错），后者是明确的书写错误 |
| D28 | 不启用 yaml 严格模式（未知字段不报错） | 保持前向兼容：新组件写了新字段时，旧版 CLI 应能忽略而不是拒绝安装 |
| D29 | Step 4 不实现"版本化服务名"转换 | 那是环境变量注入与部署文件生成（Step 11/12）的职责，届时连同 002 §10.4 的转换规则一起测 |

---

### Step 5：brickkit.yaml 解析器 — ✅ 2026-08-12

**开发方式：TDD。** 先写 45 个业务行为测试（覆盖 5.1–5.22）→ 确认全部失败 → 实现 → 再补 40 个代码层单测。internal 覆盖率 99.5%，累计 347 个测试。

**产出文件：**

| 文件 | 说明 |
| --- | --- |
| `internal/config/config.go` | `Config` / `Deploy` / `Source` / `Component` / `Resource` / `Binding` / `Installer` 结构 + 访问器（`EnabledState`、`IsPinned`、`IsDisabled`、`Ref`、`ComponentsByID`、`HasMultipleVersions`、`EnabledSources`、`RequireSignature`） |
| `internal/config/parse.go` | 四段式解析：YAML 语法（带行号）→ `${ENV_VAR}` 展开 → 结构形状检查 → 语义校验；`ExpandEnv` 导出供后续 Step 复用 |
| `internal/config/validate.go` | project / deploy / sources / components / resources 全量校验 |
| `internal/clierr/problems.go` | **重构**：把 Step 4 的问题收集器提升为共享的 `clierr.ProblemSet`，manifest 与 config 共用（后续 resolver、市场校验器也会用） |
| `internal/manifest/validate.go` | 导出 `ComponentIDProblem` / `IsExactVersion`，让 brickkit.yaml 与 Manifest 共用同一套组件 ID / 精确版本规则，不重复正则 |
| `internal/config/testdata/erp-project.yaml` | 取自 003 §2 的完整 ERP 配置（5.1 的验证输入） |

**关键设计：**

| 主题 | 实现 |
| --- | --- |
| `enabled` 三态 | 用 `*bool` 承载：`nil`=默认开启可被级联 / `true`=钉住 / `false`=显式关闭，通过 `EnabledState` 枚举暴露，`String()` 给出"钉住/默认开启/显式禁用"用于 up/status 输出 |
| `${ENV_VAR}` | 在 YAML **节点层**展开，只作用于值、不动 key（否则环境变量能改配置结构）；变量不存在时**保留原样**而非注入空串，使用者能一眼看出漏配 |
| 资源配额结构复用 | `components[].resources` 直接复用 `manifest.Resources`，Step 11 的三层合并（brickkit.yaml > component.yaml > CLI 默认值）不需要类型转换 |
| 多版本共存 | 同 ID 不同版本是**合法**的两个条目；只有同 ID 同版本重复才报错 |
| 跨条目一致性检查 | localPort 冲突、exposePort 冲突、binding 指向未声明的组件——这些只用配置本身就能判定，因此在解析阶段就拦住 |

**测试验证结果（22/22 项验证 + 85 个 config 测试全通过）：**

| # | 验证项 | 结果 |
| --- | --- | --- |
| 5.1 | 完整 ERP 项目配置解析成功 | ✅ `TestParseConfigFileFullProject` 对 003 §2 的完整配置逐字段断言 |
| 5.2 | `${ENV_VAR}` 解析为环境变量值 | ✅ MARKET_TOKEN / POSTGRES_PASSWORD / REDIS_PASSWORD 均正确替换 |
| 5.3 | 环境变量不存在时保留原样 | ✅ 保留 `${DEFINITELY_NOT_SET}` 字面量 |
| 5.4 | `enabled: true` → 钉住 | ✅ `EnabledPinned` |
| 5.5 | 不写 `enabled` → 默认开启可被级联 | ✅ `EnabledDefault` |
| 5.6 | `enabled: false` → 显式关闭 | ✅ `EnabledDisabled` |
| 5.7 | `components[].resources` 正确解析 | ✅ 只覆盖 limits.memory 时 requests 保持为空（交给 Step 11 合并） |
| 5.8–5.10 | 缺 project / 缺 deploy.target / target 非法 | ✅ 均给出字段名与合法值 |
| 5.11–5.13 | 组件缺 id / 缺 version / 非精确版本 | ✅ `components[0].xxx` 精确定位 |
| 5.14 | 同 ID 多版本解析为两个条目 | ✅ 并提供 `HasMultipleVersions` 供 Step 9 的 remove 校验使用 |
| 5.15 | sources 三种类型正确解析 | ✅ market/git/local + `enabled` 缺省为 true |
| 5.16 | resources + bindings 正确解析 | ✅ 含 database 名与 envPrefix |
| 5.17 | `installer.requireSignature` 正确解析 | ✅ 缺省为 **true**（附录 D.1） |
| 5.18–5.19 | 空 components / resources 列表 | ✅ 字段整体缺失也等价于空列表 |
| 5.20 | config 字段正确解析 | ✅ 空串/null/特殊字符/数字/布尔/数组/嵌套映射全部原样保留（CLI 不校验类型） |
| 5.21 | expose / exposePort / hostname | ✅ 并校验 K8s 环境下 expose 必须有 hostname |
| 5.22 | local / localPort | ✅ 并校验 localPort 必须与 local: true 同时出现 |

**额外提前覆盖：** 32.6–32.8（config 值边界）、32.9（空文件）、32.10（非法 YAML 带行号）、32.19（50 个组件）、13.12（localPort 冲突报错）。

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D30 | `${ENV_VAR}` 只展开值、不展开 key | 否则环境变量可以改变配置结构（如 `${KEYNAME}: value`），这是注入面 |
| D31 | 变量缺失时保留 `${VAR}` 原样 | 与 Step 11「弱依赖缺失时完全不注入」同一哲学：宁可让人看见问题，也不要静默变成空字符串 |
| D32 | 把问题收集器提到 `clierr.ProblemSet` 共享 | Step 4 已有一份，Step 5 需要第二份；后面 resolver / 市场校验器还要用。重构后 manifest 的 108 个测试全绿，证明输出格式未变 |
| D33 | 从 manifest 导出 `ComponentIDProblem` / `IsExactVersion` | 组件 ID 与精确版本是 002 的规则，brickkit.yaml 与 component.yaml 必须用同一套；复制正则迟早会漂移 |
| D34 | `installer.requireSignature` 缺省 true | 附录 D.1 明确默认 true；安全默认优先（008），本地开发需要关就显式写 false |
| D35 | localPort / exposePort 写了但没写 local / expose 时**报错** | 静默无效是最难查的配置错误；003 明确这两个字段只在对应开关打开时生效 |
| D36 | K8s + expose 缺 hostname 在解析阶段就报错 | 该规则只依赖 brickkit.yaml 本身（deploy.target 与 expose 都在同一文件），无需等到 Step 16 生成 Ingress |
| D37 | 资源 `kind` 不做枚举限制 | 003 §5.2 只是"常见"清单；kind→环境变量前缀的映射是 Step 11（006）的职责，那时才需要判定具体 kind |
| D38 | **不**实现的部分：级联计算、资源配额三层合并、localPort 自动分配、exposePort 与 deployment.port 的冲突判定、密码硬编码告警 | 它们都需要 Manifest 或运行期信息（Step 11/12/13/15）。按你的要求：现在无法完整测试的业务逻辑不写半成品，留到对应 Step 一次做完并测全 |

---

### 变更记录：对象存储由 MinIO 改为 RustFS — 2026-08-12

**背景：** 你已部署 RustFS（S3 兼容对象存储），后续不使用 MinIO。设计书 007（组件市场设计）
本身只要求"对象存储"、不绑定具体产品，因此**核心设计不受影响**，改动只落在技术选型层。

**改动清单：**

| 位置 | 改动 |
| --- | --- |
| `market-server/go.mod` | 移除 `minio/minio-go`；引入 `aws-sdk-go-v2`（core v1.32.3 / credentials v1.17.42 / service/s3 v1.66.0） |
| `market-server/internal/storage/store.go` | 重写为厂商中立的 S3 客户端：`Config`（Endpoint/AccessKey/SecretKey/Bucket/Region）+ `ConfigFromEnv` + `NewS3Client`，`BaseEndpoint` 指向 RustFS、`UsePathStyle: true` |
| `market-server/internal/storage/store_test.go` | 新增 12 个单元测试（配置校验、缺失项一次全报、环境变量读取与默认值、客户端选项断言），market-server 首次有测试 |
| `.env` | 新建（已 gitignore）：`POSTGRES_PASSWORD=q`、`RUSTFS_ENDPOINT=http://localhost:9000`、`RUSTFS_ACCESS_KEY/SECRET_KEY=rustfsadmin`、`RUSTFS_BUCKET=brickkit-artifacts` 等 |
| `.env.example` | 新建（入库）：同名变量的占位模板，不含真实密钥 |
| `开发计划.md` | 9 处 MinIO → RustFS（技术选型、Step 1 依赖、Step 18 标题与任务、18.4、19.9、功能点 12、附录 G 环境要求） |
| `市场部署与运维指南.md` | 57 处 MinIO → RustFS：架构图、组件职责、端口规划、目录结构、`.env`、compose 服务（`rustfs` + `rustfs-init`）、验证步骤、环境变量表、运维命令、FAQ、安全与总结；顺手修正"开发计划 Step 16"→"Step 18" |

**决策记录：**

| # | 决策 | 理由 |
| --- | --- | --- |
| D39 | S3 客户端选 `aws-sdk-go-v2`，而不是继续用 minio-go 指向 RustFS | 厂商中立：代码与依赖里不再出现 minio 字样，换 Ceph / S3 本体也不用改。为兼容 Go 1.22（D1），s3 钉在 v1.66.0（要求 go 1.21），未取最新版（要求 go 1.24） |
| D40 | `RUSTFS_ENDPOINT` 必须带 `http://` / `https://` scheme | S3 SDK 的 `BaseEndpoint` 需要完整 URL；用 scheme 表达是否 TLS，比再加一个 `USE_SSL` 布尔量更少出错 |
| D41 | 对象存储访问强制 `UsePathStyle: true` | 自建 S3 服务（RustFS/Ceph/MinIO）普遍不支持 AWS 的 virtual-hosted 风格域名 |
| D42 | bucket 初始化与健康检查用 `aws-cli` 走标准 S3 协议，不用厂商专用 CLI | 原指南用 `mc`（MinIO 专用）；改用 aws-cli 后这两段在任何 S3 兼容服务上都不用改 |
| D43 | 运维指南中 RustFS 的镜像标签/环境变量/Console 端口标注"按实际部署校对" | 我没有你 RustFS 部署的一手信息，按官方镜像通用约定书写并显式标注，避免把猜测写成事实 |


## 遗留与阻塞

| # | 内容 | 影响的 Step | 处理计划 |
| --- | --- | --- | --- |
| L1 | 本机未安装 podman / podman-compose | Step 34 兼容性测试 | 进入 Step 34 前安装；不影响此前所有 Step |
| L2 | 本机未安装 golangci-lint | 全程 lint | `make tools-lint` 可按需安装；当前 lint 回退 `go vet`，不阻塞 |
| L4 | brickkit.yaml 解析器尚未被任何命令调用 | Step 8/9/10 起接线 | Step 5 只交付库；`add`/`order`/`up` 实现时接入 |
| L3 | 本机未安装 cosign / grpcurl | Step 20、21+ | 分别在对应 Step 前安装（RustFS 与 PostgreSQL 已就绪） |

---

## 环境基线（2026-08-12 实测）

| 工具 | 版本 | 计划要求 | 状态 |
| --- | --- | --- | --- |
| Go | 1.26.0 | 1.22+ | ✅ |
| Docker | 29.7.1 | 20.10+ | ✅ |
| Docker Compose | v5.3.1 | V2+ | ✅ |
| Python | 3.14.4 | 3.11+ | ✅ |
| Git | 2.53.0 | — | ✅ |
| protoc | 35.1（`.tools/bin`） | 3.20+ | ✅ |
| RustFS | 已部署（http://localhost:9000） | S3 兼容对象存储 | ✅ |
| PostgreSQL | 已部署（localhost:5432） | 15 | ✅ |
| Podman | 未安装 | 4.0+ | ⬜ Step 34 前补 |
