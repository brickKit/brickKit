---
created: 2026-08-09T12:52
updated: 2026-08-11T23:28
---
# BrickKit 底层完整开发计划（重构版 v2.0）

## 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | BrickKit 底层完整开发计划 |
| 版本 | 2.0.0 |
| 最后更新 | 2026-08-12 |
| 状态 | 定稿 |
| 依赖文档 | 001–012 全部正式设计书、附录合集、市场部署与运维指南 |

---

## 0. 总览

### 0.1 开发目标

本计划的目标是从零构建 BrickKit 完整产品，包括：

| 交付物 | 说明 |
| --- | --- |
| BrickKit CLI | Go 编写的本地命令行工具，覆盖所有设计书定义的命令 |
| BrickKit Market | Go 编写的组件市场后端服务 |
| 测试组件集 | 8 个独立仓库组件，覆盖所有系统验证点 |
| API 文档系统 | HTTP（OpenAPI）+ gRPC（Reflection）统一文档暴露 |
| 端到端验证套件 | 覆盖所有设计书定义的功能点 |
| 自动化测试套件 | 单元测试 + 集成测试 + E2E 测试 + 边界测试 + 回归测试 |

### 0.2 技术选型

| 项目 | 技术 | 理由 |
| --- | --- | --- |
| CLI | Go 1.22+ / cobra / gopkg.in/yaml.v3 | 设计书要求单二进制分发 |
| 市场后端 | Go / gin / GORM | 与 CLI 统一技术栈 |
| 市场数据库 | PostgreSQL 15 | 统一 |
| 市场对象存储 | RustFS | S3 兼容对象存储，用于存储 artifacts |
| 测试框架 | Go testing + testify + testcontainers-go | 标准化测试 |
| E2E 测试 | Shell 脚本 + Go 测试框架 | 模拟真实用户操作 |
| 测试组件（Go） | gin + google.golang.org/grpc + grpc-gateway | 双协议验证 |
| 测试组件（Python） | FastAPI + grpcio | 自动生成 OpenAPI，验证跨语言 |
| 前端组件 | nginx:alpine | 设计书要求 |
| 文档组件 | Swagger UI + gRPC Reflection 聚合 | 统一文档入口 |
| 缓存 | Redis 7 | 设计书要求 |

### 0.3 组件清单与验证覆盖

| 组件 ID | 语言 | 协议 | 依赖 | 验证点 |
| --- | --- | --- | --- | --- |
| department/tree | Go | HTTP + gRPC（grpc-gateway 单端口） | 无 | 叶子节点、双协议、gRPC Reflection、migration、artifacts |
| people/basic | Python | HTTP（FastAPI）+ gRPC（grpcio 双端口，extraPorts） | department/tree | 强依赖、migration、OpenAPI、Python gRPC、跨语言、extraPorts、artifacts |
| auth/password-login | Go | HTTP | people/basic | 认证、JWT 签发 |
| authorization/rbac | Go | HTTP + gRPC | people/basic | 双协议、Redis 缓存权限 |
| erp/backend | Go | HTTP | 以上全部 + infra/redis-event-bus(弱) | 连接组件、弱依赖降级、config 覆盖、gRPC 调用 |
| portal/user-frontend | nginx | HTTP | erp/backend | 前端组件、expose、反向代理 |
| infra/redis-event-bus | Python | HTTP | 无 | 弱依赖目标、Redis Streams |
| infra/api-docs | Go | HTTP | 无（弱依赖多个组件） | 文档聚合、Swagger UI、gRPC 文档 |

### 0.4 项目仓库结构（Monorepo）

```
brickkit/                          ← Monorepo（CLI + 市场）
├── cmd/
│   └── brickkit/
│       └── main.go                # CLI 入口
├── internal/
│   ├── cli/                       # CLI 命令实现
│   ├── manifest/                  # component.yaml 解析
│   ├── config/                    # brickkit.yaml 解析（含 enabled 三种状态）
│   ├── source/                    # 安装源
│   ├── resolver/                  # 依赖解析引擎（含多版本默认共存）
│   ├── generator/
│   │   ├── compose/               # Docker 部署文件生成（含 local-debug.env）
│   │   └── k8s/                   # K8s 部署文件生成
│   ├── engine/                    # 底层引擎调用
│   ├── security/                  # 保留变量、签名
│   ├── market/                    # 市场客户端（含 Token 过期处理）
│   ├── workspace/                 # 组件源码工作区管理（sync、--repo、--repo-all）
│   ├── upgrade/                   # 升级兼容性检查（002 §7.7–7.10）
│   └── backup/                    # 配置备份
├── market-server/                 # 市场后端服务（独立 Go module）
│   ├── cmd/server/
│   ├── internal/
│   │   ├── handler/
│   │   ├── service/
│   │   ├── model/
│   │   ├── middleware/
│   │   └── validator/             # 含保留变量校验、闭源 API 契约校验
│   ├── migrations/
│   └── go.mod
├── tests/
│   ├── unit/                      # 单元测试
│   ├── integration/               # 集成测试
│   ├── e2e/                       # 端到端测试
│   ├── boundary/                  # 边界测试
│   ├── error/                     # 错误处理测试
│   ├── compat/                    # 兼容性测试（Docker/Podman）
│   ├── security/                  # 安全测试
│   ├── perf/                      # 性能基准测试
│   └── regression/                # 回归测试
├── go.mod
├── Makefile
└── .gitignore

components/                        ← 各组件独立仓库（模拟真实场景）
├── department-tree/
├── people-basic/                  # 含 extraPorts 声明
├── auth-password-login/
├── authorization-rbac/
├── erp-backend/
├── portal-user-frontend/
├── infra-redis-event-bus/
└── infra-api-docs/
```

### 0.5 开发阶段总览

| 阶段 | Step 范围 | 说明 |
| --- | --- | --- |
| Phase 1：基础设施 | Step 1–3 | 项目骨架、CLI 框架、init 命令 |
| Phase 2：核心解析引擎 | Step 4–7 | Manifest 解析、brickkit.yaml 解析、安装源、依赖解析 |
| Phase 3：CLI 命令实现 | Step 8–17 | 所有 CLI 命令的完整实现 |
| Phase 4：市场后端 | Step 18–20 | 市场 API、发布、签名 |
| Phase 5：测试组件开发 | Step 21–29 | 8 个测试组件 |
| Phase 6：集成与 E2E | Step 30–31 | 渐进式拼装、端到端验证 |
| Phase 7：专项测试 | Step 32–38 | 边界、错误、兼容性、安全、性能、回归、升级 |
| Phase 8：验收与发布 | Step 39–40 | 完整功能点验收、文档验证 |

### 0.6 测试类型定义

| 测试类型 | 说明 | 执行时机 |
| --- | --- | --- |
| 单元测试（Unit） | 测试单个函数/方法，Mock 外部依赖 | 每个 Step 完成后 |
| 集成测试（Integration） | 测试多个模块协作，使用真实依赖 | Phase 完成后 |
| E2E 测试（End-to-End） | 模拟真实用户操作，从 CLI 命令到容器运行 | Phase 6 |
| 边界测试（Boundary） | 测试极端输入、空值、超长字符串等 | Phase 7 |
| 错误处理测试（Error） | 测试所有错误场景的处理 | Phase 7 |
| 兼容性测试（Compat） | Docker vs Podman、不同 OS | Phase 7 |
| 安全测试（Security） | 保留变量、密钥、签名 | Phase 7 |
| 性能基准测试（Perf） | 大量组件、大依赖树 | Phase 7 |
| 回归测试（Regression） | 修复 Bug 后确保不引入新问题 | 持续 |
| 升级测试（Upgrade） | 组件版本升级全链路 | Phase 7 |

---

## Phase 1：基础设施

### Step 1：项目仓库初始化

**目标：** 建立 Monorepo 骨架，初始化 Go module，配置基础工具链。

**任务：**

1. 创建 Git 仓库，初始化目录结构。
2. `go mod init github.com/brickkit/brickkit`。
3. 引入依赖：CLI 用 `cobra`、`gopkg.in/yaml.v3`、`github.com/google/uuid`、`testify`；市场后端用 `aws-sdk-go-v2`（S3 客户端，对接 RustFS）。
4. 编写 `Makefile`：`build`、`test`、`lint`、`proto-gen`、`test-unit`、`test-integration`、`test-e2e` 目标。
5. 编写 `.gitignore`。
6. 安装 `protoc` 及相关插件。
7. 下载 `google/api/annotations.proto` 等到 `proto/include/`。
8. 初始化市场 Go module：`cd market-server && go mod init github.com/brickkit/market-server`。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 1.1 | `go build ./...` 编译通过 | 执行命令 | 无报错 |
| 1.2 | `go vet ./...` 无警告 | 执行命令 | 无输出 |
| 1.3 | Makefile 各 target 可执行 | 依次执行 `make build`、`make test`、`make lint` | 无报错 |
| 1.4 | `.gitignore` 包含所有必要条目 | 读取文件 | 包含 `.brickkit/generated/`、`.brickkit/credentials`、`components/`、`.env` |
| 1.5 | protoc 可正常编译测试 proto | 编译一个测试 proto 文件 | 生成 .go 文件 |

---

### Step 2：CLI 框架搭建

**目标：** 搭建 cobra 命令框架，注册所有命令骨架，建立日志和统一错误处理基础设施。

**任务：**

1. `cmd/brickkit/main.go`：cobra root 命令。
2. 注册子命令骨架：`init`、`add`、`remove`、`up`、`down`、`status`、`order`、`sync`、`reset`、`login`、`publish`、`version`。
3. 全局 flag：`--config`。
4. 日志模块：结构化 JSON 输出到 stderr。
5. 错误处理：统一错误格式（`CLIError` 包含 Code, Message, Hint, Details）。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 2.1 | `brickkit version` 输出版本信息 | 执行命令 | 输出包含版本号、支持的 Manifest 版本、部署目标 |
| 2.2 | `brickkit --help` 列出所有子命令 | 执行命令 | 输出包含 init/add/remove/up/down/status/order/sync/reset/login/publish |
| 2.3 | `brickkit <不存在的命令>` 报错 | 执行命令 | 输出错误提示，退出码非 0 |
| 2.4 | 每个子命令 `--help` 输出帮助信息 | 逐一执行 | 有输出，退出码 0 |
| 2.5 | 错误输出格式正确 | 触发一个错误 | 包含 ❌ 符号、错误描述、建议 |
| 2.6 | 日志输出为 JSON 格式 | 执行任意命令，检查 stderr | JSON 格式，包含 time/level/message |

---

### Step 3：brickkit init 命令

**目标：** 实现项目初始化，生成 `brickkit.yaml` 骨架、`.brickkit/` 目录、`components/` 目录、初始备份。必须指定项目名称。

**任务：**

1. 创建 `brickkit.yaml` 骨架。
2. 创建 `.brickkit/` 目录结构（`backup/`、`manifests/`、`artifacts/`、`generated/`）。
3. 创建 `components/` 目录和 `components/.archived/` 目录。
4. 创建 `.brickkit/backup/brickkit.yaml.initial`。
5. 追加 `.gitignore` 规则（包含 `components/`）。
6. 校验：不传项目名称时报错。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 3.1 | `brickkit init` 不传参数时报错 | 执行命令 | 输出 `❌ 请指定项目名称：brickkit init <项目名称>` |
| 3.2 | `brickkit init my-project` 成功创建目录结构 | 执行命令，检查目录 | brickkit.yaml、.brickkit/、components/ 均存在 |
| 3.3 | brickkit.yaml 骨架内容正确 | 读取文件 | 包含 project/deploy/components/resources 字段 |
| 3.4 | `.brickkit/backup/brickkit.yaml.initial` 存在 | 检查文件 | 文件存在，内容与 brickkit.yaml 一致 |
| 3.5 | `components/.archived/` 目录存在 | 检查目录 | 目录存在 |
| 3.6 | `.gitignore` 包含 `components/` | 读取文件 | 包含该条目 |
| 3.7 | 重复执行 `brickkit init` 报错 | 再次执行 | 输出"目录已存在"错误 |
| 3.8 | 项目名称含非法字符时报错 | `brickkit init "my project"` | 报错提示命名规则 |
| 3.9 | 项目名称含大写时报错 | `brickkit init MyProject` | 报错提示全部小写 |
| 3.10 | 在空目录中 init 成功 | 新建空目录，执行 init | 成功 |
| 3.11 | 在非空目录中 init（无 brickkit.yaml）成功 | 有文件的目录执行 init | 成功 |
| 3.12 | 在已有 brickkit.yaml 的目录中 init 报错 | 有 brickkit.yaml 的目录执行 init | 报错 |

---

## Phase 2：核心解析引擎

### Step 4：Manifest 解析器（component.yaml）

**目标：** 实现 `component.yaml` 的完整解析和校验，包括 `artifacts`、`extraPorts`、`deployment.resources`。

**核心 Struct：**

```go
type Artifact struct {
    Type        string   `yaml:"type"`
    Format      string   `yaml:"format,omitempty"`
    Description string   `yaml:"description,omitempty"`
    Files       []string `yaml:"files"`
}

type ExtraPort struct {
    Name string `yaml:"name"`
    Port int    `yaml:"port"`
}

type Deployment struct {
    Type       string      `yaml:"type"`
    Image      string      `yaml:"image"`
    Port       int         `yaml:"port"`
    ExtraPorts []ExtraPort `yaml:"extraPorts,omitempty"`
    Resources  *Resources  `yaml:"resources,omitempty"`
}
```

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 4.1 | 合法 Manifest 解析成功 | 用 department/tree 的 component.yaml 测试 | 无报错，所有字段正确解析 |
| 4.2 | 缺少 metadata.id 时报错 | 构造缺少 id 的 yaml | 报错，指出具体字段 |
| 4.3 | 缺少 metadata.version 时报错 | 构造缺少 version 的 yaml | 报错 |
| 4.4 | 缺少 deployment.image 时报错 | 构造缺少 image 的 yaml | 报错 |
| 4.5 | 缺少 deployment.port 时报错 | 构造缺少 port 的 yaml | 报错 |
| 4.6 | 缺少 healthCheck 时报错 | 构造缺少 healthCheck 的 yaml | 报错 |
| 4.7 | 版本号格式错误时报错 | `version: "abc"` | 报错，提示 major.minor.patch |
| 4.8 | 版本号缺少 patch 时报错 | `version: "1.0"` | 报错 |
| 4.9 | 依赖声明非精确版本时报错 | `department/tree@^1.0.0` | 报错 |
| 4.10 | 依赖声明使用 `~` 时报错 | `department/tree@~1.0.0` | 报错 |
| 4.11 | artifacts 缺少 type 时报错 | 构造缺少 type 的 artifact | 报错 |
| 4.12 | artifacts 缺少 files 时报错 | 构造缺少 files 的 artifact | 报错 |
| 4.13 | extraPorts name 重复时报错 | 两个 `name: grpc` | 报错 |
| 4.14 | extraPorts 缺少 name 时报错 | 只有 port 没有 name | 报错 |
| 4.15 | extraPorts 缺少 port 时报错 | 只有 name 没有 port | 报错 |
| 4.16 | deployment.type 非 container 时报错 | `type: static` | 报错 |
| 4.17 | migration.command 非数组时报错 | `command: "python manage.py migrate"` | 报错 |
| 4.18 | configSchema 格式不合法时报错 | 非法 JSON Schema | 报错 |
| 4.19 | 弱依赖 optional: true 正确识别 | 解析含 optional 的依赖 | 识别为弱依赖 |
| 4.20 | 空 dependencies 解析成功 | `dependencies: {components: [], resources: []}` | 无报错 |
| 4.21 | 无 dependencies 字段解析成功 | 不包含 dependencies | 无报错 |
| 4.22 | 无 artifacts 字段解析成功 | 不包含 artifacts | 无报错 |
| 4.23 | 无 extraPorts 字段解析成功 | 不包含 extraPorts | 无报错 |
| 4.24 | 无 migration 字段解析成功 | 不包含 migration | 无报错 |
| 4.25 | 无 configSchema 字段解析成功 | 不包含 configSchema | 无报错 |
| 4.26 | apiVersion 非 brickkit/v1 时报错 | `apiVersion: v2` | 报错 |
| 4.27 | kind 非 Component 时报错 | `kind: Service` | 报错 |
| 4.28 | 组件 ID 含非法字符时报错 | `id: "people basic"` | 报错 |
| 4.29 | 组件 ID 含大写时报错 | `id: "People/Basic"` | 报错 |
| 4.30 | resources 格式不合法时报错 | 缺少 requests/limits | 报错 |

**并行启动提示：** Step 4 完成后，即可并行启动 Step 21（department/tree 组件开发）。

---

### Step 5：brickkit.yaml 解析器

**目标：** 实现 `brickkit.yaml` 的完整解析，包括 `components[].resources` 覆盖、`exposePort`、`${ENV_VAR}` 解析、`enabled` 三种状态。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 5.1 | 合法 brickkit.yaml 解析成功 | 用完整的 ERP 项目配置测试 | 无报错 |
| 5.2 | `${ENV_VAR}` 正确解析为环境变量值 | 设置环境变量后解析 | 值正确 |
| 5.3 | `${ENV_VAR}` 环境变量不存在时保留原样 | 不设置环境变量 | 保留 `${ENV_VAR}` 字符串 |
| 5.4 | `enabled: true` 解析为钉住 | 解析后检查 | 状态为"钉住" |
| 5.5 | 不写 `enabled` 解析为默认开启可被级联 | 解析后检查 | 状态为"默认开启" |
| 5.6 | `enabled: false` 解析为显式关闭 | 解析后检查 | 状态为"显式关闭" |
| 5.7 | `components[].resources` 正确解析 | 解析后检查覆盖值 | 值正确 |
| 5.8 | 缺少 project 字段时报错 | 构造缺少 project 的 yaml | 报错 |
| 5.9 | 缺少 deploy.target 时报错 | 构造缺少 target 的 yaml | 报错 |
| 5.10 | deploy.target 非法值时报错 | `target: ecs` | 报错 |
| 5.11 | 组件缺少 id 时报错 | 构造缺少 id 的组件条目 | 报错 |
| 5.12 | 组件缺少 version 时报错 | 构造缺少 version 的组件条目 | 报错 |
| 5.13 | 组件版本非精确版本时报错 | `version: ^1.0.0` | 报错 |
| 5.14 | 同一组件 ID 多个版本条目解析成功 | 两个 people/basic 条目 | 解析为两个独立条目 |
| 5.15 | sources 配置正确解析 | 含 market/git/local 三种 | 全部正确解析 |
| 5.16 | resources 配置正确解析 | 含 bindings | 全部正确解析 |
| 5.17 | installer.requireSignature 正确解析 | true/false | 值正确 |
| 5.18 | 空 components 列表解析成功 | `components: []` | 无报错 |
| 5.19 | 空 resources 列表解析成功 | `resources: []` | 无报错 |
| 5.20 | config 字段正确解析 | 含多个配置项 | 值正确 |
| 5.21 | expose/exposePort/hostname 正确解析 | 含这三个字段 | 值正确 |
| 5.22 | local/localPort 正确解析 | 含这两个字段 | 值正确 |

---

### Step 6：安装源实现

**目标：** 实现三种安装源（local, market, git），实现 Manifest 和 artifacts 缓存机制。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 6.1 | local 安装源读取本地 component.yaml | 创建本地目录，验证读取 | 成功 |
| 6.2 | local 安装源路径不存在时报错 | 指定不存在的路径 | 报错 |
| 6.3 | git 安装源 clone 仓库并读取 Manifest | 用测试仓库验证 | 成功 |
| 6.4 | git 安装源 URL 不可达时报错 | 指定不存在的 URL | 报错 |
| 6.5 | market 安装源调用 API 获取 Manifest | Mock API，验证请求 | 请求正确 |
| 6.6 | market 安装源 API 不可达时报错 | 指定不可达的 URL | 报错 |
| 6.7 | Manifest 缓存到 `.brickkit/manifests/` | 验证缓存文件存在 | 文件存在 |
| 6.8 | artifacts 缓存到 `.brickkit/artifacts/` | 验证缓存文件存在 | 文件存在，目录结构正确 |
| 6.9 | `--refresh` 强制重新拉取 | 修改缓存后 refresh | 缓存更新 |
| 6.10 | 安装源优先级：靠前的优先 | 两个源有同名组件 | 使用靠前的源 |
| 6.11 | 安装源 enabled: false 时跳过 | 设置 enabled: false | 不使用该源 |
| 6.12 | artifacts 按版本化服务名组织 | 检查目录结构 | `<版本化服务名>/<type>/<文件路径>` |
| 6.13 | 多版本 artifacts 独立存储 | 两个版本的同一组件 | 两个独立目录 |

---

### Step 7：依赖解析引擎

**目标：** 实现递归解析、强/弱依赖处理、循环依赖检测、多版本默认共存、升级时依赖兼容性检查。

**核心逻辑：**

- 强依赖缺失 → 报错阻断。
- 弱依赖缺失 → 警告但继续（完全不注入环境变量）。
- 循环依赖 → 报错阻断。
- 多版本默认共存：同一组件 ID 出现多个版本时，CLI 为每个版本生成独立容器。
- 递归解析发现传递依赖版本不同 → 自动把两个版本都写入 brickkit.yaml（共存）。
- 升级时依赖兼容性检查（002 §7.7）：新版本 Manifest 可获取、强依赖可满足、资源已绑定、无循环依赖。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 7.1 | 强依赖缺失时报错阻断 | 构造缺失强依赖的场景 | 报错，指出缺失的组件 |
| 7.2 | 弱依赖缺失时警告但继续 | 构造缺失弱依赖的场景 | 警告，不阻断 |
| 7.3 | 循环依赖时报错 | 构造 A→B→C→A 的依赖环 | 报错，指出循环路径 |
| 7.4 | 递归解析去重 | 多个组件依赖同一个组件 | 只解析一次 |
| 7.5 | 多版本共存（A 依赖 X@1.0.0，B 依赖 X@2.0.0） | 验证两个版本都写入 | 两个条目 |
| 7.6 | 弱依赖标记 `optional: true` 正确识别 | 解析 Manifest | 识别为弱依赖 |
| 7.7 | 深层递归解析（3 层以上） | A→B→C→D | 全部解析 |
| 7.8 | 菱形依赖（A→B, A→C, B→D, C→D） | 验证 D 只解析一次 | 去重正确 |
| 7.9 | 自依赖时报错 | A→A | 报错 |
| 7.10 | 升级时新版本 Manifest 不可获取报错 | 指定不存在的版本 | 报错 |
| 7.11 | 升级时新版本强依赖可满足检查 | 新版本新增强依赖 | 检查通过/报错 |
| 7.12 | 升级时新版本资源依赖已绑定检查 | 新版本新增资源依赖 | 检查通过/报错 |
| 7.13 | 升级时新版本弱依赖可获取检查 | 新版本新增弱依赖 | 警告但继续 |
| 7.14 | 升级时新版本无循环依赖检查 | 新版本引入循环 | 报错 |
| 7.15 | 空依赖列表解析成功 | `dependencies: {components: []}` | 无报错 |

---

## Phase 3：CLI 命令实现

### Step 8：配置备份与恢复

**目标：** 实现 `brickkit reset` 和自动备份（`.initial`, `.last`）。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 8.1 | `brickkit init` 后 `.initial` 存在 | 检查文件 | 文件存在 |
| 8.2 | `brickkit add` 前自动创建 `.last` | add 前后对比 | `.last` 文件更新 |
| 8.3 | `brickkit remove` 前自动创建 `.last` | remove 前后对比 | `.last` 文件更新 |
| 8.4 | `brickkit reset` 恢复到初始状态 | 修改 yaml 后 reset | 恢复 |
| 8.5 | `brickkit reset --last` 恢复到上一次 | 多次操作后 reset --last | 恢复 |
| 8.6 | reset 后提示需要重新 `brickkit up` | 检查输出 | 包含提示 |
| 8.7 | 备份文件不存在时 reset 报错 | 删除备份文件后 reset | 报错 |

---

### Step 9：brickkit add / remove 命令

**目标：** 实现组件添加和移除，包括递归依赖拉取、artifacts 自动下载、`--yes`、多版本默认共存、`--repo` / `--repo-all`、remove 自动删除源码目录。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 9.1 | `brickkit add` 正常添加组件 | 执行 add | brickkit.yaml 更新 |
| 9.2 | `brickkit add` 递归拉取依赖 | 添加 erp/backend | 所有依赖被拉取 |
| 9.3 | `brickkit add` 自动下载 artifacts | 验证 `.brickkit/artifacts/` | 文件存在 |
| 9.4 | `brickkit add --yes` 非交互模式 | 执行 add --yes | 无交互提示 |
| 9.5 | 同 ID 不同版本默认共存 | add X@1.0.0 后 add X@2.0.0 | 两个条目 |
| 9.6 | 同 ID 相同版本提示已存在 | add X@1.0.0 后再次 add | 提示已存在 |
| 9.7 | 同 ID 相同版本 + `--yes` 直接刷新 | add X@1.0.0 --yes | 直接刷新 |
| 9.8 | `brickkit remove` 有强依赖方时报错 | 移除被依赖的组件 | 报错，列出依赖方 |
| 9.9 | `brickkit remove` 多版本未指定版本时报错 | 存在多版本时不指定版本 | 报错，提示指定版本 |
| 9.10 | `brickkit remove` 指定版本成功 | `remove X@1.0.0` | 成功 |
| 9.11 | `brickkit remove` 自动删除源码目录 | remove 后检查 | 目录不存在 |
| 9.12 | `brickkit remove` 清理 artifacts 缓存 | remove 后检查 | 缓存不存在 |
| 9.13 | `brickkit remove` 清理 Manifest 缓存 | remove 后检查 | 缓存不存在 |
| 9.14 | `--repo` 正常 clone 开源组件 | 执行 add --repo | components/ 下有完整 Git 仓库 |
| 9.15 | `--repo` 指定闭源组件时报错 | 对闭源组件执行 | 报错 |
| 9.16 | `--repo` 目标目录已存在时报错 | 对已有目录的组件执行 | 报错 |
| 9.17 | `--repo-all` clone 所有开源依赖 | 执行 add --repo-all | 所有开源组件被 clone |
| 9.18 | `--repo-all` 跳过闭源组件并提示 | 执行 add --repo-all | 闭源组件被跳过 |
| 9.19 | 已存在的组件再次 add + `--repo` 只执行 clone | 验证不重复写入 | brickkit.yaml 不变 |
| 9.20 | add 不写 `enabled` 字段 | 检查 brickkit.yaml | 无 enabled 字段 |
| 9.21 | add 不存在的组件报错 | add 不存在的组件 | 报错 |
| 9.22 | add 版本号格式错误报错 | `add X@abc` | 报错 |
| 9.23 | remove 不存在的组件报错 | remove 不存在的组件 | 报错 |
| 9.24 | remove 弱依赖方不阻止移除 | 移除只有弱依赖方的组件 | 成功，输出警告 |
| 9.25 | artifacts 下载失败时警告但继续 | Mock 下载失败 | 警告，安装继续 |

---

### Step 10：拓扑排序与 brickkit order

**目标：** 实现 Kahn 算法拓扑排序，弱依赖不参与排序约束。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 10.1 | 正确的拓扑排序结果 | 用 ERP 依赖链测试 | 顺序正确 |
| 10.2 | 弱依赖不参与排序约束 | 验证弱依赖不影响排序 | 正确 |
| 10.3 | `brickkit order` 输出格式正确 | 执行命令 | 包含编号、箭头、依赖图 |
| 10.4 | 循环依赖时报错 | 构造循环依赖 | 报错 |
| 10.5 | 无依赖组件排第一 | 验证 | 正确 |
| 10.6 | 多版本组件各自独立排序 | 两个版本的同一组件 | 各自独立 |
| 10.7 | 空组件列表时 order 输出提示 | 空项目执行 order | 提示无组件 |

---

### Step 11：环境变量注入引擎

**目标：** 实现完整的环境变量注入，包括额外端口（extraPorts）、保留变量保护、资源配额合并、升级时 configSchema 变更处理。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 11.1 | 主端口 ENDPOINT 注入正确 | 生成 compose，检查 | 值正确 |
| 11.2 | 额外端口 ENDPOINT 注入正确 | 检查 `PEOPLE_BASIC_GRPC_ENDPOINT` | 值正确 |
| 11.3 | 弱依赖缺失时完全不注入 | 禁用弱依赖后生成 | 变量不存在 |
| 11.4 | 弱依赖缺失时额外端口也不注入 | 同上 | 额外端口变量也不存在 |
| 11.5 | 保留变量冲突时警告并跳过 | 构造冲突的 config | 警告输出，变量不注入 |
| 11.6 | 资源配额合并优先级正确 | 分别设置三层值 | 最终值正确 |
| 11.7 | config 覆盖 configSchema 默认值 | 设置 config 覆盖 | 注入覆盖值 |
| 11.8 | 未覆盖的配置项使用默认值 | 不覆盖某配置项 | 使用默认值 |
| 11.9 | COMPONENT_ID 注入正确 | 检查 | 值正确 |
| 11.10 | COMPONENT_VERSION 注入正确 | 检查 | 值正确 |
| 11.11 | 资源连接变量注入正确 | 检查 DATABASE_* | 值正确 |
| 11.12 | envPrefix 多资源注入正确 | 检查 PRIMARY_/ARCHIVE_ | 值正确 |
| 11.13 | 升级时新增配置项使用默认值 | 新版本新增配置项 | 使用默认值 |
| 11.14 | 升级时删除配置项静默忽略 | 新版本删除配置项，brickkit.yaml 仍有覆盖 | 不注入，不报错 |
| 11.15 | 升级时修改配置项默认值生效 | 新版本修改默认值，无覆盖 | 使用新默认值 |
| 11.16 | 升级时有覆盖的配置项覆盖值优先 | 新版本修改默认值，有覆盖 | 使用覆盖值 |
| 11.17 | 驼峰转大写下划线正确 | `defaultPageSize` | `DEFAULT_PAGE_SIZE` |
| 11.18 | 组件 ID 含中划线时环境变量名正确 | `infra/redis-event-bus` | `INFRA_REDIS_EVENT_BUS_ENDPOINT` |

---

### Step 12：docker-compose.yaml 生成

**目标：** 生成完整的 `docker-compose.yaml`，包含所有必要字段。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 12.1 | 生成的 compose 文件语法合法 | `docker compose config` | 无报错 |
| 12.2 | `depends_on` 顺序正确 | 检查依赖关系 | 正确 |
| 12.3 | `healthcheck` 配置正确 | 检查 test 命令 | 正确 |
| 12.4 | `deploy.resources` 正确生成 | 检查 limits/requests | 正确 |
| 12.5 | `exposePort` 端口映射正确 | 检查 ports 字段 | 正确 |
| 12.6 | migration 一次性 service 生成 | 检查 migration service | 存在 |
| 12.7 | `local: true` 组件不生成 service | 验证 | 无 service 定义 |
| 12.8 | 网络名称正确 | 检查 networks | `brickkit-<项目名>-net` |
| 12.9 | volume 定义正确 | 检查 volumes | 存在 |
| 12.10 | restart 策略正确 | 检查 restart | `unless-stopped` |
| 12.11 | migration service restart: "no" | 检查 | 正确 |
| 12.12 | 主 service 依赖 migration 完成 | 检查 `service_completed_successfully` | 正确 |
| 12.13 | 基础资源 service 生成（host 为服务名时） | 检查 postgres/redis service | 存在 |
| 12.14 | 基础资源不生成（host 为 IP 时） | 检查 | 不存在 |
| 12.15 | 多版本组件各自独立 service | 两个版本 | 两个 service |
| 12.16 | 生成的文件头部有注释 | 检查文件头 | 包含"由 BrickKit CLI 自动生成" |

---

### Step 13：本地调试（local: true）与 local-debug.env

**目标：** 实现 `local: true` 的完整逻辑，为 local 组件的依赖自动映射宿主机端口，并生成 `local-debug.<版本化服务名>.env`。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 13.1 | `local: true` 组件不生成容器 | 生成 compose | 无该 service |
| 13.2 | `extra_hosts` 映射正确 | 检查依赖方的 extra_hosts | 正确 |
| 13.3 | 依赖端口自动映射（如 18080） | 检查被依赖组件的 ports | 正确 |
| 13.4 | `local-debug.<版本化服务名>.env` 生成 | 验证文件存在 | 文件存在 |
| 13.5 | env 文件中依赖地址指向 localhost | 检查 | `http://localhost:18080` |
| 13.6 | 多版本共存时 env 文件不冲突 | 两个版本的 local 组件 | 两个独立 env 文件 |
| 13.7 | env 文件包含 COMPONENT_ID | 检查 | 正确 |
| 13.8 | env 文件包含资源连接（host 改为 localhost） | 检查 | 正确 |
| 13.9 | env 文件包含 config 覆盖值 | 检查 | 正确 |
| 13.10 | localPort 用户指定时使用指定值 | 设置 localPort: 9999 | 使用 9999 |
| 13.11 | localPort 未指定时自动分配 | 不设置 localPort | 从 8081 开始 |
| 13.12 | localPort 冲突时报错 | 两个组件同一 localPort | 报错 |
| 13.13 | 依赖组件已有 expose + exposePort 时使用已有端口 | 检查 | 使用已有端口 |
| 13.14 | Podman 环境 extra_hosts 值正确 | Podman 环境生成 | `host-gateway`（与 Docker 相同）。**原文写的 `host.containers.internal` 是错的**：那是 Podman 注入到 /etc/hosts 的主机名，不能作为 add-host 的值，真 Podman 会拒绝创建容器（见开发进度 P23） |

---

### Step 14：数据库迁移执行（Docker）

**目标：** 生成 migration 一次性 service，主 service 等待迁移完成。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 14.1 | migration service 生成正确 | 检查 compose | 正确 |
| 14.2 | 主 service 依赖 migration 完成 | 检查 | `service_completed_successfully` |
| 14.3 | migration 失败时主 service 不启动 | 构造失败的迁移命令 | 主 service 不启动 |
| 14.4 | migration 成功时主 service 正常启动 | 构造成功的迁移命令 | 主 service 启动 |
| 14.5 | migration 使用组件自己的镜像 | 检查 image | 与主 service 相同 |
| 14.6 | migration 容器环境变量与主容器一致 | 检查 environment | 一致 |
| 14.7 | migration 容器 restart: "no" | 检查 | 正确 |
| 14.8 | 无 migration 字段时不生成 migration service | 检查 | 不存在 |
| 14.9 | migration 依赖基础资源先启动 | 检查 depends_on | 正确 |

---

### Step 15：引擎调用与生命周期命令

**目标：** 实现 `up`、`down`、`status`，包括级联禁用、镜像权限检测、`--dry-run`、`--check-resources`、`--only`、`--config`、Podman 兼容。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 15.1 | `brickkit up` 正常启动所有组件 | 执行 up | 所有容器运行 |
| 15.2 | `brickkit up` 级联禁用正确 | 禁用 erp/backend | portal/user-frontend 被级联跳过 |
| 15.3 | `brickkit up` 钉住组件不被级联 | 设置 `enabled: true` | 不被跳过 |
| 15.4 | `brickkit up` 显式关闭的组件不启动 | 设置 `enabled: false` | 不启动 |
| 15.5 | `brickkit up` 显式关闭但被强依赖时报错 | 构造场景 | 报错 |
| 15.6 | `brickkit up --dry-run` 只生成文件不启动 | 执行后检查 | 无容器运行 |
| 15.7 | `brickkit up --check-resources` 资源不可达时警告 | 构造不可达资源 | 警告 |
| 15.8 | `brickkit up --only` 只启动指定组件 | 执行 --only | 只有指定组件运行 |
| 15.9 | `brickkit up --only` 遇 `enabled: false` 报错 | 构造场景 | 报错 |
| 15.10 | `brickkit up --only @版本` 只启动指定版本 | 多版本时指定版本 | 正确 |
| 15.11 | `brickkit up --only` 不指定版本启动所有版本 | 多版本时不指定 | 所有版本运行 |
| 15.12 | `brickkit up --config` 使用指定配置文件 | 创建 prod 配置 | 正确 |
| 15.13 | `brickkit down` 不删除 volume | down 后检查 | volume 存在 |
| 15.14 | `brickkit down --only` 只停止指定组件 | 执行 | 正确 |
| 15.15 | `brickkit status` 展示运行中的组件 | 执行 status | 正确 |
| 15.16 | `brickkit status` 展示级联跳过的组件及原因 | 执行 status | 包含原因 |
| 15.17 | `brickkit status` 展示本地调试组件 | 有 local: true 时 | 包含 IDE 调试模式 |
| 15.18 | `brickkit status` 展示资源状态 | 有资源时 | 包含可达/不可达 |
| 15.19 | 镜像拉取未授权时报错提示 docker login | Mock unauthorized | 报错，提示 docker login |
| 15.20 | Podman 环境下行为一致 | Podman 环境执行 | 一致 |
| 15.21 | 停止顺序与启动顺序相反 | 检查 down 顺序 | 正确 |
| 15.22 | up 输出包含组件状态计算 | 检查输出 | 包含 ✅/⬜ 标记 |
| 15.23 | up 输出包含启动顺序 | 检查输出 | 包含编号 |
| 15.24 | up 输出包含弱依赖警告 | 有弱依赖缺失时 | 包含 ⚠️ |
| 15.25 | up 输出包含迁移执行信息 | 有 migration 时 | 包含 🔧 |

---

### Step 16：K8s 部署文件生成

**目标：** 生成 Namespace, Deployment, Service, Ingress, Secret, Job。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 16.1 | Namespace YAML 生成正确 | 检查 | 名称和 labels 正确 |
| 16.2 | Deployment YAML 生成正确 | 检查 | image、env、probe、resources 正确 |
| 16.3 | Service YAML 生成正确 | 检查 | ports、selector 正确 |
| 16.4 | extraPorts Service 包含额外端口 | 检查 ports 数组 | 包含额外端口 |
| 16.5 | Ingress YAML 生成正确（expose: true） | 检查 | host、backend 正确 |
| 16.6 | 不声明 expose 时不生成 Ingress | 检查 | 不存在 |
| 16.7 | Migration Job YAML 生成正确 | 检查 | command、backoffLimit: 0 |
| 16.8 | Secret YAML 生成正确 | 检查 | 数据引用正确 |
| 16.9 | livenessProbe 配置正确 | 检查 | path、port 正确 |
| 16.10 | readinessProbe 配置正确 | 检查 | 正确 |
| 16.11 | resources 配置正确 | 检查 | 合并后的值正确 |
| 16.12 | K8s 环境不需要 exposePort | 检查 | 无 exposePort 相关 |
| 16.13 | 目录结构正确 | 检查 generated/k8s/ | 包含所有子目录 |
| 16.14 | K8s 执行前自动清理旧 Job | 检查逻辑 | `kubectl delete job --ignore-not-found` |
| 16.15 | 环境变量通过 Secret 引用 | 检查 valueFrom | secretKeyRef 正确 |

---

### Step 17：brickkit sync 命令

**目标：** 实现 `brickkit sync` 命令，根据级联计算结果双向归档/激活组件源码。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 17.1 | 启用中的组件保留在 `components/` | sync 后验证 | 目录位置正确 |
| 17.2 | `enabled: false` 的组件移到归档目录 | sync 后验证 | 在 `.archived/` |
| 17.3 | 被级联跳过的组件移到归档目录 | sync 后验证 | 在 `.archived/` |
| 17.4 | 恢复启用的组件从归档目录移回 | 改配置后 sync | 在 `components/` |
| 17.5 | 归档后 `.git` 目录完整 | 验证 | `.git` 存在 |
| 17.6 | 归档后 Git 操作不受影响 | 执行 `git status` | 正常 |
| 17.7 | 没有 clone 过源码的组件不受影响 | 验证 | 不被操作 |
| 17.8 | `local: true` 组件一视同仁 | local 组件被级联跳过时 | 也被归档 |
| 17.9 | 双向操作正确 | 先归档再激活 | 目录位置正确 |
| 17.10 | sync 不影响运行中的容器 | sync 后检查 | 容器状态不变 |
| 17.11 | sync 输出包含归档/激活信息 | 检查输出 | 包含 ✅/📦/📂 |
| 17.12 | sync 输出包含原因 | 检查输出 | 包含"显式禁用"/"级联跳过"/"恢复启用" |
| 17.13 | 空 components/ 目录时 sync 无操作 | 执行 sync | 输出"无需整理" |

---

## Phase 4：市场后端

### Step 18：市场后端实现（含 RustFS 对象存储与高级校验）

**目标：** 实现 Market 后端，集成 RustFS（S3 兼容）存储 artifacts，实现源头防御校验。

**核心任务：**

1. RustFS 集成：基于 aws-sdk-go-v2 实现 `ArtifactStore` 接口（S3 兼容，path-style 寻址）。
2. 保留变量校验（源头防御）。
3. 闭源组件 API 契约校验。
4. migration 格式校验。
5. deployment.resources 格式校验。
6. 组件 CRUD、版本管理、可见性控制、签名校验。
7. 市场审计日志。
8. 升级时 artifacts 存储（每个版本独立存储）。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 18.1 | 组件发布 API 正常 | POST 发布 | 201 |
| 18.2 | 版本查询 API 正常 | GET 版本列表 | 数据正确 |
| 18.3 | Manifest 获取 API 正常 | GET manifest | 内容正确 |
| 18.4 | artifacts 上传到 RustFS 正常 | 发布含 artifacts 的组件 | RustFS 中有文件 |
| 18.5 | artifacts 下载正常 | GET download | 文件内容正确 |
| 18.6 | 保留变量校验（源头防御） | 发布含冲突配置项的组件 | 拒绝，返回错误 |
| 18.7 | 保留变量校验错误信息包含冲突详情 | 检查响应 | 包含 configKey、envVarName、conflictPattern |
| 18.8 | 闭源 API 契约校验 | 发布闭源组件无 API 契约 | 拒绝 |
| 18.9 | 闭源组件有 API 契约时发布成功 | 发布闭源组件含 proto | 成功 |
| 18.10 | 开源组件无 API 契约时不强制 | 发布开源组件无契约 | 成功 |
| 18.11 | migration 格式校验 | 发布 command 非数组 | 拒绝 |
| 18.12 | 可见性控制（private） | 发布 private 组件 | 未授权用户无法访问 |
| 18.13 | 审计日志记录 | 发布后查询 | 记录存在 |
| 18.14 | 版本不可重复 | 发布相同版本两次 | 第二次拒绝 |
| 18.15 | 组件搜索 API 正常 | GET /components?keyword= | 结果正确 |
| 18.16 | 组件详情 API 正常 | GET /components/{id} | 数据正确 |
| 18.17 | 版本状态变更 API 正常 | PUT 状态 | 成功 |
| 18.18 | 可见性变更 API 正常 | PUT visibility | 成功 |
| 18.19 | 用户注册 API 正常 | POST /auth/register | 成功 |
| 18.20 | 用户登录 API 正常 | POST /auth/login | 返回 Token |
| 18.21 | 未认证访问 private 组件拒绝 | 无 Token 访问 | 403 |
| 18.22 | artifacts 按版本独立存储 | 两个版本的同一组件 | 独立存储 |
| 18.23 | 健康检查 API 正常 | GET /api/v1/health | 200 |
| 18.24 | 已发布版本不可物理删除 | DELETE 版本 | 拒绝或软删除 |
| 18.25 | blocked 组件不可安装 | 安装 blocked 组件 | 拒绝 |

---

### Step 19：CLI 发布与登录功能

**目标：** 实现 `brickkit login` 和 `brickkit publish`，处理 Token 过期。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 19.1 | `brickkit login` 成功登录 | 执行 login | credentials 文件生成 |
| 19.2 | `brickkit login` 密码错误时报错 | 输入错误密码 | 报错 |
| 19.3 | Token 存储在 `.brickkit/credentials` | 验证文件 | 存在且格式正确 |
| 19.4 | credentials 文件包含 type 字段 | 检查 | `"type": "password"` |
| 19.5 | credentials 文件包含 expiresAt | 检查 | 存在 |
| 19.6 | Token 过期时报错提示重新登录 | 构造过期 Token | 报错 |
| 19.7 | `brickkit publish` 正常发布 | 发布测试组件 | 市场中有记录 |
| 19.8 | `brickkit publish` 未登录时报错 | 删除 credentials 后 publish | 报错 |
| 19.9 | `brickkit publish` 上传 artifacts | 发布含 proto 的组件 | RustFS 中有文件 |
| 19.10 | 从归档目录 publish 路径正确 | 指定 `.archived/` 路径 | 成功 |
| 19.11 | publish 输出包含校验结果 | 检查输出 | 包含 ✅ |
| 19.12 | publish Manifest 校验失败时报错 | 非法 Manifest | 报错 |
| 19.13 | publish 镜像引用无效时报错 | 无效镜像地址 | 报错 |
| 19.14 | Token 优先级：credentials > authToken | 两者都存在 | 使用 credentials |
| 19.15 | credentials 不存在时 fallback 到 authToken | 删除 credentials | 使用 authToken |

---

### Step 20：签名与安全

**目标：** 实现 cosign 签名生成和校验，`requireSignature` 配置阻断。

> **签的是 Manifest，不是镜像。** 本 Step 原稿写的是"对镜像签名"，但 008 §8.4 与 007 §7.4
> 描述的流程是 **CLI 在 `add` 时校验 Manifest 的签名**——镜像签名存在 registry 里、由集群的
> 准入控制器校验，CLI 根本碰不到它。按设计书实现，镜像签名单独登记为《延后清单》P29。
>
> **验签不需要装 cosign。** cosign 产出的就是标准 ECDSA P-256 over SHA-256，Go 标准库直接可验：
> 发布者要装 cosign，使用者零依赖。若验签也要装，"生产环境强制签名"就意味着每台机器、
> 每个 CI runner 都得装它——多数团队会因此直接把校验关掉。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 20.1 | 签名生成正常 | 对**组件版本 Manifest** 签名（`brickkit publish --sign`，cosign）| 签名存在 |
| 20.2 | 签名校验通过 | 验证有效签名 | 通过 |
| 20.3 | 签名校验失败时报错 | 篡改 **Manifest** 后验证 | 报错 |
| 20.4 | `requireSignature: true` 时未签名组件阻断 | 安装未签名组件 | 报错 |
| 20.5 | `requireSignature: false` 时未签名组件可安装 | 关闭签名校验 | 可安装 |
| 20.6 | 签名信息存储在版本记录中 | 查询版本 | 包含 signature |

---

## Phase 5：测试组件开发

### Step 21：department/tree（Go，单端口双协议）

**验证点：** 叶子节点、双协议、gRPC Reflection、migration、artifacts。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 21.1 | HTTP API 正常响应 | curl /api/v1/departments | 200 |
| 21.2 | gRPC 调用正常（单端口） | grpcurl | 正确响应 |
| 21.3 | gRPC Reflection 可用 | grpcurl list | 列出服务 |
| 21.4 | `/healthz` 返回 200 | curl /healthz | 200 |
| 21.5 | `/healthz` 不检查外部依赖 | 检查代码 | 无外部调用 |
| 21.6 | migration 执行成功 | 检查日志 | 成功 |
| 21.7 | artifacts 声明完整 | 检查 component.yaml | 包含 proto + openapi |
| 21.8 | 日志输出 JSON 格式 | 检查 stdout | JSON |
| 21.9 | 从环境变量读取配置 | 检查代码 | 无硬编码 |
| 21.10 | Dockerfile 使用非 root 用户 | 检查 Dockerfile | USER 非 root |

---

### Step 22：people/basic（Python，extraPorts 双端口）

**验证点：** 强依赖、migration、OpenAPI、Python gRPC、跨语言、extraPorts、artifacts。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 22.1 | HTTP API 正常响应（8080） | curl | 200 |
| 22.2 | gRPC 调用正常（9090） | grpcurl | 正确响应 |
| 22.3 | `PEOPLE_BASIC_GRPC_ENDPOINT` 环境变量正确注入 | 检查 env | 值正确 |
| 22.4 | `/healthz` 返回 200 | curl | 200 |
| 22.5 | migration 执行成功 | 检查日志 | 成功 |
| 22.6 | OpenAPI 自动生成 | curl /openapi.json | 200 |
| 22.7 | 强依赖 department/tree 调用正常 | 检查 | 正确 |
| 22.8 | 弱依赖使用 `os.environ.get()` 安全读取 | 检查代码 | 正确 |
| 22.9 | artifacts 声明完整 | 检查 component.yaml | 包含 proto + openapi |
| 22.10 | extraPorts 声明正确 | 检查 component.yaml | `[{name: grpc, port: 9090}]` |

---

### Step 23：auth/password-login（Go，HTTP）

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 23.1 | 登录 API 正常 | POST /api/v1/login | 返回 JWT |
| 23.2 | JWT 格式正确 | 解析 Token | 包含 exp/iat/sub |
| 23.3 | 密码错误时返回 401 | 错误密码 | 401 |
| 23.4 | 健康检查正常 | curl /healthz | 200 |

---

### Step 24：authorization/rbac（Go，HTTP + gRPC）

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 24.1 | HTTP API 正常响应 | curl | 200 |
| 24.2 | gRPC 调用正常 | grpcurl | 正确 |
| 24.3 | Redis 缓存正常 | 检查 Redis | 有缓存 |
| 24.4 | 健康检查正常 | curl /healthz | 200 |
| 24.5 | 健康检查不检查 Redis | 检查代码 | 无 Redis 调用 |

---

### Step 25：erp/backend（Go，连接组件）

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 25.1 | 所有强依赖调用正常 | 检查 | 正确 |
| 25.2 | 弱依赖（event-bus）正常时事件发送成功 | 检查 | 成功 |
| 25.3 | 弱依赖（event-bus）禁用时优雅降级 | 禁用后检查 | 不崩溃 |
| 25.4 | gRPC 调用 people/basic 正常 | 通过 extraPorts 环境变量 | 正确 |
| 25.5 | config 覆盖生效 | 检查 `SESSION_TTL_SECONDS` | 为覆盖值 |
| 25.6 | 健康检查正常 | curl /healthz | 200 |
| 25.7 | 健康检查不检查依赖组件 | 检查代码 | 无外部调用 |

---

### Step 26：portal/user-frontend（nginx）

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 26.1 | nginx 正常 serve 静态资源 | curl / | 200 |
| 26.2 | 反向代理到 erp/backend 正常 | curl /api/v1/... | 正确 |
| 26.3 | `expose: true` 时端口映射正确 | 检查 compose | 正确 |
| 26.4 | `exposePort` 自定义端口正确 | 设置 exposePort | 正确 |
| 26.5 | nginx 通过环境变量获取后端地址 | 检查配置 | `${ERP_BACKEND_ENDPOINT}` |
| 26.6 | 健康检查正常 | curl / | 200 |

---

### Step 27：infra/redis-event-bus（Python）

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 27.1 | Redis Streams 正常 | 发送/接收事件 | 正确 |
| 27.2 | 健康检查正常 | curl /healthz | 200 |
| 27.3 | 健康检查不检查 Redis | 检查代码 | 无 Redis 调用 |

---

### Step 28：infra/api-docs（Go）

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 28.1 | Swagger UI 正常展示 | 浏览器访问 | 正确 |
| 28.2 | gRPC 文档正常展示 | 检查 | 正确 |
| 28.3 | 弱依赖组件不可用时不崩溃 | 禁用依赖 | 不崩溃 |

---

### Step 29：多版本默认共存验证

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 29.1 | 多版本共存后两个容器同时运行 | add 两个版本后 up | 两个容器 |
| 29.2 | 版本化服务名正确 | 检查 | `people-basic-1-0-0` 和 `people-basic-2-0-0` |
| 29.3 | 环境变量各指各的 | 检查 | 不同组件的 ENDPOINT 指向不同版本 |
| 29.4 | 多版本 remove 必须指定版本 | 不指定版本 remove | 报错 |
| 29.5 | `--only @版本` 只启动指定版本 | 验证 | 只有一个版本运行 |
| 29.6 | `--only` 不指定版本启动所有版本 | 验证 | 所有版本运行 |

---

## Phase 6：集成与 E2E

### Step 30：渐进式拼装与端到端验证

**目标：** 模拟真实的"渐进式拼装"开发场景，验证完整链路。同时验证源码工作区管理的完整流程。

**验证脚本核心步骤：**

```bash
# ===== 1. 初始化与底层组件安装（获取契约）=====
brickkit init e2e-test
brickkit add department/tree@1.0.0
ls .brickkit/artifacts/department-tree-1-0-0/api-contract/  # 验证 proto 下载

# ===== 2. 模拟连接组件开发（基于契约生成代码）=====
protoc --go_out=. ...  # 基于下载的 proto 生成客户端

# ===== 3. 安装连接组件（自动拼装）=====
brickkit add erp/backend@1.0.0
brickkit add portal/user-frontend@1.0.0

# ===== 4. 本地调试模式验证 (local-debug.env) =====
# 修改 brickkit.yaml，设置 people/basic 为 local: true
brickkit up
cat .brickkit/generated/local-debug.people-basic-1-0-0.env  # 验证 IDE 环境变量文件生成
# 验证依赖端口自动映射（如 18080）

# ===== 5. 源码工作区管理验证（--repo + sync）=====
brickkit add people/basic@1.0.0 --repo
ls components/people/basic/.git  # 验证完整 Git 仓库
brickkit add erp/backend@1.0.0 --repo-all
ls components/  # 验证所有开源组件源码存在
# 禁用 erp/backend，执行 sync
brickkit sync
ls components/.archived/erp/backend/  # 验证归档
ls components/.archived/portal/user-frontend/  # 验证级联归档
# 恢复 erp/backend，执行 sync
brickkit sync
ls components/erp/backend/  # 验证激活移回

# ===== 6. 启动与全链路验证 =====
brickkit up
# 验证 HTTP 调用、gRPC 调用（通过 extraPorts）、弱依赖降级、前端反向代理
# 验证 brickkit down 不删 volume

# ===== 7. 级联禁用验证 =====
# 在 brickkit.yaml 中设置 erp/backend 为 enabled: false
brickkit up
# 验证 portal/user-frontend 被级联跳过
# 验证 people/basic 被级联跳过

# ===== 8. 多版本默认共存验证 =====
brickkit add people/basic@2.0.0
brickkit up
# 验证 people-basic-1-0-0 和 people-basic-2-0-0 同时运行

# ===== 9. remove 自动删除源码验证 =====
brickkit remove people/basic@2.0.0
ls components/people/  # 验证源码目录已删除

# ===== 10. 从归档目录 publish 验证 =====
brickkit sync
brickkit publish --path ./components/.archived/erp/backend  # 验证从归档目录发布成功
```

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 30.1 | 渐进式拼装完整流程 | 执行上述脚本 | 全部通过 |
| 30.2 | 安装 → 获取契约 → 生成代码 → 再安装 | 验证每步 | 正确 |
| 30.3 | local-debug.env 生成且内容正确 | 检查文件 | 正确 |
| 30.4 | 源码工作区管理完整流程 | 验证每步 | 正确 |
| 30.5 | 级联禁用正确 | 验证 | 正确 |
| 30.6 | 多版本共存正确 | 验证 | 正确 |
| 30.7 | remove 自动删除源码 | 验证 | 正确 |
| 30.8 | 从归档目录 publish 成功 | 验证 | 成功 |
| 30.9 | brickkit down 不删 volume | 验证 | volume 存在 |
| 30.10 | 全链路 HTTP 调用正常 | curl | 正确 |
| 30.11 | 全链路 gRPC 调用正常 | grpcurl | 正确 |
| 30.12 | 弱依赖降级正常 | 禁用 event-bus | 不崩溃 |
| 30.13 | 前端反向代理正常 | curl | 正确 |

---

### Step 31：完整功能点验证清单

| # | 验证项 | 状态 |
| --- | --- | --- |
| 1 | `brickkit init` 必须传项目名称 | ✅ |
| 2 | `brickkit add` 自动下载 artifacts 到 `.brickkit/artifacts/` | ✅ |
| 3 | `brickkit add --yes` 非交互模式 | ✅ |
| 4 | 弱依赖缺失警告 + 完全不注入环境变量 | ✅ |
| 5 | 多版本默认共存（brickkit.yaml 中有几个版本就启动几个容器） | ✅ |
| 6 | 多版本 remove 必须指定版本 | ✅ |
| 7 | extraPorts 环境变量注入正确（`{ID}_{NAME}_ENDPOINT`） | ✅ |
| 8 | 保留变量冲突 → 警告 + 跳过，平台值优先 | ✅ |
| 9 | `local: true` 生成 `local-debug.<版本化服务名>.env`，自动映射依赖宿主机端口 | ✅ |
| 10 | `brickkit down` 不删除 volume | ✅ |
| 11 | K8s Job 迁移 + 自动清理旧 Job | ✅ |
| 12 | 市场 RustFS 存储 artifacts 正常 | ✅ |
| 13 | 市场闭源组件 API 契约校验（源头防御） | ✅ |
| 14 | 市场 configSchema 保留变量校验（源头防御） | ✅ |
| 15 | `brickkit login` 正常，Token 存 `.brickkit/credentials` | ✅ |
| 16 | Token 过期报错提示重新登录 | ✅ |
| 17 | `brickkit add` 默认共存（同 ID 不同版本自动添加第二个条目，不写 enabled） | ✅ |
| 18 | 级联禁用（enabled 三种状态：钉住 / 默认开启可被级联 / 显式关闭） | ✅ |
| 19 | `brickkit up --config` 多环境配置 | ✅ |
| 20 | `brickkit up --only @版本` 精确启动指定版本 | ✅ |
| 21 | `brickkit up --only` 遇 `enabled: false` 报错阻断 | ✅ |
| 22 | 渐进式拼装完整流程（安装→下契约→生成代码→再安装） | ✅ |
| 23 | ~~Podman 与 Docker 体验一致~~ | ⬜ **已作废**：Podman 支持写过、跑过，因 `down` 无法可靠工作而移除（005 §7） |
| 24 | `brickkit up --dry-run` 只生成文件不启动 | ✅ |
| 25 | `brickkit up --check-resources` 资源不可达时警告但不阻断 | ✅ |
| 26 | `brickkit add --repo` 正常 clone 开源组件源码 | ✅ |
| 27 | `brickkit add --repo` 指定闭源组件时报错阻断 | ✅ |
| 28 | `brickkit add --repo` 目标目录已存在时报错阻断 | ✅ |
| 29 | `brickkit add --repo-all` clone 所有开源依赖，闭源跳过并提示 | ✅ |
| 30 | 已存在的组件再次 add + `--repo` 只执行 clone，不重复写入 brickkit.yaml | ✅ |
| 31 | `brickkit sync` 归档不启动的组件源码到 `components/.archived/` | ✅ |
| 32 | `brickkit sync` 激活恢复启用的组件源码（从归档目录移回） | ✅ |
| 33 | `brickkit sync` 双向操作正确（先归档再激活） | ✅ |
| 34 | `brickkit sync` 归档后 `.git` 目录完整，Git 操作不受影响 | ✅ |
| 35 | `brickkit sync` 不影响运行中的容器 | ✅ |
| 36 | `brickkit sync` `local: true` 组件一视同仁 | ✅ |
| 37 | `brickkit remove` 自动删除 `components/` 下对应源码目录 | ✅ |
| 38 | `brickkit remove` 清理 artifacts 缓存 | ✅ |
| 39 | `components/` 目录不提交到项目 Git 仓库（`.gitignore`） | ✅ |
| 40 | 从归档目录 `brickkit publish` 路径正确 | ✅ |
| 41 | 升级时 CLI 检查依赖兼容性（002 §7.7） | ✅ |
| 42 | 升级后调用方 Manifest 不自动切换（002 §7.8） | ✅ |
| 43 | 升级时 configSchema 变更静默处理（002 §7.9） | ✅ |
| 44 | 升级时 artifacts 自动下载新版本（002 §7.10） | ✅ |
| 45 | `brickkit up --dry-run` 升级变更摘要 | ✅ |
| 46 | 升级时旧版本 artifacts 保留 | ✅ |
| 47 | 一个组件一个仓库（不支持 monorepo） | ✅ |
| 48 | 所有组件都是 container（包括前端） | ✅ |
| 49 | 健康检查只检查本进程存活 | ✅ |
| 50 | 迁移失败 = 主服务不启动 | ✅ |

---

## Phase 7：专项测试

### Step 32：边界测试

**目标：** 测试极端输入、空值、超长字符串、特殊字符等边界场景。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 32.1 | 组件 ID 超长（256 字符） | 构造超长 ID | 报错或截断 |
| 32.2 | 组件 ID 含特殊字符 | `id: "a@b/c"` | 报错 |
| 32.3 | 版本号超长 | `version: "999999.999999.999999"` | 正常处理 |
| 32.4 | 依赖列表为空 | `components: []` | 正常 |
| 32.5 | 依赖列表超大（100 个依赖） | 构造 100 个依赖 | 正常解析 |
| 32.6 | config 值为空字符串 | `config: {key: ""}` | 注入空字符串 |
| 32.7 | config 值为 null | `config: {key: null}` | 跳过或注入空 |
| 32.8 | config 值为特殊字符 | `config: {key: "a=b&c=d"}` | 原样注入 |
| 32.9 | brickkit.yaml 为空文件 | 空文件 | 报错 |
| 32.10 | brickkit.yaml 为非法 YAML | 非法格式 | 报错，指出行号 |
| 32.11 | component.yaml 为空文件 | 空文件 | 报错 |
| 32.12 | 组件 ID 只有 scope 没有 name | `id: "people/"` | 报错 |
| 32.13 | 组件 ID 只有 name 没有 scope | `id: "basic"` | 报错 |
| 32.14 | port 为 0 | `port: 0` | 报错 |
| 32.15 | port 为负数 | `port: -1` | 报错 |
| 32.16 | port 超过 65535 | `port: 99999` | 报错 |
| 32.17 | extraPorts port 与主端口相同 | extraPorts port = deployment.port | 报错或警告 |
| 32.18 | 深层嵌套依赖（10 层） | A→B→C→...→J | 正常解析 |
| 32.19 | 大量组件（50 个） | 50 个组件的项目 | 正常处理 |
| 32.20 | 项目名称含中划线 | `my-project` | 正常 |
| 32.21 | 项目名称含数字 | `project123` | 正常 |
| 32.22 | 项目名称只有数字 | `123` | 正常 |
| 32.23 | artifacts files 列表为空 | `files: []` | 报错 |
| 32.24 | artifacts files 路径含特殊字符 | `files: ["a b/c.proto"]` | 正常处理 |
| 32.25 | configSchema properties 为空 | `properties: {}` | 正常 |

---

### Step 33：错误处理测试

**目标：** 测试所有错误场景的处理，确保错误信息清晰、退出码正确。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 33.1 | 强依赖缺失错误信息包含组件 ID 和缺失依赖 | 触发错误 | 信息完整 |
| 33.2 | 循环依赖错误信息包含循环路径 | 触发错误 | 信息完整 |
| 33.3 | 资源未绑定错误信息包含组件 ID 和资源类型 | 触发错误 | 信息完整 |
| 33.4 | 镜像拉取失败错误信息包含镜像地址和 docker login 提示 | 触发错误 | 信息完整 |
| 33.5 | 迁移失败错误信息包含组件 ID、命令、退出码、日志查看方法 | 触发错误 | 信息完整 |
| 33.6 | 端口冲突错误信息包含冲突的组件和建议 | 触发错误 | 信息完整 |
| 33.7 | 未登录发布错误信息包含 brickkit login 提示 | 触发错误 | 信息完整 |
| 33.8 | Token 过期错误信息包含过期时间和重新登录提示 | 触发错误 | 信息完整 |
| 33.9 | 保留变量冲突警告信息包含配置项名、环境变量名、冲突模式 | 触发警告 | 信息完整 |
| 33.10 | clone 失败（闭源）错误信息包含组件 ID 和来源类型 | 触发错误 | 信息完整 |
| 33.11 | clone 失败（目录已存在）错误信息包含目录路径和建议 | 触发错误 | 信息完整 |
| 33.12 | 多版本 remove 未指定版本错误信息包含版本列表 | 触发错误 | 信息完整 |
| 33.13 | `--only` 指定 enabled: false 错误信息包含组件 ID | 触发错误 | 信息完整 |
| 33.14 | 所有错误的退出码非 0 | 检查退出码 | 非 0 |
| 33.15 | 所有警告的退出码为 0 | 检查退出码 | 0 |
| 33.16 | 错误信息不暴露内部实现细节 | 检查所有错误输出 | 无堆栈/内部路径 |
| 33.17 | 错误信息包含建议（"建议："） | 检查所有错误输出 | 有建议 |
| 33.18 | brickkit.yaml 格式错误时指出具体行号 | 构造格式错误 | 包含行号 |
| 33.19 | 市场不可达时提示检查网络 | Mock 不可达 | 包含网络提示 |
| 33.20 | Git 仓库不可达时提示检查 URL | Mock 不可达 | 包含 URL 提示 |

---

### Step 34：兼容性测试（Docker）

**目标：** 验证 Docker 环境下各项行为正确。

> ⚠️ **本步原为“Docker / Podman”，Podman 那一半已作废。**
> Podman 支持写过、跑过，因 `down` 无法可靠工作而移除（005 §7）。
>
> 原表里 34.6 那行还写着“Podman 用 `host.containers.internal`”——
> **那句话本身也是错的**：`host.containers.internal` 是 Podman 自动注入到容器
> `/etc/hosts` 的**主机名**，不是 `--add-host` 能接受的**值**，
> 按它生成的容器根本创建不出来。Docker 与 Podman 都用 `host-gateway`（D 记录见 P23）。

**测试验证：**

| # | 验证项 | 预期结果 | 状态 |
| --- | --- | --- | --- |
| 34.1 | `brickkit up` 正常启动 | 容器 running/healthy | ✅ |
| 34.2 | `brickkit down` 正常停止 | 容器停止，volume 保留 | ✅ |
| 34.3 | `brickkit status` 正常展示 | 状态与端口正确 | ✅ |
| 34.4 | 健康检查正常 | healthy 才算就绪 | ✅ |
| 34.5 | migration 正常执行 | 失败则主服务不启动 | ✅ |
| 34.6 | `local: true` 的 extra_hosts | `host-gateway` | ✅ |
| 34.7 | volume 保留 | `down` 不删数据卷 | ✅ |
| 34.8 | 网络隔离 | 每个项目一个网络 | ✅ |
| 34.9 | 端口映射 | `expose` 才映射到宿主机 | ✅ |
| 34.10 | 日志输出 | 提示里的命令可直接执行 | ✅ |
| 34.11 | CLI 自动检测引擎 | 有 Docker 用 Docker；只有 Podman 时报专门的错 | ✅ |
| 34.12 | 引擎不存在时报错 | 报错并指出 `--dry-run` 不需要引擎 | ✅ |

---

### Step 35：安全测试

**目标：** 验证所有安全相关的功能点。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 35.1 | 保留变量保护（市场发布时校验） | 发布含冲突配置项 | 拒绝 |
| 35.2 | 保留变量保护（CLI 注入时检测） | 构造冲突 config | 警告并跳过 |
| 35.3 | 所有保留模式覆盖 | 逐一测试 COMPONENT_ID、*_ENDPOINT、DATABASE_*、REDIS_*、MQ_*、STORAGE_*、SEARCH_*、SMTP_*、{envPrefix}_* | 全部检测 |
| 35.4 | 密钥不硬编码在 brickkit.yaml | 检查生成的文件 | 使用 ${ENV_VAR} |
| 35.5 | .env 文件不提交 Git | 检查 .gitignore | 包含 .env |
| 35.6 | credentials 文件不提交 Git | 检查 .gitignore | 包含 .brickkit/credentials |
| 35.7 | 签名校验（requireSignature: true） | 安装未签名组件 | 阻断 |
| 35.8 | 签名校验（requireSignature: false） | 安装未签名组件 | 允许 |
| 35.9 | private 组件未授权不可访问 | 无 Token 访问 | 403 |
| 35.10 | 默认不暴露端口 | 不设置 expose | 无端口映射 |
| 35.11 | 不声明 expose 不生成 Ingress | 不设置 expose | 无 Ingress |
| 35.12 | 闭源组件必须提供 API 契约 | 发布无契约的闭源组件 | 拒绝 |
| 35.13 | 日志不输出密码 | 检查日志输出 | 无密码 |
| 35.14 | 错误信息不输出密码 | 检查错误输出 | 无密码 |
| 35.15 | blocked 组件不可安装 | 安装 blocked 组件 | 拒绝 |
| 35.16 | 容器使用非 root 用户 | 检查 Dockerfile | USER 非 root |
| 35.17 | config 字段不包含密钥 | 检查 brickkit.yaml | 密钥在 .env |
| 35.18 | 生产环境强制签名 | requireSignature: true | 强制 |

---

### Step 36：性能基准测试

**目标：** 验证大量组件、大依赖树场景下的性能。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 36.1 | 50 个组件的项目 `brickkit up` 时间 | 计时 | < 30 秒 |
| 36.2 | 10 层深度依赖树解析时间 | 计时 | < 5 秒 |
| 36.3 | 100 个依赖的组件解析时间 | 计时 | < 5 秒 |
| 36.4 | 大量 artifacts 下载时间（50 个文件） | 计时 | < 60 秒 |
| 36.5 | brickkit.yaml 解析时间（100 个组件条目） | 计时 | < 1 秒 |
| 36.6 | docker-compose.yaml 生成时间（50 个组件） | 计时 | < 5 秒 |
| 36.7 | `brickkit order` 输出时间（50 个组件） | 计时 | < 1 秒 |
| 36.8 | `brickkit status` 输出时间（50 个组件） | 计时 | < 5 秒 |
| 36.9 | `brickkit sync` 执行时间（50 个源码目录） | 计时 | < 10 秒 |
| 36.10 | 内存使用（50 个组件） | 监控 | < 100MB |

---

### Step 37：回归测试

**目标：** 建立回归测试清单，确保修复 Bug 后不引入新问题。

**回归测试清单：**

| # | 回归测试项 | 对应 Step | 状态 |
| --- | --- | --- | --- |
| R1 | `brickkit init` 基本功能 | Step 3 | ☐ |
| R2 | `brickkit add` 基本功能 | Step 9 | ☐ |
| R3 | `brickkit add` 递归依赖 | Step 9 | ☐ |
| R4 | `brickkit add` artifacts 下载 | Step 9 | ☐ |
| R5 | `brickkit add --repo` | Step 9 | ☐ |
| R6 | `brickkit remove` 基本功能 | Step 9 | ☐ |
| R7 | `brickkit up` 基本功能 | Step 15 | ☐ |
| R8 | `brickkit up` 级联禁用 | Step 15 | ☐ |
| R9 | `brickkit up` 迁移执行 | Step 14/15 | ☐ |
| R10 | `brickkit down` 不删 volume | Step 15 | ☐ |
| R11 | `brickkit status` 基本功能 | Step 15 | ☐ |
| R12 | `brickkit order` 基本功能 | Step 10 | ☐ |
| R13 | `brickkit sync` 基本功能 | Step 17 | ☐ |
| R14 | `brickkit reset` 基本功能 | Step 8 | ☐ |
| R15 | `brickkit login` 基本功能 | Step 19 | ☐ |
| R16 | `brickkit publish` 基本功能 | Step 19 | ☐ |
| R17 | 环境变量注入正确 | Step 11 | ☐ |
| R18 | 保留变量保护 | Step 11 | ☐ |
| R19 | 多版本共存 | Step 29 | ☐ |
| R20 | 弱依赖降级 | Step 25 | ☐ |
| R21 | local 调试 | Step 13 | ☐ |
| R22 | K8s 部署文件生成 | Step 16 | ☐ |
| R23 | 市场发布校验 | Step 18 | ☐ |
| R24 | 签名校验 | Step 20 | ☑ 2026-08-15：真 PostgreSQL 上改库篡改 Manifest，CLI 退出码 1 |
| R25 | 升级兼容性检查 | Step 7 | ☐ |

**回归测试执行规则：**

- 每次修复 Bug 后，执行完整的回归测试清单。
- 每次合并 PR 前，执行完整的回归测试清单。
- 回归测试失败时，阻断合并。

---

### Step 38：升级测试

**目标：** 验证组件版本升级的完整链路（002 §7.7–7.10、004 §3.5.1、011 §12.3–12.7）。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 38.1 | 修改版本号 → `brickkit up` 升级成功 | 修改版本，执行 up | 新版本运行 |
| 38.2 | 升级时新版本 Manifest 自动获取 | 检查 | 获取成功 |
| 38.3 | 升级时新版本 artifacts 自动下载 | 检查 `.brickkit/artifacts/` | 新版本目录存在 |
| 38.4 | 升级时旧版本 artifacts 保留 | 检查 | 旧版本目录仍存在 |
| 38.5 | 升级时依赖兼容性检查通过 | 正常升级 | 无报错 |
| 38.6 | 升级时强依赖不可满足报错 | 新版本新增不存在的强依赖 | 报错 |
| 38.7 | 升级时资源未绑定报错 | 新版本新增资源依赖但未绑定 | 报错 |
| 38.8 | 升级时弱依赖缺失警告 | 新版本新增弱依赖 | 警告 |
| 38.9 | 升级时循环依赖报错 | 新版本引入循环 | 报错 |
| 38.10 | 升级后调用方 Manifest 不自动切换 | 检查调用方 | 仍指向旧版本 |
| 38.11 | 升级后移除旧版本，调用方强依赖报错 | 移除旧版本 | 报错 |
| 38.12 | 升级后多版本共存，调用方正常 | 保留两个版本 | 正常 |
| 38.13 | 升级时新增配置项使用默认值 | 新版本新增配置项 | 使用默认值 |
| 38.14 | 升级时删除配置项静默忽略 | 新版本删除配置项，brickkit.yaml 仍有覆盖 | 不注入，不报错 |
| 38.15 | 升级时修改配置项默认值生效 | 新版本修改默认值，无覆盖 | 使用新默认值 |
| 38.16 | 升级时有覆盖的配置项覆盖值优先 | 新版本修改默认值，有覆盖 | 使用覆盖值 |
| 38.17 | `brickkit up --dry-run` 升级变更摘要 | 执行 --dry-run | 输出变更摘要 |
| 38.18 | 变更摘要包含依赖变更 | 检查输出 | 正确 |
| 38.19 | 变更摘要包含 configSchema 变更 | 检查输出 | 正确 |
| 38.20 | 变更摘要包含迁移信息 | 检查输出 | 正确 |
| 38.21 | 变更摘要包含 artifacts 变更 | 检查输出 | 正确 |
| 38.22 | 变更摘要包含资源配额变更 | 检查输出 | 正确 |
| 38.23 | 升级时迁移正常执行 | 新版本有 migration | 迁移成功 |
| 38.24 | 升级时迁移失败阻断启动 | 构造失败迁移 | 主服务不启动 |
| 38.25 | `brickkit add X@新版本` 升级方式 | 执行 add | 共存，不覆盖 |
| 38.26 | 升级后 `brickkit status` 显示新版本 | 执行 status | 新版本运行 |
| 38.27 | 升级后回滚（改回旧版本号） | 改回旧版本，up | 旧版本运行 |
| 38.28 | 回滚后数据库不自动回滚 | 检查数据库 | 结构已变更 |

---

## Phase 8：验收与发布

### Step 39：完整功能点验收

**目标：** 对照所有设计书，逐一验收每个功能点。

**验收清单（按设计书分组）：**

**001 平台理念与总体架构：**

| # | 验收项 | 状态 |
| --- | --- | --- |
| A1.1 | CLI 六件事全部实现 | ☐ |
| A1.2 | 服务发现 = DNS | ☐ |
| A1.3 | 版本化服务名正确 | ☐ |
| A1.4 | 统一地址格式（本地与生产一致） | ☐ |
| A1.5 | 本地调试模型（local: true + extra_hosts） | ☐ |
| A1.6 | MVP 验证目标全部通过 | ☐ |

**002 组件规范：**

| # | 验收项 | 状态 |
| --- | --- | --- |
| A2.1 | Manifest 所有字段正确解析 | ☐ |
| A2.2 | 强/弱依赖正确识别 | ☐ |
| A2.3 | 精确版本（拒绝 ^ 和 ~） | ☐ |
| A2.4 | 多版本默认共存 | ☐ |
| A2.5 | 破坏性变更协同守则 | ☐ |
| A2.6 | 所有组件都是 container | ☐ |
| A2.7 | extraPorts 正确 | ☐ |
| A2.8 | 资源配额正确 | ☐ |
| A2.9 | 保留变量保护 | ☐ |
| A2.10 | 升级兼容性检查（§7.7） | ☐ |
| A2.11 | 升级时调用方不自动切换（§7.8） | ☐ |
| A2.12 | 升级时 configSchema 变更处理（§7.9） | ☐ |
| A2.13 | 升级时 artifacts 处理（§7.10） | ☐ |
| A2.14 | 数据库迁移正确 | ☐ |
| A2.15 | 健康检查只检查本进程存活 | ☐ |
| A2.16 | 一个组件一个仓库 | ☐ |
| A2.17 | artifacts 字段正确 | ☐ |
| A2.18 | API 契约公开原则 | ☐ |

**003 项目配置规范：**

| # | 验收项 | 状态 |
| --- | --- | --- |
| A3.1 | brickkit.yaml 所有字段正确解析 | ☐ |
| A3.2 | enabled 三种状态 | ☐ |
| A3.3 | 级联禁用正确 | ☐ |
| A3.4 | local 调试正确 | ☐ |
| A3.5 | expose/exposePort 正确 | ☐ |
| A3.6 | config 覆盖正确 | ☐ |
| A3.7 | resources 覆盖正确 | ☐ |
| A3.8 | 安装源正确 | ☐ |
| A3.9 | 备份与恢复正确 | ☐ |
| A3.10 | 多环境配置正确 | ☐ |

**004 CLI 设计：**

| # | 验收项 | 状态 |
| --- | --- | --- |
| A4.1 | 所有 11 个命令实现 | ☐ |
| A4.2 | 依赖解析引擎正确 | ☐ |
| A4.3 | 部署文件生成正确 | ☐ |
| A4.4 | 升级时额外行为（§3.5.1） | ☐ |
| A4.5 | 错误处理正确 | ☐ |

**005 部署与运行规范：**

| # | 验收项 | 状态 |
| --- | --- | --- |
| A5.1 | Docker 部署正确 | ☐ |
| A5.2 | K8s 部署正确 | ☐ |
| A5.3 | Podman 兼容 | ☐ |
| A5.4 | 启动/停止顺序正确 | ☐ |
| A5.5 | 端口暴露规则正确 | ☐ |

**006 基础资源规范：**

| # | 验收项 | 状态 |
| --- | --- | --- |
| A6.1 | 资源声明正确 | ☐ |
| A6.2 | 资源绑定正确 | ☐ |
| A6.3 | 资源注入正确 | ☐ |
| A6.4 | 密钥管理正确 | ☐ |
| A6.5 | 多组件共享资源正确 | ☐ |
| A6.6 | 资源健康检查（--check-resources） | ☐ |

**007 组件市场设计：**

| # | 验收项 | 状态 |
| --- | --- | --- |
| A7.1 | 市场 API 全部实现 | ☐ |
| A7.2 | 发布流程正确 | ☐ |
| A7.3 | 可见性控制正确 | ☐ |
| A7.4 | 版本状态管理正确 | ☐ |
| A7.5 | Manifest 校验正确 | ☐ |
| A7.6 | 保留变量校验（源头防御） | ☐ |
| A7.7 | 闭源 API 契约校验 | ☐ |
| A7.8 | artifacts 存储与分发 | ☐ |

**008 安全与治理：**

| # | 验收项 | 状态 |
| --- | --- | --- |
| A8.1 | 签名校验正确 | ☐ |
| A8.2 | 默认不暴露 | ☐ |
| A8.3 | 密钥管理正确 | ☐ |
| A8.4 | 信任模型正确 | ☐ |

**009–011 开发者指南：**

| # | 验收项 | 状态 |
| --- | --- | --- |
| A9.1 | 快速入门流程可跑通 | ☐ |
| A10.1 | 发布流程可跑通 | ☐ |
| A11.1 | 安装与拼装流程可跑通 | ☐ |
| A11.2 | 升级流程可跑通（§12.3–12.7） | ☐ |
| A11.3 | 源码工作区管理流程可跑通 | ☐ |
| A11.4 | 渐进式拼装流程可跑通 | ☐ |

---

### Step 40：文档验证

**目标：** 验证所有设计书中的示例、命令、输出与实际实现一致。

**测试验证：**

| # | 验证项 | 验证方法 | 预期结果 |
| --- | --- | --- | --- |
| 40.1 | 009 快速入门中的所有命令可执行 | 逐一执行 | 无报错 |
| 40.2 | 009 快速入门中的所有输出与实际一致 | 对比 | 一致 |
| 40.3 | 010 发布指南中的所有命令可执行 | 逐一执行 | 无报错 |
| 40.4 | 011 安装指南中的所有命令可执行 | 逐一执行 | 无报错 |
| 40.5 | 004 CLI 设计中的所有命令参数正确 | 逐一验证 | 正确 |
| 40.6 | 004 CLI 设计中的所有错误输出与实际一致 | 对比 | 一致 |
| 40.7 | 附录 F Compose 示例与实际生成一致 | 对比 | 一致 |
| 40.8 | 附录 G K8s YAML 示例与实际生成一致 | 对比 | 一致 |
| 40.9 | 000 阅读指南中的文档编号和章节引用正确 | 检查 | 正确 |
| 40.10 | 所有设计书中的交叉引用正确 | 检查 | 正确 |

---

## 附录 A：数据库分布

| 数据库 | 组件 |
| --- | --- |
| PostgreSQL | department, people, auth, authorization, erp |
| Redis | rbac (缓存), event-bus (Streams) |

## 附录 B：协议覆盖

| 组件 | 协议 |
| --- | --- |
| department/tree | HTTP + gRPC（单端口 8080） |
| people/basic | HTTP (8080) + gRPC (9090, `extraPorts`) |
| erp/backend | HTTP（调用方，使用主端口和额外端口环境变量） |

## 附录 C：依赖关系总图

```
portal/user-frontend (nginx)
      ↓ 强依赖
erp/backend (Go, 连接组件)
      ├── ↓ 强依赖 → people/basic (Python, extraPorts:9090)
      │       └── ↓ 强依赖 → department/tree (Go, 单端口)
      ├── ↓ 强依赖 → auth/password-login
      ├── ↓ 强依赖 → authorization/rbac
      └── ↓ 弱依赖 → infra/redis-event-bus
```

## 附录 D：渐进式拼装角色分工

| 角色 | 做什么 | 使用什么 |
| --- | --- | --- |
| 底层组件开发者 | 发布组件，在 artifacts 中声明 proto | `brickkit publish` |
| 连接组件开发者 | 安装底层组件 → 获取契约 → 生成代码 → 开发 → 发布 | `brickkit add` + IDE + `brickkit publish` |
| 最终用户 | 安装连接组件 → 自动拼装完整系统 | `brickkit add` + `brickkit up` |

## 附录 E：源码工作区管理流程

| 角色 | 做什么 | 使用什么 |
| --- | --- | --- |
| 组件修改者 | clone 源码 → 修改 → push → publish | `brickkit add --repo` + Git + `brickkit publish` |
| 项目管理员 | 整理工作区（归档/激活） | `brickkit sync` |
| CLI | 自动删除源码（remove 时） | `brickkit remove` |

## 附录 F：测试执行命令

```bash
# 单元测试
make test-unit

# 集成测试
make test-integration

# E2E 测试
make test-e2e

# 边界测试
make test-boundary

# 错误处理测试
make test-error

# 兼容性测试（需要 Docker 和 Podman 都安装）
make test-compat

# 安全测试
make test-security

# 性能基准测试
make test-perf

# 回归测试
make test-regression

# 升级测试
make test-upgrade

# 全部测试
make test-all
```

## 附录 G：测试环境要求

| 环境 | 要求 |
| --- | --- |
| Docker | 20.10+ |
| Docker Compose | V2（2022+） |
| Podman | 4.0+（兼容性测试需要） |
| podman-compose | 1.0+（兼容性测试需要） |
| Go | 1.22+ |
| protoc | 3.20+ |
| Python | 3.11+（测试组件需要） |
| Node.js | 18+（前端组件需要） |
| PostgreSQL | 15 |
| Redis | 7 |
| RustFS | 最新稳定版（S3 兼容对象存储，默认端点 http://localhost:9000） |
| cosign | 2.0+（签名测试需要） |
| grpcurl | 最新稳定版（gRPC 测试需要） |
