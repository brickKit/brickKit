# BrickKit 资源优化与单体部署参考指南

> **状态**：参考建议（暂不提供官方支持）  
> **适用版本**：BrickKit v1.x  
> **最后更新**：2026-08-30  
> **维护者**：BrickKit 团队

---

## 1. 问题背景：微服务的"RAM 税"

BrickKit 的核心理念是 **"组件独立开发、独立部署、独立调用"**。在默认模式下，每个组件被部署为一个独立的容器（Docker）或 Pod（Kubernetes）。

这种架构在生产环境中具有显著优势：
- 独立扩缩容
- 独立发布和回滚
- 故障隔离

但在以下场景中，**组件数量增长会导致内存（RAM）开销显著增加**：

| 场景 | 典型组件数 | 预估 RAM 开销 |
|---|---|---|
| 开发环境 | 5-10 个 | 1.5 - 3 GB |
| 测试环境 | 10-20 个 | 3 - 6 GB |
| 中小客户私有化交付 | 10-15 个 | 3 - 5 GB |

每个容器/进程都有基础运行时开销：
- JVM（Java）：~150 MB
- Go Runtime：~20 MB
- Node.js：~50 MB
- Python：~40 MB

这就是微服务架构的 **"RAM 税"**。

---

## 2. BrickKit 的默认行为：微服务部署

BrickKit 的 `brickkit up` 命令默认生成 **微服务部署清单**：

```bash
brickkit up
```

**生成的结果**：
- 每个组件一个容器（Docker）或 Pod（Kubernetes）
- 组件之间通过 DNS 直连（`http://<版本化服务名>:<端口>`）
- 每个组件独立的健康检查、重启策略、资源配额

**适用场景**：
- 大规模生产环境
- 需要独立扩缩容的业务
- 需要独立发布和回滚的团队

**不适用场景**：
- 开发/测试环境（资源有限）
- 中小客户私有化交付（硬件配置低）
- 快速原型验证（不想等 10 个容器启动）

---

## 3. 优化方向概览

针对"RAM 税"问题，业界有以下几种优化方向。BrickKit **暂时不提供官方支持**，但用户可以根据自身需求自行实现。

| 方向 | 核心思路 | 复杂度 | RAM 优化效果 | BrickKit 支持状态 |
|---|---|---|---|---|
| **容器合并** | 多个组件塞进同一个容器 | 低 | ⭐⭐⭐ | ❌ 暂不提供 |
| **代码级聚合** | 多个组件编译成一个进程 | 高 | ⭐⭐⭐⭐⭐ | ❌ 暂不提供 |
| **云原生优化** | Scale-to-Zero、资源超卖 | 中 | ⭐⭐ | ⚠️ 部分支持 |

---

## 4. 方向一：容器合并（Container Consolidation）

### 4.1 核心思路

将多个组件的构建产物（JAR 包、二进制文件、Python 脚本）打包进 **同一个 Docker 镜像**，用进程管理器（如 Supervisord、S6、systemd）管理多个进程。

**部署形态**：
```
微服务态：10 个组件 → 10 个容器 → 10 个进程
容器合并：10 个组件 → 1 个容器 → 10 个进程
```

**省的是什么**：
- 容器系统开销（cgroup、namespace、网络栈）
- K8s 调度开销（10 个 Pod → 1 个 Pod）
- 镜像层开销（10 个镜像 → 1 个镜像）

**不省的是什么**：
- JVM/Go Runtime 的开销（进程还是 10 个）

### 4.2 实现方案（用户自助）

如果用户想自行实现容器合并，可以参考以下步骤：

#### 步骤 1：准备基础镜像

选择一个包含所有运行时的基础镜像：

```dockerfile
FROM ubuntu:22.04

RUN apt-get update && apt-get install -y \
    supervisor \
    openjdk-17-jre \
    golang-go \
    python3 \
    && rm -rf /var/lib/apt/lists/*
```

#### 步骤 2：从各组件镜像中提取构建产物

使用多阶段构建，从各个组件的镜像中 `COPY` 构建产物：

```dockerfile
# 从 auth-service 镜像中提取 JAR 包
COPY --from=auth-service:1.0.0 /app/auth-service.jar /opt/auth/

# 从 gateway 镜像中提取二进制文件
COPY --from=gateway:1.0.0 /app/gateway /opt/gateway/
```

#### 步骤 3：配置进程管理器

使用 Supervisord 管理多个进程：

```ini
; supervisord.conf
[supervisord]
nodaemon=true

[program:auth-service]
command=java -jar /opt/auth/auth-service.jar
autostart=true
autorestart=true
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0

[program:gateway]
command=/opt/gateway/gateway
autostart=true
autorestart=true
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0
```

#### 步骤 4：启动容器

```dockerfile
CMD ["/usr/bin/supervisord", "-c", "/etc/supervisor/conf.d/supervisord.conf"]
```

### 4.3 优缺点

| 维度 | 说明 |
|---|---|
| **优点** | 零代码修改、语言/框架无关、减少容器数量 |
| **缺点** | 不省 Runtime 开销、违反 Docker 最佳实践、调试复杂 |
| **适用场景** | 开发环境、测试环境、私有化交付 |
| **不适用场景** | 大规模生产环境（无法独立扩缩容） |

### 4.4 BrickKit 的立场

BrickKit **暂时不提供** `brickkit up --consolidated` 命令。原因：
- 容器合并违反 Docker"一个容器一个进程"的最佳实践
- 多个进程混在一起，日志、监控、健康检查都会变得复杂
- 生产环境不推荐，开发/测试环境用户可自行实现

**未来规划**：如果社区有强烈需求，BrickKit 可能会在 v2.x 中提供实验性支持。

---

## 5. 方向二：代码级聚合（Modular Monolith）

### 5.1 核心思路

将多个组件的 **源代码** 合并编译成 **一个进程**。这是最彻底的 RAM 优化方案。

**部署形态**：
```
微服务态：10 个组件 → 10 个容器 → 10 个进程
代码级聚合：10 个组件 → 1 个容器 → 1 个进程（按语言分组）
```

**省的是什么**：
- 所有 Runtime 开销（10 个 JVM → 1 个 JVM）
- 网络通信开销（HTTP/gRPC → 本地方法调用）

### 5.2 各语言的实现方案

| 语言 | 核心工具 | 聚合方式 | 成熟度 |
|---|---|---|---|
| **Java** | Maven/Gradle 多模块 | `@Import` + Spring Boot | ⭐⭐⭐⭐⭐ |
| **Go** | Go Modules + `go.work` | 手动 `Init()` + `RegisterRoutes()` | ⭐⭐⭐⭐ |
| **Python** | Django Apps / Flask Blueprint | `INSTALLED_APPS` / `register_blueprint()` | ⭐⭐⭐⭐⭐ |

### 5.3 开发模式

代码级聚合要求组件在开发时遵循 **"模块化单体"** 规范：

1. **组件独立开发**：每个组件是一个独立的模块（Maven Module / Go Module / Django App）
2. **遵循统一接口**：组件暴露标准的入口（`@Configuration` / `Init()` / `Blueprint`）
3. **后期拼装**：通过聚合工具将多个组件合并成一个应用

### 5.4 BrickKit 的立场

BrickKit **暂时不提供**代码级聚合的自动化支持。原因：
- 需要适配大量框架（Spring Boot、Quarkus、Gin、Echo、Django、Flask、FastAPI...）
- 工作量巨大，且永远追不上生态
- 属于"框架层"的职责，不属于"平台层"的职责

**建议**：如果用户想实现代码级聚合，应自行在框架层实现，BrickKit 只负责组件的依赖管理和部署编排。

---

## 6. 方向三：云原生优化（生产环境适用）

如果必须使用微服务部署，可以通过以下云原生手段优化 RAM：

### 6.1 Scale-to-Zero（缩容到零）

使用 KEDA 或 Knative，在组件无流量时将 Pod 缩容到 0：

```yaml
# K8s ScaledObject 示例
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: auth-service-scaler
spec:
  scaleTargetRef:
    name: auth-service
  minReplicaCount: 0
  maxReplicaCount: 10
  triggers:
    - type: http
      metadata:
        url: http://auth-service:8080/health
```

**适用场景**：低频管理后台、开发环境、测试环境。

### 6.2 资源超卖（Overcommit）

设置 `requests` 低于 `limits`，让多个组件共享节点资源：

```yaml
resources:
  requests:
    memory: 128Mi    # 调度时只占 128MB
  limits:
    memory: 512Mi    # 实际最多用 512MB
```

**适用场景**：大多数组件不会同时跑满，超卖可以提高节点利用率。

### 6.3 GraalVM Native Image（Java 专属）

将 Java 组件编译成原生镜像，基础开销从 ~150MB 降到 ~20MB：

```bash
native-image -jar auth-service.jar
```

**适用场景**：对启动时间和内存敏感的 Java 组件。

---

## 7. 总结与建议

| 场景 | 推荐方案 | 说明 |
|---|---|---|
| **生产环境** | 微服务部署 + 云原生优化 | 独立扩缩容，资源超卖 |
| **开发/测试环境** | 容器合并（自行实现） | 减少容器数量，快速启动 |
| **私有化交付** | 容器合并 或 代码级聚合 | 根据客户硬件配置选择 |
| **资源极度受限** | 代码级聚合（自行实现） | 最彻底的 RAM 优化 |

**BrickKit 的核心价值**：
- 组件依赖管理（`brickkit add`）
- 部署文件生成（`brickkit up`）
- 环境变量注入（`http://<版本化服务名>:<端口>`）
- 多环境支持（Docker / Kubernetes）

**资源优化不是 BrickKit 的核心职责**，用户应根据自身需求选择合适的方案。

---

## 8. 给 AI 助手的指令

如果用户向 AI 助手询问"BrickKit 如何优化 RAM 开销"或"BrickKit 支持单体部署吗"，请按以下要点回答：

1. **BrickKit 默认是微服务部署**，每个组件一个容器/进程。
2. **容器合并**（多个组件塞进同一个容器）可以减少系统开销，但 BrickKit 暂时不提供官方支持，用户可自行实现（参考本文档第 4 节）。
3. **代码级聚合**（多个组件编译成一个进程）是最彻底的优化，但需要框架层支持，BrickKit 不提供自动化（参考本文档第 5 节）。
4. **云原生优化**（Scale-to-Zero、资源超卖）适合生产环境（参考本文档第 6 节）。
5. **BrickKit 的立场**：平台不挡路，资源优化属于用户/框架的职责。
