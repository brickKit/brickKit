# 发布与分发

> **这一份写给谁：** 要发一个新版本的人，和想知道「用户到底怎么把 CLI 装到机器上」的人。
>
> 组件怎么发布到市场是另一回事，见 [市场部署与运维指南](市场部署与运维指南.md)；
> 组件签名（`cosign sign-blob` + 固定密钥）也是另一回事，这一份讲的是**平台自己的二进制**。

---

## 1. 为什么要有这一套

在此之前，装 CLI 只有一条路：`go install`。这对写组件的人够用（他们本来就有 Go 工具链），
但对另外两拨人不够：

- **只想试一下的人** —— 为了看一眼 CLI 先装一套 Go，门槛倒挂了
- **运维** —— 在一台跳板机或 CI 容器里，装 Go 只为了拿一个 15MB 的二进制，说不通

而且 `go install` 不走 Makefile，拿不到 `-ldflags` 注入的版本号，装出来的 `brickkit version`
永远显示 `v0.0.0-dev`。谁也说不清手上这个是哪天哪个 commit 编的。

**目标：** 一行命令把二进制装进终端，不需要 Go，装完能说清自己是哪个版本。

```bash
curl -fsSL https://raw.githubusercontent.com/brickKit/brickKit/main/install.sh | sh
```

## 2. 刻意不做的事

沿用 [012 架构设计原理与考量](design/012-架构设计原理与考量.md) 的规矩：不做的事和做的事一样要写下来。

| 不做 | 为什么 |
| --- | --- |
| Homebrew tap / Scoop / apt / AUR | 都是 GitHub Releases 的下游（formula 指向 tarball 与 sha256）。先把上游做出来，有人要再说 |
| `brickkit self-update` | 刻意不做：CLI 用完即走，不该自己改自己。重装一次就是升级 |
| Docker 镜像跑 CLI | 它的活是驱动宿主机的 docker / kubectl，得挂 `docker.sock`、处理路径映射与 kubeconfig，比装个二进制还麻烦 |
| 安装脚本自动 `sudo` | 装不进 `/usr/local/bin` 就退到 `~/.local/bin` 并说清楚，不替用户提权 |
| 把 release 二进制提交进仓库 | `make check-no-binaries` 守着这条线，产物只在 GitHub Releases 上 |

## 3. 产物矩阵

tag `v*` 推上去触发发布。产物命名 `brickkit_<version>_<os>_<arch>.<ext>`，包内只有一个可执行文件。

| 平台 | 包格式 | 冒烟怎么跑 |
| --- | --- | --- |
| `linux/amd64` | `.tar.gz` | `ubuntu-latest` 原生 |
| `linux/arm64` | `.tar.gz` | `ubuntu-24.04-arm` 原生（public 仓库免费） |
| `darwin/amd64` | `.tar.gz` | `macos-15-intel` 原生（`macos-13` 已退役） |
| `darwin/arm64` | `.tar.gz` | `macos-14` 原生 |
| `windows/amd64` | `.zip` | `windows-latest` 原生，**只验非 Docker 面**，见 §3.1 |

另有两份非二进制产物：

- `checksums.txt` —— 全部包的 sha256，`install.sh` **必验**
- `checksums.txt.sig` + `checksums.txt.pem` —— cosign keyless（GitHub OIDC）签名，**附赠**

### 3.1 Windows 验到哪一层

Windows 是这次唯一一个「仓库里没有任何既有验收覆盖它」的平台：23 篇试用指南和 `准备.sh`
全是 bash，没有一篇在 Windows 上跑过。既然要发它，就得说清楚发出去的是什么。

`windows-latest` 上真跑一遍**不需要 Docker 的那一半**，跑不通就不发布：

```
brickkit version
brickkit init smoke-shop
brickkit add hello@1.0.0
brickkit up --dry-run
```

**明确的空白：** `brickkit up` 真起容器、以及 K8s 那条线，在 Windows 上**没有验过**。
GitHub 的 windows runner 跑不了 Linux 容器，这不是偷懒省掉的，是这里没法验。
README 与本文都要写明这一条 —— 发一个没说清边界的产物，比不发更坏。

### 3.2 install.sh 只管 macOS 与 Linux

Windows 用户手动下 zip。等 Scoop 那一步做了再说，在此之前不假装支持。

## 4. Makefile：两个目标，各干一件事

| 目标 | 干什么 | 谁跑 |
| --- | --- | --- |
| `make release-artifacts VERSION=0.1.0` | 交叉编译五个平台 → `dist/` + `checksums.txt` | CI 跑；本地也能跑，用来复现 CI 的产物 |
| `make release VERSION=0.1.0` | 校验 → 打 tag → push → CI 接手 | 只有人跑 |

**构建逻辑放 Makefile，不放 workflow。** 这仓库所有检查都能在本地跑出和 CI 一样的结果，
发布不该是例外 —— 一个只有 CI 能复现的产物，坏掉的时候没人能在本地查。

### 4.1 `release-artifacts`

复用现有的 `LDFLAGS`（[Makefile:35](Makefile)），所以 release 二进制的 `brickkit version`
带真版本号、commit 和构建时间。产物全部落进 `dist/`（要进 `.gitignore`）。

`VERSION` 收 `0.1.0` 也收 `v0.1.0`，内部统一成带 `v` 的形式。

**自检是它的一部分：** 打包完断言 `dist/` 里的包数等于矩阵条数，对不上就失败。
一个产出零个产物却退出码为 0 的发布目标，和一个安静跳过的测试套件是同一种错。

### 4.2 `release`

`make release 0.1.0` 这种写法**不支持** —— make 会把 `0.1.0` 当第二个目标名。
要支持它得加一条 catch-all `%:` 空目标，而那会顺带吞掉所有拼错的目标名，
`make tst` 从此静默成功。为省几个字符换来一个静默失败的入口，不值。

打 tag 之前逐条检查，任何一条不过就停，**不 push 任何东西**：

1. 版本号形如 `X.Y.Z`
2. 当前分支是 `main`
3. 工作区干净（`git status --porcelain` 为空）
4. 与 `origin/main` 同步（先 `git fetch`）
5. `v0.1.0` 这个 tag 本地与远端都还不存在
6. `make lint test-unit` 全绿

然后打印将要发生的事 —— 打 tag、push 到 origin、CI 随即发布一个**公开** release ——
并等一个明确的 `y`。非交互环境（CI、管道）不默认放行，要显式 `CONFIRM=yes`。

确认后 `git tag -a` + `git push origin v0.1.0`，最后打印 Actions 与 release 页面链接。

> **第 6 条现在过不了。** `make lint` 有两处失败：012 §2.21 没有任何指南指过去，
> 以及 `up --consolidated` 这个参数文档里写了但 CLI 没有。发第一个 tag 之前必须先修掉 ——
> 发布正是这道门该拦一次的时刻。

## 5. CI：build → smoke → sign → publish

`.github/workflows/release.yml`，**这个仓库的第一份 CI**。触发条件：push tag `v*`。

| 阶段 | 做什么 | 失败了会怎样 |
| --- | --- | --- |
| build | `make release-artifacts`，上传 `dist/` 为 job artifact | 没有任何东西被发布 |
| smoke | 按 §3 的矩阵在各 runner 上跑真二进制，五个平台**全在真硬件上** | 同上；**任何一个平台跑不通就整个发布终止** |
| sign | cosign keyless 签 `checksums.txt`（`id-token: write`） | 同上 |
| publish | `gh release create` 上传全部产物 | —— |

顺序不能改：**先验后发**。一个已经挂到 release 页面上、再回头发现跑不起来的二进制，
撤不干净 —— 已经有人下走了。

## 6. install.sh 行为规格

放仓库根目录（curl 的 URL 惯例指向那里）。POSIX sh，不要 bash 特性。

**正常路径：**

1. `uname -s` / `uname -m` 定平台，`x86_64|amd64` → `amd64`，`aarch64|arm64` → `arm64`
2. 取版本：默认查最新 release，`BRICKKIT_VERSION=v0.1.0` 可指定
3. 下 tarball 与 `checksums.txt` 到临时目录（`trap` 保证退出时清掉）
4. **校验 sha256**
5. 解包，装到目标目录，`chmod 0755`
6. 打印装到哪、版本号，以及 PATH 提示（如果需要）

**目标目录：** `/usr/local/bin` 可写就装那儿；不可写就退到 `~/.local/bin` 并打印
`export PATH` 那一行。`BRICKKIT_INSTALL_DIR` 覆盖一切。

**错误路径 —— 每一条都必须退出码非 0，并说清下一步：**

| 情况 | 行为 |
| --- | --- |
| 平台认不出来 | 列出脚本支持的四个平台（Windows 请手动下 zip），让人手动下。**不猜** |
| 缺 `curl`/`tar`/`sha256sum`(或 `shasum`) | 明说缺哪个 |
| 下载失败 / 404 | 带上 URL 和版本号，提示检查版本是否存在 |
| **sha256 对不上** | 删掉临时文件，报「校验失败，可能被篡改或下载不完整」，**绝不安装** |
| 目标目录不可写且 fallback 也不可写 | 提示用 `BRICKKIT_INSTALL_DIR` |

**cosign 签名不参与安装流程。** 装的人不需要有 cosign —— 强制它会把绝大多数人挡在门外，
而他们本来会得到 sha256 这一层保护。想验签的人按文档手动验。

> **关于 `curl | sh`：** 这个仓库在 012 里已经接受「安装即信任」的立场，所以不自相矛盾。
> 但文档同时给出**先下载再执行**的等价写法与校验和 —— 不把管道当唯一入口。

## 7. 怎么知道这套没坏

`make check-install-sh`，进 `make lint`：

1. 有 `shellcheck` 就跑；**没有就响亮跳过**（打印为什么跳过，不假装通过）
2. 拿本地 `dist/` 起一个 `file://` 源，走完整安装路径，确认装出来的 `brickkit version` 是对的
3. **故意把 checksums 改坏，确认它真的失败** —— 一个不会失败的校验等于没有校验

第 3 条是这个检查存在的理由。校验和逻辑最容易坏成「永远通过」，而那种坏法不会有人发现，
直到某天真有一个包被换掉。

## 8. 文档改动

| 文件 | 改什么 |
| --- | --- |
| `README.md` | 「安装」一节改序：curl 升方式一，`go install` 降方式二，源码构建方式三。删掉「暂时没有预编译二进制」那条备注，换成 Windows 边界（§3.1）与包管理器现状 |
| `llms.txt` | 补一行指向本文 |
| `AI-CONTEXT.md` | 安装方式那处（如有）跟着改 |
| 本文 | 就是这一份 |

## 9. 前置任务

发第一个 tag 之前必须先修掉，否则 §4.2 第 6 条卡死：

1. `design/012-架构设计原理与考量.md` §2.21 —— 没有任何一篇试用指南指过去
2. `up --consolidated` —— `design/012-架构设计原理与考量.md:508` 与 `组件合并部署.md:49` 写了这个参数，而 CLI 里也没有它。先查清是当初删掉了还是文档凭空写的，再决定改文档还是补参数

## 10. 已知空白

写在这儿，免得将来有人把它们当 bug 报：

- **Windows 上的 Docker / K8s 流程没验过**（§3.1）—— 不是不支持，是没验过，两者不一样
- **没有包管理器分发** —— brew / scoop / apt 全都没有，`install.sh` 是唯一的一行式入口
- **install.sh 不做升级检测** —— 重装即升级，装之前不问你原来那个哪来的
