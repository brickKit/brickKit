# BrickKit 部署模式

> **这份文档回答一个问题：东西跑在哪儿，怎么用。**
>
> BrickKit 有**两样**需要部署的东西，别把它们混在一起：
>
> | | 是什么 | 谁部署 | 本文档章节 |
> | --- | --- | --- | --- |
> | **组件市场（Market）** | 一个后端服务，回答"有什么可以装、谁有权装" | 平台方 / 公司运维，**一次** | 第 1–3 章 |
> | **你拼装出来的项目** | 用 `brickkit` 装好组件后跑起来的那套业务系统 | 每个使用者，**每个项目一次** | 第 4 章 |
>
> 状态说明：✅ = 已实现并验证过；🚧 = 正在做；⬜ = 计划中（对应开发计划的 Step）。

---

## 1. 市场的三种部署模式

| 模式 | 跑在哪 | 适合谁 | 数据 | 状态 |
| --- | --- | --- | --- | --- |
| **A. 本地开发** | 你自己的笔记本 | 开发平台本身、试用、离线演示 | 本机目录 | ✅ |
| **B. 单机自托管** | 公司内网一台服务器 | 大多数团队；私有组件不出内网 | 该机器的磁盘 | ✅ |
| **C. 云端** | 云主机 / 托管数据库 + 对象存储 | 需要多人异地协作、要高可用 | 云托管 | 🚧 见 §3 |

三种模式**用的是同一个镜像、同一份配置项**，区别只在于 PostgreSQL 和对象存储是自己起的容器还是云托管服务。市场本身不写本地磁盘——元数据在 PostgreSQL，产物在对象存储，所以它可以随时重建、随时横向扩容。

---

## 2. 模式 A / B：本地与单机（✅ 已验证）

两者用的是同一套编排文件 `deploy/market/docker-compose.yaml`，差别只有 `.env` 里的口令和端口。

### 2.1 起一套

```bash
cd deploy/market
cp .env.example .env
vi .env                      # ⚠️ 至少要改掉三个口令：POSTGRES_PASSWORD / RUSTFS_SECRET_KEY / ADMIN_PASSWORD
chmod 600 .env

docker compose up -d --build   # 首次会从源码构建市场镜像
```

仓库根目录也有等价的快捷方式：

```bash
make market-up      # 起（首次会提示你先改 .env）
make market-logs    # 看日志
make market-down    # 停（数据保留）
```

起来之后会有三个容器：

| 服务 | 作用 | 是否对外暴露 |
| --- | --- | --- |
| `market-api` | 市场 API | ✅ `MARKET_PORT`（默认 8080） |
| `postgres` | 元数据 | ❌ 只在 compose 内网 |
| `rustfs` | 产物（proto / OpenAPI 等） | 仅 Console `RUSTFS_CONSOLE_PORT`（默认 9001） |

**第一次启动时市场会自己做三件事**，不需要手工跑迁移脚本、不需要手工建 bucket：

1. 建库表（全部 `CREATE TABLE IF NOT EXISTS`，重启幂等）；
2. 建对象存储 bucket；
3. 按 `ADMIN_USERNAME` / `ADMIN_PASSWORD` 引导出管理员账号。

### 2.2 确认它活着

```bash
curl http://localhost:8080/api/v1/health
# {"success":true,"data":{"status":"ok","version":"dev","time":"..."}}
```

`version` 回显的是构建时注入的版本号，用来确认线上跑的到底是哪个镜像。

### 2.3 怎么用：发布一个组件

市场的所有接口都在 `/api/v1` 下，认证用 `Authorization: Bearer <token>`。

```bash
# 1. 登录，拿 Token
TOKEN=$(curl -s -X POST http://localhost:8080/api/v1/auth/login \
  -d '{"username":"admin","password":"<你的 ADMIN_PASSWORD>"}' \
  | python3 -c 'import sys,json;print(json.load(sys.stdin)["data"]["token"])')

# 2. 发布一个版本（先建成 draft，此时文件还没上传）
curl -X POST http://localhost:8080/api/v1/components/people/basic/versions \
  -H "Authorization: Bearer $TOKEN" -d @publish.json

# 3. 上传该版本声明的每个产物文件
curl -X POST "http://localhost:8080/api/v1/components/people/basic/versions/1.0.0/artifacts/art-0/upload?file=openapi.json" \
  -H "Authorization: Bearer $TOKEN" --data-binary @openapi.json

# 4. 文件传齐后转成 stable，这一步才真正可被安装
curl -X PUT http://localhost:8080/api/v1/components/people/basic/versions/1.0.0 \
  -H "Authorization: Bearer $TOKEN" -d '{"status":"stable"}'
```

**为什么是三步？** 发布请求里只有元数据，文件还没上传；而"上传的文件必须与 `files` 列表一致"这条规则要等文件到齐才能判。所以 draft 是"已登记、待上传"的中间态，转 stable 时统一校验。少传一个文件就转不了 stable，装不到半成品组件。

> `brickkit publish` 已把这三步包成一条命令（✅ Step 19），日常发布用它即可；
> 上面三步是它内部做的事，排障时才需要手动走。

### 2.4 怎么用：从市场装组件

在你的项目里，把市场配成安装源：

```yaml
# brickkit.yaml
sources:
  - id: my-market
    type: market
    url: http://localhost:8080/api/v1      # 单机部署换成 http://<服务器IP>:8080/api/v1
    enabled: true
```

然后：

```bash
brickkit add people/basic@1.0.0
```

CLI 会去市场取 Manifest、把该版本声明的产物下载到 `.brickkit/artifacts/<版本化服务名>/`，并把组件写进 `brickkit.yaml`。

**私有组件**需要先登录（`brickkit login`，✅ Step 19）；也可以直接在安装源上配 `authToken`。public 组件的查询与下载不需要任何认证。

### 2.5 数据在哪、怎么备份

```
deploy/market/data/
├── postgres/     # 组件元数据、版本、用户、访问策略
└── rustfs/       # 产物文件
```

备份就是停服后打包这个目录；迁移到新机器就是把整个 `deploy/market/` 复制过去再 `docker compose up -d`。

> 这两个目录由容器创建，属主是容器内的用户，宿主机上删不动是正常的。要清空用：
> `docker run --rm -v "$PWD/data:/data" busybox rm -rf /data/postgres /data/rustfs`

---

## 3. 模式 C：云端（🚧 部分可用）

**现在就能做的**：市场镜像本身对环境没有任何假设——它只需要一个 PostgreSQL 地址和一个 S3 兼容端点。所以把 compose 里的 `postgres` / `rustfs` 两个服务删掉，把环境变量指向云托管服务，就能跑在任何云上：

```bash
DATABASE_HOST=your-db.rds.amazonaws.com      # 或阿里云 RDS / 腾讯云 TDSQL
DATABASE_PORT=5432
DATABASE_NAME=brickkit_market
DATABASE_USER=brickkit
DATABASE_PASSWORD=<强口令>
DATABASE_SSLMODE=require                      # ⚠️ 云上必须开，默认值 disable 只适合 compose 内网

RUSTFS_ENDPOINT=https://s3.ap-east-1.amazonaws.com   # 或 OSS / COS 的 S3 兼容端点，必须带 scheme
RUSTFS_ACCESS_KEY=<AK>
RUSTFS_SECRET_KEY=<SK>
RUSTFS_BUCKET=brickkit-artifacts
RUSTFS_REGION=ap-east-1

ADMIN_USERNAME=admin
ADMIN_PASSWORD=<强口令>
PORT=8080
```

市场只依赖 S3 协议本身，因此 AWS S3、阿里云 OSS、腾讯云 COS、Ceph、RustFS 都可以，换存储不用改代码。

**云上必须自己补的三件事**（市场不做，也不该做）：

| 事项 | 说明 | 状态 |
| --- | --- | --- |
| **HTTPS** | 市场只监听明文 HTTP。公网暴露必须挂在网关 / Nginx / ALB 后面做 TLS 卸载——否则 Bearer Token 是明文传输的 | ⬜ 由你的入口层负责 |
| **备份** | 云数据库的自动备份 + 对象存储的版本化/跨区复制 | ⬜ 由云服务商配置 |
| **多副本** | 市场进程无本地状态，可以直接起多份挂在负载均衡后面 | ✅ 设计上支持，⬜ 未做压测验证 |

**K8s 部署清单与 Helm chart 都已就绪**：kustomize 清单在 `deploy/market/k8s/`（含 `in-cluster-deps` overlay，可连 PostgreSQL 与对象存储一起部署），Helm chart 在 `deploy/market/helm/brickkit-market/`，两者都在 minikube 上真部署验证过。自建自用推荐 kustomize（不用多装工具）；把市场当产品交付给第三方时用 chart —— 它多给的是 `values.schema.json`（写错的值在 install 之前就拦下）与 `helm rollback`。

---

## 4. 你拼装出来的项目跑在哪

这是**另一件事**：市场是"组件从哪来"，这一节是"装好的组件在哪跑"。

由 `brickkit.yaml` 里的 `deploy.target` 决定：

```yaml
deploy:
  target: docker          # docker | k8s
```

| 目标 | 含义 | 适合 | 状态 |
| --- | --- | --- | --- |
| `docker` | 生成 docker-compose 编排，在单机上跑 | 本地开发、演示、小规模生产 | ✅ Step 12 生成编排、Step 15 `brickkit up`（八组件真实装配验证过） |
| `k8s` | 生成 K8s 清单，交给集群 | 云端、需要弹性伸缩 | ✅ Step 16（含 context 钉住、PSA、Ingress class / TLS） |

命令形态（均已实现）：

```bash
brickkit add people/basic@1.0.0   # 装组件（递归拉依赖与产物）
brickkit up --dry-run                    # 看启动顺序（拓扑排序）
brickkit up                       # 按顺序把整套系统拉起来（生成部署文件 + 执行迁移 + 启动）
brickkit down                     # 停止（不删除 volume）
```

---

## 5. 常见问题

**Q：为什么 `docker compose up` 之后 rustfs 一直重启，日志是 `Permission denied (os error 13)`？**

容器内 RustFS 以 uid 10001 运行，而 bind mount 出来的宿主机目录默认属 root。编排文件里已经有一个 `rustfs-init` 一次性容器负责把属主改对，正常情况下不会遇到；如果你改过 volume 路径，记得让新目录属于 10001。

**Q：`.env` 里明明改了口令，容器里却还是旧值？**

`docker compose` 中**宿主机 shell 里已导出的同名环境变量优先级高于 `.env` 文件**。如果你的 `~/.zshrc` 里 export 过 `RUSTFS_ACCESS_KEY` 之类的变量，它会盖掉 `.env`。用 `docker compose config` 看最终生效的值，或者 `env -u RUSTFS_ACCESS_KEY docker compose up -d`。

**Q：忘记管理员口令了？**

改 `.env` 里的 `ADMIN_PASSWORD`，把 `ADMIN_PASSWORD_RESET` 置为 `true`，重启一次市场，然后**记得改回 `false`**。重置会同时吊销该账号已签发的全部令牌——会走到这一步通常意味着凭据已经不可信了。

**Q：市场重启会不会丢数据？**

不会。数据在 PostgreSQL 与对象存储里，市场进程本身不存任何状态。`docker compose down` 保留数据，`down -v` 才会删卷。

---

## 6. 相关文档

| 文档 | 内容 |
| --- | --- |
| 《市场部署与运维指南》 | 单机部署的完整步骤、日常运维、升级回滚、故障排查 |
| `design/007-组件市场设计.md` | 市场的 API、权限模型、版本状态机 |
| `design/003-项目配置规范.md` | `brickkit.yaml` 的安装源与 `deploy.target` |
| `design/005-部署与运行规范.md` | 拼装出来的项目怎么跑 |
