---
created: 2026-08-12
updated: 2026-08-15
---

# 延后实现清单（跨 Step 回填）

> 总览与导航见 [README.md](README.md)。

> **这张表是"⚠️ 步骤到底缺什么"的唯一权威来源。** 每条都是当前 Step 有意不做的部分——
> 因为它依赖后续 Step 才有的信息，现在做只能做半个、也无法完整测试。
> 进入某个 Step 前，先在这里筛出"回填 Step = 该 Step"的条目，与该 Step 自身的任务合并执行。
> 回填完成后把状态改为 ✅ 并写明落地位置，不要删行（保留可追溯性）。

| # | 来源 Step | 延后的内容 | 为什么现在不做 | 回填 Step | 状态 |
| --- | --- | --- | --- | --- | --- |
| P1 | 5 | 级联计算（`enabled` 三态 → 实际启停集合） | 需要 Manifest 的依赖图，Step 6/7 之后才具备。**Step 7 的 `resolver.ResolveConfig` 目前不过滤 `enabled`**，级联在这里落地 | 11 | ✅ Step 11：`internal/cascade`，与 003 §4.3 的示例逐行对齐 |
| P2 | 5 | 资源配额三层合并（brickkit.yaml > component.yaml > CLI 默认值） | 需要 Manifest 才能合并；生成部署文件时才用得到 | 11 / 12 | ✅ Step 11 合并 + Step 12 写进 `deploy.resources`（含 K8s→compose 单位转换） |
| P3 | 5 | `localPort` 自动分配 | 需要全项目端口视图与运行期信息 | 13 | ✅ Step 13：统一的宿主机端口台账（localPort / exposePort / 自动映射共用一张表）；默认值改为组件声明的 `deployment.port`，见 D178 |
| P4 | 5 | `exposePort` 与 `deployment.port` 的冲突判定 | 需要 Manifest 中的 `deployment.port` | 12 | ✅ Step 12：生成阶段就报冲突，并指出改哪个字段 |
| P5 | 5 | 资源密码硬编码告警 | 属于 up 前的体检输出，需命令层承载 | 15 | ✅ Step 15-C：`up` 每次都查，警告不阻断，且**不打印密码本身** |
| P6 | 6 | 组件的 `sourceType`（git/registry）与 `gitUrl` | 只有 `--repo` / `--repo-all` 需要；且命中 Manifest 缓存时这两个字段不可得 | 9 | ✅ 落在 `source.Client.Origin`（绕开 Manifest 缓存直接问安装源） |
| P7 | 6 | 把 `internal/source` 接线到命令（`add` / `up` 调用、终端输出 📦/⚠️ 渲染） | Step 6 只交付库 | 9 | ✅ `add` 已接线并渲染；`up` 的接线属 Step 15 |
| P8 | 6 | ~~签名校验（`installer.requireSignature` + cosign）接入下载路径~~ | — | 20 | ✅ 2026-08-15：20-A 交付核心（`internal/security`，真 cosign 双向验证），20-B 接线到 publish / 市场 / add。真 PostgreSQL 上改数据库篡改 Manifest，CLI 退出码 1 拦住 |
| P9 | 6 | 缓存清理（remove 时删除 `.brickkit/manifests` 与 `.brickkit/artifacts` 中的对应条目） | 属于 `brickkit remove` 的职责（9.12 / 9.13） | 9 | ✅ 落在 `cli.cleanupComponent` |
| P10 | 6 | 升级路径：`up` 检测到版本变化时拉取新版本 Manifest + 产物（004 §3.5.1） | 需要 up 的执行流与旧/新版本对比 | 15 | ✅ Step 15-C：新版本 Manifest 与产物自动拉取，旧版本产物保留；真升级验证过 |
| P11 | 6 | 市场端点契约落地：`GET …/manifest` 的 JSON 信封、`GET …/artifacts/{id}/download?file=` | 客户端已按 D47/D48 实现并有 Mock 覆盖，但**服务端还不存在**，真实市场未验证过 | 18 | ✅ 18-D：服务端已实现，且用**真实 CLI 二进制**对**真实市场进程**跑通了 `add`（见 18-D 完成记录的端到端验证） |
| P12 | 6 | ~~git 安装源指定分支 / tag / commit~~ | — | 17 后 | ✅ 2026-08-15：003 §6.3 补上 `ref` 字段；实现先浅 clone 再回落完整 clone（`clone --branch` 不认 commit SHA），5 个用例用真 Git 仓库跑 |
| P13 | 7 | 解析结果写回 brickkit.yaml（递归依赖入库、多版本自动写两个条目） | 解析器只产出依赖图；写文件是 `brickkit add` 的职责 | 9 | ✅ 落在 `config.Edit`（保注释、保 `${ENV_VAR}`） |
| P14 | 7 | 强依赖**被禁用**（`enabled: false`）时报错（004 §10.3） | 与"取不到"是两回事，需要级联启停结果（P1）才能判定 | 11 | ✅ Step 11：只在**钉住**的组件上报错，并打印完整依赖链 |
| P15 | 7 | `CheckUpgrade` 接线到 `brickkit up` 的升级流程（004 §3.5.1） | 需要 up 的执行流与"版本号变化"检测 | 15 | ✅ Step 15-C：检测到版本变更即执行 002 §7.7 的五项检查，阻断项在启动前报错 |
| P16 | 8 | **在 `add` / `remove` 执行前自动调用 `backup.SaveLast`**（8.2 / 8.3） | 备份能力已实现并测全，但两个命令当时还没实现 | 9 | ✅ 两个命令均在改配置前备份，`reset --last` 可撤销 |
| P21 | 22 | **22.3 `PEOPLE_BASIC_GRPC_ENDPOINT` 只验证到注入引擎层**：`inject` 已能算出这个变量（Step 11 有测试），但还没有生成的 compose 可以拿去看 | 需要 Step 12 把注入结果写进 docker-compose.yaml，才能按验证项要求"检查 env" | 12 | ✅ Step 12 |
| P20 | 11 | 把注入引擎接线到命令：`internal/inject` 已算出每个组件的环境变量与资源配额，但**还没有任何命令消费它** | 消费方是生成 docker-compose.yaml（Step 12）与 K8s 清单（Step 13）。Step 11 只交付引擎本身，接线是下一步的事 | 12 | ✅ Step 12：`up --dry-run` 消费注入结果生成 compose |
| P22 | 13 | **宿主机端口冲突只查项目内**：CLI 能发现两个组件抢同一个端口，但发现不了宿主机上**别的进程**已经占着这个端口 | 需要真的去 bind 一下才知道，属于启动前的体检；真实验证时就撞上了——`localPort: 9001` 被本机无关进程占用，生成一切正常，跑起来才 503 | 15 | ✅ Step 15-C：`--check-resources` 探测要占的宿主机端口，并排除本项目自己的容器（否则重复 up 会自己报自己） |
| P24 | 18 | ~~市场的组织管理端点未实现~~ | — | 17 后 | ✅ 2026-08-15：三个端点全部实现，**并修掉一个越权漏洞**——注册时自报 `orgId` 即可读走该组织全部 private 组件 |
| P23 | 13 | ~~13.14 Podman 只到单元测试~~ | — | 16-D 后 | ✅ 2026-08-15：真 Podman 5.7.0 上验证，**抓到设计书与验证项本身写错了**——`host.containers.internal` 不能作 add-host 的值，已全部改为 `host-gateway` |
| P19 | 18-E | ~~市场的 Kubernetes 部署清单与 Helm chart~~ | — | 17 后 | ✅ 2026-08-15：`deploy/market/k8s/`（kustomize，base + in-cluster-deps），minikube 上真部署跑通并通过 restricted PSA。Helm chart 部分另记为 P28 |
| P18 | 18-D | **CLI 未解析市场错误信封**：拿到 4xx 时只看状态码，把 403 一律说成"市场认证失败，请执行 brickkit login" | 端到端验证时实测：安装一个 blocked 组件，服务端正确返回 403 + `COMPONENT_BLOCKED` + "该版本已被市场下架"，但 CLI 显示的是"认证失败 → 去登录"，把使用者引向完全错误的方向。修法是 `internal/source/market.go` 的 `get()` 解析信封里的 code/message 再决定文案 | 19 | ✅ Step 19 修复：错误信封解析抽到 `internal/market`，read/write 两侧共用；已用真实 blocked 组件复验 |
| P25 | 16 | ~~K8s 清单只做到生成与人工核对~~ | — | 16 | ✅ 2026-08-15：minikube 上跑通完整生命周期（含迁移 Job 与幂等重跑），并抓到 3 个真 bug |
| P27 | 结欠账（一） | ~~`podman compose` 的端到端 up 未验证~~ | — | 立即 | ✅ 2026-08-16：**Podman 支持已移除**，005 §7 留下完整说明。当初记的理由（「docker CLI 版本与 podman 兼容 API 不合」）**是错的**——协商正常（1.41），真实原因是本终端在 VS Code 的 snap 沙箱里、XDG_DATA_HOME 被改写，podman CLI 与服务读的不是同一个存储。对齐后 up / status / 业务 / 幂等全通，只有 down 失败（rootless netns: kill network process: permission denied），而纯 podman compose down 不经过 BrickKit 也一样失败。**不留半个**：坏的偏偏是 down，一个停不掉的项目比不支持更糟。调查中另查出一个真 bug（stdout/stderr 合并）已修 |
| P28 | 结欠账（二） | ~~市场没有 Helm chart~~ | — | 立即 | ✅ 2026-08-16：`deploy/market/helm/brickkit-market/`。与 kustomize 并存而不是替代——自建自用仍推荐 kustomize（不用多装工具），chart 面向“把市场当产品交付给第三方”。Helm 真正多给的是 **values.schema.json**（写错的值在 install 之前就拦下，而 configMapGenerator 对值不做任何校验）与 `helm rollback`。minikube 上**真部署**验过：健康检查 200、登录返回真 token、改配置后 ReplicaSet 换新。`make market-chart-check` 可重复验证 |
| P26 | 16-D | ~~K8s 清单不生成 NetworkPolicy / ServiceAccount / PodDisruptionBudget~~ | — | 结欠账（四） | ✅ 2026-08-15：前两样已实现（opt-in），**在 `--cni=calico` 的集群上验到策略真被执行**——非依赖方连不上、依赖方连得上、删掉 ingress 那条规则站点立刻打不开。当初"凭空生成多半是错的"这条判断漏了一件事：**依赖图在平台手里**。PDB 实测确认单副本下必然把节点卡到排不空，另记为 **P35** |
| P36 | 结欠账（四） | ~~NetworkPolicy 挡掉了依赖图之外的合法访问方~~ | — | 立即 | ✅ 2026-08-16：新增 `deploy.networkPolicy.allowFrom`——声明**意图**（谁、在哪个命名空间、什么标签、哪些端口），由平台合成进每个组件的策略。`name` 必填并写进 `brickkit.io/allow-from` 注解，半年后能看出这口子是为谁开的。calico 集群上真跑**三路验证**：monitoring + 正确标签 → 200；同命名空间但标签不对 → 超时；标签对但命名空间不对 → 超时——后两条同时被拦正好实证了 AND 语义（写成两个 from 元素就是 OR，那时它们都会通，且没有任何迹象）。另做 A/B 对照：去掉 allowFrom 立刻回到超时，加回来又是 200 |
| P39 | 探讨中提出 | **无法引用「已经由别人部署好」的组件**：两个不相干的项目需要共享同一个**持有权威状态**的组件（事件总线、用户中心、权限系统）时，现在只能各部一份——而那对有状态组件来说语义就是错的（两个项目各有一套用户，不叫共享用户中心） | 方向是给组件条目加 `external:`：照常进依赖图、照常做版本兼容检查、照常注入地址给依赖方、照常下载 artifacts，**只是不生成 Deployment、不跑它的迁移、`down` 时不碰它**。共享的那些东西自己就是一个 BrickKit 项目（有自己的负责人与升级节奏）——让它出现在消费方的 brickkit.yaml 里、由消费方负责部署，本身就是错的：消费方一执行 `down`，别的项目就挂了。**版本安全是白送的**：服务名带版本（`event-bus-1-0-0`），要么连上声明的那一版、要么 DNS 解析不出来当场报错，不存在静默连错版本。真正剩下的是「对面部没部」的可达性问题，可套用 `--check-resources` 现成的探测机制。见 005 §5.2.1 | 待定 | ⬜ |
| P38 | 探讨中发现 | ~~K8s 升级后旧版本变成孤儿~~ | — | 立即 | ✅ 2026-08-16：`up` 在 rollout 就绪后按 `brickkit.io/project` 标签比对集群实际资源与本次期望集合，删掉多余的并**逐条打印**。`--only` 时完全不清理（那时没点名的组件不是孤儿，清了就是把线上组件下线）。minikube 上真跑验证：1.0.0 → 2.0.0 升级后只剩新版本且如实汇报；**并单独验了最危险的那条路**——多版本共存下 `--only` 点名 2.0.0，在跑的 1.0.0 没被删 |
| P37 | 结欠账（四） | ~~不生成 Egress：出站方向完全不管~~ | — | 立即 | ✅ 2026-08-16：新增 `deploy.networkPolicy.egress`。难点不在生成规则，而在**漏一项就出事**，所以能推导的都由平台做：**DNS 自动放行**（集群内任意命名空间的 UDP/TCP 53，不钉死 kube-system）、**组件依赖从依赖图推导**、**资源端口从 `resources[].port` 取**；只有资源位置要声明，而**声明不全就在生成阶段阻断**（平台从 bindings 知道谁绑了哪个资源）。calico 上真跑：漏声明→阻断且给出可粘贴的配置；补全后跨命名空间连库返回真实数据。**实测修正了一个我原先的错误判断**——见完成记录 |
| P35 | 结欠账（四） | **不生成 PodDisruptionBudget** | 当前 `replicas` 固定为 1（多副本是 005 §5.8 的后期能力），单副本下 PDB 无论怎么写都是死路：`minAvailable: 1` 让 `ALLOWED DISRUPTIONS` 恒为 0、`kubectl drain` 永远排不空（已在 calico 集群上真跑确认）；`maxUnavailable: 1` 又等于没写。代价还落在几个月后升级集群的运维身上。结论已固化成 `TestNoPodDisruptionBudgetGenerated`，并断言 `replicas` 仍是写死的 1——前提一变就会有人被提醒回来 | 多副本（005 §5.8）之后 | ⬜ |
| P40 | 36 | **`add` 下载产物是串行的**：`downloadArtifacts` 逐个组件调 `DownloadArtifacts`，50 个组件就是 50 次串行往返。发现于 Step 36 排查 36.4 时——与 36.1 那个已修的 `checkImages` 是同一处结构 | **没有照抄同样的修法，是因为两者的瓶颈不是一回事**：`CheckImage` 是**延迟受限**的元数据请求（每次几百 ms 全花在等待上），并发 8 就是接近 8 倍，实测 31 倍；产物下载是**带宽受限**的文件传输，并发只是把同一条管道切成几份，收益小得多，开太多反而互相抢带宽、还让进度显示没法看。真要做得先量清楚「产物一般多大、慢在等待还是慢在传输」，而不是先并发了再说。另外 `add` 是一次性操作且产物有缓存，反复付这个代价的场景本来就少 | 有人真的抱怨 `add` 慢，或产物体积/数量出现数量级增长时 | ⬜ |
| P29 | 20-A | ~~镜像签名未实现~~ | — | 立即 | ✅ 2026-08-16：**换了个解法,不做镜像签名。** `deployment.image` 本来就在 Manifest 里、已被签名覆盖,真正的缺口只有“tag 可变”一条。`publish` 默认把 tag 解析成 digest **再签名**(顺序反了消费方会一律验签失败),缺口就此关上,不需要任何新的密码学机制。解析失败阻断发布(取不到 digest 意味着消费方也拉不到,而版本号不可回收),`--no-pin-digest` 可跳过但会警告。**只用 buildx 查询、不做 fallback**:`docker manifest inspect -v` 给的是单平台 digest,钉住它会把组件锁死在 amd64。cosign 签镜像本身仍不做——那由集群准入控制器负责,CLI 不在链路上。见 008 §8.3.2 |
| P31 | 25 | ~~25.2 用的是事件总线替身，不是真组件~~ | — | 27 | ✅ 2026-08-15：Step 27 建好真组件后复验——起完整七层链，审批 → `eventPublished:true` → 事件总线读到 → `redis-cli XLEN` 确认真进了 Redis Stream |
| P33 | 首次真实装配 | ~~brickkit.yaml 的未知字段被静默忽略~~ | — | 立即 | ✅ 2026-08-15：新增 `internal/config/unknown.go`，逐层比对字段名并**猜出使用者想写的那个**。没用 `KnownFields(true)`——它只会说 "field user not found in type config.Resource"，没有行号、没有中文、更不会给建议。现在报的是 `resources[0].user：未知字段（第 51 行），是不是想写 username？` |
| P34 | 首次真实装配 | ~~资源 `host` 用宿主机地址时无法工作~~ | — | 立即 | ✅ 2026-08-15：`host: host.docker.internal` 时自动为**绑定了它的**组件（含迁移容器）加 `extra_hosts: host.docker.internal:host-gateway`。真跑验证：原本 `no such host` 的场景现在直接起来了 |
| P32 | 28 | ~~Go 组件不在运行时暴露 `/openapi.json`~~ | — | 立即 | ✅ 2026-08-15：四个 Go 组件都加了 `GET /openapi.json`（`go:embed` 打进二进制）。真跑验证：文档中心六个组件全部 `ok`，其中两个同时提供 openapi+grpc |
| P30 | 20-B | ~~市场不做密码学校验~~ | — | 立即 | ✅ 2026-08-16：**想清楚之后决定不做**，并把理由写进 008 §8.2。探索过两种形态：TOFU（首次自动钉密钥）与账号密钥登记（GitHub 模型）。后者更简单——密钥轮换就是普通账号操作，不需要专门设计证明持有的协议。但两者成立的**唯一前提都是权限分离**：发布凭据不能有管理密钥的权限。BrickKit 的 Token 目前没有 scope，前提不成立——偷到发布 Token 的人同样能换掉登记的公钥，“市场验过签”就成了空话，比不验更糟。而 CLI 侧的校验本来就是完整且更强的保护（信任锚点在使用者的 installer.publicKeys，不依赖市场）。**重新考虑的条件：Token 有了 scope 之后。**副产物保留：`testdata/signature-golden.json` 把规范化输出钉死——那套规则一改，此前发布的每个签名都会安静失效 |
| P17 | 10 | `brickkit order` 按级联结果过滤组件（`enabled: false` 与被级联跳过的组件不该出现在启动顺序里） | 级联计算是 Step 11 的职责（P1）。当前 order 列出 brickkit.yaml 中的**全部**组件；`TestOrderCurrentlyIgnoresEnabledFalse` 锁定了这一现状，Step 11 实现后该用例会失败，提醒回来改 | 11 | ✅ Step 11：order 先算级联再排序，并输出「组件状态计算」一节；那条自曝用例已按新行为重写 |

---
