---
created: 2026-08-12
updated: 2026-08-15
---

# 决策索引

> 全部 432 条决策的一句话摘要与出处。**理由不在这里**——
> 每条决策的完整理由写在它所属的完成记录里，点出处进去看。

> 总览与导航见 [README.md](README.md)。

| # | 决策 | 出处 |
| --- | --- | --- |
| D1 | `go.mod` 固定 `go 1.22`（而非 Go 1.26 默认写入的 1.25） | [Step 1：项目仓库初始化](完成记录/01-平台核心.md) |
| D2 | 对象存储客户端只放 `market-server/go.mod`，不放主 module | [Step 1：项目仓库初始化](完成记录/01-平台核心.md) |
| D3 | proto 自检生成物输出到 `.tools/proto-check/` | [Step 1：项目仓库初始化](完成记录/01-平台核心.md) |
| D4 | `make lint` 在缺少 golangci-lint 时回退 `go vet` | [Step 1：项目仓库初始化](完成记录/01-平台核心.md) |
| D5 | 各 `test-*` 目标在目录无 `*_test.go` 时跳过而非报错 | [Step 1：项目仓库初始化](完成记录/01-平台核心.md) |
| D6 | 暂不执行 `make tidy` | [Step 1：项目仓库初始化](完成记录/01-平台核心.md) |
| D7 | 日志默认级别 info，每次执行输出"命令开始执行/执行完成"两条 | [Step 2：CLI 框架搭建](完成记录/01-平台核心.md) |
| D8 | `msg` 键改名为 `message` | [Step 2：CLI 框架搭建](完成记录/01-平台核心.md) |
| D9 | 非 `*clierr.Error` 的错误一律按用法错误（退出码 2）翻译 | [Step 2：CLI 框架搭建](完成记录/01-平台核心.md) |
| D10 | 用 `ExecuteC()` 而非 `Execute()` | [Step 2：CLI 框架搭建](完成记录/01-平台核心.md) |
| D11 | 中文化 cobra 的 usage 模板与内置 help/completion 命令 | [Step 2：CLI 框架搭建](完成记录/01-平台核心.md) |
| D12 | `--config` 放在 root 的 persistent flag | [Step 2：CLI 框架搭建](完成记录/01-平台核心.md) |
| D13 | 覆盖率政策：`make cover-check` 门槛 95%，当前实际 100% | [Step 2：CLI 框架搭建](完成记录/01-平台核心.md) |
| D14 | `init` 在**当前目录**初始化，不创建以项目名命名的子目录 | [Step 3：`brickkit init` 命令](完成记录/01-平台核心.md) |
| D15 | 3.1 的文案采用 004 §3.2 的单行形式 `❌ 请指定项目名称：brickkit init <项目名称>` | [Step 3：`brickkit init` 命令](完成记录/01-平台核心.md) |
| D16 | 3.7 的错误文案用「项目已初始化，无需重复执行 init」+ 列出已存在的 `brickkit.yaml`/`.brickkit/` | [Step 3：`brickkit init` 命令](完成记录/01-平台核心.md) |
| D17 | 项目名长度上限 54 | [Step 3：`brickkit init` 命令](完成记录/01-平台核心.md) |
| D18 | `Options.WorkDir` 注入项目根目录，命令层不依赖进程 cwd | [Step 3：`brickkit init` 命令](完成记录/01-平台核心.md) |
| D19 | 名称非法时给出「建议：改用 xxx」 | [Step 3：`brickkit init` 命令](完成记录/01-平台核心.md) |
| D20 | 覆盖率 99.2%：余下 3 条语句豁免 | [Step 3：`brickkit init` 命令](完成记录/01-平台核心.md) |
| D21 | `metadata.name` 与 `metadata.description` 也强制必填 | [Step 4：Manifest 解析器（component.yaml）](完成记录/01-平台核心.md) |
| D22 | 版本号严格 `major.minor.patch`，拒绝 `1.0.0-beta` / `+build` / `v` 前缀 | [Step 4：Manifest 解析器（component.yaml）](完成记录/01-平台核心.md) |
| D23 | 组件 ID 上限 63 字符 | [Step 4：Manifest 解析器（component.yaml）](完成记录/01-平台核心.md) |
| D24 | `extraPorts.name` 按 K8s Service 端口名规则校验（≤15 字符、小写、首尾字母数字） | [Step 4：Manifest 解析器（component.yaml）](完成记录/01-平台核心.md) |
| D25 | `extraPorts.port` 与主端口相同判为**错误**（32.17 允许"报错或警告"） | [Step 4：Manifest 解析器（component.yaml）](完成记录/01-平台核心.md) |
| D26 | artifacts 文件路径必须是相对路径且不得用 `..` 越出仓库根 | [Step 4：Manifest 解析器（component.yaml）](完成记录/01-平台核心.md) |
| D27 | 组件不能依赖自己；同一依赖不得重复声明 | [Step 4：Manifest 解析器（component.yaml）](完成记录/01-平台核心.md) |
| D28 | 不启用 yaml 严格模式（未知字段不报错） | [Step 4：Manifest 解析器（component.yaml）](完成记录/01-平台核心.md) |
| D29 | Step 4 不实现"版本化服务名"转换 | [Step 4：Manifest 解析器（component.yaml）](完成记录/01-平台核心.md) |
| D30 | `${ENV_VAR}` 只展开值、不展开 key | [Step 5：brickkit.yaml 解析器](完成记录/01-平台核心.md) |
| D31 | 变量缺失时保留 `${VAR}` 原样 | [Step 5：brickkit.yaml 解析器](完成记录/01-平台核心.md) |
| D32 | 把问题收集器提到 `clierr.ProblemSet` 共享 | [Step 5：brickkit.yaml 解析器](完成记录/01-平台核心.md) |
| D33 | 从 manifest 导出 `ComponentIDProblem` / `IsExactVersion` | [Step 5：brickkit.yaml 解析器](完成记录/01-平台核心.md) |
| D34 | `installer.requireSignature` 缺省 true | [Step 5：brickkit.yaml 解析器](完成记录/01-平台核心.md) |
| D35 | localPort / exposePort 写了但没写 local / expose 时**报错** | [Step 5：brickkit.yaml 解析器](完成记录/01-平台核心.md) |
| D36 | K8s + expose 缺 hostname 在解析阶段就报错 | [Step 5：brickkit.yaml 解析器](完成记录/01-平台核心.md) |
| D37 | 资源 `kind` 不做枚举限制 | [Step 5：brickkit.yaml 解析器](完成记录/01-平台核心.md) |
| D38 | **不**实现的部分：级联计算、资源配额三层合并、localPort 自动分配、exposePort 与 deployment.port 的冲突判定、密码硬编码告警 | [Step 5：brickkit.yaml 解析器](完成记录/01-平台核心.md) |
| D39 | S3 客户端选 `aws-sdk-go-v2`，而不是继续用 minio-go 指向 RustFS | [变更记录：对象存储由 MinIO 改为 RustFS](完成记录/07-复核与结欠账.md) |
| D40 | `RUSTFS_ENDPOINT` 必须带 `http://` / `https://` scheme | [变更记录：对象存储由 MinIO 改为 RustFS](完成记录/07-复核与结欠账.md) |
| D41 | 对象存储访问强制 `UsePathStyle: true` | [变更记录：对象存储由 MinIO 改为 RustFS](完成记录/07-复核与结欠账.md) |
| D42 | bucket 初始化与健康检查用 `aws-cli` 走标准 S3 协议，不用厂商专用 CLI | [变更记录：对象存储由 MinIO 改为 RustFS](完成记录/07-复核与结欠账.md) |
| D43 | 运维指南中 RustFS 的镜像标签/环境变量/Console 端口标注"按实际部署校对" | [变更记录：对象存储由 MinIO 改为 RustFS](完成记录/07-复核与结欠账.md) |
| D44 | 目录型安装源取产物前先比对 `component.yaml` 的 id + version | [Step 6：安装源实现（local / market / git）](完成记录/01-平台核心.md) |
| D45 | git 源 clone 到**临时目录**，命令结束即删 | [Step 6：安装源实现（local / market / git）](完成记录/01-平台核心.md) |
| D46 | git 仓库支持两种布局：`<scope>/<name>/component.yaml` 与根目录 `component.yaml` | [Step 6：安装源实现（local / market / git）](完成记录/01-平台核心.md) |
| D47 | 市场 Manifest 端点同时接受 JSON 信封 `{"success","data":{"manifest":…}}` 与裸 YAML 正文 | [Step 6：安装源实现（local / market / git）](完成记录/01-平台核心.md) |
| D48 | 产物下载定为两步：`GET …/artifacts` 取列表 → `GET …/artifacts/{artifactId}/download?file=<路径>` | [Step 6：安装源实现（local / market / git）](完成记录/01-平台核心.md) |
| D49 | 凭据只在 `marketUrl` 与安装源 url 一致（或缺失）时使用 | [Step 6：安装源实现（local / market / git）](完成记录/01-平台核心.md) |
| D50 | 任一安装源"真失败"就报该错误；全部只是"没有"才报组件未找到 | [Step 6：安装源实现（local / market / git）](完成记录/01-平台核心.md) |
| D51 | 缓存损坏或内容对不上时忽略缓存重新拉取，不报错 | [Step 6：安装源实现（local / market / git）](完成记录/01-平台核心.md) |
| D52 | 产物下载失败只警告，Manifest 获取失败才阻断 | [Step 6：安装源实现（local / market / git）](完成记录/01-平台核心.md) |
| D53 | **不**实现 `sourceType` / `gitUrl`（开源/闭源标记） | [Step 6：安装源实现（local / market / git）](完成记录/01-平台核心.md) |
| D54 | `ServiceName` 放在 `manifest` 包 | [Step 6：安装源实现（local / market / git）](完成记录/01-平台核心.md) |
| D55 | 覆盖率 99.4%：余下 3 条语句豁免 | [Step 6：安装源实现（local / market / git）](完成记录/01-平台核心.md) |
| D56 | 行为测试跑**真实的 internal/source + 本地安装源**，只有"Manifest 校验器不允许写出来的图"才用构造的 Manifest | [Step 7：依赖解析引擎](完成记录/01-平台核心.md) |
| D57 | 强依赖缺失的错误指出**直接**依赖方，而不是根组件 | [Step 7：依赖解析引擎](完成记录/01-平台核心.md) |
| D58 | 弱依赖也参与循环检测 | [Step 7：依赖解析引擎](完成记录/01-平台核心.md) |
| D59 | 缺失依赖的"原因"如实透传底层错误（市场不可达 ≠ 组件不存在） | [Step 7：依赖解析引擎](完成记录/01-平台核心.md) |
| D60 | 循环路径只打印环上的节点 | [Step 7：依赖解析引擎](完成记录/01-平台核心.md) |
| D61 | `Node.Dependents` 在解析时顺手记录 | [Step 7：依赖解析引擎](完成记录/01-平台核心.md) |
| D62 | 资源绑定检查一次报出全部未满足项，并区分"未声明"与"已声明但未绑定" | [Step 7：依赖解析引擎](完成记录/01-平台核心.md) |
| D63 | `CheckResourceBindings` 导出 | [Step 7：依赖解析引擎](完成记录/01-平台核心.md) |
| D64 | `EnvPrefix` / `EndpointEnvVar` 放在 `manifest` 包 | [Step 7：依赖解析引擎](完成记录/01-平台核心.md) |
| D65 | `ResolveConfig` 不过滤 `enabled` | [Step 7：依赖解析引擎](完成记录/01-平台核心.md) |
| D66 | 8.2 / 8.3 拆成"库层现在测全 + 命令接线延后" | [Step 8：配置备份与恢复（reset）](完成记录/01-平台核心.md) |
| D67 | `reset` **不**为当前状态创建备份 | [Step 8：配置备份与恢复（reset）](完成记录/01-平台核心.md) |
| D68 | 恢复时不校验备份内容是否为合法 YAML | [Step 8：配置备份与恢复（reset）](完成记录/01-平台核心.md) |
| D69 | `.last` 缺失与 `.initial` 缺失给不同的建议 | [Step 8：配置备份与恢复（reset）](完成记录/01-平台核心.md) |
| D70 | `Options.Now` 作为时钟注入点 | [Step 8：配置备份与恢复（reset）](完成记录/01-平台核心.md) |
| D71 | 输出里的备份路径用相对项目根的形式 | [Step 8：配置备份与恢复（reset）](完成记录/01-平台核心.md) |
| D72 | 配置写入走 YAML 节点层，不经过 `Config` 结构体 | [Step 9：`brickkit add` / `remove` 命令](完成记录/01-平台核心.md) |
| D73 | `Save` 后按原文还原顶层块之间的空行 | [Step 9：`brickkit add` / `remove` 命令](完成记录/01-平台核心.md) |
| D74 | `--repo` 的资格检查（闭源 / 目录已存在 / 无仓库地址）放在**写配置之前** | [Step 9：`brickkit add` / `remove` 命令](完成记录/01-平台核心.md) |
| D75 | `--repo-all` 遇到"目录已存在"**跳过**而不是报错（`--repo` 仍报错） | [Step 9：`brickkit add` / `remove` 命令](完成记录/01-平台核心.md) |
| D76 | `Origin` 绕开 Manifest 缓存直接问安装源 | [Step 9：`brickkit add` / `remove` 命令](完成记录/01-平台核心.md) |
| D77 | remove 的依赖方检查不做递归解析 | [Step 9：`brickkit add` / `remove` 命令](完成记录/01-平台核心.md) |
| D78 | 源码目录只在同 ID 最后一个版本被移除时删除 | [Step 9：`brickkit add` / `remove` 命令](完成记录/01-平台核心.md) |
| D79 | 参数值写错（版本号非法、ID 非法）统一按用法错误（退出码 2） | [Step 9：`brickkit add` / `remove` 命令](完成记录/01-平台核心.md) |
| D80 | add 一律先 `SaveLast` 再动配置，即使最终没有改动 | [Step 9：`brickkit add` / `remove` 命令](完成记录/01-平台核心.md) |
| D81 | 排序只用强依赖成边 | [Step 10：拓扑排序与 `brickkit order`](完成记录/01-平台核心.md) |
| D82 | 同层内按 ID + 版本号排序（版本按数字比较） | [Step 10：拓扑排序与 `brickkit order`](完成记录/01-平台核心.md) |
| D83 | `← 依赖 N` 列的是**直接强依赖**的序号 | [Step 10：拓扑排序与 `brickkit order`](完成记录/01-平台核心.md) |
| D84 | "必须最后启动"= 启动顺序中的最后一个组件 | [Step 10：拓扑排序与 `brickkit order`](完成记录/01-平台核心.md) |
| D85 | 排序阶段独立做环检测，而不是信任解析阶段 | [Step 10：拓扑排序与 `brickkit order`](完成记录/01-平台核心.md) |
| D86 | order 不做级联过滤（登记为 P17） | [Step 10：拓扑排序与 `brickkit order`](完成记录/01-平台核心.md) |
| D87 | 自测组件放 `tests/components/<组件>/`，各自独立 Go module | [插入 A：平台自测组件（demo/hello、demo/caller）](完成记录/06-测试组件.md) |
| D88 | 不占用 Phase 5（Step 21–28）的组件 ID，另起 `demo/` scope | [插入 A：平台自测组件（demo/hello、demo/caller）](完成记录/06-测试组件.md) |
| D89 | 迁移命令不做真实建表，而是"可达性检查 + 可控失败开关" | [插入 A：平台自测组件（demo/hello、demo/caller）](完成记录/06-测试组件.md) |
| D90 | 组件零外部依赖（只用标准库） | [插入 A：平台自测组件（demo/hello、demo/caller）](完成记录/06-测试组件.md) |
| D91 | 仓库根 `.gitignore` 的 `components/` 改为 `/components/` | [插入 A：平台自测组件（demo/hello、demo/caller）](完成记录/06-测试组件.md) |
| D92 | 市场**独立实现** Manifest 结构与校验，不复用 CLI 的 `internal/manifest` | [Step 18-A：市场领域模型与发布校验器](完成记录/04-市场.md) |
| D93 | 先做形状检查再解码结构体 | [Step 18-A：市场领域模型与发布校验器](完成记录/04-市场.md) |
| D94 | `{envPrefix}_*` 保留模式在发布时**不校验** | [Step 18-A：市场领域模型与发布校验器](完成记录/04-市场.md) |
| D95 | 依赖解析同时兼容三种写法 | [Step 18-A：市场领域模型与发布校验器](完成记录/04-市场.md) |
| D96 | 请求级问题与 Manifest 问题合并成一个错误上报，错误码以 Manifest 优先 | [Step 18-A：市场领域模型与发布校验器](完成记录/04-市场.md) |
| D97 | 仓储层写一份**行为契约测试**，内存与 PostgreSQL 共用 | [Step 18-B：仓储层与产物存储](完成记录/04-市场.md) |
| D98 | 版本排序在 Go 里做，不在 SQL 里做 | [Step 18-B：仓储层与产物存储](完成记录/04-市场.md) |
| D99 | 可见性作为**查询条件**下推到仓储层 | [Step 18-B：仓储层与产物存储](完成记录/04-市场.md) |
| D100 | 建表 SQL 用 `go:embed` 打进二进制，启动时幂等执行 | [Step 18-B：仓储层与产物存储](完成记录/04-市场.md) |
| D101 | 对象键把版本放在路径里 | [Step 18-B：仓储层与产物存储](完成记录/04-市场.md) |
| D102 | `TruncateAll` 只给测试用，并在注释里写明 | [Step 18-B：仓储层与产物存储](完成记录/04-市场.md) |
| D103 | 发布分两步：`POST 版本`（draft）→ 上传产物 → `PUT 状态=stable` | [Step 18-C：服务层与认证](完成记录/04-市场.md) |
| D104 | 版本号**不可回收**：软删除的版本仍占位 | [Step 18-C：服务层与认证](完成记录/04-市场.md) |
| D105 | blocked 返回 `COMPONENT_BLOCKED` 而不是 404 | [Step 18-C：服务层与认证](完成记录/04-市场.md) |
| D106 | 密码错误与用户不存在返回**同样**的错误 | [Step 18-C：服务层与认证](完成记录/04-市场.md) |
| D107 | 审计写失败不阻断主流程 | [Step 18-C：服务层与认证](完成记录/04-市场.md) |
| D108 | 匿名不是错误 | [Step 18-C：服务层与认证](完成记录/04-市场.md) |
| D109 | `Options.BcryptCost` 作为测试注入点 | [Step 18-C：服务层与认证](完成记录/04-市场.md) |
| D110 | 内部错误统一包成 `INTERNAL`，原始错误放 `details.cause` | [Step 18-C：服务层与认证](完成记录/04-市场.md) |
| D111 | 自己写路由器，不用 `http.ServeMux` | [Step 18-D：HTTP 层与服务装配](完成记录/04-市场.md) |
| D112 | 错误码 → 状态码的映射是对外契约 | [Step 18-D：HTTP 层与服务装配](完成记录/04-市场.md) |
| D113 | 上传端点是 `POST …/artifacts/{artifactId}/upload?file=` | [Step 18-D：HTTP 层与服务装配](完成记录/04-市场.md) |
| D114 | 大小上限用自己的 `limitReader` 而非 `http.MaxBytesReader` | [Step 18-D：HTTP 层与服务装配](完成记录/04-市场.md) |
| D115 | `total` 下推到仓储层（新增 `CountComponents`） | [Step 18-D：HTTP 层与服务装配](完成记录/04-市场.md) |
| D116 | 认证在 handler、鉴权在 service，中间件里一条权限规则都不写 | [Step 18-D：HTTP 层与服务装配](完成记录/04-市场.md) |
| D117 | 令牌用不透明随机串存库，不用 JWT | [Step 18-D：HTTP 层与服务装配](完成记录/04-市场.md) |
| D118 | 管理员引导只补权限、不覆盖口令 | [Step 18-D：HTTP 层与服务装配](完成记录/04-市场.md) |
| D119 | 启动时自动建表、自动建桶 | [Step 18-D：HTTP 层与服务装配](完成记录/04-市场.md) |
| D120 | 配置缺失项一次全报，端口/有效期写错当场失败 | [Step 18-D：HTTP 层与服务装配](完成记录/04-市场.md) |
| D121 | HEAD 路由到 GET 的处理器 | [Step 18-E：部署形态交付与真实部署复验](完成记录/04-市场.md) |
| D122 | 编排里放一个 `rustfs-init` 一次性容器改属主 | [Step 18-E：部署形态交付与真实部署复验](完成记录/04-市场.md) |
| D123 | 依赖版本服从最低 Go 版本要求，而不是反过来 | [Step 18-E：部署形态交付与真实部署复验](完成记录/04-市场.md) |
| D124 | 令牌重置连带吊销该账号全部令牌 | [Step 18-E：部署形态交付与真实部署复验](完成记录/04-市场.md) |
| D125 | 管理员提权只能改库，不提供 API | [Step 18-E：部署形态交付与真实部署复验](完成记录/04-市场.md) |
| D126 | 部署文档分成两份：**《部署模式》讲"跑在哪、怎么用"，《运维指南》讲单机部署的每一步** | [Step 18-E：部署形态交付与真实部署复验](完成记录/04-市场.md) |
| D127 | 新建 `internal/market` 承载市场协议，与 `internal/source` 分工 | [Step 19：CLI 发布与登录](完成记录/04-市场.md) |
| D128 | 所有本地校验都在联网之前做完 | [Step 19：CLI 发布与登录](完成记录/04-市场.md) |
| D129 | Manifest 以 **component.yaml 原文转 JSON** 上传，不走结构体 | [Step 19：CLI 发布与登录](完成记录/04-市场.md) |
| D130 | 可见性放在转 stable **之后**设置 | [Step 19：CLI 发布与登录](完成记录/04-市场.md) |
| D131 | 来源类型按组件目录的 git remote 推断，闭源时不外泄仓库地址 | [Step 19：CLI 发布与登录](完成记录/04-市场.md) |
| D132 | `--sign` 直接报"Step 20 未实现"，不静默忽略 | [Step 19：CLI 发布与登录](完成记录/04-市场.md) |
| D133 | 密码在任何路径下都不回显 | [Step 19：CLI 发布与登录](完成记录/04-市场.md) |
| D134 | 用户名与密码共用一个 `bufio.Reader` | [Step 19：CLI 发布与登录](完成记录/04-市场.md) |
| D135 | 级联只沿**强依赖**展开，弱依赖不会被拉起 | [Step 11：环境变量注入引擎 + 级联启停](完成记录/01-平台核心.md) |
| D136 | 只被弱依赖指向的组件不算根组件 | [Step 11：环境变量注入引擎 + 级联启停](完成记录/01-平台核心.md) |
| D137 | 强依赖被禁用：**钉住的报错，没钉住的静默级联跳过** | [Step 11：环境变量注入引擎 + 级联启停](完成记录/01-平台核心.md) |
| D138 | 注入引擎不产出文件，只产出"注入什么" | [Step 11：环境变量注入引擎 + 级联启停](完成记录/01-平台核心.md) |
| D139 | 环境变量按名字排序输出 | [Step 11：环境变量注入引擎 + 级联启停](完成记录/01-平台核心.md) |
| D140 | 没有默认值也没有覆盖的配置项**不注入** | [Step 11：环境变量注入引擎 + 级联启停](完成记录/01-平台核心.md) |
| D141 | 资源配额逐字段合并，不整块覆盖 | [Step 11：环境变量注入引擎 + 级联启停](完成记录/01-平台核心.md) |
| D142 | 保留变量表在 CLI 与市场各存一份，但算法必须一致 | [Step 11：环境变量注入引擎 + 级联启停](完成记录/01-平台核心.md) |
| D143 | 一份业务逻辑，HTTP 与 gRPC 都只是薄壳 | [Step 21：department/tree（Go，单端口双协议）](完成记录/06-测试组件.md) |
| D144 | 单端口双协议用 h2c + Content-Type 分流，不用 cmux | [Step 21：department/tree（Go，单端口双协议）](完成记录/06-测试组件.md) |
| D145 | 组件直连真实 PostgreSQL，**不做内存回退** | [Step 21：department/tree（Go，单端口双协议）](完成记录/06-测试组件.md) |
| D146 | 子树查询用递归 CTE 在库里做 | [Step 21：department/tree（Go，单端口双协议）](完成记录/06-测试组件.md) |
| D147 | 敏感字段在日志出口统一打码 | [Step 21：department/tree（Go，单端口双协议）](完成记录/06-测试组件.md) |
| D148 | 迁移与服务共用同一个二进制 | [Step 21：department/tree（Go，单端口双协议）](完成记录/06-测试组件.md) |
| D149 | Python 组件的测试跑在容器里 | [Step 22：people/basic（Python，extraPorts 双端口）](完成记录/06-测试组件.md) |
| D150 | 部门名**不在本组件存副本**，每次去问 department/tree | [Step 22：people/basic（Python，extraPorts 双端口）](完成记录/06-测试组件.md) |
| D151 | 强依赖不可用 → 503，弱依赖不可用 → 静默降级 | [Step 22：people/basic（Python，extraPorts 双端口）](完成记录/06-测试组件.md) |
| D152 | 去掉跨请求缓存，改为单次请求内去重 | [Step 22：people/basic（Python，extraPorts 双端口）](完成记录/06-测试组件.md) |
| D153 | openapi.json 由 FastAPI 导出而非手写 | [Step 22：people/basic（Python，extraPorts 双端口）](完成记录/06-测试组件.md) |
| D154 | 把 department/tree 的 proto vendored 进本组件仓库 | [Step 22：people/basic（Python，extraPorts 双端口）](完成记录/06-测试组件.md) |
| D155 | 建表 SQL 必须是独立的、有版本号的文件，不写在代码里 | [修订：把建表 SQL 从代码里搬进真正的 migration 文件](完成记录/04-市场.md) |
| D156 | 迁移与版本记录同事务 | [修订：把建表 SQL 从代码里搬进真正的 migration 文件](完成记录/04-市场.md) |
| D157 | 失败的迁移不记录版本 | [修订：把建表 SQL 从代码里搬进真正的 migration 文件](完成记录/04-市场.md) |
| D158 | 内存实现不再参与迁移 | [修订：把建表 SQL 从代码里搬进真正的 migration 文件](完成记录/04-市场.md) |
| D159 | `CREATE DATABASE` 不进组件的 migration | [修订：把建表 SQL 从代码里搬进真正的 migration 文件](完成记录/04-市场.md) |
| D160 | `schema_migrations` 主键是 `(component_id, version)` | [修订：把建表 SQL 从代码里搬进真正的 migration 文件](完成记录/04-市场.md) |
| D161 | 共用数据库只警告不阻断 | [修订：把建表 SQL 从代码里搬进真正的 migration 文件](完成记录/04-市场.md) |
| D162 | down 脚本缺失时回退**报错** | [修订：把建表 SQL 从代码里搬进真正的 migration 文件](完成记录/04-市场.md) |
| D163 | `down` / `reset` 只面向开发与测试 | [修订：把建表 SQL 从代码里搬进真正的 migration 文件](完成记录/04-市场.md) |
| D164 | 建库命令写进组件 README，并预设库名 | [修订：把建表 SQL 从代码里搬进真正的 migration 文件](完成记录/04-市场.md) |
| D165 | 这些规则写进设计书而不是只留在组件 README | [修订：把建表 SQL 从代码里搬进真正的 migration 文件](完成记录/04-市场.md) |
| D166 | 不写 `version: "3.8"` | [Step 12：docker-compose.yaml 生成](完成记录/02-部署与命令.md) |
| D167 | 迁移 service 同时覆盖 `entrypoint` 与 `command` | [Step 12：docker-compose.yaml 生成](完成记录/02-部署与命令.md) |
| D168 | 弱依赖不进 `depends_on` | [Step 12：docker-compose.yaml 生成](完成记录/02-部署与命令.md) |
| D169 | 依赖没有健康检查的组件时用 `service_started` | [Step 12：docker-compose.yaml 生成](完成记录/02-部署与命令.md) |
| D170 | environment 用 `KEY=value` 列表而非 map | [Step 12：docker-compose.yaml 生成](完成记录/02-部署与命令.md) |
| D171 | Step 12 只实现 `up --dry-run` | [Step 12：docker-compose.yaml 生成](完成记录/02-部署与命令.md) |
| D172 | 生成时输出"需要预先创建哪些数据库" | [Step 12：docker-compose.yaml 生成](完成记录/02-部署与命令.md) |
| D173 | local 组件的 env 文件从**容器版的注入结果**出发，只改"怎么连过去" | [Step 13：本地调试（local: true）与 local-debug.env](完成记录/02-部署与命令.md) |
| D174 | 宿主机端口优先 `10000 + 容器端口`，撞了才顺序扫描 | [Step 13：本地调试（local: true）与 local-debug.env](完成记录/02-部署与命令.md) |
| D175 | `localPort` / `exposePort` / 自动映射共用一张端口台账，使用者写死的先占位 | [Step 13：本地调试（local: true）与 local-debug.env](完成记录/02-部署与命令.md) |
| D176 | 外部资源（host 是 IP/域名）**不**改写成 localhost | [Step 13：本地调试（local: true）与 local-debug.env](完成记录/02-部署与命令.md) |
| D177 | local 组件的额外端口不重新分配，保持声明值 | [Step 13：本地调试（local: true）与 local-debug.env](完成记录/02-部署与命令.md) |
| D178 | `localPort` 默认取组件声明的 `deployment.port` | [Step 13：本地调试（local: true）与 local-debug.env](完成记录/02-部署与命令.md) |
| D179 | local 组件的迁移不代跑，但必须警告 | [Step 13：本地调试（local: true）与 local-debug.env](完成记录/02-部署与命令.md) |
| D180 | `setVar` 只改已存在的变量，不补新的 | [Step 13：本地调试（local: true）与 local-debug.env](完成记录/02-部署与命令.md) |
| D181 | 引擎检测放在命令层，`BRICKKIT_ENGINE` 可覆盖 | [Step 13：本地调试（local: true）与 local-debug.env](完成记录/02-部署与命令.md) |
| D182 | env 文件权限 0600 | [Step 13：本地调试（local: true）与 local-debug.env](完成记录/02-部署与命令.md) |
| D183 | 迁移容器与主容器共享 `extra_hosts` | [Step 14：数据库迁移执行（Docker）](完成记录/02-部署与命令.md) |
| D184 | 组件入口必须对未知参数非零退出（写进 002，成为组件的硬性要求） | [Step 14：数据库迁移执行（Docker）](完成记录/02-部署与命令.md) |
| D185 | 参数校验放在读环境变量、连数据库之前 | [Step 14：数据库迁移执行（Docker）](完成记录/02-部署与命令.md) |
| D186 | 本地安装源不吃 Manifest 与产物缓存 | [Step 14：数据库迁移执行（Docker）](完成记录/02-部署与命令.md) |
| D187 | 只扫到第一个非本地源为止来判定"是否来自本地源" | [Step 14：数据库迁移执行（Docker）](完成记录/02-部署与命令.md) |
| D188 | 容器引擎做成接口 + `Options.Engine` 注入点 | [Step 15-A：引擎抽象与 `brickkit up` 真启动](完成记录/02-部署与命令.md) |
| D189 | 显式传 `-p brickkit-<项目名>` | [Step 15-A：引擎抽象与 `brickkit up` 真启动](完成记录/02-部署与命令.md) |
| D190 | `up` 用 `--wait` | [Step 15-A：引擎抽象与 `brickkit up` 真启动](完成记录/02-部署与命令.md) |
| D191 | 引擎失败取输出**末尾**三行 | [Step 15-A：引擎抽象与 `brickkit up` 真启动](完成记录/02-部署与命令.md) |
| D192 | `CheckImage` 先看本地镜像，本地没有才问 registry | [Step 15-A：引擎抽象与 `brickkit up` 真启动](完成记录/02-部署与命令.md) |
| D193 | 镜像失败分三类（网络 / 未授权 / 不存在），**网络优先判** | [Step 15-A：引擎抽象与 `brickkit up` 真启动](完成记录/02-部署与命令.md) |
| D194 | 健康检查生成 wget/curl 双备选，并把"镜像必须自带其一"写进 002 | [Step 15-A：引擎抽象与 `brickkit up` 真启动](完成记录/02-部署与命令.md) |
| D195 | 启动后逐个核对 service 状态，有没起来的就非零退出 | [Step 15-A：引擎抽象与 `brickkit up` 真启动](完成记录/02-部署与命令.md) |
| D196 | `--only` / `--check-resources` 暂仍报"尚未实现" | [Step 15-A：引擎抽象与 `brickkit up` 真启动](完成记录/02-部署与命令.md) |
| D197 | down / status 共用 `loadProject`，且都不重新生成部署文件 | [Step 15-B：`brickkit down` 与 `brickkit status`](完成记录/02-部署与命令.md) |
| D198 | 整项目 down 时不传 service 列表 | [Step 15-B：`brickkit down` 与 `brickkit status`](完成记录/02-部署与命令.md) |
| D199 | `--only` 停部分组件用 `rm -sf` 而不是 `down` | [Step 15-B：`brickkit down` 与 `brickkit status`](完成记录/02-部署与命令.md) |
| D200 | 资源可达性按托管 / 外部分流，判据从 compose 包导出复用 | [Step 15-B：`brickkit down` 与 `brickkit status`](完成记录/02-部署与命令.md) |
| D201 | 资源不可达**不**让 status 失败 | [Step 15-B：`brickkit down` 与 `brickkit status`](完成记录/02-部署与命令.md) |
| D202 | status 不画 ┌─┬─┐ 表格，沿用其余命令的缩进风格 | [Step 15-B：`brickkit down` 与 `brickkit status`](完成记录/02-部署与命令.md) |
| D203 | 迁移容器与资源容器不出现在组件列表 | [Step 15-B：`brickkit down` 与 `brickkit status`](完成记录/02-部署与命令.md) |
| D204 | 没有部署文件时，先说"为什么一个都不会跑"再说"尚未启动过" | [Step 15-B：`brickkit down` 与 `brickkit status`](完成记录/02-部署与命令.md) |
| D205 | `--only` 取**强依赖闭包**，不是字面上的那几个 | [Step 15-C：`--only`、`--check-resources` 与升级路径](完成记录/02-部署与命令.md) |
| D206 | 收窄启动集合的 `Restrict` 放在 cascade 包里 | [Step 15-C：`--only`、`--check-resources` 与升级路径](完成记录/02-部署与命令.md) |
| D207 | 升级判据是"本地有没有这个版本的东西"，不是"版本号变没变过" | [Step 15-C：`--only`、`--check-resources` 与升级路径](完成记录/02-部署与命令.md) |
| D208 | 升级检查在**解析依赖图之前**做 | [Step 15-C：`--only`、`--check-resources` 与升级路径](完成记录/02-部署与命令.md) |
| D209 | 产物下载失败不阻断启动 | [Step 15-C：`--only`、`--check-resources` 与升级路径](完成记录/02-部署与命令.md) |
| D210 | `--check-resources` 只探**外部**资源 | [Step 15-C：`--only`、`--check-resources` 与升级路径](完成记录/02-部署与命令.md) |
| D211 | 端口体检要排除本项目自己的容器 | [Step 15-C：`--only`、`--check-resources` 与升级路径](完成记录/02-部署与命令.md) |
| D212 | 明文密码告警里不打印密码 | [Step 15-C：`--only`、`--check-resources` 与升级路径](完成记录/02-部署与命令.md) |
| D213 | `--check-resources` 与 `--dry-run` 同用时照样体检，且不要求引擎可用 | [Step 15-C：`--only`、`--check-resources` 与升级路径](完成记录/02-部署与命令.md) |
| D214 | CLI 打印出去的命令必须能直接照抄执行，并有用例盯住 | [设计书复核：把真跑出来的结论回灌文档](完成记录/07-复核与结欠账.md) |
| D215 | 设计书里的样例输出以**实际输出**为准 | [设计书复核：把真跑出来的结论回灌文档](完成记录/07-复核与结欠账.md) |
| D216 | `up` / `down` / `status` 在 Step 16 之前明确拒绝 `deploy.target: k8s` | [实现 vs 设计书全面比对](完成记录/07-复核与结欠账.md) |
| D217 | ~~D202~~ 反转：status 用框线表 | [实现 vs 设计书全面比对](完成记录/07-复核与结欠账.md) |
| D218 | 表格内容限制在"汉字 + 全角括号 + ASCII" | [实现 vs 设计书全面比对](完成记录/07-复核与结欠账.md) |
| D219 | K8s 生成器放 `internal/k8s`，与 `internal/compose` 并列 | [Step 16-A / 16-B：K8s 清单生成](完成记录/03-Kubernetes.md) |
| D220 | 敏感变量由**注入引擎**标记（`Var.SecretKey`），不由渲染器按变量名猜 | [Step 16-A / 16-B：K8s 清单生成](完成记录/03-Kubernetes.md) |
| D221 | Secret 按**资源**归集，不按组件 | [Step 16-A / 16-B：K8s 清单生成](完成记录/03-Kubernetes.md) |
| D222 | `${VAR}` 展开不了就**阻断生成**，不留占位符 | [Step 16-A / 16-B：K8s 清单生成](完成记录/03-Kubernetes.md) |
| D223 | `local: true` + `target: k8s` **报错**，不静默跳过 | [Step 16-A / 16-B：K8s 清单生成](完成记录/03-Kubernetes.md) |
| D224 | `expose: true` 缺 `hostname` 在生成阶段再拦一次 | [Step 16-A / 16-B：K8s 清单生成](完成记录/03-Kubernetes.md) |
| D225 | Service 端口一律带 `name` | [Step 16-A / 16-B：K8s 清单生成](完成记录/03-Kubernetes.md) |
| D226 | 迁移 Job 用 `brickkit.io/role: migration`，替换设计书原来的 `brickkit.io/migration: "true"` | [Step 16-A / 16-B：K8s 清单生成](完成记录/03-Kubernetes.md) |
| D227 | `WriteFiles` 先整个删掉再写 | [Step 16-A / 16-B：K8s 清单生成](完成记录/03-Kubernetes.md) |
| D228 | Secret 文件权限 `0600`，其余 `0644` | [Step 16-A / 16-B：K8s 清单生成](完成记录/03-Kubernetes.md) |
| D229 | `internal/deploy` 承载两种目标共用的结论（当前只有「要先建哪些库」） | [Step 16-C：K8s 目标接线（up / down / status）](完成记录/03-Kubernetes.md) |
| D230 | kubectl 复用 `Engine` 接口，`UpRequest.File` 在 K8s 下解释为**目录** | [Step 16-C：K8s 目标接线（up / down / status）](完成记录/03-Kubernetes.md) |
| D231 | `resolveEngineFor` 按 `deploy.target` 选引擎 | [Step 16-C：K8s 目标接线（up / down / status）](完成记录/03-Kubernetes.md) |
| D232 | `status` 查 **Deployment** 而不是 Pod | [Step 16-C：K8s 目标接线（up / down / status）](完成记录/03-Kubernetes.md) |
| D233 | K8s 下 `CheckImage` 是空操作 | [Step 16-C：K8s 目标接线（up / down / status）](完成记录/03-Kubernetes.md) |
| D234 | `${VAR}` 的取值顺序：进程环境 → 项目根 `.env` | [Step 16-C：K8s 目标接线（up / down / status）](完成记录/03-Kubernetes.md) |
| D235 | `.env` 只认最基本的 `KEY=value` | [Step 16-C：K8s 目标接线（up / down / status）](完成记录/03-Kubernetes.md) |
| D236 | 明文密码告警看**原文**，不看解析后的值 | [Step 16-C：K8s 目标接线（up / down / status）](完成记录/03-Kubernetes.md) |
| D237 | K8s 下 `down` 不提"数据卷"，改说"基础资源由运维部署" | [Step 16-C：K8s 目标接线（up / down / status）](完成记录/03-Kubernetes.md) |
| D238 | 引擎调用一律显式传 `--project-directory` | [接上真集群（minikube）+ 试用指南](完成记录/03-Kubernetes.md) |
| D239 | "谁不做替换，就在生成时替换给谁" | [接上真集群（minikube）+ 试用指南](完成记录/03-Kubernetes.md) |
| D240 | `local-debug.env` 求不出值时**原样保留**，不阻断 | [接上真集群（minikube）+ 试用指南](完成记录/03-Kubernetes.md) |
| D241 | K8s 下不从本机探测资源可达性 | [接上真集群（minikube）+ 试用指南](完成记录/03-Kubernetes.md) |
| D242 | 试用指南单独成册，不并进设计书 | [接上真集群（minikube）+ 试用指南](完成记录/03-Kubernetes.md) |
| D243 | `deploy.context` 不一致就阻断，且**每条 kubectl 都带 `--context`** | [Step 16-D：面向真实集群](完成记录/03-Kubernetes.md) |
| D244 | 取不到 current-context 时**不拦** | [Step 16-D：面向真实集群](完成记录/03-Kubernetes.md) |
| D245 | 命名空间不是我们建的就不能由我们删；`down` 时逐个子目录删，不用 `-R` | [Step 16-D：面向真实集群](完成记录/03-Kubernetes.md) |
| D246 | `podSecurity` 默认**什么都不生成** | [Step 16-D：面向真实集群](完成记录/03-Kubernetes.md) |
| D247 | 不生成 `readOnlyRootFilesystem` | [Step 16-D：面向真实集群](完成记录/03-Kubernetes.md) |
| D248 | restricted + 端口 < 1024 在生成阶段就警告 | [Step 16-D：面向真实集群](完成记录/03-Kubernetes.md) |
| D249 | `ingressAnnotations` 原样透传，平台不认识也不该认识 | [Step 16-D：面向真实集群](完成记录/03-Kubernetes.md) |
| D250 | 用 `os.Rename` 整目录搬家，不复制 | [Step 17：`brickkit sync`](完成记录/02-部署与命令.md) |
| D251 | **只动 brickkit.yaml 里声明过的组件** | [Step 17：`brickkit sync`](完成记录/02-部署与命令.md) |
| D252 | 目标目录已存在时**报错阻断**，绝不覆盖 | [Step 17：`brickkit sync`](完成记录/02-部署与命令.md) |
| D253 | 归档原因优先取级联结果里的原话 | [Step 17：`brickkit sync`](完成记录/02-部署与命令.md) |
| D254 | 按**组件 ID** 归集，与版本无关 | [Step 17：`brickkit sync`](完成记录/02-部署与命令.md) |
| D255 | sync 不碰引擎、不写配置、不生成部署文件 | [Step 17：`brickkit sync`](完成记录/02-部署与命令.md) |
| D256 | Docker 与 Podman 的 `extra_hosts` 用**同一个值** `host-gateway` | [结欠账（一）：P23 Podman 真机 + P12 git ref](完成记录/07-复核与结欠账.md) |
| D257 | 引擎检测加入 `podman compose` 子命令，排在 `podman-compose` 之后 | [结欠账（一）：P23 Podman 真机 + P12 git ref](完成记录/07-复核与结欠账.md) |
| D258 | `ref` 先浅 clone 再回落完整 clone，不一上来就完整 clone | [结欠账（一）：P23 Podman 真机 + P12 git ref](完成记录/07-复核与结欠账.md) |
| D259 | 生产环境建议 `ref` 钉 tag 或 commit（写进 003 §6.3） | [结欠账（一）：P23 Podman 真机 + P12 git ref](完成记录/07-复核与结欠账.md) |
| D260 | 用 kustomize，不用 Helm | [结欠账（二）：P19 市场的 K8s 部署清单](完成记录/04-市场.md) |
| D261 | base 只部署市场本身，依赖在集群外 | [结欠账（二）：P19 市场的 K8s 部署清单](完成记录/04-市场.md) |
| D262 | 口令只放一份，在 `base/.env` | [结欠账（二）：P19 市场的 K8s 部署清单](完成记录/04-市场.md) |
| D263 | ConfigMap / Secret 名字**带内容哈希** | [结欠账（二）：P19 市场的 K8s 部署清单](完成记录/04-市场.md) |
| D264 | `market-api` 默认 1 个副本 | [结欠账（二）：P19 市场的 K8s 部署清单](完成记录/04-市场.md) |
| D265 | 清单里**不写 `${VAR}`** | [结欠账（二）：P19 市场的 K8s 部署清单](完成记录/04-市场.md) |
| D266 | 签的是 **Manifest**，不是镜像 | [Step 20-A：签名核心（`internal/security`）](完成记录/05-签名与安全.md) |
| D267 | 验签只用 Go 标准库，不依赖 cosign 二进制 | [Step 20-A：签名核心（`internal/security`）](完成记录/05-签名与安全.md) |
| D268 | 签名生成仍然调 **外部 cosign** | [Step 20-A：签名核心（`internal/security`）](完成记录/05-签名与安全.md) |
| D269 | 签名对象是**规范化载荷**（键名字典序的 JSON），不是原始字节 | [Step 20-A：签名核心（`internal/security`）](完成记录/05-签名与安全.md) |
| D270 | 规范化时**重复键直接拒绝** | [Step 20-A：签名核心（`internal/security`）](完成记录/05-签名与安全.md) |
| D271 | 公钥**只**来自 `installer.publicKeys`，找不到即失败 | [Step 20-A：签名核心（`internal/security`）](完成记录/05-签名与安全.md) |
| D272 | `VerifyManifest` 额外核对 `metadata.id` / `version` | [Step 20-A：签名核心（`internal/security`）](完成记录/05-签名与安全.md) |
| D273 | 签名固定 `--tlog-upload=false` | [Step 20-A：签名核心（`internal/security`）](完成记录/05-签名与安全.md) |
| D274 | `publicKeyRef` 必填，CLI 不代为编造默认值 | [Step 20-A：签名核心（`internal/security`）](完成记录/05-签名与安全.md) |
| D275 | 没配 `publicKeys` **不是**配置错误 | [Step 20-A：签名核心（`internal/security`）](完成记录/05-签名与安全.md) |
| D276 | `publicKeys` 相对路径以**项目根目录**为基准 | [Step 20-A：签名核心（`internal/security`）](完成记录/05-签名与安全.md) |
| D277 | 把 `cosign.key` 当公钥配进来时**点名说是私钥** | [Step 20-A：签名核心（`internal/security`）](完成记录/05-签名与安全.md) |
| D278 | **钥匙串是总开关**：没配 `publicKeys` 时 `requireSignature` 整体失效（放行 + 警告） | [Step 20-B：签名接线（publish / 市场 / add）](完成记录/05-签名与安全.md) |
| D279 | `requireSignature: false` **仍然拒绝验不过的签名** | [Step 20-B：签名接线（publish / 市场 / add）](完成记录/05-签名与安全.md) |
| D280 | **只有市场安装源受签名约束** | [Step 20-B：签名接线（publish / 市场 / add）](完成记录/05-签名与安全.md) |
| D281 | 校验放在 `internal/source`，不放在 `add` 命令里 | [Step 20-B：签名接线（publish / 市场 / add）](完成记录/05-签名与安全.md) |
| D282 | **缓存也要校验**，并在旁边存 `<name>.sig.json` 信封 | [Step 20-B：签名接线（publish / 市场 / add）](完成记录/05-签名与安全.md) |
| D283 | 缓存校验不过时**退回重新拉取**，不直接报错 | [Step 20-B：签名接线（publish / 市场 / add）](完成记录/05-签名与安全.md) |
| D284 | **先验后存**：验不过的内容绝不写入缓存 | [Step 20-B：签名接线（publish / 市场 / add）](完成记录/05-签名与安全.md) |
| D285 | 市场**只做结构校验**，不做密码学校验 | [Step 20-B：签名接线（publish / 市场 / add）](完成记录/05-签名与安全.md) |
| D286 | 市场**不强制**签名 | [Step 20-B：签名接线（publish / 市场 / add）](完成记录/05-签名与安全.md) |
| D287 | 但市场**挡住结构非法的签名**（算法不认识 / 缺 ref / value 不是 base64） | [Step 20-B：签名接线（publish / 市场 / add）](完成记录/05-签名与安全.md) |
| D288 | `publish --sign` 在**联网之前**签名 | [Step 20-B：签名接线（publish / 市场 / add）](完成记录/05-签名与安全.md) |
| D289 | `publicKeyRef` 默认按 `--key` 的 `.key` → `.pub` 推导 | [Step 20-B：签名接线（publish / 市场 / add）](完成记录/05-签名与安全.md) |
| D290 | 签完在终端打印"使用者需要在 `installer.publicKeys` 下声明 xxx" | [Step 20-B：签名接线（publish / 市场 / add）](完成记录/05-签名与安全.md) |
| D291 | 只渲染**验过的**组件（`🔏 签名：✅ 已校验`），不列"未校验" | [Step 20-B：签名接线（publish / 市场 / add）](完成记录/05-签名与安全.md) |
| D292 | 扫描 `.brickkit/manifests/` 时**只认 `.yaml`** | [Step 20-B：签名接线（publish / 市场 / add）](完成记录/05-签名与安全.md) |
| D293 | `remove` 一并删签名缓存 | [Step 20-B：签名接线（publish / 市场 / add）](完成记录/05-签名与安全.md) |
| D294 | 口令在本组件、身份在 people/basic | [Step 23：auth/password-login（Go，HTTP）](完成记录/06-测试组件.md) |
| D295 | 强依赖故障报 **503**，人不在报 **401** | [Step 23：auth/password-login（Go，HTTP）](完成记录/06-测试组件.md) |
| D296 | 用户不存在与口令错误返回**完全相同**的响应 | [Step 23：auth/password-login（Go，HTTP）](完成记录/06-测试组件.md) |
| D297 | JWT 用 `golang-jwt/jwt/v5`，不手搓 | [Step 23：auth/password-login（Go，HTTP）](完成记录/06-测试组件.md) |
| D298 | 校验时**只接受 HS256**，不看令牌自称的算法 | [Step 23：auth/password-login（Go，HTTP）](完成记录/06-测试组件.md) |
| D299 | 弱密钥（< 32 字节）**拒绝启动** | [Step 23：auth/password-login（Go，HTTP）](完成记录/06-测试组件.md) |
| D300 | 缺 `JWT_SECRET` 绝不回落到内置默认值 | [Step 23：auth/password-login（Go，HTTP）](完成记录/06-测试组件.md) |
| D301 | 口令哈希用 PBKDF2-SHA256 600000 轮 + 随机盐 | [Step 23：auth/password-login（Go，HTTP）](完成记录/06-测试组件.md) |
| D302 | 比较用 `subtle.ConstantTimeCompare` | [Step 23：auth/password-login（Go，HTTP）](完成记录/06-测试组件.md) |
| D303 | 损坏的哈希一律判为**失败** | [Step 23：auth/password-login（Go，HTTP）](完成记录/06-测试组件.md) |
| D304 | 增加 `/api/v1/verify`（计划里没有） | [Step 23：auth/password-login（Go，HTTP）](完成记录/06-测试组件.md) |
| D305 | 调用强依赖必须有超时（5s） | [Step 23：auth/password-login（Go，HTTP）](完成记录/06-测试组件.md) |
| D306 | 敏感字段名扩到含 `hash` / `credential` | [Step 23：auth/password-login（Go，HTTP）](完成记录/06-测试组件.md) |
| D307 | 样例账号写进 seed 迁移，且可单独回退 | [Step 23：auth/password-login（Go，HTTP）](完成记录/06-测试组件.md) |
| D308 | `lib/pq` 钉在 v1.10.9 | [Step 23：auth/password-login（Go，HTTP）](完成记录/06-测试组件.md) |
| D309 | 权限 = 人的角色 ∪ **部门**的角色 | [Step 24：authorization/rbac（Go，HTTP + gRPC + Redis）](完成记录/06-测试组件.md) |
| D310 | **Redis 挂了照常回源**，不报错 | [Step 24：authorization/rbac（Go，HTTP + gRPC + Redis）](完成记录/06-测试组件.md) |
| D311 | 没绑定 `cache` 资源时用**进程内缓存**，不报错 | [Step 24：authorization/rbac（Go，HTTP + gRPC + Redis）](完成记录/06-测试组件.md) |
| D312 | **people/basic 挂了且缓存未命中 → 503，不做部分降级** | [Step 24：authorization/rbac（Go，HTTP + gRPC + Redis）](完成记录/06-测试组件.md) |
| D313 | 先查缓存**再**调 people/basic | [Step 24：authorization/rbac（Go，HTTP + gRPC + Redis）](完成记录/06-测试组件.md) |
| D314 | 缓存键带组件前缀 `authorization-rbac:` | [Step 24：authorization/rbac（Go，HTTP + gRPC + Redis）](完成记录/06-测试组件.md) |
| D315 | 缓存**必须有 TTL** | [Step 24：authorization/rbac（Go，HTTP + gRPC + Redis）](完成记录/06-测试组件.md) |
| D316 | 缓存内容损坏时**当作未命中**并删掉 | [Step 24：authorization/rbac（Go，HTTP + gRPC + Redis）](完成记录/06-测试组件.md) |
| D317 | 空权限集也要缓存 | [Step 24：authorization/rbac（Go，HTTP + gRPC + Redis）](完成记录/06-测试组件.md) |
| D318 | `check` 没有权限时返回 **200 + allowed:false**，不是 403 | [Step 24：authorization/rbac（Go，HTTP + gRPC + Redis）](完成记录/06-测试组件.md) |
| D319 | 权限与角色**排序去重**后返回 | [Step 24：authorization/rbac（Go，HTTP + gRPC + Redis）](完成记录/06-测试组件.md) |
| D320 | 授权指向不存在的角色时**跳过**而不报错 | [Step 24：authorization/rbac（Go，HTTP + gRPC + Redis）](完成记录/06-测试组件.md) |
| D321 | `GrantsFor` 一次查完"人 + 部门" | [Step 24：authorization/rbac（Go，HTTP + gRPC + Redis）](完成记录/06-测试组件.md) |
| D322 | `reason` 只在"确实有权限"时才算 | [Step 24：authorization/rbac（Go，HTTP + gRPC + Redis）](完成记录/06-测试组件.md) |
| D323 | **不绑定任何资源**，订单放内存 | [Step 25：erp/backend（Go，连接组件）](完成记录/06-测试组件.md) |
| D324 | 订单只存 `ownerId`，姓名每次现取 | [Step 25：erp/backend（Go，连接组件）](完成记录/06-测试组件.md) |
| D325 | 弱依赖失败时**业务状态照常改变** | [Step 25：erp/backend（Go，连接组件）](完成记录/06-测试组件.md) |
| D326 | 弱依赖地址**不进**"缺少必需配置"校验 | [Step 25：erp/backend（Go，连接组件）](完成记录/06-测试组件.md) |
| D327 | 弱依赖缺席时用 `disabledEventBus` 空实现，不用 nil | [Step 25：erp/backend（Go，连接组件）](完成记录/06-测试组件.md) |
| D328 | 响应里如实回 `eventPublished` | [Step 25：erp/backend（Go，连接组件）](完成记录/06-测试组件.md) |
| D329 | 事件总线超时（2s）比强依赖超时（5s）**更短** | [Step 25：erp/backend（Go，连接组件）](完成记录/06-测试组件.md) |
| D330 | `grpcTarget()` 剥掉 scheme | [Step 25：erp/backend（Go，连接组件）](完成记录/06-测试组件.md) |
| D331 | 用 `grpc.NewClient` 而不是 `grpc.Dial` | [Step 25：erp/backend（Go，连接组件）](完成记录/06-测试组件.md) |
| D332 | 401 / 403 / 503 三者严格分开 | [Step 25：erp/backend（Go，连接组件）](完成记录/06-测试组件.md) |
| D333 | 一屏订单里同一个人只查一次 people/basic | [Step 25：erp/backend（Go，连接组件）](完成记录/06-测试组件.md) |
| D334 | 订单所有者已从人员系统删除时，姓名留空而不是整个列表失败 | [Step 25：erp/backend（Go，连接组件）](完成记录/06-测试组件.md) |
| D335 | `people.proto` 的 `go_package` 由**调用方**添加 | [Step 25：erp/backend（Go，连接组件）](完成记录/06-测试组件.md) |
| D336 | `proxy_pass` 用**变量**而不是写死主机名 | [Step 26：portal/user-frontend（nginx）](完成记录/06-测试组件.md) |
| D337 | 配套加 `resolver`，并带上 `$request_uri` | [Step 26：portal/user-frontend（nginx）](完成记录/06-测试组件.md) |
| D338 | DNS 地址用官方的 `${NGINX_LOCAL_RESOLVERS}`，不写死也不自己实现 | [Step 26：portal/user-frontend（nginx）](完成记录/06-测试组件.md) |
| D339 | nginx 监听 **8080** 而不是 80 | [Step 26：portal/user-frontend（nginx）](完成记录/06-测试组件.md) |
| D340 | 健康检查用 `location = /healthz` 精确匹配 | [Step 26：portal/user-frontend（nginx）](完成记录/06-测试组件.md) |
| D341 | 页面里**只用相对路径** `/api/...` | [Step 26：portal/user-frontend（nginx）](完成记录/06-测试组件.md) |
| D342 | 用真 nginx 跑 `nginx -t` 作为测试 | [Step 26：portal/user-frontend（nginx）](完成记录/06-测试组件.md) |
| D343 | 占位符检查用正则 `\$\{[A-Z_][A-Z0-9_]*\}` 而不是找 `${` | [Step 26：portal/user-frontend（nginx）](完成记录/06-测试组件.md) |
| D344 | 不声明 `configSchema` | [Step 26：portal/user-frontend（nginx）](完成记录/06-测试组件.md) |
| D345 | 用 Redis **Streams** 而不是 Pub/Sub | [Step 27：infra/redis-event-bus（Python）](完成记录/06-测试组件.md) |
| D346 | Redis 在这里是**唯一数据源**，缺了就启动失败 | [Step 27：infra/redis-event-bus（Python）](完成记录/06-测试组件.md) |
| D347 | 收不下的事件**如实报 503**，绝不假装收下 | [Step 27：infra/redis-event-bus（Python）](完成记录/06-测试组件.md) |
| D348 | 启动时**不 ping Redis** | [Step 27：infra/redis-event-bus（Python）](完成记录/06-测试组件.md) |
| D349 | 健康检查也不 ping Redis | [Step 27：infra/redis-event-bus（Python）](完成记录/06-测试组件.md) |
| D350 | 发布返回 **202** 而不是 200 | [Step 27：infra/redis-event-bus（Python）](完成记录/06-测试组件.md) |
| D351 | 额外字段**原样存下** | [Step 27：infra/redis-event-bus（Python）](完成记录/06-测试组件.md) |
| D352 | 缺时间戳时由本组件补上 | [Step 27：infra/redis-event-bus（Python）](完成记录/06-测试组件.md) |
| D353 | 发布方自己填的 `id` 被忽略 | [Step 27：infra/redis-event-bus（Python）](完成记录/06-测试组件.md) |
| D354 | 流用 `maxlen` + approximate 裁剪 | [Step 27：infra/redis-event-bus（Python）](完成记录/06-测试组件.md) |
| D355 | 契约由**调用方**先定下，本组件照着实现 | [Step 27：infra/redis-event-bus（Python）](完成记录/06-测试组件.md) |
| D356 | 硬编码守卫改用 `ast` 只查代码里的字符串 | [Step 27：infra/redis-event-bus（Python）](完成记录/06-测试组件.md) |
| D357 | 四种状态严格分开，每种都给出"接下来该做什么" | [Step 28：infra/api-docs（Go）](完成记录/06-测试组件.md) |
| D358 | Swagger UI 从 `swaggerapi/swagger-ui` 镜像拷，**不从 CDN 加载** | [Step 28：infra/api-docs（Go）](完成记录/06-测试组件.md) |
| D359 | OpenAPI 由本组件**代理**，不让浏览器直连组件 | [Step 28：infra/api-docs（Go）](完成记录/06-测试组件.md) |
| D360 | 聚合状态里**不含组件的内部地址** | [Step 28：infra/api-docs（Go）](完成记录/06-测试组件.md) |
| D361 | 探测**并发**且各自捕获错误 | [Step 28：infra/api-docs（Go）](完成记录/06-测试组件.md) |
| D362 | 探测结果缓存 30 秒，且会过期 | [Step 28：infra/api-docs（Go）](完成记录/06-测试组件.md) |
| D363 | 用真 protobuf 描述符解析反射结果，不在字节里找字符串 | [Step 28：infra/api-docs（Go）](完成记录/06-测试组件.md) |
| D364 | 反射服务自身（`grpc.reflection.*`）不列出来 | [Step 28：infra/api-docs（Go）](完成记录/06-测试组件.md) |
| D365 | **没有任何必需配置** | [Step 28：infra/api-docs（Go）](完成记录/06-测试组件.md) |
| D366 | Go 组件不加 `/openapi.json`，如实显示 `no-docs` | [Step 28：infra/api-docs（Go）](完成记录/06-测试组件.md) |
| D367 | 自己走一遍字段比对，不用 `KnownFields(true)` | [结欠账：P32 / P33 / P34 全部关闭](完成记录/07-复核与结欠账.md) |
| D368 | 猜字段时**前缀优先**于编辑距离 | [结欠账：P32 / P33 / P34 全部关闭](完成记录/07-复核与结欠账.md) |
| D369 | 前缀猜测要求至少 3 个字符 | [结欠账：P32 / P33 / P34 全部关闭](完成记录/07-复核与结欠账.md) |
| D370 | `component.config` 底下不查未知字段 | [结欠账：P32 / P33 / P34 全部关闭](完成记录/07-复核与结欠账.md) |
| D371 | `extra_hosts` 只给**绑定了该资源**的组件加 | [结欠账：P32 / P33 / P34 全部关闭](完成记录/07-复核与结欠账.md) |
| D372 | 迁移容器同样要加 `extra_hosts` | [结欠账：P32 / P33 / P34 全部关闭](完成记录/07-复核与结欠账.md) |
| D373 | 组件在运行时也暴露 `/openapi.json`，与市场产物并存 | [结欠账：P32 / P33 / P34 全部关闭](完成记录/07-复核与结欠账.md) |
| D374 | 用 `go:embed` 而不是 `COPY` 进镜像 | [结欠账：P32 / P33 / P34 全部关闭](完成记录/07-复核与结欠账.md) |
| D375 | nginx 的 FQDN 问题修在**组件**里，不修在平台注入侧 | [试用指南 16 / 17：K8s 完整装配 + 开发自己的组件](完成记录/07-复核与结欠账.md) |
| D376 | 只在 search 域里看到 `.svc.` 时才补 FQDN | [试用指南 16 / 17：K8s 完整装配 + 开发自己的组件](完成记录/07-复核与结欠账.md) |
| D377 | 钩子文件名必须以 `.envsh` 结尾 | [试用指南 16 / 17：K8s 完整装配 + 开发自己的组件](完成记录/07-复核与结欠账.md) |
| D378 | 示例组件放进仓库（`试用指南/示例组件/`） | [试用指南 16 / 17：K8s 完整装配 + 开发自己的组件](完成记录/07-复核与结欠账.md) |
| D379 | NetworkPolicy 只生成 **Ingress** 方向，不生成 Egress | [结欠账（四）：P26 —— NetworkPolicy / ServiceAccount 生成](完成记录/03-Kubernetes.md) |
| D380 | 没人依赖的组件也要生成一份**空 ingress** 的策略 | [结欠账（四）：P26 —— NetworkPolicy / ServiceAccount 生成](完成记录/03-Kubernetes.md) |
| D381 | 弱依赖与强依赖**一样放行** | [结欠账（四）：P26 —— NetworkPolicy / ServiceAccount 生成](完成记录/03-Kubernetes.md) |
| D382 | 只放行**本次真的会跑起来**的依赖方 | [结欠账（四）：P26 —— NetworkPolicy / ServiceAccount 生成](完成记录/03-Kubernetes.md) |
| D383 | 端口收敛到组件**声明过**的那些；Ingress 那条只放行主端口 | [结欠账（四）：P26 —— NetworkPolicy / ServiceAccount 生成](完成记录/03-Kubernetes.md) |
| D384 | `namespaceSelector` 与 `podSelector` 必须写在**同一个 from 元素**里 | [结欠账（四）：P26 —— NetworkPolicy / ServiceAccount 生成](完成记录/03-Kubernetes.md) |
| D385 | 开了策略 + 有 `expose: true` + 没写 `ingressController` → **阻断** | [结欠账（四）：P26 —— NetworkPolicy / ServiceAccount 生成](完成记录/03-Kubernetes.md) |
| D386 | `podSelector` 不写时只按命名空间放行 | [结欠账（四）：P26 —— NetworkPolicy / ServiceAccount 生成](完成记录/03-Kubernetes.md) |
| D387 | ServiceAccount 的重点是 `automountServiceAccountToken: false` | [结欠账（四）：P26 —— NetworkPolicy / ServiceAccount 生成](完成记录/03-Kubernetes.md) |
| D388 | 写了 `serviceAccountName` 就只引用、不生成，且**不写** automount | [结欠账（四）：P26 —— NetworkPolicy / ServiceAccount 生成](完成记录/03-Kubernetes.md) |
| D389 | **不生成 PodDisruptionBudget**，等多副本之后再说 | [结欠账（四）：P26 —— NetworkPolicy / ServiceAccount 生成](完成记录/03-Kubernetes.md) |
| D390 | 子目录名收敛到 `k8s.ManifestDirs()` 单一来源，删除顺序是部署顺序的反序 | [结欠账（四）：P26 —— NetworkPolicy / ServiceAccount 生成](完成记录/03-Kubernetes.md) |
| D391 | 维持「一个项目 = 一个命名空间」，不做组件级命名空间 | [探讨：命名空间、共享组件与升级孤儿](完成记录/07-复核与结欠账.md) |
| D392 | 组件多到不好管时用**标签**分组，不用命名空间 | [探讨：命名空间、共享组件与升级孤儿](完成记录/07-复核与结欠账.md) |
| D393 | 共享组件走「外部依赖」，不走「跨命名空间部署」 | [探讨：命名空间、共享组件与升级孤儿](完成记录/07-复核与结欠账.md) |
| D394 | 共享前先分「有没有权威状态」 | [探讨：命名空间、共享组件与升级孤儿](完成记录/07-复核与结欠账.md) |
| D395 | 共享分**三**种形态，不是两种 | [探讨：命名空间、共享组件与升级孤儿](完成记录/07-复核与结欠账.md) |
| D396 | 判据是「**组件自己**持有权威状态吗」，不是「听上去像不像中枢」 | [探讨：命名空间、共享组件与升级孤儿](完成记录/07-复核与结欠账.md) |
| D397 | 形态二（共享资源）不需要任何新机制 | [探讨：命名空间、共享组件与升级孤儿](完成记录/07-复核与结欠账.md) |
| D398 | 清理放在 rollout 就绪**之后** | [修复 P38：K8s 升级后旧版本变成孤儿](完成记录/03-Kubernetes.md) |
| D399 | `--only` 时完全不清理，用「命令层不给选择器」表达 | [修复 P38：K8s 升级后旧版本变成孤儿](完成记录/03-Kubernetes.md) |
| D400 | 清理范围不含 Namespace 与 Secret | [修复 P38：K8s 升级后旧版本变成孤儿](完成记录/03-Kubernetes.md) |
| D401 | 查询失败只警告，不让整次 `up` 失败 | [修复 P38：K8s 升级后旧版本变成孤儿](完成记录/03-Kubernetes.md) |
| D402 | 删掉的资源逐条打印，且带类型前缀 | [修复 P38：K8s 升级后旧版本变成孤儿](完成记录/03-Kubernetes.md) |
| D403 | `allowFrom` 加到**每一个**组件的策略上 | [修复 P36：网络策略挡掉了监控](完成记录/03-Kubernetes.md) |
| D404 | 不写 `ports` 时放行组件声明过的全部端口 | [修复 P36：网络策略挡掉了监控](完成记录/03-Kubernetes.md) |
| D405 | `name` 必填，并写进 `brickkit.io/allow-from` 注解 | [修复 P36：网络策略挡掉了监控](完成记录/03-Kubernetes.md) |
| D406 | 两处来源共用 `namespacedSource` 一个出口 | [修复 P36：网络策略挡掉了监控](完成记录/03-Kubernetes.md) |
| D407 | 出站单独一个 `egress.enabled`，不复用 networkPolicy 的开关 | [实现 P37：出站策略](完成记录/03-Kubernetes.md) |
| D408 | DNS 由平台自动放行，放行集群内任意命名空间的 53（UDP+TCP） | [实现 P37：出站策略](完成记录/03-Kubernetes.md) |
| D409 | 资源端口从 `resources[].port` 取，写了 `resource` 就禁止再写 `ports` | [实现 P37：出站策略](完成记录/03-Kubernetes.md) |
| D410 | 资源声明不全**在生成阶段阻断** | [实现 P37：出站策略](完成记录/03-Kubernetes.md) |
| D411 | 资源类目标只加给真的绑了它的组件 | [实现 P37：出站策略](完成记录/03-Kubernetes.md) |
| D412 | 移除 Podman，而不是标成「实验性」 | [移除 Podman 支持（P27 收尾）](完成记录/02-部署与命令.md) |
| D413 | 「只装了 Podman」单独报一条错，不并进「没找到引擎」 | [移除 Podman 支持（P27 收尾）](完成记录/02-部署与命令.md) |
| D414 | 错误文案写出具体卡在哪，不只说「不支持」 | [移除 Podman 支持（P27 收尾）](完成记录/02-部署与命令.md) |
| D415 | `run` 成功只返回 stdout，失败才合并 stderr | [移除 Podman 支持（P27 收尾）](完成记录/02-部署与命令.md) |
| D416 | Helm 与 kustomize **并存**，不替代 | [P28：市场 Helm chart](完成记录/04-市场.md) |
| D417 | 集群内依赖不做成 chart dependency | [P28：市场 Helm chart](完成记录/04-市场.md) |
| D418 | 口令二选一，两者都不给就渲染失败 | [P28：市场 Helm chart](完成记录/04-市场.md) |
| D419 | chart 不生成 Namespace | [P28：市场 Helm chart](完成记录/04-市场.md) |
| D420 | 每个资源的 `app.kubernetes.io/name` 必须各自给，不共用 | [P28：市场 Helm chart](完成记录/04-市场.md) |
| D421 | 钉 digest，而不是给镜像再签一次 | [P29：镜像 digest 钉住](完成记录/05-签名与安全.md) |
| D422 | 钉 digest **必须在签名之前** | [P29：镜像 digest 钉住](完成记录/05-签名与安全.md) |
| D423 | digest 解析失败**阻断发布** | [P29：镜像 digest 钉住](完成记录/05-签名与安全.md) |
| D424 | 只用 buildx 查 digest，不做 fallback | [P29：镜像 digest 钉住](完成记录/05-签名与安全.md) |
| D425 | 查 registry，不查本地 daemon 的 RepoDigests | [P29：镜像 digest 钉住](完成记录/05-签名与安全.md) |
| D426 | cosign 签镜像本身仍不做（属集群准入控制器） | [P29：镜像 digest 钉住](完成记录/05-签名与安全.md) |
| D427 | 市场**不做**密码学校验（Token 没有 scope，前提不成立） | [P30：市场侧密码学校验 —— 决定不做](完成记录/05-签名与安全.md) |
| D428 | 真要做的话选账号密钥登记，不选 TOFU | [P30：市场侧密码学校验 —— 决定不做](完成记录/05-签名与安全.md) |
| D429 | 保留规范化黄金样本，删掉市场侧验签 | [P30：市场侧密码学校验 —— 决定不做](完成记录/05-签名与安全.md) |
| D430 | 市场存的 manifest_json 是关键内容，不是「一些配置」 | [P30：市场侧密码学校验 —— 决定不做](完成记录/05-签名与安全.md) |
| D431 | digest 钉住写进签名那一节，不新开一节 | [收尾复核：P26 → P39 全程对照](完成记录/07-复核与结欠账.md) |
| D432 | `defaultDigestResolver` 不做单元测试，改为手工对真 registry 验证 | [收尾复核：P26 → P39 全程对照](完成记录/07-复核与结欠账.md) |
| D433 | config 密钥告警判据取**展开前的原文**（新增 `ConfigFromEnv`） | [Step 35：安全测试](完成记录/05-签名与安全.md) |
| D434 | 密钥关键词**刻意收窄**，宁可漏报不误报 | [Step 35：安全测试](完成记录/05-签名与安全.md) |
| D435 | 性能测试拆成"报数字的 Benchmark"与"守量级的 Test"，不把计划阈值写成断言 | [Step 36：性能基准测试](完成记录/07-复核与结欠账.md) |
| D436 | 内存量 `TotalAlloc` 而不是 `HeapAlloc` 差值（差值实测为负） | [Step 36：性能基准测试](完成记录/07-复核与结欠账.md) |
| D437 | `checkImages` 并发化，上限 8——真跑 50.5 秒 → 1.6 秒 | [Step 36：性能基准测试](完成记录/07-复核与结欠账.md) |
| D438 | 预检失败时跑完全部再报，且按输入顺序取第一个错误 | [Step 36：性能基准测试](完成记录/07-复核与结欠账.md) |
| D439 | 产物下载**不**跟着并发化（P40）：带宽受限与延迟受限不是一回事 | [Step 36：性能基准测试](完成记录/07-复核与结欠账.md) |
| D440 | 回归清单不新写测试，只做「承诺 → 已有测试」的映射 | [Step 37：回归测试](完成记录/07-复核与结欠账.md) |
| D441 | 清单放在 `清单.tsv`，开发计划里的表是它的投影 | [Step 37：回归测试](完成记录/07-复核与结欠账.md) |
| D442 | 测试存在性用 `go test -list` 判，不搜源码 | [Step 37：回归测试](完成记录/07-复核与结欠账.md) |
| D443 | `make test-regression` **先**校验清单**再**执行 | [Step 37：回归测试](完成记录/07-复核与结欠账.md) |
| D444 | 升级摘要补齐设计书规定的六项，没变化的写"无"而不隐藏 | [Step 38：升级测试](完成记录/07-复核与结欠账.md) |
| D445 | 旧 Manifest 读不到时报"未知"，**不报"无"** | [Step 38：升级测试](完成记录/07-复核与结欠账.md) |
| D446 | 资源依赖计入"依赖变更" | [Step 38：升级测试](完成记录/07-复核与结欠账.md) |
| D447 | 38.28 不写测试，由文档承担 | [Step 38：升级测试](完成记录/07-复核与结欠账.md) |
| D448 | 验收表加"证据"列，不只是打勾 | [Step 39：完整功能点验收](完成记录/07-复核与结欠账.md) |
| D449 | 审计脚本先自检再采信，基线必须含 market-server | [Step 39：完整功能点验收](完成记录/07-复核与结欠账.md) |
| D450 | A6.5 同时守"库名不同"与"实例相同"两个方向 | [Step 39：完整功能点验收](完成记录/07-复核与结欠账.md) |
| D451 | 新增 `check-cli-docs` 并接进 `make lint` | [Step 40：文档验证](完成记录/07-复核与结欠账.md) |
| D452 | 命令名靠**探测**认定，不靠解析帮助文本 | [Step 40：文档验证](完成记录/07-复核与结欠账.md) |
| D453 | 检查器必须报出"检查了多少处" | [Step 40：文档验证](完成记录/07-复核与结欠账.md) |
| D454 | 附录 F 就地写明 reservations 与 requests 的区别 | [Step 40：文档验证](完成记录/07-复核与结欠账.md) |
| D455 | 市场指南新增 §4.5「准备镜像」，排在启动之前 | [Step 41：市场部署与运维](完成记录/07-复核与结欠账.md) |
| D456 | §4.6 改用 `docker compose ps -a` | [Step 41：市场部署与运维](完成记录/07-复核与结欠账.md) |
| D457 | §4.3 警告补上"它不报错、服务照样健康" | [Step 41：市场部署与运维](完成记录/07-复核与结欠账.md) |
| D458 | 《部署模式》的过期 ⬜ 标记更新而不是删掉 | [Step 41：市场部署与运维](完成记录/07-复核与结欠账.md) |
| D459 | `external` 写**项目名**而不是命名空间或地址 | [P39：引用别的项目部署的组件](完成记录/07-复核与结欠账.md) |
| D460 | K8s 侧**不生成同名 Service**（会劫持流量且背后零个 Pod） | [P39：引用别的项目部署的组件](完成记录/07-复核与结欠账.md) |
| D461 | Docker 侧只把**依赖方**接进共享网络 | [P39：引用别的项目部署的组件](完成记录/07-复核与结欠账.md) |
| D462 | 共享网络声明为 `external: true`，只引用不创建 | [P39：引用别的项目部署的组件](完成记录/07-复核与结欠账.md) |
| D463 | 命名规则下沉到 `internal/deploy`，避免 inject↔k8s 成环 | [P39：引用别的项目部署的组件](完成记录/07-复核与结欠账.md) |
| D464 | `up` 专门列出外部依赖并提醒"对方没部署会连接失败" | [P39：引用别的项目部署的组件](完成记录/07-复核与结欠账.md) |
| D465 | P35 连同多副本一起做，不单做 | [P35：多副本与 PodDisruptionBudget](完成记录/07-复核与结欠账.md) |
| D466 | `replicas` 默认 1，且仅 K8s 生效 | [P35：多副本与 PodDisruptionBudget](完成记录/07-复核与结欠账.md) |
| D467 | `replicas: 0` 报错并指向 `enabled: false` | [P35：多副本与 PodDisruptionBudget](完成记录/07-复核与结欠账.md) |
| D468 | PDB 用 `maxUnavailable: 1` 而非 `minAvailable` | [P35：多副本与 PodDisruptionBudget](完成记录/07-复核与结欠账.md) |
| D469 | 孤儿判定对**条件生成**的类型按显式名单判 | [P35：多副本与 PodDisruptionBudget](完成记录/07-复核与结欠账.md) |
| D470 | 推翻 Step 36 的判断，产物下载并发化 | [P40：产物下载并发](完成记录/07-复核与结欠账.md) |
| D471 | 并发在**组件**层而非文件层 | [P40：产物下载并发](完成记录/07-复核与结欠账.md) |
| D472 | 结果按依赖图顺序汇总，不按完成顺序 | [P40：产物下载并发](完成记录/07-复核与结欠账.md) |
| D473 | 丢弃自建的延迟代理测量，不用它下结论 | [P40：产物下载并发](完成记录/07-复核与结欠账.md) |
| D474 | 空表的用例改成显式 `t.Skip` 而不是静默通过 | [跨全阶段收尾复核](完成记录/07-复核与结欠账.md) |
| D475 | 补钉"组件被改成 external 后旧资源要清理" | [跨全阶段收尾复核](完成记录/07-复核与结欠账.md) |
| D476 | 003 §3.2 与附录 D 补齐 `deploy` 全部字段 | [设计书 vs 实现：第二轮全面比对](完成记录/07-复核与结欠账.md) |
| D477 | 错误码补进 004 §10.2.1，并加守卫测试 | [设计书 vs 实现：第二轮全面比对](完成记录/07-复核与结欠账.md) |
| D478 | 错误码表按码值显式分类，不按源码注释解析 | [设计书 vs 实现：第二轮全面比对](完成记录/07-复核与结欠账.md) |
| D479 | 附录 C 加一句指向 006 §5.2 | [设计书 vs 实现：第二轮全面比对](完成记录/07-复核与结欠账.md) |
| D480 | 多副本 + PDB 单独成篇（试用指南 09） | [试用指南：真跑复核 + 补齐缺口](完成记录/07-复核与结欠账.md) |
| D481 | 17.7 保留原实验，只改结论 | [试用指南：真跑复核 + 补齐缺口](完成记录/07-复核与结欠账.md) |
| D482 | `准备.sh` 保留跳过行为但**说出来** | [试用指南：真跑复核 + 补齐缺口](完成记录/07-复核与结欠账.md) |
| D483 | 市场进阶单独成篇（试用指南 12） | [试用指南：其余各篇与市场线全部真跑](完成记录/07-复核与结欠账.md) |
| D484 | 09.3 改用开源发布并解释两个参数 | [试用指南：其余各篇与市场线全部真跑](完成记录/07-复核与结欠账.md) |
| D485 | 把 `go test ./...` 的坑写进 09 | [试用指南：其余各篇与市场线全部真跑](完成记录/07-复核与结欠账.md) |
| D486 | 环境清单单独成篇（试用指南 00b），不并进 00-准备 | [装上 cosign、跑通指南 11](完成记录/07-复核与结欠账.md) |
| D487 | 指南 11.2 补齐发布者项目的搭建步骤 | [装上 cosign、跑通指南 11](完成记录/07-复核与结欠账.md) |
| D488 | 只补缺前置的 5 篇，不统一改写已有的 16 篇 | [试用指南：每篇顶部统一标注前置](完成记录/07-复核与结欠账.md) |
| D489 | 前置里显式写"不需要 Docker" | [试用指南：每篇顶部统一标注前置](完成记录/07-复核与结欠账.md) |
| D490 | 指南冒烟只挑 8 步分层跑，不全量 | [指南冒烟检查 + 两小时上手路径](完成记录/07-复核与结欠账.md) |
| D491 | 缺环境时打印跳过并计数，不静默通过 | [指南冒烟检查 + 两小时上手路径](完成记录/07-复核与结欠账.md) |
| D492 | 清单为空时报错退出，不报成功 | [指南冒烟检查 + 两小时上手路径](完成记录/07-复核与结欠账.md) |
| D493 | 两小时路径要写明跳过了什么、何时回来 | [指南冒烟检查 + 两小时上手路径](完成记录/07-复核与结欠账.md) |
| D494 | 试用指南**不重编号**，改掉假依赖 | [试用指南顺序复核](完成记录/07-复核与结欠账.md) |
| D495 | README 给出两条走法而不是钦定一条 | [试用指南顺序复核](完成记录/07-复核与结欠账.md) |
| D496 | 03/04 标"可后看"，但保留"别不看" | [试用指南顺序复核](完成记录/07-复核与结欠账.md) |
| D497 | **推翻 D494**，试用指南按理解重排 | [试用指南按「理解」重排](完成记录/07-复核与结欠账.md) |
| D498 | 00 系列不参与重排 | [试用指南按「理解」重排](完成记录/07-复核与结欠账.md) |
| D499 | 用带自检的迁移脚本，不手改 | [试用指南按「理解」重排](完成记录/07-复核与结欠账.md) |
| D500 | 批量替换后逐条核对 diff | [试用指南按「理解」重排](完成记录/07-复核与结欠账.md) |
